# Red Team — 2026-09-06-engine-and-audit-verdict-integrity (2026-09-07)
**Verdict:** ADJUDICATED — one sweep over the 17-probe run at campaign base `ba08a77` (0.21.12): 28 blocker rows and 9 needsDecision rows (sixteen roots after de-duplication across probes) were patched in place and Lead-stamped under `/war-campaign --afk`; no probe was re-run, so the gate terminates ADJUDICATED, not CLEARED (ADR 0043). Every new Done-when and End-state check leg the patch introduced was re-executed at base and confirmed red.
**Rounds:** 1
<!-- Cumulative grill sweeps: the Step-1 seed + this run's sweeps. Strict form — the next run's seeding re-reads exactly this line; an integer, nothing else. -->

Run: Workflow `wf_151b7bde-a87` (script `.claude/red-team/engine-and-audit-verdict-integrity-rt.js`), artifact kind `impl-plan`, model opus / effort high (run config `agents.redteam`), round limit 3 (`run.redteamRoundLimit`), provision `[]` (no `.war-provision.json`, structural fallback empty), `analyzedAgentType` omitted (`Explore` present). Escape guard: snapshot before launch, post-run `--baseline` check exit 0 (no probe residue, no foreign delta).

## Attack surface
Spine: claims-vs-reality, executable-proof, coverage-vs-source (merged arm — the plan is its own source), consistency-placeholders, dependency-feasibility, intent-vs-plan. Bespoke: anchor-snippet-fidelity, done-when-red-at-base, literal-drift-at-base, default-flip-old-absent, unguarded-new-mirror, guard-split-deps-edge, touched-doc-fact-coverage, backstop-legitimacy-and-judge-tags, backgrounded-gate-lifetime, budget-feasibility, parallel-task-interference. ff-topology not triggered (no merge-topology evidence anchors); no `judge:` / `HARD at audit_sha` tags. Executed in sandbox (`git clone --no-hardlinks` of the linked worktree): executable-proof, done-when-red-at-base, literal-drift-at-base, default-flip-old-absent, backgrounded-gate-lifetime, budget-feasibility, parallel-task-interference.
Per-probe status: claims-vs-reality (analyzed, fail); executable-proof (executed, fail); coverage-vs-source (analyzed, fail); consistency-placeholders (executed, fail); dependency-feasibility (analyzed, fail); intent-vs-plan (analyzed, fail); anchor-snippet-fidelity (analyzed, fail); done-when-red-at-base (executed, fail); literal-drift-at-base (executed, warn); default-flip-old-absent (executed, fail); unguarded-new-mirror (analyzed, fail); guard-split-deps-edge (analyzed, fail); touched-doc-fact-coverage (analyzed, warn); backstop-legitimacy-and-judge-tags (analyzed, fail); backgrounded-gate-lifetime (executed, fail); budget-feasibility (executed, fail); parallel-task-interference (executed, fail).
Fallback: none. Warn probes: literal-drift-at-base, touched-doc-fact-coverage. Coverage: expected 17, on-target 17, off-target 0, dropped 0.

## Executed proof
- Every task Done-when and End-state check run verbatim at base: all Done-whens red except Task 6.1 (engine suite alone — green); End states 5, 6, 7, 10, 11, 12, 13, 19 green at base (engine suite alone). Both patched; every new leg re-executed red at base by the Lead after the patch.
- Byte literals re-measured at `ba08a77`: refiner card 36,734 B (130 B headroom, unchanged); auditor 25,009; worker 10,537; `skills/war/SKILL.md` 72,801 (927 B headroom, was 228); `CONTEXT.md` 115,151; release slots 0.21.12 in lock-step.
- Refiner-card eviction trial: the two submodule sections named as candidates are already pointer stubs (0 B and 595 B evictable); `## Diff probe` 863 B and `## Gate contract` 790 B hold body prose; the D3-fallback sentence is byte-pinned by `reference-link-integrity.test.mjs`; `workflow-template.test.mjs` asserts exactly four plugin-root `refiner-recovery.md` pointers.
- Backgrounded-gate lifetime: a `run_in_background` Bash task was alive after the starting agent's call returned; lifetime past the agent's turn is unobservable from inside a run.
- Phase-1 interference: a new `skills/war/references/*.md` file reds `doc-cli-consistency.test.mjs`'s default-deny placement census (owned by Task 1.4 in Phase 1); `skill-doc-contracts.test.mjs` and `war-config.test.mjs` (Tasks 1.2/1.3) pin refiner-card sentences.
- `grep -c 'plus the s'` = 1 at both `ffb3ab6` and HEAD; `NEVER touch version/release-slot literals` = 2 sites; `src.match(/const\s+MERGE_RESULT` = 4 sites (2 over-capturing).

## Findings
### Critical / Major / Minor
- [Major] adjudicated claims-vs-reality: Region "Budget and doc surfaces" (line 38): "the `resume-and-recovery.md` sentence to reconcile is the `held:land-failed` Outcome-handling bullet and the `Segmented land (in-band marker) and filing-on-held` bullet, **bot → Only ONE of the two bullets is pinned there. `grep -ci 'segmented\|filing-on-held' skills/war/assets/skill-doc-contracts.test.mjs` = 0. The single D21 pin (skill-doc-contracts.test.mjs:610, `test('D21 — held:land-failed bullet names both environment arms...')` (ref: line 38 (Budget and doc surfaces); Task 1.2, line 168)
- [Major] adjudicated executable-proof: Region E / D26 / Notes correction (5): "#1817 is resolved ... and closes on a land-time re-check (verified: `grep -c 'plus the s' workflow-template.js` = 0 at `ffb3ab6`)". → The grep returns 1, not 0 — at ffb3ab6 AND at current HEAD (ba08a77). The hit is not the `demote()` header at all; it is `minorsOf`'s comment at workflow-template.js:1195, `// rows' seats[] corroboration list, plus the seat's echoed audit_sha — validated throu (ref: ## Context — Region E; Resolved design tree D26; Notes / conscious deviations, anchor correction (5))
- [Major] adjudicated executable-proof: Task 1.4 Done-when: `... && grep -c 'fix-round-doctrine.md' agents/war-worker.md skills/snipe/SKILL.md && grep -ci 'sibling sweep' ... && echo FIX-DOCTRINE-OK` gates D24's two-surface pointer deliverable (worker card AND → `grep -c` over TWO files exits 0 when ANY file matches, so the guard passes with `skills/snipe/SKILL.md` carrying no pointer at all — exactly half the deliverable missing. `skills/war/assets/assert-done-when.sh` reads the command's own exit status only (its `e (ref: Phase 1 / Task 1.4 Done-when; End states 3, 16, 17)
- [Major] adjudicated executable-proof: Task 1.2 Done-when: `test "$(grep -c 'held:land-failed. for the' CONTEXT.md)" -eq 0` proves D22's revert of `CONTEXT.md`'s Clean-handoff emit set to `landed` + `held:escalation`. → Recorded sentence-case/re-position false-negative class. CONTEXT.md is hard-wrapped (~100 cols) — the target sits at :977 as `... and \`held:land-failed\` for the` with the paragraph continuing on :978. Re-wrapping the paragraph (what an in-place edit to that  (ref: Phase 1 / Task 1.2 Done-when; D22; End state 20)
- [Major] adjudicated executable-proof: End state 18 / Task 12.1 "Old-value absence (rule 6)": `test "$(grep -c 'NEVER touch version/release-slot literals' skills/war/assets/workflow-template.js)" -eq 0` proves the OLD sweep-prompt directive is gone. → Two defects in one guard. (a) It is case-sensitive on an ALL-CAPS prose directive whose replacement the plan itself writes in lowercase (`never move a version literal or the CHANGELOG head heading`); a worker who lowercases the surviving sentence to match the  (ref: End state 18; Phase 12 / Task 12.1 (Old-value absence, rule 6); ## Context — Region H)
- [Major] adjudicated coverage-vs-source: D22 (Part 1, line 80): "the 2026-08-25 plan's stale barrier sentence (#2093) is a record surface and is not edited"; Task 1.2's own slice (line 168): "No `docs/plans/` file is edited (record surfaces — Non-goals)." → Task 1.2's Files list (line 167) includes `docs/plans/2026-08-25-engine-reliability-and-filing-fidelity.md`; the SAME slice (line 168), a few sentences after the no-edit sentence, orders "In `docs/plans/2026-08-25-engine-reliability-and-filing-fidelity.md`, re (ref: D22 (line 80) · Task 1.2 slice + Done-when (lines 167–169) · Non-goals (line 109) · Notes lines 465(c), 468)
- [Major] adjudicated coverage-vs-source: Evidence consumed block (lines 6–14): "one row per linked artifact" — with each cited issue's `## Evidence artifacts` section read where present, and unread artifacts recorded with a reason (lines 12–13 do this for two l → Several artifacts that cited issues list under `## Evidence artifacts` appear in the block neither as read nor as unread-with-reason: (a) #2094 lists `skills/war/assets/gate-pin-status.sh` — absent from the block's read list on line 10 (the plan clearly consul (ref: Evidence consumed block, lines 6–14)
- [Major] adjudicated consistency-placeholders: Task 1.2's own slice states "No `docs/plans/` file is edited (record surfaces — Non-goals)." (plan line 168), and D22 states "the 2026-08-25 plan's stale barrier sentence (#2093) is a record surface and is not edited" (l → The SAME Task 1.2 slice, ~200 characters later, orders the opposite: "(#2093, living-plans ruling 2026-09-06) In `docs/plans/2026-08-25-engine-reliability-and-filing-fidelity.md`, rewrite the End state 9 / D11 sentences …"; `docs/plans/2026-08-25-engine-reliab (ref: Task 1.2 (line 168), D22 (line 80), Non-goals (line 109), Notes (c) (line 465), living-plans ruling (line 468))
- [Major] adjudicated consistency-placeholders: Task 6.3 (`Files: skills/war/references/schemas.md`, `docs/learnings/zero-commit-task-branch-…md`) and Task 10.2 (`Files: CONTEXT.md`) both declare `requiresTest: true`. → Neither task's file footprint contains a test file, and neither slice orders one; every other doc-only task in this plan sets `requiresTest: false` (2.2, 3.2, 4.2, 4.3, 5.2, 9.2, 12.2, 13.2, 14.1). `requiresTest:true` is exactly what arms the refiner's test-in (ref: Task 6.3 (lines 302-308), Task 10.2 (lines 364-370))
- [Major · needsDecision] adjudicated dependency-feasibility: Task 1.1 (D1, PIN-5): evict tier-2 refiner-card prose until >= 2,048 B of headroom remains — 'candidates are the submodule arms (`## Submodule-as-repo provisioning`, `### Submodule phase (2A WAR-owned / 2B PR-and-hold)`) → Both named candidate sections are ALREADY fully-evicted ADR-0042 pointer stubs into `refiner-recovery.md` — there is no tier-2 body left in either to move. `sed -n '19,24p' agents/war-refiner.md` is a heading plus one sentence: 'When a phase's `target repo` is (ref: Task 1.1 (Phase 1); D1; A12; End state 1)
- [Major] adjudicated dependency-feasibility: Task 6.3 and Task 10.2 each declare `requiresTest: true`. → Neither task's declared `Files:` list contains a path the test floor can match, so the merge-task floor exits 1 and routes the task to `no-test`. Task 6.3's Files are `skills/war/references/schemas.md` and `docs/learnings/zero-commit-task-branch-is-vacuously-a (ref: Task 6.3 and Task 10.2 (`requiresTest: true` lines))
- [Major] adjudicated intent-vs-plan: End state 19: "Every dispatch site's agent death classifies `env-died` naming the site and never emits a content verdict" — and Purpose: "every dispatch death is classified as an environment event". → The plan's own delivering decision contradicts this. D21 (line 79) reads: "a tagged death on an audit/ace/re-audit seat demotes the current subset with the existing abandon reason and falls through; on merge/floor-fix/endstate/gate-audit/servitor/filing it cla (ref: Commander's Intent → End state 19; Purpose bullet 3; D21)
- [Major · needsDecision] adjudicated intent-vs-plan: Purpose: "every refiner status is cross-checked against its own fields" — with End states 6, 7 and 8 offered as the delivering conditions. → The End states cross-check exactly three signatures: the PIN_PROBE's `already_upstream` (ES6), the land `status:'error' && land_segment:'incomplete'` pairing (ES7), and the merge-task `gate_segment:'incomplete'` pairing (ES8). `MERGE_RESULT.status` carries ele (ref: Commander's Intent → Purpose bullet 2; End states 6, 7, 8; D4/D5/D7)
- [Major] adjudicated intent-vs-plan: Six doc-only tasks carry `Done when: None` and name End-state fixtures as their validators — Task 2.2 ("End state 4's fixtures and Task 2.1's registry row are the validators"), Task 3.2 ("End state 5's fixtures"), Task 4 → Those End-state fixtures are engine-behaviour and prompt-content asserts on `workflow-template.js` / the agent cards; none of them reads the doc file the task edits. End state 5's named fixtures are `parkAsk collision: corroborator carries file/title/fork and  (ref: Commander's Intent → End states 4–19; Tasks 2.2, 3.2, 4.2, 5.2, 9.2, 13.2)
- [Major] adjudicated anchor-snippet-fidelity: Task 12.1 / End state 18 / D20: "the sweep prompt's `NEVER touch version/release-slot literals` becomes `never move a version literal or the CHANGELOG head heading`" with the acceptance leg `Old-value absence (rule 6): g → The literal exists at TWO distinct dispatched-prompt builds in skills/war/assets/workflow-template.js at HEAD (and 2 at ffb3ab6 — `git show ffb3ab6:... | grep -c` = 2, no drift). Line 4333 is the `PHASE-CLOSE COHERENCE SWEEP` build (the one the plan names). Li (ref: Task 12.1 (Phase 12) slice + Done-when; End state 18; D20; Region H)
- [Major] adjudicated done-when-red-at-base: The plan's own doctrine — grill patch (e) ("vacuous Done-when/check legs replaced by tokens that are 0 at base") and operator patch (1) ("each new phase keeps ... a Done-when whose tokens are 0 at `ffb3ab6`") — asserts e → Task 6.1's Done-when is `node --test skills/war/assets/workflow-template.test.mjs` and nothing else. That suite is entirely GREEN at base, so the Done-when exits 0 before a single line of Task 6.1 is written. Task 6.1 is the ONLY task in the plan with this pro (ref: Task 6.1: Derive-and-skip needs commits; provenance floor round 2; early-done `pendingAbsorbs` — `- Done when:` (plan line 287))
- [Major] adjudicated done-when-red-at-base: Same doctrine (grill patch (e)) covers End-state `check:` legs, and every End state is an individually-checkable attestation the endstate seat proves by running its `check:` command. → Eight of the twenty End-state check commands are GREEN at base as literally written. Seven of them (5, 6, 10, 11, 12, 13, 19) are `node --test skills/war/assets/workflow-template.test.mjs` alone; End state 7 is `node --test skills/war/assets/workflow-template. (ref: Commander's Intent → `- **End state:**` items 5, 6, 7, 10, 11, 12, 13, 19 (plan lines 133, 134, 135, 138, 139, 140, 141, 147))
- [Major] adjudicated default-flip-old-absent: Task 12.1: "Old-value absence (rule 6): `grep -c 'NEVER touch version/release-slot literals' workflow-template.js` = 0" — i.e. the task retires the OLD sweep-prompt sentence in favour of `never move a version literal`. → Task 12.1's own `Done when:` is `node --test skills/war/assets/workflow-template.test.mjs skills/war/assets/prompt-surface-budgets.test.mjs && grep -c 'never move a version literal' skills/war/assets/workflow-template.js` — a NEW-present grep only. No OLD-abse (ref: ### Task 12.1: Blurb absorbs ride the per-task ace — `Done when:` line)
- [Major] adjudicated default-flip-old-absent: Task 12.2: "`skills/war/SKILL.md`'s `--ace` bullet drops the file-based routing sentence". → That is an OLD-wording retirement on an enumerated surface, and Task 12.2's `Done when:` (`node --test skills/war/assets/prompt-surface-budgets.test.mjs && grep -ci 'version literal' skills/war/references/disposition-eligibility.md && grep -c 'barrier:release- (ref: ### Task 12.2: Eligibility doctrine, `--ace` bullet, ADR 0013 second Decision-log line)
- [Major] adjudicated default-flip-old-absent: Task 4.2: "Done when: None — doc rows; End states 6, 7 and 9's fixtures are the validators", covering a straggler sweep whose "decisive OLD token is any `floor-skipped` sentence lacking the escalation-arm/sweep/terminal- → End states 6, 7 and 9 are engine fixtures in `workflow-template.test.mjs` / `land-decision.test.mjs` (`pin-transfer: #1973 verbatim replay merges the task`, `segmented-land: …`, `MERGE_RESULT: floor_route pinned …`). None of them reads those three reference do (ref: ### Task 4.2: Land-path prose — `Done when: None — doc rows; End states 6, 7 and 9's fixtures are the validators`)
- [Major] adjudicated unguarded-new-mirror: Task 5.1 (D7/A4) adds a NEW optional in-band field `gate_segment` to the inline `MERGE_RESULT` schema in workflow-template.js — "declare the optional field on `MERGE_RESULT`, no status enum change" — mirroring the `land_ → The plan orders NO schema pin for `gate_segment`. Task 5.1's named fixtures are only `segmented-gate: merge-task incomplete re-dispatches`, `segmented-gate: partial gate log reruns the gate`, `gate-log stamp`, `gate-log fallback: unthreaded path marker` — all  (ref: Task 5.1 (plan line 266); D7 (line 65); A4 (line 93); End state 8 (line 136))
- [Major · needsDecision] adjudicated guard-split-deps-edge: PIN-3 / A12 / Task 1.1: "a new `references/` file is permitted (A12) only with its `QUALIFIED_HEADERS` row in this same task (PIN-3)" — i.e. the sole registration duty for a new skills/war/references/ file is reference-l → There is a SECOND, default-deny registration duty the plan states only for Task 1.4: skills/_shared/doc-cli-consistency.test.mjs's placement census. It readdir-scans every skills/*/references/*.md and asserts the scan set deepEquals EVICTION_DESTINATIONS ∪ VER (ref: Pivotal constraints PIN-3 · A12 · Task 1.1 · Task 1.2 · Task 11.2 · Task 12.2)
- [Major · needsDecision] adjudicated backstop-legitimacy-and-judge-tags: Backstop row 3 (plan line 448) declares a legitimate deferred validation with a named runner and timing: "Does a backgrounded gate (`run_in_background`) outlive the refiner agent that started it (D7)? · why deferred: the → The named runner is not an accountable actor and the entry is mis-filed as deferred. (a) `/red-team` has no `spine` slot a plan can enlist: `skills/red-team/SKILL.md` Step 2 fixes the runnable set as "the six **spine** lenses always run" plus the four drift-gu (ref: ## Deferred validations (backstops), row 3 (line 448); D7 (line 62); Task 5.1)
- [Major · needsDecision] adjudicated backstop-legitimacy-and-judge-tags: Backstop row 3 closes "whichever way it falls, the D7 partial-log rerun rule holds" — i.e. the plan asserts the probe's outcome changes nothing. → If that assertion is true, the row is not a validation at all: a check whose result cannot change any decision, surface, fixture or design is box-ticking, and the plan's own doctrine (`## Commander's Intent`: "No check, gate, floor, or backstop is waived (ADR  (ref: ## Deferred validations (backstops), row 3 (line 448); D7 (line 62); Task 5.1; End state 8)
- [Major] adjudicated backgrounded-gate-lifetime: D7's re-dispatch fallback: "on re-dispatch a gate log whose last line is not `exit_code:` (the D8 stamp) means the gate is RERUN from scratch — never read as a partial result", with D8 stamping `tip_sha: <sha>` first so  → The rule is one-sided. It keys EXCLUSIVELY on the last line, and the gate-log path is keyed by taskId alone — `${refineryP}/.war/gate-${taskId}.log` (`gateCaptureClause` at skills/war/assets/workflow-template.js:1920; all three call sites — lines 3348, 3482, 3 (ref: D7 row (decision table, `| D7 | Gate that outruns the dispatch |`), D8 row (`| D8 | Gate-log stamp and fallback |`), Task 5.1 plan slice, End state 8 and End state 10, Task 5.2 `resume-and-recovery.md` slice)
- [Major] adjudicated budget-feasibility: Task 1.1 Plan slice: "evict tier-2 prose until >= 2,048 B of headroom remains (PIN-5, D1) — candidates are the submodule arms (`## Submodule-as-repo provisioning`, `### Submodule phase (2A WAR-owned / 2B PR-and-hold)`) a → All three named candidates are ALREADY ADR-0042 pointer stubs; together they can yield at most 595 B against the 1,918 B the Done-when requires (36,864 hard − 36,734 measured = 130 B headroom; 2,048 − 130 = 1,918 B). Measured per-section at HEAD ba08a77 (ident (ref: Phase 1 / Task 1.1: Refiner card eviction (ADR 0042) — Plan slice, the candidate enumeration)
- [Major] adjudicated budget-feasibility: Task 1.1 Plan slice: "Never evict a sentence a registry row in `workflow-template.test.mjs` or a pin in `refinery-surface.test.sh` binds — check both before choosing; a pinned sentence is not a Phase 1 candidate (this ta → The two-item checklist misses the binding that actually fires on the plan's own top-named candidate. The single piece of non-pointer prose inside `## Submodule-as-repo provisioning` — the 183 B D3 fallback sentence at L23 — is byte-pinned by skills/war/assets/ (ref: Phase 1 / Task 1.1: Refiner card eviction (ADR 0042) — Plan slice, the pre-eviction binding check)
- [Major] adjudicated parallel-task-interference: Task 1.1 can safely evict tier-2 refiner-card prose to reach ≥ 2,048 B headroom by checking only two guard surfaces (workflow-template.test.mjs registry rows and refinery-surface.test.sh pins), with the submodule arms pl → Two OTHER guard files pin agents/war-refiner.md sentences, and BOTH are owned by parallel same-phase siblings that Task 1.1 cannot edit: skills/war/assets/skill-doc-contracts.test.mjs (Task 1.2) pins the file-followups parked-ask sentence, the gitlink-bump sub (ref: Phase 1 — Task 1.1 (Refiner card eviction, ADR 0042): Files list + Plan slice ("Never evict a sentence a registry row in workflow-template.test.mjs or a pin in refinery-surface.test.sh binds — check both before choosing") and its Done-when; siblings Task 1.2 (owns skills/war/assets/skill-doc-contracts.test.mjs) and Task 1.3 (owns skills/war/assets/war-config.test.mjs))
- [Major · needsDecision] adjudicated dependency-feasibility: Task 1.1 (D1, PIN-5): evict tier-2 refiner-card prose until >= 2,048 B of headroom remains — 'candidates are the submodule arms (`## Submodule-as-repo provisioning`, `### Submodule phase (2A WAR-owned / 2B PR-and-hold)`) → Both named candidate sections are ALREADY fully-evicted ADR-0042 pointer stubs into `refiner-recovery.md` — there is no tier-2 body left in either to move. `sed -n '19,24p' agents/war-refiner.md` is a heading plus one sentence: 'When a phase's `target repo` is (ref: Task 1.1 (Phase 1); D1; A12; End state 1)
- [Major · needsDecision] adjudicated intent-vs-plan: Purpose: "every refiner status is cross-checked against its own fields" — with End states 6, 7 and 8 offered as the delivering conditions. → The End states cross-check exactly three signatures: the PIN_PROBE's `already_upstream` (ES6), the land `status:'error' && land_segment:'incomplete'` pairing (ES7), and the merge-task `gate_segment:'incomplete'` pairing (ES8). `MERGE_RESULT.status` carries ele (ref: Commander's Intent → Purpose bullet 2; End states 6, 7, 8; D4/D5/D7)
- [Major · needsDecision] adjudicated guard-split-deps-edge: PIN-3 / A12 / Task 1.1: "a new `references/` file is permitted (A12) only with its `QUALIFIED_HEADERS` row in this same task (PIN-3)" — i.e. the sole registration duty for a new skills/war/references/ file is reference-l → There is a SECOND, default-deny registration duty the plan states only for Task 1.4: skills/_shared/doc-cli-consistency.test.mjs's placement census. It readdir-scans every skills/*/references/*.md and asserts the scan set deepEquals EVICTION_DESTINATIONS ∪ VER (ref: Pivotal constraints PIN-3 · A12 · Task 1.1 · Task 1.2 · Task 11.2 · Task 12.2)
- [Minor · needsDecision] adjudicated touched-doc-fact-coverage: Task 1.2: "mirror the `attempts[]` shape into `schemas.md`'s manifest section (update `schemas-manifest.test.sh` where it pins the MUST-carry set)" … Guard choice (rule 8): "the manifest shape is guarded by `schemas-mani → `skills/war/references/schemas-manifest.test.sh` is a doc-scan of the `.war-provision.json` PROVISIONING-manifest contract, not the run manifest. Its own header reads "Doc-scan test for the .war-provision.json manifest contract in schemas.md" and its nine chec (ref: Task 1.2 (plan line 168) — Files/Plan slice/Guard choice sentence)
- [Minor · needsDecision] adjudicated touched-doc-fact-coverage: Task 1.2 rewrites `schemas.md`'s handoff literal to `followUps: [ { issue, reason, merged?, drainCause? } ]` (#1793) and asserts "Every rewritten fact here restates engine behavior; the guard choice per rule 8 is: …" — t → The `followUps` row shape is machine-source-derivable — it is the engine's explicit-key projection at `skills/war/assets/workflow-template.js:5107` (`followUps: minorsFiled.map(m => ({ issue: …, reason: …, ...(mergedRowsOf(m).length ? { merged: … } : {}) }))`) (ref: Task 1.2 (plan line 168) — Guard choice (rule 8) sentence; End state 20 (line 148))
- [Minor · needsDecision] adjudicated touched-doc-fact-coverage: Task 10.2: "the `unverified` row enumerates both artifact states (#1781)" on `CONTEXT.md`; the task's only stated mechanism sentence is "Every entry de-mirrors — it names the engine construct (`normalizeFinding`, the seg → `intake_lint` and `cmd_bytes_mismatch` are engine literals (`skills/war/assets/workflow-template.js:336-338` schema comment; the END-STATE CHECK build at :3773 and :3778), so the `unverified` row restates two source-derived tokens by name — it cannot de-mirror (ref: Task 10.2 (plan lines 364-367); End state 16 (line 144))
- [Minor · needsDecision] adjudicated touched-doc-fact-coverage: Task 13.2: "`schemas.md` gains the env-died site table (site → classification → what the Lead does) … De-mirror: the table names `dispatchAgent` as the source of the site tags rather than restating the tag strings." → The de-mirror covers only the tag strings; the table's load-bearing content is the per-site CLASSIFICATION mapping D21 introduces — audit/ace/re-audit seats demote the subset, while merge/floor-fix/endstate/gate-audit/servitor/filing classify `env-died` soft ( (ref: Task 13.2 (plan lines 424-427); D21 (line 79); End state 19 (line 147))
- [Major · needsDecision] adjudicated backstop-legitimacy-and-judge-tags: Backstop row 3 (plan line 448) declares a legitimate deferred validation with a named runner and timing: "Does a backgrounded gate (`run_in_background`) outlive the refiner agent that started it (D7)? · why deferred: the → The named runner is not an accountable actor and the entry is mis-filed as deferred. (a) `/red-team` has no `spine` slot a plan can enlist: `skills/red-team/SKILL.md` Step 2 fixes the runnable set as "the six **spine** lenses always run" plus the four drift-gu (ref: ## Deferred validations (backstops), row 3 (line 448); D7 (line 62); Task 5.1)
- [Major · needsDecision] adjudicated backstop-legitimacy-and-judge-tags: Backstop row 3 closes "whichever way it falls, the D7 partial-log rerun rule holds" — i.e. the plan asserts the probe's outcome changes nothing. → If that assertion is true, the row is not a validation at all: a check whose result cannot change any decision, surface, fixture or design is box-ticking, and the plan's own doctrine (`## Commander's Intent`: "No check, gate, floor, or backstop is waived (ADR  (ref: ## Deferred validations (backstops), row 3 (line 448); D7 (line 62); Task 5.1; End state 8)
- [Minor] claims-vs-reality: Region E (line 28) and D26 (line 84): "#1817 is resolved by the in-band-absorb rewrite and closes on a land-time re-check (verified: `grep -c 'plus the s' workflow-template.js` = 0 at `ffb3ab6`)" — D26 makes that named g → The named grep returns 1, not 0, at both `ffb3ab6` and HEAD. The hit is `workflow-template.js:1195` — `// rows' seats[] corroboration list, plus the seat's echoed audit_sha — validated through`. The substantive claim (the `plus the six` / `(roundLimit − 2)` ra (ref: Region E, line 28; D26, line 84)
- [Minor] claims-vs-reality: Region C (line 24): "the `MERGE_RESULT` block regex `(?=\n\nconst )` over-captures — at **two** sites in the test file, not one (verified: both `src.match(/const\s+MERGE_RESULT` sites at `ffb3ab6`)"; Task 4.1 (line 237)  → There are FOUR `src.match(/const\s+MERGE_RESULT` sites in `workflow-template.test.mjs`, not two — identical at `ffb3ab6` and HEAD. Two of them (lines 2511, 9564) ALREADY use the corrected `(?=\n\n)` lookahead; only lines 1275 and 11843 carry the over-capturing (ref: Region C, line 24; Task 4.1, line 237)
- [Minor] claims-vs-reality: Region C (line 24) and D6/Task 4.1 (lines 64, 237) present the un-wrapped `environment-proceed`/`baseline-proceed` re-merge sites as the #1736 defect ("`routedMr(` wraps the primary merge and the floor retry only; the `e → The omission is deliberate and annotated in the engine at both sites, and the plan never names those annotations for update. `workflow-template.js` carries two `// ponytail: routedMr is deliberately NOT applied to ep|bp — the un-normalized 'no-test' IS a HARD_ (ref: Region C, line 24; D6, line 64; Task 4.1, line 237)
- [Minor] claims-vs-reality: Task 11.1 (line 377): "The auditor card's split-panel / ESCALATE-BOUNDARY prose and the dispatched `REBUTTAL ROUND` prompt both carry the same sentence … (D18, PIN-22) the ESCALATE-BOUNDARY CONTRACT becomes two-sided on  → `agents/war-auditor.md` contains no split-panel prose, no rebuttal prose, and no `ESCALATE-BOUNDARY` heading at all: `grep -ci 'split\|rebuttal\|ESCALATE-BOUNDARY' agents/war-auditor.md` = 0. The card's only escalate-boundary content is the `escalate` disposit (ref: Task 11.1, line 377)
- [Minor] claims-vs-reality: Region F (line 30): "`stampDrainCause` stamps `f.drainCause` at three sites but no filing-prompt row or `followUps` projection reads it (verified: issue #1799 (2026-08-26); at `ffb3ab6`)". → There are two stamp sites, not three. `grep -c 'stampDrainCause'` = 3 counts the helper definition plus two call sites; `grep -c 'stampDrainCause('` = 2, and `f.drainCause` is assigned at exactly one place (inside the helper). Identical at `ffb3ab6`. The rest  (ref: Region F, line 30)
- [Minor] executable-proof: Task 1.2's `test "$(grep -c 'confirms or overrules' skills/war/SKILL.md)" -eq 0` (D22 overrule clause → ratify-or-record-dissent) and Task 11.2's `test "$(grep -c 'rebuttal round\*\* → resolve or escalate' skills/war/SKI → Same recorded false-negative class as above. Both are case-sensitive prose-clause greps, and the Task 11.2 guard is additionally anchored on markdown emphasis syntax (`**`) rather than on the clause's own words — dropping the bold markers while leaving the cla (ref: Phase 1 / Task 1.2 Done-when; Phase 11 / Task 11.2 Done-when)
- [Minor] executable-proof: Context Region H: "the blurb-fix gap is on the doctrine and prompt surfaces: `disposition-eligibility.md`'s \"touches no version/release slot\", the sweep prompt's `NEVER touch version/release-slot literals`, and no ace- → `disposition-eligibility.md` carries no such sentence at ffb3ab6 — 0 hits. Part 2 contradicts Part 1 on this exact point: Task 12.2's slice records "`disposition-eligibility.md` carries no `touches no version/release slot` sentence at `ffb3ab6` (0 hits — the s (ref: ## Context — Region H; Phase 12 / Task 12.2 Plan slice)
- [Minor] executable-proof: Pivotal constraints, PIN-2: "No prose doc enumerates the hard set (verified: `unrunnable-deps` hits only in tests, `land-decision.mjs` and ADR 0023 at `ffb3ab6`), so D6's doc footprint is the drift-guard rows only." → The stated census is false as run. `unrunnable-deps` also hits `skills/war/assets/workflow-template.js` (7 lines, including the hand-mirrored `HARD_ESCALATION_REASONS` neighbourhood the same PIN requires D6 to edit), a `docs/learnings/` lesson, and 14 record s (ref: ## Pivotal constraints — Enum discipline (PIN-2); Notes anchor correction (15))
- [Minor] coverage-vs-source: Region E (line 28) and Notes correction (5) (line 453): "The `demote()` header comment is no longer ragged — the spec's `plus the six` / `(roundLimit − 2) mid-bisection` fragment has zero hits; #1817 is resolved ... and  → `grep -c 'plus the s' skills/war/assets/workflow-template.js` returns 1, not 0 — at both `ffb3ab6` (checked with `git show`) and at the plan's actual base HEAD `ba08a77`. The hit is line 1195, `// rows' seats[] corroboration list, plus the seat's echoed audit_ (ref: Region E (line 28) · Notes correction (5) (line 453) · D26 (line 84))
- [Minor] coverage-vs-source: Region H (line 34) / D20 (line 78) / Task 12.1 (line 397): "the sweep prompt's `NEVER touch version/release-slot literals` becomes `never move a version literal or the CHANGELOG head heading`" — a single named site. → The literal occurs at TWO distinct prompt builds in `skills/war/assets/workflow-template.js`: line 4333 (the bounded polish/sweep prompt) and line 4488 (the terminal Ace-Charge fix prompt). Task 12.1's own rule-6 assert and End state 18 require `grep -c 'NEVER (ref: Region H (line 34) · D20 (line 78) · Task 12.1 (line 397))
- [Minor] coverage-vs-source: Region C (line 24): "the `MERGE_RESULT` block regex `(?=\n\nconst )` over-captures — at **two** sites in the test file, not one (verified: issue #1806 (2026-08-26); both `src.match(/const\s+MERGE_RESULT` sites at `ffb3ab → There are FOUR `src.match(/const\s+MERGE_RESULT` sites in `skills/war/assets/workflow-template.test.mjs` — lines 1275, 2511, 9564 and 11843 — not two. Only lines 1275 and 11843 carry the over-capturing `(?=\n\nconst )` lookahead; lines 2511 and 9564 already us (ref: Region C (line 24) · Task 4.1 (line 237))
- [Minor] coverage-vs-source: The plan uses bare `rule N` references throughout Part 2 ("Old-value absence (rule 6)", "the guard choice per rule 8", "one cohesive unit (rule 1)", "rule 7 covers a drift guard forced out of its fact's task"), which res → Task 1.4 (line 186) breaks that convention and uses bare `rule 6` and `Rule 7` for D24's fixer rules, colliding with the authoring rules of the same numbers used elsewhere in the same plan. "(rule 6 applied to the block itself: one constant, three interpolatio (ref: Task 1.4 slice (line 186))
- [Minor] consistency-placeholders: Six task slices cite "rule 3 of D24" as the authority for de-mirroring count words in comments and census headers (lines 217, 237, 286, 337, 377, 417 — e.g. Task 13.1: "Rule 3 of D24: the census header names the one perm → D24's rule (3) is "every new guard gets an oracle that does not share the code under test"; the count-word rule is D24's rule (7) — "no count words or ordinals in comments a diff can invalidate — name the members". The plan contradicts itself on this: Task 1.4 (ref: Tasks 3.1, 4.1, 6.1, 9.1, 11.1, 13.1 (lines 217, 237, 286, 337, 377, 417) vs D24 (line 82) and Task 1.4 (line 186))
- [Minor] consistency-placeholders: Region H, D20, Task 12.1 and End state 18 all speak of a single site: "the sweep prompt's `NEVER touch version/release-slot literals`" becomes "never move a version literal or the CHANGELOG head heading", with the gate ` → The literal exists at TWO dispatched prompt builds in the engine: the PHASE-CLOSE COHERENCE SWEEP build (line 4333) and the TERMINAL PASS build (line 4488). The plan names only the sweep prompt and prescribes replacement wording only for it, while its own gate (ref: Task 12.1 (line 397), D20 (line 78), Region H (line 34), End state 18 (line 146))
- [Minor] consistency-placeholders: Region C and Notes (10) census the #1806 over-capture as "both `src.match(/const\s+MERGE_RESULT` sites at `ffb3ab6`", and Task 4.1 orders "the `MERGE_RESULT` block regexes at both `src.match(/const\s+MERGE_RESULT` sites  → There are FOUR `src.match(/const\s+MERGE_RESULT` sites in `workflow-template.test.mjs`, not two. Two of them (lines 1275 and 11843) carry the over-capturing `(?=\n\nconst )` lookahead the plan means; the other two (2511, 9564) already use `(?=\n\n)`. The censu (ref: Region C (line 24), Task 4.1 (line 237), Notes item (10) (line 453))
- [Minor] dependency-feasibility: Region E / D26 / Notes item (5): '#1817 is resolved by the in-band-absorb rewrite and closes on a land-time re-check (verified: `grep -c 'plus the s' workflow-template.js` = 0 at `ffb3ab6`)', and D26 states each such iss → `grep -c 'plus the s' skills/war/assets/workflow-template.js` returns 1, not 0 — at HEAD and at `ffb3ab6` alike (`git show ffb3ab6:skills/war/assets/workflow-template.js | grep -c 'plus the s'` = 1). The single hit is an unrelated comment, line 1195: `// rows' (ref: Region E (#1817 sentence); D26; Notes / conscious deviations item (5))
- [Minor] dependency-feasibility: Region C: 'the `MERGE_RESULT` block regex `(?=\n\nconst )` over-captures — at **two** sites in the test file, not one (verified: issue #1806; both `src.match(/const\s+MERGE_RESULT` sites at `ffb3ab6`)', and Task 4.1: 'th → There are FOUR `src.match(/const\s+MERGE_RESULT` sites in `workflow-template.test.mjs`, not two — at `ffb3ab6` and at HEAD identically. Two of them (lines 2511 and 9564) already carry the `(?=\n\n)` lookahead the plan prescribes; only lines 1275 and 11843 carr (ref: Region C (#1806 sentence); Task 4.1 test-side slice)
- [Minor] dependency-feasibility: Region F: '`stampDrainCause` stamps `f.drainCause` at three sites but no filing-prompt row or `followUps` projection reads it (verified: issue #1799 (2026-08-26); at `ffb3ab6`)'. → `f.drainCause` is stamped at two sites, not three. `grep -c stampDrainCause` = 3 counts the definition plus two call sites; `grep -n drainCause` over the engine returns exactly one line — the helper's own assignment — confirming there is no fourth or third sta (ref: Region F (#1799 sentence))
- [Minor] anchor-snippet-fidelity: Region E and D26: "The `demote()` header comment is no longer ragged — the spec's `plus the six` / `(roundLimit − 2) mid-bisection` fragment has zero hits; #1817 is resolved by the in-band-absorb rewrite and closes on a  → `grep -c 'plus the s' skills/war/assets/workflow-template.js` = 1 at HEAD **and** = 1 at ffb3ab6 — the claim was already false at the plan's own conversion base, not drift. The hit is line 1195, an unrelated comment about the seats[] corroboration list, not th (ref: Region E (`## Context`); D26 (`Resolved design tree`); Notes / conscious deviations item (5))
- [Minor] anchor-snippet-fidelity: Region H: "the blurb-fix gap is on the doctrine and prompt surfaces: `disposition-eligibility.md`'s \"touches no version/release slot\", the sweep prompt's `NEVER touch version/release-slot literals`, and no ace-prompt c → The sentence does not exist there — 0 hits at HEAD and at ffb3ab6. Task 12.2 states the opposite and is correct ("`disposition-eligibility.md` carries no `touches no version/release slot` sentence at `ffb3ab6` (0 hits — the spec's quoted OLD phrase does not ex (ref: Region H (`## Context`) vs. Task 12.2 (Phase 12) `Old-value absence (rule 6)` clause)
- [Minor] anchor-snippet-fidelity: Region C and Task 4.1: "the `MERGE_RESULT` block regex `(?=\n\nconst )` over-captures — at **two** sites in the test file, not one (verified: issue #1806 (2026-08-26); both `src.match(/const\s+MERGE_RESULT` sites at `ffb → There are FOUR `src.match(/const\s+MERGE_RESULT` sites in skills/war/assets/workflow-template.test.mjs (lines 1275, 2511, 9564, 11843), at HEAD and at ffb3ab6 alike. Only two of them (1275, 11843) carry the over-capturing `(?=\n\nconst )`; the other two (2511, (ref: Region C (`## Context`); Task 4.1 (Phase 4) test-side clause; Notes / conscious deviations item (10))
- [Minor] anchor-snippet-fidelity: Pivotal constraints, PIN-2: "No prose doc enumerates the hard set (verified: `unrunnable-deps` hits only in tests, `land-decision.mjs` and ADR 0023 at `ffb3ab6`), so D6's doc footprint is the drift-guard rows only." → The grep census is incomplete. At ffb3ab6 and HEAD `unrunnable-deps` also hits `skills/war/assets/workflow-template.js` (9 hits — the inline engine mirror PIN-2 itself requires changing, so it is plainly not "tests, land-decision.mjs and ADR 0023"), plus `.tou (ref: Pivotal constraints → "Enum discipline (PIN-2 ‡)"; D6)
- [Minor] literal-drift-at-base: `grep -c 'plus the s' skills/war/assets/workflow-template.js` = 0 at ffb3ab6. → It is 1 — at ffb3ab6 AND at HEAD. Line 1195 of skills/war/assets/workflow-template.js reads `// rows' seats[] corroboration list, plus the seat's echoed audit_sha — validated through`, which contains the substring `plus the s`. (ref: Part 1, Region E (line 28): "#1817 is resolved by the in-band-absorb rewrite and closes on a land-time re-check (verified: `grep -c 'plus the s' workflow-template.js` = 0 at `ffb3ab6`)")
- [Minor] literal-drift-at-base: There are exactly two `src.match(/const\s+MERGE_RESULT` sites in skills/war/assets/workflow-template.test.mjs, and both are the #1806 over-capture sites Task 4.1 must fix. → There are FOUR such sites, identical at ffb3ab6 and HEAD: lines 1275, 2511, 9564, 11843. Only two carry the over-capturing `(?=\n\nconst )` lookahead (1275 and 11843); the other two (2511, 9564) already use `(?=\n\n)`. The substantive #1806 claim ("two over-ca (ref: Part 1 Region C (line 24) "both `src.match(/const\s+MERGE_RESULT` sites at `ffb3ab6`"; Notes correction (10) (line 453); Task 4.1 Plan slice (line 237) "the `MERGE_RESULT` block regexes at both `src.match(/const\s+MERGE_RESULT` sites take a `(?=\n\n)` lookahead (#1806)")
- [Minor] literal-drift-at-base: Snapshot literals at `ffb3ab6`: agents/war-refiner.md 36,734 B (130 B headroom / 36,864 ceiling); agents/war-auditor.md 25,011 B (3,661 B headroom / 28,672 ceiling, 435 B over the 24,576 B advisory); agents/war-worker.md → Every one of these is CORRECT at ffb3ab6, and every ceiling/advisory matches prompt-surface-budgets.test.mjs exactly. HEAD (ba08a77, 43 commits later, release 0.21.12) has drifted: war-auditor.md 25,011 -> 25,009 (headroom 3,663; still 433 B over advisory); wa (ref: Part 1 line 38 (budget/doc surfaces), line 43 (script cap), line 90 (A1), line 159 (Task 1.1), line 168 (Task 1.2), line 406 (Task 12.2); D12 snapshot rule at line 42.)
- [Minor] default-flip-old-absent: Task 12.1 slice: "the sweep prompt's `NEVER touch version/release-slot literals` becomes `never move a version literal or the CHANGELOG head heading`" — singular, one prompt build. → The retired sentence lives at TWO distinct dispatched builds in `skills/war/assets/workflow-template.js`: line 4333 (the phase-close sweep/polish build, `phaseCloseQueue` rows) and line 4488 (the terminal-pass build, `terminalCharge` / `terminalRows`). End sta (ref: ### Task 12.1: Blurb absorbs ride the per-task ace — Plan slice, `the sweep prompt's NEVER touch version/release-slot literals becomes …`)
- [Minor] default-flip-old-absent: Task 1.2: "Gate asserts the OLD prose absent on every surface (rule 6)", covering `skills/war/SKILL.md`'s `confirms or overrules`, `CONTEXT.md`'s OLD emit sentence, AND the emit-on-`held:land-failed` disjunct in `skills/ → The Done-when carries OLD-absent legs for only two of the three surfaces (`confirms or overrules` = 0, `held:land-failed. for the` = 0). The `resume-and-recovery.md` disjunct is left to the "extended D21 pin" in `skill-doc-contracts.test.mjs` that this very ta (ref: ### Task 1.2: Reference-doc truth — `Done when:` line)
- [Minor] default-flip-old-absent: Task 1.2: "drop the emit-on-`held:land-failed` disjunct from `resume-and-recovery.md`'s `held:land-failed` Outcome-handling bullet AND its `Segmented land (in-band marker) and filing-on-held` bullet (#1801)" — two bullet → Only one of the two named bullets carries any handoff/emit claim. The `Segmented land (in-band marker) and filing-on-held` bullet does (its item (2)). The `held:land-failed` Outcome-handling bullet contains no mention of the handoff block or its emit gate at a (ref: ### Task 1.2: Reference-doc truth — Plan slice, `drop the emit-on-held:land-failed disjunct from resume-and-recovery.md's ... Outcome-handling bullet and its Segmented land ... bullet (#1801)`)
- [Minor] unguarded-new-mirror: PIN-3 (plan line 47) states the whole registration duty for a new reference destination as: "A new `references/` destination joins `reference-link-integrity.test.mjs`'s `QUALIFIED_HEADERS` in the same task", and Task 1.1 → The repo carries a SECOND default-deny registry over the same corpus that a new references file must join, and Task 1.1 names neither it nor its file. skills/_shared/doc-cli-consistency.test.mjs:354 `test('verb-scan placement census (D2): every skills/*/refere (ref: Pivotal constraints PIN-3 (line 47); Task 1.1 Files + slice (lines 158-160))
- [Minor] guard-split-deps-edge: Task 4.1: "`CLAUDE.md`'s Guard architecture section enumerates the floor routes as three names (`no-test`, `unpackaged`, `done-unmet`) — extend that enumeration in the same commit so it names `budget-uncited`". → The enumeration is not in the Guard architecture section. `CLAUDE.md:54` ("Floor scripts exit 0/1/2: 1 = the named route (`no-test`, `unpackaged`, `done-unmet`) …") sits under `## Execution architecture (what a /war phase actually does)` (heading at line 50);  (ref: Task 4.1 — Doc straggler (operator patch 2026-09-06, Q1))
- [Minor] guard-split-deps-edge: Task 12.2 rewrites `skills/war/references/disposition-eligibility.md`'s absorb-eligibility rule (CHANGELOG/README eligible when no version literal, CHANGELOG head heading or Status token moves) with `deps: []` and a Done → That doc surface is pinned by two guards no Phase-12 task can edit. (1) skill-doc-contracts.test.mjs:2201-2210 extracts the absorb block with ``/`disposition:'absorb'`[\s\S]*?(?=\n\n|\n- )/`` and asserts the token `write footprint` — the token lives on the NEX (ref: Task 12.2 — Eligibility doctrine, `--ace` bullet, ADR 0013 second Decision-log line)

## Resolutions applied (grill decisions)
Self-adjudicated under `/war-campaign --afk` (AI-declared). Roots and patches:
- Task 1.1 eviction infeasible as named → candidates re-scoped to `## Diff probe`, `## Gate contract` (minus pinned sentences), the merge-task two-worktree rationale, the 595 B submodule-phase tail; destination fixed to `refiner-recovery.md`; no new references file in Phase 1; the five fence files named; pointer must not add a fifth plugin-root `refiner-recovery.md` link; Done-when runs the three fence suites. D1 and PIN-3 updated (PIN-3 now names the doc-cli-consistency placement census).
- Purpose over-claims → bullet 2 narrowed to land/merge-gate/pin-transfer statuses (D4, D5, D7); bullet 3 and End state 19 rewritten to D21's two-sided form.
- Task 1.2: the resume-and-recovery disjunct lives only in the Segmented-land bullet and is unpinned → new extraction pin; `schemas-manifest.test.sh` mis-anchor → de-mirror `attempts[]` to `run-manifest.md`; `followUps` row → explicit de-mirror; stale "no docs/plans edit" sentence struck (D22, Notes (c) reconciled); CONTEXT.md OLD-emit grep made whitespace-folded and case-insensitive; third OLD-absent leg added.
- Multi-file `grep -c` legs split per file (Task 1.4, End states 3, 16, 17, 20); `/snipe` loop bound gated.
- Release-slot literal at two builds (sweep + terminal pass) → Region H, D20, Task 12.1, End state 18 name both; case-insensitive OLD-absent leg added to Task 12.1's Done-when; Task 12.2 gains `skill-doc-contracts.test.mjs`, the SKILL.md OLD-absent leg, the blockquote-placement rule, `requiresTest: true`.
- Vacuous gates → Task 6.1 and End states 5, 6, 7, 10, 11, 12, 13, 19 gain base-zero fixture-title legs; doc tasks 2.2, 3.2, 4.2, 4.3, 5.2, 9.2, 13.2 gain real Done-whens over their own surfaces (the false "End state N's fixtures are the validators" attributions removed).
- Task 6.3 `requiresTest: false`; Task 10.2 gains `skill-doc-contracts.test.mjs` and a drift row over `intake_lint`/`cmd_bytes_mismatch`.
- `gate_segment` schema pin fixture added (Task 5.1, End state 8).
- D7 read rule made two-sided (`tip_sha:` first must equal the gated sha AND `exit_code:` last) on D7, Task 5.1, Task 5.2, End state 8; backstop row 3 retired with the probe result recorded under Notes.
- Task 13.2 env-died table → genuine two-sentence de-mirror plus a `dispatchAgent` pointer.
- Evidence-consumed rows added (`gate-pin-status.sh`, the 2026-08-25 plan, #2099/#1731/#1664 untracked artifacts).
- Minors auto-fixed: `plus the six` re-check; four `MERGE_RESULT` regex sites; ponytail comments rewritten with an OLD-absent leg; auditor card has no split-panel prose (Task 11.1 reworded); `stampDrainCause` two sites; PIN-2 census restated; D24 rule numbering (rule 7, not 3) and the rule-namespace note; SKILL.md 927 B headroom re-stamped; CLAUDE.md anchor is the Execution architecture section; campaign base noted in Pivotal constraints.

## Adjudications
- Purpose bullet 2 reads `a refiner-reported land, merge-gate or pin-transfer status whose own fields contradict it is refused (D4, D5, D7)` supersedes `every refiner status is cross-checked against its own fields` — intent-vs-plan needsDecision, narrowed to the ruled scope rather than adding a task — AI-declared
- Task 1.1 eviction source = `## Diff probe`, `## Gate contract`, the merge-task two-worktree rationale, the 595 B submodule-phase tail, destination `refiner-recovery.md`, no new file supersedes the submodule arms + recovery prose with a new file permitted — dependency-feasibility / budget-feasibility — AI-declared
- Backstop row 3 deleted; process-lifetime result recorded under Notes; D7 read rule two-sided supersedes the `/red-team` spine runner row — backstop-legitimacy / backgrounded-gate-lifetime — AI-declared
- Task 1.2 de-mirrors `attempts[]` to `run-manifest.md` and the `followUps` row to the engine projection supersedes the `schemas-manifest.test.sh` guard claim — touched-doc-fact-coverage — AI-declared
- Task 13.2 env-died doc = two-sentence rule + `dispatchAgent` pointer supersedes the per-site table — touched-doc-fact-coverage — AI-declared
- Task 10.2 gains `skill-doc-contracts.test.mjs` and a token-extraction drift row supersedes the presence-grep-only gate — touched-doc-fact-coverage — AI-declared
- PIN-3 names both default-deny registries (QUALIFIED_HEADERS + doc-cli-consistency placement census) and forbids a new references file where a task owns neither test supersedes the QUALIFIED_HEADERS-only duty — guard-split-deps-edge — AI-declared
- Version literals: none adjudicated (the plan defers every slot to land time).

## Residual risk
- Every ruling above is AI-declared under `--afk`; the operator should eyeball the Task 1.1 candidate set (which operative refiner prose goes cold) and the narrowed Purpose bullet at the next Checkpoint.
- Task 1.1's pointer form is left to the worker within the exact-four constraint; if neither an extended pointer nor a relative link satisfies both tests, the worker must stop and escalate rather than raise the ceiling.
- The two `warn` probes (literal-drift-at-base, touched-doc-fact-coverage) carry env-gap notes only, demoted by the gate.
- Literals stamped `ffb3ab6` are re-measured at each task base (D12); the campaign base is `ba08a77`.

## Safety
Executed probes ran only in `mktemp` clones (`git clone --no-hardlinks`) and temp dirs; the escape guard's post-run baseline check returned exit 0; the only writes are this report and the plan patch.
