# Red Team — 2026-07-24-land-advance-exit-contract-truth (2026-07-24)
**Verdict:** CLEARED — one Major sufficiency gap (End state 9 did not verify the seed tarball member body) found, patched in place, and re-verified.

Run: Workflow `wf_b1743d49-0b1` (13 agents, 0 errors), artifactKind `impl-plan`, model opus / effort high. Escape guard clean (rc 0). Gate coverage whole: 12/12 probes on-target, 0 off-target, 0 dropped, 11 pass / 1 fail (the fail resolved below).

## Attack surface
Spine (6): claims-vs-reality, executable-proof, coverage-vs-source, consistency-placeholders, dependency-feasibility, intent-vs-plan.
Bespoke (6): header-contract-truth, head-arm-reachable, anchor-existence, unfiltered-sweep, seed-pack-feasibility, ff-topology.
Executed in sandbox: executable-proof, header-contract-truth, head-arm-reachable, unfiltered-sweep, seed-pack-feasibility, ff-topology.
Lead-run doctrine checks: `unguarded-new-mirror` (vacuous — plan introduces no inline mirror; it never touches `workflow-template.js`); `default-flip-old-absent` (see Residual risk); backstop-legitimacy (2 entries — both legitimate, below).
Fallback: none — every analyzed probe dispatched on `Explore`.

## Executed proof
- **Premise holds:** the `cmd_land_advance` header in `skills/war/assets/provision-worktrees.sh` already states the exit contract as 0/2/3/6 — exit 6 = wrong-HEAD precheck refusal (no push, refs untouched); exit 3's triggers include unresolvable HEAD and unresolvable `<new-sha>`. The plan's "align records to the header" premise is sound.
- **Unfixturable waiver refuted:** built the T2.5d fixture in a sandbox (setup_origin_pair + seed_working_branch, resolvable origin-distinct `<new-sha>` committed on the default branch, then `git checkout --orphan <fresh>` → unborn HEAD). `land-advance` dies **exit 3** with die text containing `could not resolve HEAD to a commit`; local follower + origin refs byte-unchanged. Confirmed `git checkout --orphan <existing-branch>` refuses rc 128 (so the orphan branch must be a fresh name). The arm is deterministically reachable — the phase-close "unfixturable" claim was wrong (as the plan's own code-trace argued).
- **Anchors exist:** every named edit anchor resolves verbatim (lesson `**Exit contract:**` / `**How to apply:**` lead-ins + one-line `description` + nested `metadata.keywords`; seed-manifest slug entry; ADR 0023 `Consequence — the point of the change.` lead-in + `the 0/2/3 contract are byte-unchanged` sentence + `(B)` bullet; test-file `T2.5c` / `T2.9` block comments incl. `shared by T2.3/T2.6`).
- **Sweep completeness:** repo-wide file-type-unfiltered `grep -rn '0/2/3'` surfaces exactly the plan's claimed live surfaces; every other hit falls in the §4.4 convention-exempt leave set (historical specs/plans, archived lesson, D15 rows, T2.9 arm-local CLASSIFY citation). No unadjudicated live surface. (The gzip member is invisible to a plain grep — the reason ES9 needed the tar-based body predicate below.)
- **Seed mechanism:** `seed-pack.mjs` exposes `pack`/`verify`; the member has the claimed stripped shape (no phase/tasks/related/originSessionId, no Related line); `seed-pack verify docs/seed` exits 0 and the seed-set suite is green at baseline (pins are pair-integrity, not content).
- **ff-topology robust:** the End-state-6 anchor `git diff --name-only <frozen base>...<integrated tip>` yields the same file union under fast-forward and true-merge integration (frozen base is an ancestor of the tip, so three-dot == the intended two-dot union). Vacuous-safe.

## Findings
### Major
- [Major, needsDecision] **intent-vs-plan sufficiency gap.** The Commander's Intent Purpose names the "portability-stripped twin [that] ships the stale contract to fresh installs via the plugin seed corpus" as a surface that must state 0/2/3/6, but End state 9 verified only the seed **manifest** (`seed-manifest.json` grep + `seed-pack verify` + `seed-set.test.mjs` sha-integrity). None of those inspect the tarball **member body**: the manifest `description` regenerates from the member *frontmatter*, the repo-wide sweep can't see inside the gzip, and verify/seed-set only check sha-integrity. Evidence: `tar -xzOf docs/seed/seed.tar.gz land-advance-push-first-cas-rejected-token.md | grep '0/2/3'` prints the member's `description` and `**How to apply:**` lines (bare `0/2/3`); the `**Exit contract:**` line carries three arms with no exit-6. A worker correcting only the frontmatter would satisfy every ES9 predicate green while still shipping the stale contract in the member body. → **Resolved.**

## Resolutions applied (grill decisions)
- **ES9 sufficiency gap → operator chose "strengthen ES9"** → patched End state 9 (`docs/plans/2026-07-24-land-advance-exit-contract-truth.md`) to add a direct member-body predicate mirroring End state 1's dual check on the repo-root twin: `tar -xzOf docs/seed/seed.tar.gz land-advance-push-first-cas-rejected-token.md | grep '0/2/3' | grep -v '0/2/3/6'` empty **and** grep-positive for the exit-6 wrong-HEAD-precheck arm. Re-verified by an independent `intent-vs-plan` re-run: **PASS** — the member body is now an individually-checkable acceptance predicate owned by Task 1.1 (whose `Files:` includes `docs/seed/seed.tar.gz` and whose slice closes "Verify per End state 9"). The exit-6 grep-positive half is load-bearing: the `Exit contract:` line spells arms as `0 = / 2 = / 3 =`, not a `0/2/3` token, so the emptiness check alone would miss a missing exit-6 arm.

## Adjudications
<!-- Machine-readable: the WAR Lead reads these rows and threads them into the auditor prompts (auditPrompt() and the gate-audit seats). Version precedence: task instruction > red-team adjudication > plan body literal. Leave empty (or omit) when no authoritative value was adjudicated — an empty block is byte-identical no-op for the auditor. -->
- None — no authoritative value (version / release-slot literal) was adjudicated; the sole resolution strengthened a validation criterion (End state 9), not a plan literal the auditors must prefer.

## Residual risk
- **Backstops legitimate (both).** (1) `war-memory.mjs lint docs/learnings/` is CI-only, never gate-composed — a plan End state citing it would be structurally SOFT (recorded lesson `gate-artifact-never-includes-war-memory-lint`); runner CI (`memory-audit.yml`) on the PR. (2) Integrated-tip `0/2/3` sweep re-check is a whole-repo property over three tasks that each adjudicate at their own frozen base — only the integrated tip proves the union; runner the Lead at Phase-1 land. Neither is AI-declared.
- **`default-flip-old-absent` doctrine — no committed OLD-absent guard, by design (accepted).** The plan corrects existing stale mirrors to an already-canonical source (the script header); it ships no standing drift guard binding the records to the header (spec decision 2, §9 — a general lesson-body guard is a separate, unrequested proposal). A blanket committed `0/2/3`-absent test would false-positive on the convention-exempt historical prose (specs/plans, D15's pinned dated prose, the archived lesson); the correct mechanism is the Lead-run *adjudicated* sweep (backstop 2), which distinguishes live from historical. Deferral accepted.
- **MEMORY.md projection** deliberately out of scope (untracked local root, refreshed by the servitor / next `/lessons-learned`).
