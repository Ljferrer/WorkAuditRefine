# Workflow compatibility: #2299 / #2300

Base: `668e4ff990f1c689fcf9710768c3d73c2601920c`. Branch: `codex/workflow-compatibility-2299-2300`. Patch: 0.21.14. Snipe budget: four audit/fix cycles; none launched yet. Requested profile retained: gpt-5.6-sol / high, `4 auto`.

- #2299: the API refuses PIN_TRANSFER's top-level allOf before the refiner starts. Removed the combinators; kept the flat properties and consumer guards. Sweep: every schema dispatched by the behavioral harness is checked for allOf/anyOf/oneOf at the root. Nested finding conditionals remain supported. The consumer still checks destination, independent Git proof, patch/content equality, upstream membership and fresh re-audit where required. Schema documentation updated.
- #2300: gateEpoch eagerly reads Date.now and Math.random, forbidden by the Workflow sandbox. Derive from encoded runId and phase plus the existing per-dispatch counter; mktemp retains physical allocation. Replay uses the same prefix; fresh Recovery runId, phase and dispatch separate captures. Existing return-path admission and same-tip retry rejection remain unchanged.
- Before-fix proof: targeted test run failed both tests: `pin-transfer:t1: unsupported top-level allOf` and `Workflow refuses Date.now()`, zero tasks landed. After-fix: same two tests pass; sandbox fixture reaches land, repeats identical captures under identical args and distinguishes fresh runs and phases.
- Consequences: no new config/seat, no weakening of audit/Git proof authority, no release or merge. Live hosted Workflow execution is not available in this Codex harness; tests model the two API constraints reported by the operator.

- Mutation evidence: see `mutations.json`. All five substitutions fail their independent assertions. Run/phase checks compare the capture component, not different parent worktree paths; dispatch-counter removal fails the existing same-tip baseline-retry regression.
- Pre-panel validation: targeted regressions pass; version slots 10/10 and redaction lint pass. Full JS and shell suites are running. The first full run observed a release-history pointer mismatch, corrected by restoring the original pointer and verified by the version suite.


## Cycle 1 and verified repairs

Snipe reviewed `ec949bb9dda7d7358a74905e49b6518a671af0fb`: complete, stable, four high-confidence approvals (correctness, cascading-impact, test-fidelity, security), with two Minor findings. Exact report: [cycle-1.md](cycle-1.md); scope/seat manifest: [cycle-1.json](cycle-1.json).

- Receipt evidence: reproduced missing and unverified patch IDs in successful mismatch receipts. The probeRow producer serves transferred, mismatch and already_upstream receipts; all now use independently measured proof values and preserve empty strings. Existing transfer checks still govern approval; no new status or routing policy. Three response-shape regressions failed before and pass after. Reverting either receipt field to probe data fails the regression. Schema reference now names this provenance and the meaningful empty value.
- Schema contract: strengthened actual-dispatch checks and added PIN_TRANSFER to the existing evaluated-schema census. Legal statuses/minimal error replies remain valid; missing/unknown status, non-object responses and wrong evidence-field types are rejected. Removing object type, required status, enum or an evidence type each fails its assertion. The report's literal whole-schema deletion example is broader than needed: the consumer also reads the enum; the independently reproduced gap is loss of wire-only requirements while the consumer's enum remains.
- Focused verification after repairs: 50/50 tests passed, including schema, transfer/re-audit, receipt and deterministic Workflow regressions. Six negative mutations: [cycle-2-mutations.json](cycle-2-mutations.json).
- First full validation: 2,216/2,217 JS tests passed (sole README pointer failure corrected; affected version suite 10/10); all 31 shell suites and redaction lint passed. A fresh full JS run covers the revised receipt candidate. No hosted Workflow run is claimed.
- PR opened as draft: https://github.com/Ljferrer/WorkAuditRefine/pull/2301. Audit budget: one panel completed; next is cycle 2 of at most 4.


## Cycle 2 and launch-identity repair

Snipe reviewed `aa08f21a1f81a65d2520d3da03abde1dad39e49c`: complete and stable. Correctness/test-fidelity approved; cascading-impact/security requested changes on the same Major class: explicit-path launches can omit identity and admit an earlier gate artifact. Exact report: [cycle-2.md](cycle-2.md); manifest: [cycle-2.json](cycle-2.json). Full JS on that candidate passed 2,221/2,221.

- Reproduced the reported consequence using an explicit submodule worktree: two launches without runId forwarded the first launch's gate_log_path into the second launch's evidence consumers. The prior-path rejection assertion failed before the guard.
- Rule: every launch requires a nonempty string runId and a safe-integer/nonempty-string phase.id, regardless of explicit, derived or empty tasks. Refusal is at entry, before agent dispatch/Git mutation. Fresh launches mint new run IDs; journal replay retains identity. Explicit paths still bypass path derivation, but cannot replace launch identity. Recovery already mandates a fresh runId in resume-and-recovery.md; normal Lead provisioning already supplies it. Legacy explicit-path fixtures now pass identity, and the formerly weak doesNotReject fixture proves a worker actually spawns.
- Tuple encoding preserves the boundary between run and phase values. Delimiter-shaped values cannot alias. Guard regressions cover missing/null/blank, non-scalar and invalid numeric inputs, explicit/derived/empty task shapes, and actual stale-log rejection by downstream evidence consumers. All thirteen guard/encoding mutations fail: [cycle-3-mutations.json](cycle-3-mutations.json).
- Consumer/sibling sweep: the shared newGateCapture/admitGateResult path covers task, retry, recovery, evidence and land captures. Error guidance no longer suggests explicit paths can cure missing identity. The schema reference documents the compatibility tightening. The generic assert-args-complete interpolation floor remains scoped to pt-field derivation; its comment now identifies the engine's separate unconditional identity guard.
- Validation caught three stale fixture/registry expectations plus their dependent negative control: empty-phase identity, documentation proximity, and the changed diagnostic literal head. Updated their fixtures/placement/registry without weakening the assertions; all nine affected and identity tests pass. A final full JS and shell run will validate the complete revised candidate.
- Budget: two panels completed; the next panel is cycle 3 of at most 4. PR remains draft until the substantive finding is closed by verification and review.


## Cycle 3 and producer/consumer closure

Snipe reviewed `a874bd6b366166b4eb6da99564347dc7909ac9ac`: complete and stable; test-fidelity/security approved, correctness/cascading-impact requested changes. The findings collapse to two classes: dated logical IDs can recur under canonical callers, and phase identity accepted values rejected by provisioning. The explicit-path runbook exception is part of the first class. Exact report: [cycle-3.md](cycle-3.md); manifest: [cycle-3.json](cycle-3.json). That candidate's full validation passed 2,225 JS tests and all 31 shell suites plus redaction lint.

- Caller root cause: the prior repair checked presence but did not close the date-based minting/staging contract. A four-line Lead-side `new-run-id.mjs` now emits a random UUID. New WAR runs/plain reruns/Recovery relaunches invoke it; normal phase progression and journal replay retain identity. The command runs outside Workflow. Runtime stays deterministic. The canonical launch procedure persists one ID across args, ledger, manifest and paths before staging; fresh directories prevent the stager's write-if-absent arm from reusing old embedded args. Recovery keeps Git/owned-ref continuity and carries the predecessor's phase telemetry into the new manifest before archival.
- Producer/reader sweep: Setup and launch paragraph, staging reference, Recovery procedure, manifest, schema sheet, review's run selector and glossary now agree. ADR 0037's dated amendment explicitly supersedes same-day recurrence in its older decision and referenced historical plan/spec; frozen comparison refs remain untouched. No runtime parses a plan/date from runId; human display names still derive from the separate planSlug. Old IDs remain readable history/replay input.
- Independent regression: invokes the actual UUID CLI twice for the same logical plan/phase, stages both actual embedded payloads under the minted run directories, executes the staged scripts with no supplied args, and injects the first attempt's gate path into the second. Fresh attempt rejects it; replay of identical staged bytes retains the capture. A valid-but-constant UUID producer fails this regression.
- Provisioning consumer: entry accepts nonnegative safe integers or digit-only strings, matching ensure-integration/teardown-phase's numeric grammar. Negative/nonnumeric/fractional/non-scalar/unsafe-number cases refuse before dispatch; zero and digit strings retain their existing shell acceptance. The live provisioning CLI independently rejected -1, 4b, y, 1.5 and a space-prefixed digit before Git access. Removed the prior mocked-success string-phase fixture; five legitimate scalar fixtures replace it.
- Eleven final-repair mutations fail their assertions: [cycle-4-mutations.json](cycle-4-mutations.json). Includes numeric grammar, scalar type/arms, safe number, repeating UUID producer, and each of five canonical procedure pointers. Documentation-contract, link, CLI and prompt-budget suites pass. Added the new reference to the default-deny placement registry after its full-suite failure, then verified the affected suites green.
- Budget: three panels completed. The next is the fourth and final permitted panel. No fifth panel will be launched.
