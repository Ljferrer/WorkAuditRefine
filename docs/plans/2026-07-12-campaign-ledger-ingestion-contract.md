# Campaign-ledger ingestion contract — first-table-only roadmap ingestion, backticked-Files contract, bare-path fallback

Source spec: docs/specs/2026-07-12-campaign-ledger-ingestion-contract-design.md
Issues addressed: #738, #739, #816

## AI-Commander's Intent

- Purpose: make `campaign-ledger.mjs init --roadmap` work on the pipeline's own ratified output — a `/war-machine` roadmap (with its chain table) and a `/war-machine` plan (with possibly-bare `Files:` paths) must ingest cleanly — while pinning the producer/consumer contract on both sides so they cannot silently drift apart again.
- Method: structural, name-blind fixes only (no `../specs/` or `-design.md` heuristics — ratified ceiling (c) stands). Consumer side: first-table-only, code-fence-blind table ingestion in `resolveRoadmapPlans`, plus a backtick-presence-keyed `isPathShaped` fallback in `extractFiles` that never dilutes backtick precision when any backtick is present. Producer side: the war-strategy §2 `- Files:` template line states the backtick contract and gains a structure-test lock; a new advisory `bare-files-path` lint rule ships AND is wired into the `/war-machine` drafter step so it provably reaches drafters; war-machine §3 mandates plan-index-table-first with a matching lock in `war-pipeline-structure.test.sh`. Both fail-loud throw messages become self-diagnosing (first-table-only clause + `--plans` escape hatch; backtick/comma authoring hint) and are content-asserted. Every comment describing changed mechanics (ceilings block, decoy fixture comment, throw messages) lands in the same commit as its mechanism. Fail-loud posture preserved throughout.
- End state:
  1. Chain-table regression fixture: a roadmap with a plan-index table (2 plans) followed by a chain table linking two on-disk `../specs/*-design.md` files — one spec with NO `Files:` block, one WITH a parsable backticked `Files:` block — plus a backticked `.md` decoy in a chain-table cell. `init --roadmap` yields exactly the 2 plan entries in row order; neither spec path nor the second-table decoy appears in the ledger. RED without the first-table change via BOTH mechanisms: the no-Files spec's unparseable-footprint throw AND the with-Files spec's phantom ledger entry (a direct catch, not throw-absence).
  2. In a sandbox, `init --roadmap docs/roadmaps/2026-07-11-war-resilience-and-recovery-roadmap.md` succeeds with exactly 4 entries (the shape that today ingests 13 and throws). In-task worker probe with evidence in the done report — NOT a committed test (a repo-doc-reading test rots and breaks the asset's cwd-independence). It validates the combined task: two of the four linked plans carry bare `Files:` lines today, so it needs both fixes.
  3. `extractFiles` fallback keyed on backtick PRESENCE, not zero-files: a zero-backtick `Files:` block yields the comma-split `isPathShaped` tokens; a block containing ≥1 backtick anywhere stays backtick-only — the fixture whose sole backticked token is non-path-shaped (`` `TODO` ``) yields `[]` and init without positions throws. A zero-backtick block with space-separated (uncomma'd) multi-paths also yields `[]` and throws. A plan with no `Files:` block still throws. RED without the fallback (unit-level `extractFiles` tests — structurally independent of the parser change).
  4. `resolveRoadmapPlans` is code-fence-blind: lines inside ``` fences are invisible to BOTH accepted forms. Fixture: a fenced example table carrying a `.md` link before the real plan-index table ingests only the real table (RED without fence-skip). A stray leading-`|` prose line before the plan index start-and-closes the table and 0-plan-throws — a fixture pins that semantics so the failure is loud and named.
  5. The 0-plan throw message names first-table-only ingestion AND the `--plans` explicit-seed escape hatch; a content assertion goes red if either clause is deleted; the existing `/0 plans/` + path assertions stay green unmodified in kind. The unparseable-footprint throw message gains the authoring hint (paths must be backticked and comma-separated, per war-strategy §2), content-asserted; the existing `/unparseable|explicit position/i` assertion stays green.
  6. `plan-literal-lint.mjs` has a fifth `LINT_PATTERNS` rule `bare-files-path` (with a small `stripBackticks` entry guard in `lint()`: backtick spans are removed before the rule's regex runs). Fixtures: flagged — bare path on a `- Files:` line and on the indented `  - Files:` template form; clean — all-backticked line, `Files:` mentioned mid-prose (not a `- Files:` line), and a backtick span that itself contains commas and a path. CLI still exits 0 by default. The `LINT_PATTERNS: four named patterns, no gaps` structural test becomes five, same commit.
  7. The war-strategy §2 plan-template `- Files:` line shows backticked placeholder paths and states the extractFiles-reads-backticks contract; `war-strategy-structure.test.sh` gains a `check_f` lock on the rewritten line (presence-only, sibling-consistent; lock fragment chosen without embedded double quotes per the quote-anchor-fragility lesson).
  8. `skills/war-machine/SKILL.md` §3 step 1 states the plan-index table MUST be the document's first table (auxiliary tables after it; the ledger ingests only the first table), locked by a new check in `war-pipeline-structure.test.sh` in the same commit. `skills/war-machine/SKILL.md` §2 gains a drafter-step lint invocation: the drafter runs `node skills/war-strategy/assets/plan-literal-lint.mjs <plan>` on each finalized plan and surfaces hits in the conversion report — closing the delivery-path gap (today only war-strategy step 5 runs the lint).
  9. The decoy test stays green and its fixture-builder comment names `assertOrderable`'s unparseable-footprint throw as the first-line catch, the exactly-2 assertions as the backstop, and warns against ever giving the decoy a `Files:` block (#816). Comment-only.
  10. The ceilings comment above `resolveRoadmapPlans` is rewritten to the first-table + fence-skip mechanics: (a) narrowed to first-link-per-row within the first table; (c) superseded for the chain-table shape but retained for a roadmap whose first table links non-plans. The `extractFiles` comment documents the fallback ceilings: `isPathShaped` over-acceptance (dot-suffixed prose tokens; bounded — worst case an over-wide footprint, never a throw) and the parenthetical keep-rule asymmetry (only a sole BACKTICKED path token survives `stripAnnotations`; a bare parenthesized path is stripped before the fallback — accepted, fail-loud backstopped).
  11. Token sweeps done per spec VC 7–9 (`out of contract` / `first markdown link` / `ENOENT` in the two ledger files; `Files:` across the three producer SKILL.md surfaces; `resolveRoadmapPlans` / `plan-index table` across `skills/`), each grep followed by the manual same-scope title/comment survey, stragglers listed as survey-derived corrections.
  12. `CONTEXT.md` carries the new domain term **Plan-index table**.
  13. Full suite green: `node --test 'skills/**/*.test.mjs'` and the self-discovery shell-test gate (reference `resolveGate` in `war-config.mjs`; never enumerate suites — the resolved gate already runs every `*.test.sh`, including the two structure suites this plan extends).
  14. All four version slots bumped in lock-step in a trailing release phase (directive form below).

## Build order (for /war)

- Phase 1 — Contract on both sides (two parallel, file-disjoint tasks; no waves)
- Phase 2 — Release (version bump, lands last)

## Phase 1 — Contract on both sides

### Task 1.1: Ledger consumer — first-table + fence-blind ingestion, backtick-presence fallback, self-diagnosing throws, comment lock-step

- Files: `skills/war-campaign/assets/campaign-ledger.mjs`, `skills/war-campaign/assets/campaign-ledger.test.mjs`
- Plan slice:
  - In `resolveRoadmapPlans`: extend the existing single loop with a fence toggle (`inFence`, flipped on ```-prefixed lines; fenced lines are invisible to both forms) and the two-flag first-table state machine — ANY leading-`|` line (link-bearing or not) opens the table; the first non-table line after ≥1 table line sets `tableClosed`; leading-`|` lines are considered for link extraction only while `tableClosed` is false. Bare bulleted/numbered `.md` lines (legacy form 1) remain accepted anywhere outside fences. Single pass, row order preserved. Extend the 0-plan throw message: table ingestion is first-table-only, and explicit seeding via `--plans` is the escape hatch. Extend `assertOrderable`'s unparseable-footprint message with the authoring hint (backticked, comma-separated `Files:` paths per war-strategy §2). Rewrite the ceilings comment block in the same commit per End state 10.
  - In `extractFiles`: the fallback trigger is backtick ABSENCE in the cleaned block (design-tree wording wins over the spec-Mechanics zero-files wording — constraint 6): if the annotation-stripped block contains no backtick character at all and is non-empty, iterate the comma-split segments, trim, accept whole segments passing `isPathShaped()` (dedup as today). If ≥1 backtick is present anywhere, extraction stays backtick-only — a block whose only backticked token is non-path-shaped yields `[]` and surfaces via the assertOrderable throw. Document both fallback ceilings in the `extractFiles` comment (End state 10).
  - Tests in `campaign-ledger.test.mjs`: chain-table regression fixture per End state 1 (one chain-linked spec WITH a parsable backticked `Files:` block + second-table backticked decoy — direct phantom-ingestion catch); fence-skip and stray-pipe fixtures per End state 4; backtick-presence fallback fixtures per End state 3 (bare-comma'd, backticked-non-path-only, space-separated-bare, mixed-defers-to-backticks, no-`Files:`-block); throw-message content assertions per End state 5; reword the fixture-builder comment above `setupCanonicalRoadmap` per End state 9 (comment-only — the decoy keeps proving within-first-table backtick precision; the new second-table decoy pins the structural-ignore mechanism separately).
  - RED-first evidence: the two mechanisms' RED claims are structurally independent — the fallback tests call `extractFiles` directly (untouched by the parser change), and the chain-table fixture goes red without the parser change via both the no-Files throw and the phantom entry regardless of the fallback. The worker's done report additionally includes two delete-the-feature bracket runs (each mechanism's hunk reverted, its tests shown red); the auditor verifies independence structurally at the pinned `audit_sha` — no stash access needed.
  - Sandbox verification (End state 2): run `init --roadmap` against `docs/roadmaps/2026-07-11-war-resilience-and-recovery-roadmap.md` into a throwaway campaign dir; assert exactly 4 entries; evidence (command + output) in the done report.
  - Token sweep (spec VC 7): grep both files for `out of contract`, `first markdown link`, `ENOENT`; handle every match against the new mechanics; then hand-scan both files' same-scope comments and test titles (every fixture-builder comment and every test name describing table extraction or footprint failure modes); list stragglers as survey-derived corrections.
- requiresTest: true
- requiresPackaging: false
- deps: []
- target repo: superproject

### Task 1.2: Producer surfaces — template contract, structure locks, lint rule + drafter wiring, war-machine clause, glossary

- Files: `skills/war-strategy/SKILL.md`, `skills/war-strategy/war-strategy-structure.test.sh`, `skills/war-strategy/assets/plan-literal-lint.mjs`, `skills/war-strategy/assets/plan-literal-lint.test.mjs`, `skills/war-machine/SKILL.md`, `skills/war-machine/war-pipeline-structure.test.sh`, `skills/war/SKILL.md`, `CONTEXT.md`
- Plan slice:
  - Rewrite the §2 plan-template `- Files:` line in `skills/war-strategy/SKILL.md` to show backticked placeholder paths with a terse annotation that every path is backticked (and comma-separated) because the campaign ledger's `extractFiles` reads backticked tokens. The current line (`  - Files: <exact paths this task touches>`) carries no `check_f` lock, so the edit is free; add a new `check_f` verbatim lock on the rewritten line in `war-strategy-structure.test.sh` beside the existing template-internal locks, in the same commit. Lock is presence-only like every sibling (accepted ceiling — see Notes); the worker drafts the exact bytes, choosing a fragment free of embedded double quotes; the audit roster ratifies the wording with the commit. Keep every other locked line byte-identical.
  - New `LINT_PATTERNS` entry `bare-files-path` in `plan-literal-lint.mjs`: `requireOnLine` anchored to a leading (optionally indented) `- Files:` prefix; new optional entry guard `stripBackticks` in `lint()` (remove backtick spans from the line before running the entry's regex — a ~3-line engine extension in the existing array idiom); the rule regex flags a remaining path-shaped token (contains `/`, or a dot-extension token). The predicate is a DELIBERATELY LOOSE, independent advisory heuristic — NOT a mirror of the ledger's `isPathShaped` (see Notes); the rule comment states this and names the fail-loud ledger throw as the real backstop. Message points at the §2 backtick requirement. Same advisory posture as siblings (report-only, exit 0 default, `--strict` opt-in).
  - Tests in `plan-literal-lint.test.mjs`: the five fixtures of End state 6 (flagged bare / flagged indented-template-form bare / clean all-backticked / clean mid-prose mention / clean comma-bearing backtick span). Update the `LINT_PATTERNS: four named patterns, no gaps` structural test to five patterns including `bare-files-path` — same commit, per the relaxed-assertion-and-title-move-together lesson.
  - `skills/war-machine/SKILL.md` §3 step 1: add one clause — the plan-index table MUST be the first table in the document; auxiliary tables (issue→spec→plan chain, shared-file contention) come after it; the ledger ingests only the first table. Add a matching lock (check-style grep on a stable fragment of the clause) to `war-pipeline-structure.test.sh` in the same commit.
  - `skills/war-machine/SKILL.md` §2: add the drafter lint step — after finalizing each plan, run `node skills/war-strategy/assets/plan-literal-lint.mjs <plan>` and surface hits in the conversion report (advisory, never blocks). This is the surface change that makes the spec's "reaches /war-machine drafters" justification true.
  - `CONTEXT.md`: add the domain term **Plan-index table** — the first table in a roadmap document; the only table `campaign-ledger.mjs init --roadmap` ingests; all later tables are ledger-inert.
  - Token sweeps (spec VC 8–9): grep `skills/war-strategy/SKILL.md`, `skills/war-machine/SKILL.md`, `skills/war/SKILL.md` for `Files:` and handle every match restating the Files contract without the backtick requirement; grep the `skills/` tree for `resolveRoadmapPlans` and `plan-index table` and confirm consistency with first-table-only ingestion (pre-check: zero prose matches exist today — the sweep is a re-verification at land state). After each grep, hand-scan the same-scope surrounding prose (the §2 "Reference the live artifact" Files/locators bullet, the war-strategy roadmap-template section, war-machine §3 and handoff prose) and list stragglers as survey-derived corrections. Stragglers inside this task's Files list are fixed here; any outside it are listed in the done report for Lead adjudication, never silently edited.
- requiresTest: true
- requiresPackaging: false
- deps: []
- target repo: superproject

## Phase 2 — Release

### Task 2.1: Version bump

- Files: `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`, `README.md`
- Plan slice: bump all four slots to the next free patch above the live integration base at land time, per the /war-strategy §2 next-free-patch convention (`plugin.json` `version`, `marketplace.json` `metadata.version` and `plugins[0].version`, `README.md` `## Status` replace-in-place). `version-slots.test.mjs` is the lock-step arbiter. Release blurb: one line on first-table-only roadmap ingestion + the backticked-Files contract with bare-path fallback; describe what the parser now does, not repo-name semantics. This plan stacks FIFTH this run — expected integration base is the tip after the four prior plans (war-launch-entry-validation, audit-gate-evidence-fidelity, floor-script-correctness, confinement-scope-hooks) have landed, each carrying its own release phase on the same four slots. Standalone fallback: a plan run through plain `/war` resolves the next free patch from the four slots itself.
- requiresTest: false
- requiresPackaging: false
- deps: []
- target repo: superproject

## Deferred validations (backstops — AI-declared)

- End-to-end `/war-campaign` init on the next real `/war-machine` roadmap (chain table present, plans with backticked Files) succeeds without hand-seeding `--plans` · why deferred: needs a future campaign run; the in-task sandbox live-corpus probe in Task 1.1 covers the committed corpus · runner: the next `/war-campaign` invocation.
- `bare-files-path` lint hits actually surface in a `/war-machine` conversion report via the new §2 drafter step · why deferred: exercised only by a future `/war-machine` run; unit tests prove the rule and the SKILL.md lock proves the instruction, not the live execution · runner: the next `/war-machine` conversion.

## Notes / conscious deviations

- **Fallback trigger (grill Q1):** backtick PRESENCE wins over the spec-Mechanics "zero files" wording — the design-tree row is the ratified resolution (constraint 6: backticks stay the precision mechanism). A block whose only backticked token is non-path-shaped yields `[]` and throws; End state 3 carries the exact fixture.
- **Comma-only bare contract (Q2):** ratified — comma separation matches the existing backticked split. Space-separated bare paths knowingly stay unparseable; the extended unparseable-footprint hint and the lint message both route drafters to comma-separated backticked paths (End states 3, 5, 6).
- **Parenthetical keep-rule asymmetry (Q3):** kept — no bare-path twin. A bare parenthesized path is an annotation shape; failure mode is a narrow/empty footprint surfacing via the fail-loud throw. Documented as an `extractFiles` ceiling (End state 10).
- **Code fences (Q4):** fence-skip chosen over a documented ceiling — ~4 lines eliminating a silent-wrong-ingestion class (a fenced linked table would otherwise BECOME the first table); spec is silent, this is a plan-added mechanic with its own RED fixture (End state 4).
- **"Table closed" semantics (Q5):** ratified per spec wording — any leading-`|` line opens the table (link or not); any non-table line after ≥1 table line closes it permanently. The stray-pipe-prose disqualification shape gets its own fixture pinning the 0-plan throw (End state 4).
- **Chain-table fixture strength (Q6):** one chain-linked spec carries a parsable backticked `Files:` block so a first-table regression is caught as a phantom ledger entry directly, not by throw-absence — survives the fallback landing in the same task.
- **Live-corpus check (Q7):** in-task worker-run sandbox probe with done-report evidence, not a committed test (repo-doc rot + cwd-independence) and not a deferred backstop (it executes inside the task). It discriminates the combined task, not #738 alone — two of the four linked plans (`drift-guard-tightening`, `servitor-redaction-at-source`) have bare `Files:` lines today. Per-fix discriminators are the committed unit tests.
- **RED-first independence in one commit (Q8):** structural — fallback tests are unit-level `extractFiles` calls; the chain fixture reddens via the no-Files spec's throw independent of the fallback. Worker adds two delete-the-feature bracket runs to the done report; auditor verifies structurally at `audit_sha`.
- **Template line + lock coupling and gate (Q9):** one task (1.2), one commit. The resolved gate (`resolveGate`) already discovers and runs every `*.test.sh`, and the test floor's default pattern accepts `**/*.test.sh` diffs as test evidence — `requiresTest: true` stands, no explicit gate step needed.
- **Lock anchoring (Q10):** presence-only `check_f`, consistent with every sibling lock; the presence-anywhere ceiling (structure-test-check-f lesson) is accepted, not fixed here. Exact bytes drafted by the worker (fragment without embedded double quotes), ratified by the audit roster with the commit.
- **Lint predicate (Q11):** deliberately-loose independent heuristic, NOT a mirror of `isPathShaped` (which is unexported; importing would add a cross-skill dep edge and serialize the phase). Declared non-mirror in the rule comment: divergence costs advisory noise or a missed nudge, backstopped by the ledger's fail-loud throw. No drift-guard row because there is no sync contract.
- **Lint delivery path (Q12):** the spec's "reaches /war-machine drafters" claim was false as written — only war-strategy step 5 runs the lint. Task 1.2 adds the §2 drafter lint step to `skills/war-machine/SKILL.md` (file already in the surface table; new section-touch recorded here). Indented-`- Files:` and comma-bearing-backtick-span fixtures added (End state 6).
- **Throw-message prose (Q13):** both extended messages get content assertions (delete-the-clause red). Existing assertions (`/0 plans/` + path; `/unparseable|explicit position/i`) are regex/substring matches that survive extension — verified against the current test file; no same-commit breakage expected, and any retitle follows the relaxed-assertion lesson.
- **War-machine clause lock (Q14):** locked, not advisory — a check in `war-pipeline-structure.test.sh` lands in the same commit as the clause, mirroring the war-strategy side's treatment. `war-pipeline-structure.test.sh` added to Task 1.2 Files (not in the spec surface table).
- **Second-table decoy (Q15):** added to the chain-table fixture — pins the structural-ignore mechanism separately from the within-first-table backtick-precision mechanism the on-disk decoy proves.
- **Sweeps ownership (Q16):** VC 7 is an in-task Task 1.1 step; VC 8–9 are in-task Task 1.2 steps. `skills/war/SKILL.md` added to Task 1.2 Files as sweep-owned (its one `Files:` mention today describes workflow threading, not the extraction contract — likely no edit). Stragglers outside a task's Files list are reported for Lead adjudication, never silently edited — preserving file-disjoint parallelism.
- **Release phase (Q17):** plan-carried (Phase 2, directive form). Spec is silent; this is a behavior-bearing plugin change and every prior plan in the campaign carries its own release phase.
- **Rollback (Q18):** the extended 0-plan throw names the `--plans` escape hatch (the #738 documented workaround). Task 1.1 lands as one commit — mechanics, comments, throw messages, and tests are a single revert unit; nothing else shares it (Task 1.2 is a separate commit, release a separate phase).
- `CONTEXT.md` is not in the spec §5 surface table but §6 mandates the new domain term — added to Task 1.2 deliberately.
- The `LINT_PATTERNS` count-test update (four → five) and the small `stripBackticks` engine guard are implied by spec §5 "Rule tests" / §4 rule shape; called out explicitly so rule, guard, and structural test land in one commit.
- Contention (fifth in stack): all Phase 1 files are untouched by the four prior plans of this campaign. The only shared files are the release slots, contended with all four prior plans; the directive form (next free patch above the live base at land time) absorbs that, and version literals in any roadmap row are non-authoritative.
- Legacy spec-index roadmaps (first table links specs) remain out of contract exactly as before (spec §8). No ADR (spec §7).
- Predecessor-consistency: heading is `## AI-Commander's Intent` per ADR 0014 (--afk conversion), matching the other two AFK-converted 2026-07-12 plans; Purpose/Method/End-state tone, backstop heading form, and release-directive form match predecessors. No divergence found.

## Open decisions

None — all grill survivors self-adjudicated above (AFK, ADR 0014); the spec's design tree plus these Notes fully resolve conversion.
