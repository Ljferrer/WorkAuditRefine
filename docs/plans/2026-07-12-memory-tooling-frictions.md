# Memory tooling frictions — projection dedup + budget headroom, mining-citation hygiene, migrate pre-flight guard

Source spec: docs/specs/2026-07-12-memory-tooling-frictions-design.md
Issues addressed: #820, #821, #822, #823
Stacks on: `docs/plans/2026-07-12-land-path-verification-hygiene.md` (ADR 0011 — this plan lands eighth; overlap with predecessors is only `docs/learnings/` lesson content and the four release slots, both absorbed by stack order)

## AI-Commander's Intent

- **Purpose:** Restore MEMORY.md projection headroom and close four verified memory tool-chain frictions — promoted lessons double-counting one fact as two rows against the single hard cap (#821), repo rows starving the cap with no root-aware remediation lever (#820), survey-corps mining citations self-tripping the fail-closed redaction lint (#822), and the `/lessons-learned` migrate pre-flight failing on the most common state, an absent config (#823) — without touching promotion semantics (ADR 0022), lint posture, the budget constants, or the `war-config.mjs` CLI contract.
- **Method:** Fix #821/#820 mechanically in `skills/_shared/war-memory.mjs` only, as **one task** (both mechanics edit the same two files — the code-boundary rule forbids splitting them): slug dedup inside `buildProjection` (repo copy wins; the **deduped** set feeds `archiveCandidates`) plus one root clause in the `archiveCandidates` comparator (equal tier → local before repo → oldest). The dedup key is bare slug: cross-root slug collision **is ratified as** same-fact (Gate-2 promotion is the only cross-root copy path), stated in the rewritten `buildProjection` invariant comment. Then recover headroom with a reviewed, lint-gated `docs/learnings/` description diet + explicit-slug archives, measured deterministically against a **scratch empty local root** (repo-rows-only; the live two-root check is a deferred backstop). Fix #822 and #823 in prose at the sole exposed surfaces: `skills/survey-corps/SKILL.md` mandates **slug-only citations in issue bodies/titles** (the `<local-root>` placeholder survives only as skill-internal prose, never resolved), and `skills/lessons-learned/SKILL.md` gains an explicit `test -f` absent-config branch ahead of the `--fill-defaults` invocation, drift-guarded by a new `lessons-learned-doc-contract.test.mjs` lock. Every mechanic carries its spec-mandated token sweep; grep is a floor — hand-scan same-scope prose/comments and record hits + stragglers (empty lists stated) as a `Survey:` block in the implementation commit message body, auditor-verifiable at the pinned `audit_sha` via `git log`.
- **End state:**
  1. `node --test skills/_shared/war-memory.test.mjs` green, including four new cases: (a) two-root same-slug fixture → exactly one projection row, carrying the `[repo]` marker; (b) a local-only control slug still renders; (c) a shadowed-duplicate fixture **padded past `WARN_BYTES` via the file's existing long-description padding idiom** so `buildProjection` returns a non-empty `candidates`, asserting the shadowed local slug is absent — the assertion goes through `buildProjection`, proving it feeds `archiveCandidates` the deduped set (the #821 claim), not through a direct `archiveCandidates` call; (d) an equal-tier local/repo pair **with the local record's date strictly newer than the repo record's** orders local-first — with the root clause deleted, the existing oldest-first tiebreak orders repo first, so the test reds without the feature.
  2. The pre-dedup invariant prose is gone in the same commit as the code: the `Invariant hot ≡ indexed` comment inside `buildProjection` and the `render: hot ≡ indexed — every hot lesson one row` test title state the post-dedup invariant ("every hot *fact* gets exactly one row; a promoted lesson's local twin is shadowed by its repo row; cross-root slug collision ⇒ same fact — Gate-2 promotion is the only cross-root copy path").
  3. After the `docs/learnings/` remediation, `node skills/_shared/war-memory.mjs render-index --local <scratch-empty-dir> --repo docs/learnings` (scratch `--local`; render output measurement-only, discarded) reports projection bytes ≤ 20,400 (≥ 4,000 B headroom under `HARD_BYTES`), lines ≤ 200, verdict **`warn` — not `refuse`**. The steady-state `warn` verdict (20,400 > `WARN_BYTES` = 17,000) **is the designed post-remediation state**, not a failed remediation: the constants are ratified (spec constraint 3) and cutting below 17,000 B would gut roughly a third of the repo corpus.
  4. `node skills/_shared/war-memory.mjs lint docs/learnings/` exits 0; every archived lesson is a move into `docs/learnings/archive/` + note (no deletion in the diff), had `inboundCiters` checked (hubs stay), and still returns from `query` by its slug terms. Every **description-tightened** lesson passes a retrieval check: the dropped discriminating terms already sit in (or are added to) `metadata.keywords`, and the lesson still returns from `query` by its former description's key terms.
  5. `skills/survey-corps/SKILL.md` Step 0 "Mine" step 3 mandates **slug-only** lesson citations in issue bodies/titles for local-root lessons (the greppable `Lesson: <slug>` line is the citation; the `<local-root>/<slug>.md` path form is dropped from issue-body text) and forbids resolving `<local-root>` anywhere; a drafter following it verbatim produces a citation that passes `lint` via the step-5 temp-file recipe. `LINT_PATTERNS` untouched.
  6. `skills/lessons-learned/SKILL.md` migrate pre-flight branches on `test -f .claude/war/config.json` ahead of the `--fill-defaults` invocation: absent → effective defaults (`memory.commitLearnings: false`), resolver call skipped; present → existing command unchanged. `lessons-learned-doc-contract.test.mjs` gains a lock asserting the `test -f` absent-file branch (case-tolerant, mid-sentence anchor), and its eleven existing locks stay green — the Accept branch (`--stdin --fill-defaults`) and the `opt-in / off by default` phrasing they anchor on are untouched. `skills/war/assets/war-config.mjs` untouched.
  7. All three token sweeps (§4.1 `buildProjection`/`projectionRow`, §4.4 `<local-root>`, §4.5 `--fill-defaults`) report grep hits **and** manual same-scope survey stragglers (empty lists stated, not implied) as a `Survey:` block in the owning task's implementation commit message body.
  8. The four release slots are bumped together to the next free patch above the live integration base (Phase 2).

## Build order (for /war)

- Phase 1 — mechanics + prose fixes (wave 1: Tasks 1.1, 1.2, 1.3 in parallel — file-disjoint; wave 2: Task 1.4 after 1.1 merges)
- Phase 2 — release bump (trailing, lands last)
- **Predecessor-consistency check:** intent heading (`## AI-Commander's Intent`), bold Purpose/Method/numbered End state, `Stacks on:` line, directive-form release phase, commit-message `Survey:` block convention, and backstops-section style all match the 2026-07-12 predecessors (spot-checked against `land-path-verification-hygiene` and `audit-gate-evidence-fidelity`). No divergence beyond the logged deviations.
- **Contention (stacks eighth):** `skills/_shared/war-memory.mjs` + its test are untouched by all seven prior plans (checked against their footprints). Four predecessors each edit one named `docs/learnings/*.md` lesson (`verbatim-mirror-directive-context-mismatch-at-destination`, `scope-hook-blind-to-bash-write-path`, `red-team-env-gap-warn-is-agent-directive-not-code-enforced`, `ensure-origin-swallows-stderr-unlike-sibling-subcommands`) — those edits are in Task 1.4's base by landing order; Task 1.4 must **not archive** those four slugs (a just-edited lesson is live by definition) and prefers leaving their descriptions alone unless headroom is unreachable otherwise. The release slots are shared with every plan by design; the trailing directive-form phase absorbs the base movement.

## Phase 1 — projection dedup, citation hygiene, pre-flight guard, headroom remediation

### Task 1.1: `buildProjection` cross-root dedup + root-aware `archiveCandidates` (#821 + #820 mechanics)

- Files: `skills/_shared/war-memory.mjs`, `skills/_shared/war-memory.test.mjs`
- Plan slice: One task for both mechanics — they edit the same two files (code-boundary rule; a deps edge must never dodge a same-file collision). In `buildProjection` (after the `temperature === 'hot'` filter, before the `projectionRow` map), collapse the hot set by slug — when a slug appears in both roots keep the `root === 'repo'` record, drop the local twin from the projection input only. `walkCorpus`, query ranking, `inboundCiters`, and lint walks keep seeing both copies (spec §3 — dedup lives in projection rendering, never promotion or indexing). `archiveCandidates` is called on the **deduped** set (a shadowed twin costs zero projection bytes; nominating it recovers nothing). In the `archiveCandidates` comparator, insert one root clause between the existing `tierRank` comparison and the date tiebreak: equal tier → `local` before `repo` → oldest first. The dedup key is bare slug — cross-root collision is ratified as same-fact and the rewritten invariant comment says so (End state 2's wording).
  Tests (End state 1's four cases, with the two anti-vacuity constraints): the candidates fixture pads past `WARN_BYTES` using the existing long-description padding idiom already in this test file, so the shadowed-slug-absent assertion goes through `buildProjection`; the comparator fixture pins the local record's date **strictly newer** than the repo record's so the case reds when the root clause is deleted.
  Token sweep (spec §4.1): grep `buildProjection` and `projectionRow` across `skills/` (current callers: only `war-memory.mjs` and its test — re-verify at implementation time); hand-scan `war-memory.mjs` and `war-memory.test.mjs` same-scope titles and comments — the `Invariant hot ≡ indexed` comment, the projection-render section header above `projectionRow`, the `render: hot ≡ indexed` test title — and rewrite each to the post-dedup invariant in the same commit. Record hits + stragglers (empty stated) as a `Survey:` block in the commit message body.
- requiresTest: true
- requiresPackaging: false
- deps: []
- target repo: superproject

### Task 1.2: survey-corps mining-citation slug-only mandate (#822)

- Files: `skills/survey-corps/SKILL.md`
- Plan slice: Amend Step 0 "Mine" step 3: the lesson citation for a **local-root** lesson in an issue body/title is **slug-only** — the fixed `Lesson: <slug>` line is the citation, and the `(...or <local-root>/<slug>.md)` path form is removed from the issue-body template; resolving `<local-root>` to a real filesystem path is forbidden anywhere in drafted text — a resolved home path trips the `home-path` entry in `LINT_PATTERNS` and withholds the whole otherwise-clean issue. (Repo-root lessons keep the `docs/learnings/<slug>.md` path form — it is repo-relative and lint-clean.) No lint change, no softening — the lint is correct, the directive is the gap (spec constraint 1). This tightens the spec's placeholder-or-slug either/or: a literal `<local-root>/<slug>.md` in a published issue reads as broken template text, and leaving the choice open re-introduces the drafter discretion that caused #822.
  Token sweep (spec §4.4): grep `<local-root>` in `skills/survey-corps/SKILL.md`, handle every occurrence; hand-scan the remaining steps for other prose inviting a resolved memory-root path — step 1's "resolved exactly as `war-memory render-index` resolves the local root" enumeration prose and the step-5 lint recipe — adding the keep-it-out-of-issue-text caveat where needed (step 1 legitimately resolves the root to *read* lessons; the prohibition scopes to text destined for issue bodies/titles). Validate per spec §10.5: run the step-5 temp-file `lint` recipe against a sample drafted citation line. Record hits + stragglers as a `Survey:` block in the commit message body.
- requiresTest: false
- requiresPackaging: false
- deps: []
- target repo: superproject

### Task 1.3: `/lessons-learned` migrate pre-flight absent-config guard (#823)

- Files: `skills/lessons-learned/SKILL.md`, `skills/lessons-learned/lessons-learned-doc-contract.test.mjs`
- Plan slice: Amend the "Pre-flight — the opt-in gate" step: first `test -f .claude/war/config.json`; absent → effective defaults (`memory.commitLearnings: false`), skip the resolver call entirely; present → run the existing `--fill-defaults` command unchanged. The edit must preserve the anchors the existing doc-contract locks grep — the `opt-in / off by default` phrase (locks 8/9) and the Accept branch's `--stdin --fill-defaults` write path (lock 8), which already handles the absent-file case and is untouched. No `skills/war/assets/war-config.mjs` change (spec constraint 4: /war's fail-loud consumption is deliberate). Extend `lessons-learned-doc-contract.test.mjs` with one lock asserting the pre-flight carries the `test -f .claude/war/config.json` absent-file branch — case-tolerant, mid-sentence anchor per the prompt-only-clause grep-guard lesson — and run the whole file to prove the eleven existing locks stay green.
  Token sweep (spec §4.5): grep `--fill-defaults` across `skills/` (known consumers: `skills/red-team/SKILL.md`, `skills/lessons-learned/SKILL.md`, `skills/war/SKILL.md`, `skills/war/references/schemas.md`, `skills/war-room/SKILL.md`), confirm each either guards existence first or is deliberately fail-loud/fail-open, and that no other surface instructs a bare `<path> --fill-defaults` without an absent-file posture; hand-scan `skills/lessons-learned/SKILL.md`'s same-scope steps for other config-state reads (the evict-mode `commitLearnings` prompt) needing the absent-file caveat. Record hits + stragglers as a `Survey:` block in the commit message body.
- requiresTest: true
- requiresPackaging: false
- deps: []
- target repo: superproject

### Task 1.4: repo-side headroom remediation — description diet + explicit-slug archives (#820)

- Files: `docs/learnings/` (frontmatter `description` edits on the longest rows; moves of lowest-tier candidates into `docs/learnings/archive/` + note per move)
- Plan slice: Content work, gated by the same fail-closed redaction lint CI runs. Rebase onto the integration tip (Task 1.1 merged — the wave edge exists so candidate selection and measurement run through the deduped `buildProjection`; merged suffices, nothing here needs 1.1 *landed*). **Measurement is deterministic and worker-runnable:** create a scratch **empty** directory as `--local` (a worker has no `$CLAUDE_MEMORY_LOCAL`, and `render-index` writes MEMORY.md into whatever local root it is given — never point it at an operator store); the projection is then repo-rows-only. Acceptance: `render-index --local <scratch> --repo docs/learnings` → bytes ≤ 20,400, lines ≤ 200, verdict `warn` (not `refuse`) — the steady-state `warn` is designed (End state 3). Always pass `--repo docs/learnings` — the on-branch trap silently drops every `[repo]` row and the measurement lies. Candidate selection: take the ranked `candidates` from that same render (repo-only rows — the new root clause is a no-op within a single root, so ordering equals the ratified tier+age ranking; no hand-pick divergence). Archive **by explicit slug** only (never `archive --candidates --apply` — it archives the whole candidate set): `node skills/_shared/war-memory.mjs archive <slug> --local <scratch> --repo docs/learnings` — the tool appends the note and `git mv`s the repo file; the MEMORY.md it re-renders into the scratch root is measurement-only and discarded. Check `inboundCiters` before each archive; hubs stay; never archive the four predecessor-edited slugs (Build order) or any lesson added by this campaign's own servitor commits. Tighten the longest `description` lines (descriptions, not bodies, drive projection bytes); for each tightened lesson, confirm the dropped discriminating terms sit in `metadata.keywords` (add them if not — keywords weigh ~8× in retrieval) and that the lesson still returns from `query` by its former description's key terms. Confirm each archived lesson still returns from `query` by its slug terms. No deletions in the diff — moves only. `node skills/_shared/war-memory.mjs lint docs/learnings/` exits 0. Byte figures in the spec are point-in-time; re-measure, do not assert them.
- requiresTest: false
- requiresPackaging: false
- deps: [1.1]
- target repo: superproject

## Phase 2 — release

### Task 2.1: version bump — four slots, next free patch

- Files: `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`, `README.md`
- Plan slice: Bump all four slots together to the **next free patch above the live integration base at land time** — `.claude-plugin/plugin.json` `version`, `.claude-plugin/marketplace.json` `metadata.version` and `plugins[0].version`, and the `README.md` `## Status` line (replace-in-place, never a badge, never an empty field); `skills/war/assets/version-slots.test.mjs` is the lock-step arbiter. Never emit a resolved semver literal in this plan — this plan stacks eighth, behind seven stacked release bumps, so any plan-time literal lags by up to seven patches (the stacked-release lesson). Standalone fallback: resolve the next free patch from the four slots themselves at land time.
- requiresTest: false
- requiresPackaging: false
- deps: []
- target repo: superproject

## Deferred validations (backstops — AI-declared)

- **Live two-root headroom** (spec §10.2 as written): `render-index --local "$CLAUDE_MEMORY_LOCAL" --repo docs/learnings` ≥ 4,000 B headroom on the operator's real store · why deferred: Task 1.4's in-run acceptance is repo-rows-only against a scratch local (deterministic; a worker has no operator store, and the live store grows with every servitor write during this same campaign) · runner: operator (or `/aftermath`) runs it once after the campaign lands; a miss is actionable locally (non-promoted local rows are the delta and are `/lessons-learned`'s to trim).
- **Fresh-clone migrate pre-flight walkthrough** (spec §10.6: no `.claude/war/config.json`, happy path reaches `commitLearnings: false` with no nonzero-exit command) · why deferred: prose-directive behavior — exercised by an agent following the skill, not a unit test; the Task 1.3 doc-contract lock guards the prose's presence, not its execution · runner: **/red-team sandbox probe on this plan** (temp checkout without the config), fallback the next real `/lessons-learned migrate` invocation.
- **`cmdArchive` slug-shadowing residual (accepted):** `cmdArchive` keys hot records in a `Map` by bare slug and `walkCorpus` walks local-then-repo, so on a promoted twin the repo record wins — `archive <slug>` acts on the repo file and the local twin is un-archivable via the CLI · why deferred: archiving the local promoted copy conflicts with the ratified recurrence-edit contract (spec §9 non-goal), so no in-plan fix; Task 1.1's dedup also stops the twin from ever being *nominated* · runner/tripwire: the first future need to archive a local twin adds root disambiguation (e.g. `--root local`) to `cmdArchive` then — until that need exists, the residual is inert.
- **Per-root byte budgets insufficiency tripwire** (spec §9 deferral): dedup + diet are presumed sufficient · observable tripwire: the next `/lessons-learned` housekeeping pass re-measures headroom via the two-root render; a `refuse` verdict recurring — or headroom under ~1,000 B — despite this plan's remediation files a `war-followup` issue proposing per-root caps · runner: next `/lessons-learned` invocation (its Phase-5 render already produces the number; the threshold check is one comparison).

## Notes / conscious deviations

- **§4.1 + §4.2 are one task (grill Q1).** Both mechanics edit `war-memory.mjs` + its test; two tasks with a deps edge would be exactly the same-file-collision dodge the code-boundary rule forbids. Task 1.1's slice names both issues' test cases explicitly so the auditor reads the combined scope as planned, not crept.
- **Task 1.4 is a same-phase wave edge, not a phase edge (Q2).** It needs 1.1 *merged* (candidate selection and measurement run through the deduped `buildProjection`), not *landed*. Hand-pick divergence is moot: candidates come from the render's own ranked list, and within a single root the new root clause is order-neutral (tier+age unchanged).
- **Servitor `docs/learnings/` collision: accepted as rebase churn (Q3).** Servitor commits add *new* lesson files; Task 1.4 edits/moves *existing* ones — disjoint paths, so the serial-merge rebase is clean by construction. The real hazard is scope, not conflict: 1.4 must not archive or diet lessons this campaign just wrote/edited (Build order + Task 1.4 slice). Disabling `commitLearnings` for the run would silently drop the campaign's own learnings — worse than churn.
- **Acceptance is repo-rows-only with a scratch `--local` (Q4).** Deterministic and worker-runnable; the live two-root ≥ 4,000 B check is the first deferred backstop with a named runner.
- **Steady-state `warn` is intended (Q5).** The ≤ 20,400 B target sits above `WARN_BYTES` = 17,000, so every post-remediation render verdicts `warn` with a candidate list — by design, stated in End state 3 so neither an auditor nor red-team grades the warning as failed remediation. Dieting below 17,000 B would cut ~a third of 112 repo lessons for an advisory threshold; declined.
- **Comparator-test vacuity closed (Q6):** the equal-tier fixture pins the local record's date strictly newer than the repo record's — the existing oldest-first tiebreak alone would order repo first, so the case reds with the root clause deleted.
- **Candidates-test plumbing (Q7):** the shadowed-slug fixture pads past `WARN_BYTES` (existing padding idiom in this test file) so the assertion goes through `buildProjection` and proves the deduped feed — the actual #821 claim — rather than unit-testing `archiveCandidates` in isolation.
- **`cmdArchive` slug shadowing is an accepted residual (Q8)** — recorded in Deferred validations (entry 3), not prose, per ADR 0017 discipline.
- **Dedup key is bare slug; slug-collision-implies-twin is ratified (Q9).** No `metadata.promoted` gate: Gate-2 promotion is the only cross-root copy path, so an unrelated same-slug pair cannot arise from the machinery. The rewritten `buildProjection` invariant comment states this so the auditor knows it is deliberate.
- **Description-diet retrieval regression guarded per-lesson (Q10):** Task 1.4 checks each tightened lesson's dropped discriminating terms are in `metadata.keywords` (adding them if absent) and re-queries by the former description's key terms. No corpus-wide before/after probe — per-lesson confirmation is cheaper and targets the actual risk.
- **survey-corps mandate is knowingly unguarded prose (Q11).** No contract test exists for that skill and the spec scopes #822 to prose; the standing backstop is the fail-closed lint itself — if the mandate rots, a resolved path is still *withheld*, so the failure mode regresses to the current friction, never a redaction leak. Task 1.3's doc-contract extension, by contrast, is in-scope because the test file already exists and the recipe would otherwise rot (process-recipe lesson).
- **requiresTest mapping (Q12):** 1.1 true (`.test.mjs` in-diff), 1.3 true (carried by the doc-contract extension), 1.2 false (prose-only; eats the `no-test` floor route by design), 1.4 false/false (pure content; gate evidence = redaction lint exit 0 + the scratch-local render measurement).
- **Spec validation 6's runner is named (Q13):** the /red-team sandbox probe (deferred backstop 2); the Task 1.3 doc-contract lock maps the in-run evidence.
- **§4.3 archive mechanics (Q14):** Task 1.4 runs `archive <slug>` with the scratch `--local` — the tool does the note + `git mv`; the re-rendered scratch MEMORY.md is measurement-only and discarded. Never the operator's real root; never hand-`git mv` (the tool's note append is the ratified move+note shape).
- **Sweep reports live in the implementation commit message body as a `Survey:` block (Q15)** — the convention End state 9 of the `land-path-verification-hygiene` predecessor established; addressable at the pinned `audit_sha` via `git log`, which is in the auditor guard's read-only verb allowlist.
- **Per-root-caps deferral has a tripwire (Q16):** deferred backstop 4 — the next `/lessons-learned` pass's render is the re-measure; recurrence of `refuse` (or < ~1,000 B headroom) files a `war-followup` issue. Not nobody's job.
- **#822 either/or tightened to slug-only for issue text (Q17):** a literal `<local-root>/<slug>.md` in a published issue reads as broken template text, and keeping the choice open re-creates the discretion that caused #822. The placeholder survives only in skill-internal prose. Deviation from the spec's "(or slug-only)" permissiveness, recorded here.
- **Query-side duplicate rows stay, with the cost named (Q18):** `walkCorpus` still returns both copies to the FTS index, so a promoted lesson can surface twice in `query` output and can spend two of a seat's prefetch `topK` slots on one fact — accepted prompt-budget cost; deduping the index would hide the canonical recurrence-edit target from retrieval (spec §8). A red-team probe surfacing double rows in query output grades as designed.
- **Release phase exists (Q19):** Phase 2, directive form, four slots, next free patch at land time — this is the plugin repo and the campaign convention is a trailing release phase per plan.
- **Predecessor-consistency check:** heading is `## AI-Commander's Intent` (ADR 0014 — /war-machine --afk synthetic intent), bold Purpose/Method/End-state keys, `Stacks on:` line, contention bullet, directive-form release task, and `Survey:`-block convention all match the 2026-07-12 predecessors. The draft's non-bold intent keys were the only divergence; fixed to match.

## Open decisions

None. (The spec's design tree is fully resolved; per-root budgets and any `war-config.mjs` CLI change are explicit deferrals with tripwires above, not open questions.)
