# Red-team adjudicated-clear verdict, drift-guard deps-edge doctrine, and ref-diff escape guard

Source spec: [2026-08-02-redteam-doctrine-and-guards-design](../specs/2026-08-02-redteam-doctrine-and-guards-design.md)
Issues addressed: #1207, #1224, #1242, #1244.
Issue → task mapping: #1207 → Task 1 (gate ADJUDICATED arm + verdict pins), Task 3 (new ADR), Task 4 (Step 5 rewrite, lenses enumerations/schema/template, verdict-enumeration drift guard), Task 6 (verdict-enumeration cascade); #1224 → Task 3 (carve-out granularity subsection) + Task 4 (lenses `## Adjudications` guidance); #1242 → Task 3 (ADR 0025 amendment), Task 4 (probe arm 3 on both prose surfaces + presence-pair guard), Task 5 (war-machine drafter directive + war-strategy §3 authoring rule); #1244 → Task 2 (guard snapshot/baseline modes + tests) + Task 4 (SKILL.md Step 3/4 snapshot, `--baseline`, containment rule). `/war` files its own epic + task issues regardless (war-execution-must-file-issues); closing the four source issues is Lead checkpoint work at phase close (war-checkpoint-must-close-task-issues), never assumed from the epic close.

## AI-Commander's Intent

- Purpose: four ratified-but-unenforced red-team doctrines become first-class, mechanical, and greppable — the operator's adjudicated-clear directive becomes a distinct gate-emitted verdict instead of Lead-invented prose (and the fake-CLEARED-by-removal hole closes); adjudication G's carve-out granularity is recorded once, canonically; the guard-split deps-edge rule gets forcing surfaces on every plan-authoring path so the recorded recurrence class dies; and the escape guard's ref half graduates from a name heuristic a probe already slipped to an exact pre/post ref-diff.
- Method: a typed-flag gate arm plus doctrine prose plus a two-mode floor script, decomposed so every mirror and its guard travel together, or else carry the `deps` edge this plan's own doctrine requires (the End-state-18 sandbox-idiom mirror is the deliberate deps-edge instance: its `workflow-scaffold.js` half rides Task 2, its SKILL.md half and its guard ride Task 4 via `deps: [2]`) — code tasks land first, one prose task owns each shared doc file whole and dep-rebases onto the landed code so its prose documents landed behavior. Judgment guardrails: the gate stays pure and typed — the new input is a per-finding `adjudicated` flag keyed strictly on `=== true` (the `deliverableAbsence`/`envGap` pattern), never NLP on finding text; back-compat is scoped precisely — no `adjudicated` flag anywhere → today's verdict table byte-for-byte, and the guard's no-`--baseline` check mode is byte-equivalent to today in exit codes, stdout, and check behavior, with exactly one new stderr advisory as the sole delta (verified safe: the existing suite's runner discards output); `INCOMPLETE` remains never-waivable and outranks everything; ADR 0025 is amended append-only (body sentences byte-intact; the Status currency line exempt); the floor exit contract 0 = clean / 1 = escape / 2 = git-or-infra error never collapses (missing baseline is 2, never 1 or a silent pass); the SKILL.md + lenses.md probe prose lands in one commit per the standing mirror discipline, with its presence-pair guard, the verdict-enumeration drift guard, and the sandbox-idiom drift guard in the same task (this plan practices the very one-task/deps-edge rule it lands); the count-word flip is treated as a default-flip — the guard asserts the OLD count absent, never NEW-present alone; the D7 two-contract pins and the `'pass'`-only demoting set are untouched; everything anchors by named construct, never line number. Sweep-token hygiene (coupling-comment-restating-grep-pattern lesson): every new test that greps for a token family builds its pattern from split fragments (the `'PLACEHOLDER-' + 'BUDGET'` idiom), and new banners/comments never restate a swept phrase contiguously — this plan file itself quotes swept tokens and sits outside every sweep scope (docs/plans/) by design. Auditor calibration: seats verify source shape at the pinned `audit_sha` via Read/Grep; suite runs, sweep-survey outcomes, and demonstrated-red evidence are done-report/gate-audit execution evidence (ADR 0041 ladder), never a seat's hold.
- End state (each condition individually checkable; HARD = verifiable at the pinned tip / in the diff, SOFT = done-report evidence, sanctioned via the backstops):
  1. [HARD] `verdict()` in `skills/red-team/assets/red-team-gate.mjs` emits the five-verdict precedence `INCOMPLETE` > `BLOCKED` (any blocker/needsDecision without `adjudicated === true`) > `ADJUDICATED` (≥ 1 adjudicated blocker/needsDecision, zero unadjudicated) > `CLEARED-WITH-NOTES` > `CLEARED`; the flag is read by `verdict()` only — `classify()` bucketing is unchanged and adjudicated blockers still appear in `blockers`; a zero-blocker run can never emit `ADJUDICATED`; `summarize()`, the envelope unwrap, the zero-probe refusal, and the fingerprint/coverage layers are untouched; the `verdict()` header comment (today's "the Lead removes resolved ones during the grill loop … loop until it is not BLOCKED" prose) and the CLI header comment are rewritten in the same commit to state the new precedence and the stamp-vs-remove split.
  2. [HARD] `skills/red-team/assets/red-team-gate.test.mjs` carries the new fixture-level verdict pins: all-adjudicated blockers → `ADJUDICATED`; mixed adjudicated/unadjudicated → `BLOCKED`; an adjudicated `needsDecision` counts toward `ADJUDICATED`; zero blockers + minors → `CLEARED-WITH-NOTES` (never `ADJUDICATED`) — **the fixture's Minor must itself carry `adjudicated: true`** (red-team adjudication 6, 2026-08-02): worded without the flag this pin is vacuous, and a probe proved it, building the wrong implementation (flag read over *all* findings instead of the blockers+needsDecision union) and running End state 2's pin list verbatim against it — the whole list went green while End state 1's "a zero-blocker run can never emit `ADJUDICATED`" was violated, and the existing 67-test suite stayed green too. With an adjudicated Minor in the fixture the pin discriminates that implementation (the specified one still returns `CLEARED-WITH-NOTES`); this is the repo's own delete-the-feature `weak-test-assertion-passes-without-feature-being-exercised` class. **A second fixture is required alongside it — all-adjudicated blockers PLUS at least one UNadjudicated `Minor` note → `ADJUDICATED`** (red-team adjudication 10, 2026-08-02): a re-verify round proved the adjudicated-Minor fixture alone is still non-discriminating, because a *union-gated scan-all* implementation — gate on the `blockers`+`needsDecision` union but run the unadjudicated scan over `[...union, ...minors]` — passes the entire pin list **and** the standing 67-test suite, while making `ADJUDICATED` unreachable in practice (real reports routinely carry Minor notes, and the Step-5 mechanic stamps only blockers/needsDecision, so one unstamped Minor pins the run at `BLOCKED` forever). This second fixture pins that the flag scan is scoped to the union **only** and that unstamped Minors never hold the verdict; End state 1's shorter "zero unadjudicated" phrasing is read as "zero unadjudicated **union members** — Minors are never scanned", which Task 1's plan slice already states explicitly. Strict-flag delete-and-trace (`adjudicated: 'true'` string and `adjudicated: false` both stay `BLOCKED`); no flag anywhere → today's verdict table byte-for-byte. **Both discriminating Minor fixtures are required, not one** — the zero-blockers case whose Minor carries `adjudicated: true`, AND the all-adjudicated-blockers case with at least one UNadjudicated Minor still returning `ADJUDICATED`; the second is what pins the flag scan to the union and keeps `ADJUDICATED` reachable in real reports (verified at re-verify: the specified implementation is 14/14 green, and the union-gated scan-all survivor is red only on that second fixture). The INCOMPLETE-outranks pin has the non-vacuous fixture shape: ONE adjudicated finding set, piped to `verdict()` twice — once with an incomplete coverage object (non-empty `offTarget` or `dropped`, or `ran < expected`) asserting `INCOMPLETE`, once with complete coverage asserting `ADJUDICATED` — so the pin fails against any wrong precedence order and cannot pass vacuously via off-target discard (weak-test-assertion lesson). The existing Step-4 doc-lock and D7 pins are unmodified.
  3. [HARD] `skills/red-team/assets/assert-no-repo-escape.sh` implements two modes. `--snapshot <file>`: first the pre-run local integrity checks — porcelain must be clean and no local ref may match the b1 junk pattern; a hit is exit 1 (refusing to baseline pre-existing residue or dirt — the #1244 cross-run collision shape, detected before launch; origin is deliberately not polled pre-run, b2 stays a check-mode-only floor); then write the full local ref set (`git for-each-ref --format='%(refname) %(objectname)'`) and exit 0 (2 on git or file-write error). Check mode with `--baseline <file>`: today's checks a/b1/b2 unchanged as the unconditional floor, then an exact ref-diff — any added, removed, or SHA-moved ref **under `refs/heads/` or `refs/tags/`** versus the snapshot is an escape (exit 1) regardless of name; a missing/unreadable baseline file is exit 2, never 1 or a pass. **Within those namespaces the diff is fully name-agnostic — no allowlist, no name pattern** (a name exclusion would reintroduce the #1244 heuristic hole the diff exists to close); the *namespace* scoping is a different thing and is load-bearing (red-team adjudication 4, 2026-08-02): `git for-each-ref` also covers **`refs/remotes/*`**, which moves on any background or IDE `git fetch` and is never a probe writing into the repo (origin-side probe pushes remain the b2 ceiling), and at the phase base this repo carries **137 refs across 47 linked worktrees sharing one ref store**, so an unscoped diff turns any concurrent WAR activity into "escape detected" and — via End state 6's containment rule — makes `CLEARED` unreachable until someone deletes a legitimate ref. Reproduced both ways at the phase base: a plain `git fetch` produced a `refs/remotes/origin/*` delta, and a branch created in a *different* worktree of the same repo produced a `refs/heads/*` delta. The residual **over-detection** class that namespace scoping cannot remove — a concurrent local branch created by another session or worktree during the run window — is named in the rewritten ponytail and wired into SKILL.md Step 4's triage: diagnose a delta by **action-provenance first** (the standing self-confound gate), and a delta no probe authored is not an escape; it is recorded and the run proceeds. Separately, the sanctioned sandbox idiom is changed so it stops writing into the target at all, per **End state 18**. Check mode without `--baseline`: byte-equivalent to today in exit codes, stdout, and check behavior, plus exactly one stderr advisory naming the heuristic ceiling and the `--baseline` upgrade. Path containment at arg-parse time, before any check: a `--snapshot`/`--baseline` path that is not absolute, or that resolves under the `--repo` working tree, is `die … 2` (an in-tree artifact would false-trip the porcelain half); `--snapshot` combined with `--baseline` is a usage error → `die … 2`. **Both sides of the containment compare are resolved physically before comparing** — `repo_norm=$(cd "$repo_dir" && pwd -P)` and `art_norm=$(cd "$(dirname "$artifact")" 2>/dev/null && pwd -P)/$(basename "$artifact")`, with a missing parent directory → `die … 2` (red-team adjudication 4, 2026-08-02). A one-sided normalization is not sufficient and would break on this very harness: macOS `mktemp -d` — which End state 4 mandates for these files and which the existing `setup_repo` already uses — returns the `/var/folders/…` symlink alias of `/private/var/folders/…` (reproduced at the phase base), so under `pwd -P` on the repo side only, the plan's own in-tree case goes red, and under logical `pwd` the mirror-image alias silently bypasses containment. The existing `..` traversal refusal is **extended to `--snapshot`/`--baseline`** (a literal prefix compare alone misses an absolute path containing `..`). The `ponytail:` header paragraph is rewritten in the same commit (the ref-diff is no longer a "deferred upgrade path"; the remaining ceilings are **three**: the b2 origin-side probe-invented-name gap; pattern-slipping refs that predate the first baselined run; and — newly named, red-team adjudication 5, 2026-08-02 — **gitignored leak paths**, since `git status --porcelain` does not report ignored files (reproduced: a leaked `ignored.log` is invisible to porcelain and visible only under `--ignored`), so a probe writing into `node_modules/`, `*.log`, `screenshots/`, `.claude/teams/`, or `war-memory-queries.jsonl` is invisible to check (a), invisible to the snapshot-mode pre-run refusal, and adds no ref for the diff to see. The pre-existing header sentence "This half is EXACT." (line-anchored today at the porcelain-half description) is **qualified in the same commit** — left as written it becomes a false code-fact of exactly the source-comment-lags class this plan polices; the rewritten wording states the porcelain half is exact *for tracked and untracked-but-not-ignored paths*. Widening check (a) to `--ignored` is deliberately **not** taken here (it needs its own back-compat pin and a ruling on legitimately-ignored dirs) and is recorded as the **gitignored-leak-paths backstop** (cited by subject, not ordinal — the backstop list is unnumbered bullets). macOS bash 3.2, exit contract intact, every `die` call site passes an explicit code (the standing call-site lock is the arbiter).
  4. [HARD] `skills/red-team/assets/assert-no-repo-escape.test.sh` adds throwaway-temp-repo cases: snapshot mode writes refname+objectname lines and exits 0; snapshot mode on a repo with a b1-pattern branch → 1, and with a stray working-tree file → 1 (pre-run refusal arms); snapshot/baseline path inside the repo tree → 2; relative snapshot path → 2; `--snapshot` + `--baseline` combined → 2; identical pre/post ref set + clean tree + `--baseline` → 0; the #1244 demonstrated-red repro — a probe-invented, pattern-slipping branch (`rogue`) with a clean working tree: without `--baseline` → exit 0 with the stderr advisory present (this case IS the recorded pre-change miss, since no-baseline behavior is check-equivalent to the pre-change script — the equivalence and the history live in the case banner, never as an auditor-checkable pre-change run), with `--baseline` → exit 1; a moved sibling-branch SHA → 1; a deleted ref → 1; **a `refs/remotes/*` delta between snapshot and check → 0 (the namespace exclusion, pinned — without this case the whole filter can be deleted and the list stays green)**; **a created or moved `refs/tags/*` ref → 1 (the namespace inclusion, pinned)**; `--baseline` pointing at a missing file → 2, asserted ≠ 1; baseline/snapshot files always created outside the target repo's working tree (mktemp); the advisory case uses a stderr-capturing run variant (the existing `run_guard` discards output). The pre-existing cases (including the source/call-site die-default locks) stay green unmodified; the test header's case list is extended and names all three residual ceilings (b2 origin-side; pattern-slipping refs predating the first baseline; gitignored leak paths).
  5. [HARD] `skills/red-team/SKILL.md` Step 5 is rewritten per the spec: patch-and-adjudicate is the default; a probe re-run is owed only when BOTH arms hold (the probe was executed AND the patch changes what that probe measures); a blocker resolved by a re-run probe is removed (probe-proven — `CLEARED` stays reachable), a patched-but-not-re-verified blocker is stamped `adjudicated`, never removed; the ≤ 2-rounds bound and one-at-a-time grill are retained; the terminal rule gains the `ADJUDICATED` arm (patched + adjudication-rowed at loop end terminates `ADJUDICATED`; only an unpatched root or unsettled `needsDecision` leaves `BLOCKED` with residual open questions); the terminal verdict line is gate-emitted, and the stamping mechanic is named so two Leads cannot diverge: the Lead maintains a working copy of the gate-input object (`{ probeResults, fingerprint, expected, repo }`, from the Workflow return or the persisted task-output file — the persisted file itself is evidence and is never edited in place), stamps `adjudicated: true` on finding objects in that working copy, and re-pipes it through the gate via `--stdin`; a top-level `probeResults` always wins over a `.result` envelope, so the stamped copy is never shadowed.
  6. [HARD] `skills/red-team/SKILL.md` Steps 3/4 carry the ref-snapshot flow: the pre-run snapshot is taken immediately before launching the Workflow, once per run, at a run-scoped scratch path outside the target repo's working tree (absolute path — the guard refuses in-tree or relative paths); a snapshot-mode exit 1 means residue or dirt predates this run — resolve it via the containment rule before launching, never launder it into the baseline; the post-run guard invocation passes `--baseline`; the containment rule is present — after any escape containment (`reset --hard` / `clean -fd`), containment is complete only when the guard re-runs exit 0 **with `--baseline`**, a porcelain-clean restore alone never closes the incident. **Step 4 also carries the action-provenance triage for a nonzero guard, and the precedence between it and the containment rule is stated explicitly** (red-team adjudication 14, 2026-08-02 — without this the two rules contradict and `CLEARED` becomes unreachable in a shared ref store): on a nonzero guard, diagnose every delta by **action-provenance first** (the standing self-confound gate); the containment rule binds **probe-authored** deltas only, and a delta whose provenance clears it as foreign — a concurrent branch or tag from another session or worktree, which the Lead cannot and must not delete — is **recorded in the report and does not block `CLEARED`**. A guard exit 1 with at least one un-cleared or probe-authored delta still forbids `CLEARED`, exactly as today. This is the sole sanctioned reading of Step 4's "never report `CLEARED` until the state is clean": clean means no probe-authored residue, not a byte-identical ref set in a repo other sessions share. The three token families the Step-4 doc-lock in `red-team-gate.test.mjs` pins (task-output shapes, the `.result` unwrap, the zero-probe refusal) survive verbatim-in-substance so that doc-lock stays green unmodified.
  7. [HARD] Probe arm 3 `guard-split-deps-edge` (analyzed) is present in `skills/red-team/SKILL.md` §Drift-guard spine probes AND mirrored in `skills/red-team/references/lenses.md` §Drift-guard spine probes, in the same commit: a plan task that authors a drift guard over a fact authored by a different task in the same phase must carry a `deps` edge onto that task (same wave is insufficient — every task worktree is cut from one frozen phase base); absence is a plan defect → `needsDecision`, grilled until the plan adds the edge, merges the tasks, or moves the guard a phase later; scope is wider than `unguarded-new-mirror` (any drift guard split from the task authoring its fact — prose mirrors and doc-guard tests included, not just inline consts); vacuous when no such split exists; not skipped under `--fast`. Every "two doctrine probes"-family count word on both surfaces reads three (including the Step-2 sentence "The two drift-guard spine probes (below) run every red-team too" and the "neither is skipped under `--fast`" phrasing), with each probe's vacuity condition updated to cover all three arms.
  8. [HARD] A `guard-split-deps-edge` prose presence-pair test in `skills/red-team/assets/workflow-scaffold.test.mjs` pins, on both `SKILL.md` and `references/lenses.md`, using **two deliberately different scopes** (red-team adjudication 2, 2026-08-02 — the `ff-topology` ±320-char window idiom is *not* the model here and must not be copied: measured at the phase base, the SKILL.md drift-guard-probe section is 2,472 chars and the lenses.md one 1,175, so no ±320 window spans a section, and the Step-2 count sentence sits **7,455 chars** before the SKILL.md section heading — under a window scope the markdown-normalization clause would be inoperative and reverting the Step-2 word would leave the guard green):
     - **Section-delimited region** (from the `## Drift-guard spine probes` heading to the next `## ` heading — a real region, not a window) for the presence half: the arm's name and its load-bearing clauses (`deps` edge requirement, vacuous, `--fast`), plus the NEW-count assert that the region names three arms.
     - **File-wide** for the OLD-count-ABSENT assert, over markdown-normalized text (bold markers stripped, whitespace collapsed — the live Step-2 instance splits the token with `**`), matched with a **precise phrase-family pattern** — `two\s+(?:universal\s+)?(?:doctrine|drift-guard spine)\s+probes`, case-insensitive — never a bare `\btwo\b`. File-wide scope is safe *because* the pattern is precise: measured at the phase base it matches exactly the three live count instances the plan flips (SKILL.md Step-2 bold-split + SKILL.md in-section + lenses.md in-section) and nothing else, whereas a bare `\btwo\b` matches 8 places in total across the two surfaces — the 3 swept count instances plus 5 unrelated ones (`two values`, `two paths back-to-back`, `splits two ways`, `Two-contract summary`, `this two-contract sentence`) — so an OLD-absent assert built on it would be permanently red.
     Every pattern is built from split fragments so the test never joins the self-match class. Removing the arm from either surface, or reverting **any** of the three count words — the Step-2 bold-split instance included — turns it red. The same test block pins the `--baseline` token in SKILL.md's Step 3/4 region.
     **Plus a sandbox-idiom drift guard over End state 18's two-surface mirror** (red-team adjudication 11, 2026-08-02 — the round-2 patch created a new mirror and, caught by this plan's own `unguarded-new-mirror` doctrine, initially shipped it unguarded): pin the isolating idiom present on **both** `skills/red-team/assets/workflow-scaffold.js` and `skills/red-team/SKILL.md` Step 3, with an OLD-absent assert that the **ref-writing bare `worktree add <tmp>` form** (no `--detach`) appears on neither surface, split-fragment-patterned. The pre-existing pin at `workflow-scaffold.test.mjs`'s executed-probe-copies-the-repo assertion is **not** sufficient and must be tightened in the same edit: its `/cp -R|worktree add/` alternation matches the bare and `--detach` forms identically, so a revert to the ref-writing form leaves it green while re-arming the false-escape End state 18 exists to prevent. Task 4 already `deps: [2]`, so the landed preamble is readable at this task's base.
  9. [HARD] A verdict-enumeration drift guard in `skills/red-team/assets/workflow-scaffold.test.mjs` (same task as the lenses edits — extraction + equality per ADR 0025, not presence-hope): extract the verdict string literals from the `verdict()` function body of `red-team-gate.mjs` (function-scoped source slice, named-construct anchor) and assert every extracted verdict appears in `references/lenses.md`'s §Severity & gate verdict enumeration AND in the report template's verdict line — the next verdict widening reds this guard instead of relying on a one-shot sweep. The extracted set is **all five** verdicts including `INCOMPLETE` (`if (isIncomplete(coverage)) return 'INCOMPLETE'` is inside the function body, so any function-scoped slice picks it up) — there is **no carve-out**: End state 10's template line carries `INCOMPLETE` precisely so this guard's universal quantifier is true on the commit that introduces it (red-team adjudication 1, 2026-08-02). **The guard is a default-deny exact compare, not a presence loop** (red-team adjudication 9, 2026-08-02 — a presence loop was built and defeated two independent ways): (i) a **non-empty floor plus an exact-set assert**, `assert.deepEqual([...literals].sort(), ['ADJUDICATED','BLOCKED','CLEARED','CLEARED-WITH-NOTES','INCOMPLETE'])`, because a behaviour-preserving refactor that hoists the verdict strings to a module-level constant table empties the function-scoped slice and a bare loop then passes with **zero** verdicts checked — proven, and a sixth undocumented verdict (`PENDING-OPERATOR`) was added to that table with the loop still green, which falsifies the guard's own "the next verdict widening reds this guard" claim; and (ii) a **delimiter-split token-set compare** on each documentation surface — split the surface's verdict list into tokens and assert set equality against the extracted literals — because plain containment lets `CLEARED-WITH-NOTES` satisfy the check for standalone `CLEARED`. **A `\b` word boundary does NOT close that hole and must not be used** (red-team adjudication 12, 2026-08-02 — this corrects an earlier round-2 patch of this same clause): `-` is a non-word character, so `/\bCLEARED\b/` matches inside `CLEARED-WITH-NOTES` and mutation proof (b) below stays green under it, verified. Use the token-set compare; a hyphen-aware lookaround (`(?<![A-Z-])CLEARED(?![A-Z-])`) is an acceptable equivalent, verified to RED on the mutated line and pass on the correct one. **Both surfaces are single lines, named exactly** (red-team adjudication 13): the `- **Verdict:**` enumeration bullet inside `## Severity & gate`, and the report template's `**Verdict:**` line — *not* the enclosing sections, because a section-scoped read is satisfied by a verdict appearing only in an adjacent comment or fenced example (proven: with `ADJUDICATED` deleted from the enumeration bullet and mentioned only in a `<!-- TODO -->` comment elsewhere in the same section, a section-scoped guard stayed green). Mutation proofs, mandatory and all four: deleting `INCOMPLETE` from the report-template verdict line REDs the guard; deleting standalone `CLEARED` from it REDs the guard; hoisting the literals out of the function body REDs the guard on the non-empty floor rather than passing vacuously; and a verdict present only in a comment inside `## Severity & gate` but absent from the enumeration bullet REDs the guard. This is End state 2's own anti-vacuity discipline applied to End state 9's guard (`structural-test-blind-spot-narrowing-needs-negative-reference-and-default-deny-census`).
  10. [HARD] `skills/red-team/references/lenses.md`: the §Severity & gate verdict enumeration and the §Scope-lock `INCOMPLETE` bullet's verdict list both gain `ADJUDICATED` with its one-line definition (every blocker/needsDecision patched + adjudicated; distinct from `CLEARED` — no probe re-proof); the finding-schema block documents the `adjudicated` flag beside `deliverableAbsence` (typed, Lead-stamped at grill time, strict `=== true`); the report template's verdict line reads `CLEARED | CLEARED-WITH-NOTES | ADJUDICATED | BLOCKED | INCOMPLETE — <one line>` (**`INCOMPLETE` is added by this plan** — it is absent from today's line while `verdict()` has emitted it since the coverage layer landed, so the line is a live doc-truth straggler of exactly the class §10.4 sweeps for; adding it is what makes End state 9's guard satisfiable, and SKILL.md Step 6's "reported as its own line with the `offTarget`/`dropped` probes" governs the *additional* detail line, not membership in the verdict enumeration); the `## Adjudications` block's guidance comment gains the per-row provenance token (`operator-ratified (<date>)` or `AI-declared`, ADR 0014 — per-row, not per-block) and the carve-out granularity rule with its citation to the new ADR. The D7-pinned two-contract sentence is byte-untouched. The provenance token rides inside each preformatted row string — the row shape the downstream consumers describe is unchanged, and the done report states this explicitly for each of the four consumers: `skills/war/SKILL.md` step 5, `skills/war/references/schemas.md` ("row shapes unchanged"), `workflow-template.js`'s `adjudicationClause`, `agents/war-auditor.md`.
  11. [HARD] The new ADR exists (next free number; `0043-adjudicated-clear-distinct-terminal-verdict.md` at drafting time — resolve at land) and carries: the CLEARED-vs-ADJUDICATED split; stamp-never-remove; the two-arm re-verify trigger; the ADR 0017 boundary sentence verbatim-in-substance ("an adjudication waives re-verification of a patch, never the validation itself"); the ADR 0014 per-row provenance marker; a carve-out granularity subsection resolving #1224's anchor-vs-span question ("the extraction's read is the carve-out"; ordered/positional-key regions atomic; unread residue shrink-eligible; bounded to runtime prompt strings by the registry-carveout lesson — never standing-doc card spans), citing adjudications G and J of `docs/red-team/2026-07-28-prompt-surface-simplification.md` as precedent (quoted, never edited); and the 2026-07-26 same-wave-adjudication-was-insufficient outcome recorded as the acknowledged residual risk of proceeding without re-proof.
  12. [HARD] `docs/adr/0025-drift-guard-discipline.md` gains a dated amendment note under Consequences recording the file-disjoint-collision resolution (a `deps` edge from the guard task onto the mirror-authoring task; same phase + same wave insufficient — frozen phase base), naming the red-team probe arm, the `/war-machine` directive, and the `/war-strategy` §3 authoring rule as the forcing functions and citing the lesson slug `guard-task-split-from-mirror-task-needs-deps-edge-same-wave-insufficient`. Append-only is owned: the Task-3 auditor seats verify at the pinned `audit_sha`, against the task's dispatch base (never a ref literal), that the diff on this file is additions-only plus at most the Status currency line (adr-amendment lesson).
  13. [HARD] `skills/war-machine/SKILL.md` carries a **Guard-split deps-edge directive** paragraph placed with the existing Release-phase directive: a drafted plan task that authors a drift guard over a fact authored by a different same-phase task must carry `deps: [<that task>]`; the grill agent asks it; a split the drafter cannot edge (e.g. a cycle) is raised via the ADR triad, never silently shipped. `skills/war-strategy/SKILL.md` §3's Drift-guard coverage subsection gains authoring rule 7 stating the same rule for every plan authored or converted there (with the subsection's own "two authoring rules" count word updated), so the non-machine conversion path carries the same forcing function. Both texts are pinned in `skills/war-machine/war-pipeline-structure.test.sh` in the same task — a **`has_i`** pin per surface, anchored on a **mid-sentence** token (`guard-split deps-edge` plus a clause anchor such as `same wave is insufficient`), never on a heading-position capital (red-team adjudication 7, 2026-08-02: that file documents `has()` as `grep -qF` case-**sensitive** and reserved for "Flag/token literals that never re-case", and ships `has_i()` explicitly "For PROSE tokens that a benign re-casing (sentence case) must not false-negate"; `Guard-split deps-edge` is prose whose leading capital exists only because it opens a bold heading, so a `has` pin reds on a compliant re-cased rewrite while the doctrine is intact) — the guards-follow-their-text pattern; the spec's grep-for-`deps` acceptance alone fails the delete-the-feature test. **The `two authoring rules` → `three` count flip in `skills/war-strategy/SKILL.md` §3 is a default-flip and carries its own OLD-absent assert** in the same task, split-fragment-patterned (red-team adjudication 8, 2026-08-02): the claim that it is "covered by the structure-test pins" is false — those pins are presence checks on a NEW phrase, which cannot assert an OLD phrase absent, and the only existing guard over that subsection, `check '^### Drift-guard coverage'` in `skills/war-strategy/war-strategy-structure.test.sh`, is a prefix regex matching "two authoring rules" and "three authoring rules" identically, so without this assert the worker could add rule 7, leave the heading reading "two", and stay green — the exact failure mode of rule 6 (`default-flip-must-audit-all-doc-surfaces`) that lives *inside* the subsection being edited.
  14. [HARD] The verdict-enumeration cascade is landed: `skills/war/references/design.md`'s Outcome bullet ("patches the plan in place until CLEARED"), `skills/war-campaign/SKILL.md`'s halt-and-hold clause ("when a plan can't CLEAR /red-team"), and `README.md`'s /red-team overview sentence ("patches the plan in place until it is **CLEARED**" — a live straggler verified outside the spec's §10.4 scope) each recognize `ADJUDICATED` as a proceed verdict (one-line edits); `CONTEXT.md`'s red-team glossary section carries the five new domain terms of spec §6 (ADJUDICATED (verdict), Adjudicated flag, Adjudication provenance marker, Ref-diff baseline, Guard-split deps edge); `CONTEXT.md`'s escape-guard "never CLEARED" line and ADR 0033's identical sentence remain as written (extend-don't-rewrite only on demonstrated ambiguity; ADR 0033 is never edited — an out-of-footprint ambiguity routes as a follow-up finding).
  15. [HARD grep floor / SOFT survey] The two spec §10 sweeps ran on the integrated tip and their outcomes are recorded: (§10.4, scope widened) `grep -rn 'CLEARED-WITH-NOTES'` across `skills/`, `docs/adr/`, `CONTEXT.md`, `README.md` — every enumeration lists `ADJUDICATED` or is individually justified in the done report — plus the mandatory manual same-scope survey of `skills/red-team/SKILL.md` (including its frontmatter description and "drives it to CLEARED" body prose), `references/lenses.md`, `red-team-gate.mjs` comments, both red-team test files' titles/comments, and the four `## Adjudications` consumers named in End state 10, for verdict-list prose the token misses; (§10.5) `grep -in 'two'` within the drift-guard-probe sections of both prose surfaces plus a hand-scan of the surrounding Step-2 prose and `workflow-scaffold.test.mjs` banner comments for count words the grep misses. Stragglers inside a task's footprint are fixed in-task as survey-derived corrections; out-of-footprint stragglers are recorded as follow-up findings, never fixed in-task and never prose-waived (ADR 0017).
  16. [HARD via gate artifact] `node --test 'skills/**/*.test.mjs'`, `bash skills/red-team/assets/assert-no-repo-escape.test.sh`, and `bash skills/war-machine/war-pipeline-structure.test.sh` green at the task tips, and the self-discovery gate (`resolveGate` in `war-config.mjs`) green at merge, with the refiner's captured gate log threaded (full-gates-green-without-threaded-gate-log lesson). Redaction lint unaffected.
  17. [HARD] All four release slots bumped in lock-step to the next free patch above the live integration base at land time (directive form — no version literal in this plan is authoritative); the README `## Status` blurb replaced in place describing this release.
  18. [HARD] **The sanctioned sandbox idiom is genuinely isolating, and it stops writing into the target repo** (added at red-team round 2; End state 3's ref-diff is unusable without it — the two conditions are read together). `skills/red-team/assets/workflow-scaffold.js`'s executed-probe SCOPE-LOCK preamble and `skills/red-team/SKILL.md` Step 3 today offer sandbox idioms that are unsafe in this repo's actual shape — the preamble offers `cp -R <repo> <tmp>` **or** `git -C <repo> worktree add <tmp>`; Step 3 offers only `git -C <target> worktree add <tmp>` (and carries no `cp -R` at all). Reproduced in throwaway fixtures at the phase base:
      - **A bare `git worktree add` writes `refs/heads/<tmp-basename>` into the target's ref store, and the ref survives `rm -rf` of the worktree directory** — so under End state 3's name-agnostic diff the *sanctioned* idiom reports an escape, and SKILL.md Step 4 forbids `CLEARED` on a nonzero guard. The guard would fire on its own prescribed workflow.
      - **`cp -R` does not isolate a linked worktree at all.** A linked worktree's `.git` is a *file* (`gitdir: …/.git/worktrees/<name>`), so `cp -R` copies the pointer and every `git -C <sandbox>` command drives the **real** repo — reproduced: a branch created in the "sandbox" appeared in the target and survived the `rm -rf`, the target's HEAD was detached, and the target's porcelain went dirty without the probe touching a target file. This is exactly this campaign's shape (`/red-team` runs in a linked worktree), so it is not hypothetical.
      Both surfaces are therefore changed to prescribe an idiom chosen by what the probe actually does. **If the probe runs `git` at all: `git clone --no-hardlinks <repo> <tmp>`** (verified isolating even when `<repo>` is a linked worktree) — with the stated caveat that **a clone carries committed state only** (verified: uncommitted edits and untracked files do not come across), so a probe needing the working tree as it stands must overlay it explicitly (copy the dirty paths in, or apply `git -C <repo> diff`) rather than assume the clone reflects it. **If the probe only reads and edits files, `cp -R` remains fine** — but with an explicit "when `<repo>/.git` is a *file* (a linked worktree) this copy is NOT git-isolated; never run `git` against it" clause. `--detach` is kept only for read-only inspection, stating that it shares the target's ref store so a probe must create no branches or tags there. (Note the two surfaces are not a byte-identical mirror: `workflow-scaffold.js`'s preamble offers `cp -R` *and* the worktree form and governs the probe's throwaway sandbox, while `SKILL.md` Step 3 has no `cp -R` at all — it offers only `git -C <target> worktree add <tmp>` and governs the Lead's clean-checkout for the `--repo` argument. The End-state-8 guard therefore pins the isolating idiom **per surface**, not one shared literal.) Disposal is specified, not left to `rm -rf`: a `--detach` sandbox is removed with `git -C <repo> worktree remove --force <tmp>` (or `rm -rf <tmp> && git -C <repo> worktree prune`), because `rm -rf` alone leaves a prunable `.git/worktrees/<name>/` admin entry that accumulates and later blocks reuse of the same path — residue invisible to the guard, so this is hygiene, not detection. The preamble edit rides **Task 2** (the guard task — the idiom and the guard that must not trip on it travel together); the SKILL.md Step 3 edit rides Task 4, which already `deps: [2]`. Residual, named in the rewritten ponytail: a probe that ignores the preamble and runs a ref-writing form still trips the diff — a true positive about an unsanctioned action, said so by the rewritten ponytail and the SKILL.md Step-4 triage (**not** by the no-baseline stderr advisory, which by construction is not emitted in `--baseline` mode and whose content stays pinned to its two subjects).

## Build order (for /war)

- Phase 1 — Gate, guard, and doctrine (Tasks 1–6; wave 1 = Tasks 1, 2, 3 parallel; wave 2 = Task 4 `deps: [1, 2, 3]` and Task 5 `deps: [3]`; wave 3 = Task 6 `deps: [1, 3, 4]`)
- Phase 2 — Release (Task 7)

The wave edges are dependencies, never collision dodges: every task's file set is disjoint. Note the near-miss that is **not** a collision: Task 2 owns `skills/red-team/assets/workflow-scaffold.js` (the End-state-18 preamble edit) while Task 4 owns `skills/red-team/assets/workflow-scaffold.test.mjs` — sibling paths, different files, and Task 4 `deps: [2]` already orders them. Task 4 rebases onto the landed gate + guard + ADRs so its prose documents landed flags/verdict strings, its verdict-enumeration guard extracts from landed gate source, and its ADR citation resolves to the real filename; Task 5 cites the amended ADR 0025; Task 6 runs the spec §10.4 sweep against the integrated tip that already contains Task 4's prose (within-phase-dep-gate-must-rerun-on-integrated-tip). The release phase trails per the decomposition rule. Task 6 (Phase 1) and Task 7 (Phase 2) both touch `README.md` — a phase edge, never a same-phase collision.

## Phase 1 — Gate, guard, and doctrine

### Task 1: Gate — ADJUDICATED verdict arm + verdict-table pins

- Files: `skills/red-team/assets/red-team-gate.mjs`, `skills/red-team/assets/red-team-gate.test.mjs`
- Plan slice: In `verdict(findings, coverage)`, after the existing `isIncomplete` check, partition the union of `blockers` + `needsDecision` from `classify()` into adjudicated (`f.adjudicated === true`, strict — the `deliverableAbsence`/`envGap` typed-flag pattern) and not: any unadjudicated member → `BLOCKED`; otherwise a non-empty adjudicated partition → `ADJUDICATED`; otherwise fall through to the existing minors check. `classify()` is unchanged in bucketing (adjudicated blockers still appear in `blockers` — the report must still show them); the flag is read by `verdict()` only. CLI output shape unchanged apart from the new possible `verdict` string; `summarize()`, `unwrapEnvelope`, the zero-probe refusal, and the fingerprint/coverage layers untouched. Rewrite the `verdict()` header comment ("Compute the gate over the currently-OPEN findings (the Lead removes resolved ones during the grill loop). BLOCKED = work remains; loop until it is not BLOCKED." — false under stamp-never-remove) and the CLI header comment to state the new precedence and the stamp-vs-remove split — a comment lagging the rewrite is the recorded failure class (source-comment-lags-emitted-prompt-after-rewrite); because grep is the floor, also hand-scan this file's remaining comments for verdict-enumeration prose and fix in-task stragglers (out-of-footprint stragglers → follow-up findings in the done report). Add the End-state-2 pins to `red-team-gate.test.mjs` as fixture-level tests (no fs), including the strict-flag delete-and-trace, the no-flag back-compat table, and the INCOMPLETE-outranks pin in its paired-coverage fixture shape (one adjudicated finding set, incomplete coverage → `INCOMPLETE`, complete coverage → `ADJUDICATED` — the pin must fail under any wrong precedence order and must not park the adjudicated blockers on an off-target probe, where discard would make it vacuous). New test titles/comments never restate a full verdict enumeration contiguously (sweep-token hygiene). Do NOT touch the Step-4 doc-lock test (Task 4 preserves its pinned tokens; the lock needs no update) and do NOT touch the D7 pins — the `'pass'`-only demoting set and the two-contract sentence are explicitly out of scope. Any new prose pin over Task-4-owned surfaces is forbidden here (it would be red at this task's frozen base — the very guard-split defect this plan codifies); prose guards live in Task 4's `workflow-scaffold.test.mjs`.
- requiresTest: true
- requiresPackaging: false
- deps: []
- target repo: superproject

### Task 2: Escape guard — snapshot/baseline ref-diff modes + demonstrated-red tests

- Files: `skills/red-team/assets/assert-no-repo-escape.sh`, `skills/red-team/assets/assert-no-repo-escape.test.sh`, `skills/red-team/assets/workflow-scaffold.js`
- Plan slice: Implement design rows 11–12 in the guard script, per End state 3. Argument layer first, before any check: `--snapshot <file>` and `--baseline <file>` accepted; combined → `die … 2`; a snapshot/baseline path that is not absolute, or that resolves under the `--repo` working tree, → `die … 2` — resolving **both** sides physically before the prefix compare per End state 3 (`cd … && pwd -P` on the repo dir *and* on the artifact's parent dir; missing parent → `die … 2`), because macOS `mktemp -d` hands back the `/var/folders/…` alias of `/private/var/folders/…` and a one-sided normalization makes this plan's own in-tree case red or vacuous depending on which side is normalized; extend the existing `..` traversal refusal to both new flags. Snapshot mode: run the pre-run local integrity refusal (porcelain clean + no b1-pattern local ref; a hit → `escape` exit 1, worded as pre-run residue/dirt detection — deliberately local-only, origin is not polled pre-run), then write the full local ref set via `git for-each-ref --format='%(refname) %(objectname)'` and exit 0; git or file-write failure → `die … 2`. Check mode with `--baseline`: existing checks a/b1/b2 unchanged as the unconditional floor, then the exact ref-diff — recompute the live ref set and flag any added, removed, or SHA-moved ref versus the snapshot as `escape` exit 1, naming the delta ref(s); a missing or unreadable baseline file → `die … 2` before any escape conclusion (infra, never a silent pass). Check mode without `--baseline`: byte-equivalent to today in exit codes, stdout, and check behavior, plus exactly one stderr advisory naming the heuristic ceiling and the `--baseline` upgrade. Rewrite the `ponytail:` header paragraph in the same commit — it currently records the ref-diff as a deferred upgrade path, which becomes a false code-fact; the rewritten ponytail names the **three** remaining ceilings (b2 origin-side probe-invented names — the ref-diff half is local by design, network flake stays out of a floor script; pattern-slipping refs that predate the first baselined run, which snapshot mode cannot see and the pre-run refusal cannot pattern-match; and gitignored leak paths, which `git status --porcelain` never reports, leaving them invisible to check (a), to the pre-run refusal, and to the ref-diff alike), and qualifies the pre-existing "This half is EXACT." sentence to say the porcelain half is exact for tracked and untracked-but-not-ignored paths. The ponytail also names the two things that are **not** under-detection ceilings and must not be counted among the three: the **over-detection** class (a concurrent local branch or tag created by another session/worktree during the run window, which the namespace scoping cannot remove — triaged by action-provenance per End state 6) and the End-state-18 residual (a probe that ignores the preamble and writes a ref anyway — a true positive). Also in this task, per End state 18: change the executed-probe SCOPE-LOCK preamble in `skills/red-team/assets/workflow-scaffold.js` to the End-state-18 idiom set: `git clone --no-hardlinks <repo> <tmp>` whenever the probe runs `git` (with the committed-state-only caveat and the overlay instruction), `cp -R` for read/edit-only work carrying the "if `<repo>/.git` is a file this is NOT git-isolated" clause, `--detach` for read-only inspection only, and disposal via `git -C <repo> worktree remove --force <tmp>` rather than bare `rm -rf`; state the one-clause reason that the bare `worktree add` form writes a `refs/heads/<basename>` into the target that outlives `rm -rf` and would trip the new ref-diff; touch nothing else in that file (it is otherwise out of this plan's scope, and Task 4 owns `workflow-scaffold.test.mjs`, a different file). Update the header Usage/Checks prose and keep the exit-contract paragraph, the die/escape split, the `..` traversal guard, and macOS bash 3.2 compatibility intact — every new `die` call site passes an explicit code (the standing call-site lock in the test file is the arbiter). In the test file, add the End-state-4 cases using the existing `setup_repo`/`fresh_cwd` harness (snapshot/baseline files via mktemp, always outside the target repo's working tree; a stderr-capturing runner variant for the advisory case, since `run_guard` discards output), add the two namespace-scoping cases End state 4 now requires (a `refs/remotes/*` delta → 0; a created/moved `refs/tags/*` ref → 1) without which the filter is deletable with the list still green, extend the header case list, and name all three residual ceilings in the header (b2 origin-side; pattern-slipping refs predating the first baseline; gitignored leak paths — matching the ponytail); the pre-existing cases (including the source-lock and call-site-lock cases) stay green unmodified — because grep is the floor, hand-scan the test file's case banners and comments after the additions for any count/enumeration prose the edits stale (plan-mandated-banner-count lesson) and fix in-task. The demonstrated-red banner states the equivalence claim (no-baseline check mode is check-equivalent to the pre-change script, so the `rogue`/no-baseline → exit 0 case IS the recorded miss) as fixture history — never as a pre-change run an auditor must reproduce.
- requiresTest: true
- requiresPackaging: false
- deps: []
- target repo: superproject

### Task 3: ADRs — the adjudicated-clear ADR + the ADR 0025 deps-edge amendment

- Files: `docs/adr/0043-adjudicated-clear-distinct-terminal-verdict.md`, `docs/adr/0025-drift-guard-discipline.md`
- Plan slice: Author the new ADR per End state 11 (filename resolves to the next free ADR number at land time — 0043 at drafting; if taken, take the next free and record the resolution in the done report so Task 4's citation, authored after the deps rebase, points at the real file). Contents: the operator directive as ratified input (2026-07-28, not re-litigated); the CLEARED-vs-ADJUDICATED split and why removal-as-resolution produced a fake CLEARED; stamp-never-remove; the two-arm re-verify trigger (probe executed AND patch changes what it measures); the ADR 0017 boundary sentence; the ADR 0014 per-row provenance marker; the carve-out granularity subsection (the extraction's read is the carve-out; ordered/positional-key regions atomic per adjudication J; unread residue shrink-eligible per adjudication G; bounded to runtime prompt strings by the registry-carveout lesson), citing the 2026-07-28 report's G and J rows as precedent — read-only, quoted, never edited; and the acknowledged residual risk record. Append the dated amendment note to ADR 0025 under Consequences per End state 12 — append-only: every pre-existing body sentence byte-intact (the Status currency line exempt per the adr-amendment lesson); the note records the file-disjoint-collision resolution (`deps` edge from guard task onto mirror-authoring task; same phase + same wave insufficient — frozen phase base), names the `guard-split-deps-edge` probe arm, the `/war-machine` directive, and the `/war-strategy` §3 authoring rule as forcing functions, and cites the lesson slug. The append-only condition is checked by this task's auditor seats at the pinned `audit_sha` against the task's dispatch base (End state 12 — never a ref literal; verify-stacked-plan-facts lesson). Anchor everything by named construct, never line numbers.
- requiresTest: false
- requiresPackaging: false
- deps: []
- target repo: superproject

### Task 4: Red-team prose surfaces — Step 3/4/5 rewrite, probe arm 3, lenses mirror + guards

- Files: `skills/red-team/SKILL.md`, `skills/red-team/references/lenses.md`, `skills/red-team/assets/workflow-scaffold.test.mjs`
- Plan slice: First action is the dep rebase (deps: [1, 2, 3]) so the prose documents the landed gate verdict string, the landed guard flags and refusal arms, and the landed ADR filename. In `SKILL.md`: rewrite Step 5 per End state 5, naming the stamping mechanic exactly (working-copy stamp; `--stdin` re-pipe; top-level `probeResults` wins over the `.result` envelope; the persisted task-output file is evidence, never edited in place); extend Steps 3/4 per End state 6 (snapshot immediately before Workflow launch, once per run, run-scoped absolute scratch path outside the repo tree; the snapshot exit-1 pre-run-residue arm and its resolve-before-launch rule; post-run `--baseline`; the containment rule — the branch-enumeration step is mechanical, not manual; **the action-provenance triage for a nonzero guard and its stated precedence over the containment rule** (End state 6 — a provenance-cleared foreign delta is recorded and does not block `CLEARED`); **and, per End state 18, change Step 3's own sandbox idiom** (default `git clone --no-hardlinks`; `cp -R` only with the linked-worktree caveat; `--detach` read-only, with its `worktree remove --force` disposal), matching the preamble Task 2 edits, so the Lead's prescribed clean-checkout step stops writing a `refs/heads/*` into the target that the new ref-diff would read as an escape), preserving verbatim-in-substance the three token families the Step-4 doc-lock in `red-team-gate.test.mjs` pins (task-output envelope, `.result` unwrap, zero-probe refusal) — if preservation is genuinely impossible, escalate; never edit that test from this task (Task 1 owns the file); add probe arm 3 `guard-split-deps-edge` to §Drift-guard spine probes per End state 7 and update every count word and vacuity/`--fast` clause in the section and in the Step-2 sentence; verify the Invariants never-CLEARED-on-incomplete-coverage sentence still quantifies over all verdicts (extend to name ADJUDICATED only if the quantification reads ambiguous); update the frontmatter `description` line and the "drives it to CLEARED" body prose to recognize ADJUDICATED as a proceed verdict (spec §10.4 known stragglers). In `references/lenses.md`, same commit: mirror arm 3 verbatim-in-substance into §Drift-guard spine probes (count word Two → Three); apply End state 10 (verdict enumerations, `adjudicated` schema flag, report-template verdict line, `## Adjudications` guidance comment gaining the per-row provenance token and the carve-out granularity rule citing the Task-3 ADR by its landed filename); leave the D7 two-contract sentence byte-untouched; record in the done report, per consumer, that the row shape is unchanged for the four `## Adjudications` consumers (End state 10). In `workflow-scaffold.test.mjs`: add the End-state-8 presence-pair test — **do not copy the `ff-topology` ±320-char window scoping**; use the two scopes End state 8 mandates (a heading-delimited section region for the arm name, its `deps`/vacuous/`--fast` clauses, and the three-arms NEW-count assert; file-wide markdown-normalized matching with the precise `two … probes` phrase-family pattern for the OLD-count-ABSENT assert), all patterns split-fragment-built; the **sandbox-idiom drift guard** over End state 18's two-surface mirror (pin the isolating idiom per surface on `workflow-scaffold.js` and on SKILL.md Step 3, plus an OLD-absent assert that the ref-writing bare `worktree add <tmp>` form appears on neither) **and, in the same edit, tighten the pre-existing executed-probe-copies-the-repo assertion**, whose `/cp -R|worktree add/` alternation matches the bare and `--detach` forms identically and would stay green through a revert; plus the `--baseline` token pin on SKILL.md's Step 3/4 region and the End-state-9 verdict-enumeration drift guard in the exact shape End state 9 mandates — extract the verdict literals from the `verdict()` function body of the landed `red-team-gate.mjs`, assert a **non-empty floor** and an **exact-set** `assert.deepEqual` against the five sorted literals, then compare that set against each of the two named single lines (the `- **Verdict:**` enumeration bullet inside `## Severity & gate`, and the report template's `**Verdict:**` line) by **delimiter-split token-set equality**, never plain containment and never `\b` (a `\b` boundary matches inside `CLEARED-WITH-NOTES`); run all four mandated mutation proofs — both guards land in the same task as the prose surfaces they guard, deliberately practicing the one-task rule this plan's doctrine enforces. Run the End-state-15 count-word sweep (§10.5) scoped to this task's surfaces — grep is the floor; the manual hand-scan of Step-2 prose and `workflow-scaffold.test.mjs` banner comments is mandatory and its outcome is a mandatory done-report statement even when "zero stragglers"; new banners avoid count words and never restate swept phrases contiguously.
- requiresTest: true
- requiresPackaging: false
- deps: [1, 2, 3]
- target repo: superproject

### Task 5: Authoring surfaces — war-machine directive + war-strategy rule 7 + structure-test pins

- Files: `skills/war-machine/SKILL.md`, `skills/war-strategy/SKILL.md`, `skills/war-machine/war-pipeline-structure.test.sh`
- Plan slice: In `skills/war-machine/SKILL.md`, add the **Guard-split deps-edge directive** paragraph per End state 13, placed with the existing Release-phase directive (the same standing-directive pattern): a drafted plan task that authors a drift guard over a fact authored by a different same-phase task must carry `deps: [<that task>]` — same wave is insufficient (frozen phase base); the grill agent asks it; a split the drafter cannot edge (e.g. would create a cycle) is raised via the ADR triad, never silently shipped. In `skills/war-strategy/SKILL.md` §3, extend the "Drift-guard coverage" subsection with authoring rule 7 carrying the same rule for plans authored or converted through `/war-strategy` (the non-machine path `/war-machine` consumes by reference), and update the subsection's own "two authoring rules" count word — a default-flip that carries **its own OLD-absent assert** in the structure test below (split-fragment-patterned), because the presence pins cannot supply one and the only pre-existing guard, `check '^### Drift-guard coverage'` in `skills/war-strategy/war-strategy-structure.test.sh`, is a prefix regex blind to the count word; the in-task hand-scan of the subsection remains, as the floor it always was. Both new texts cite the amended ADR 0025 (landed by Task 3 — the dep rebase makes it citable) as the canonical record and name the `/red-team` `guard-split-deps-edge` probe arm as the downstream enforcement (defined-but-not-yet-emitted at this task's base; produced in Task 4, same phase — the arm name is this plan's literal, not a file citation). In `skills/war-machine/war-pipeline-structure.test.sh`, add **`has_i`** pins asserting the guard-split deps-edge doctrine on both edited surfaces — case-insensitive and anchored mid-sentence (`guard-split deps-edge` plus a clause anchor such as `same wave is insufficient`), never on the heading-position capital, because that file reserves case-sensitive `has()` for "Flag/token literals that never re-case" and ships `has_i()` for exactly this prose class — plus the OLD-absent assert for the `two authoring rules` phrase (split-fragment-built so the test never self-matches). The file already pins MACHINE and WAR_STRATEGY prose — the guards-follow-their-text pattern; the pins land in the same task as the text they pin, per this plan's own doctrine. No other change to the three files.
- requiresTest: true
- requiresPackaging: false
- deps: [3]
- target repo: superproject

### Task 6: Verdict-enumeration cascade + CONTEXT.md domain terms + the §10.4 sweep

- Files: `CONTEXT.md`, `skills/war/references/design.md`, `skills/war-campaign/SKILL.md`, `README.md`
- Plan slice: First action is the dep rebase (deps: [1, 3, 4]) so the sweep measures the integrated tip carrying Task 4's landed prose. One-line cascade edits per End state 14: in `skills/war/references/design.md`, the `/red-team` Outcome bullet ("patches the plan in place until **CLEARED**") gains ADJUDICATED as a proceed verdict; in `skills/war-campaign/SKILL.md`, the halt-and-hold clause ("when a plan can't `CLEAR` `/red-team`") is qualified so an ADJUDICATED outcome proceeds (only a truly-unresolvable BLOCKED/INCOMPLETE halts); in `README.md`, the /red-team overview sentence ("patches the plan in place until it is **CLEARED**") gains the same qualification — a survey-verified straggler the spec's own scope missed (grep-is-a-floor practiced on the spec itself); the README `## Status` section is NOT touched here (Task 7, next phase, owns it). In `CONTEXT.md`, add the five spec-§6 terms with the corpus's term/_Avoid_ shape, placed in the red-team glossary section (`### Red-team plan-vs-state grading`) beside the existing **Adjudication** and **Sandbox-escape guard** terms (**Guard-split deps edge** may sit with the `### Drift-guard discipline` terms if that section reads more coherent — placement anchored by named subsection, worker's judgment): **ADJUDICATED (verdict)**, **Adjudicated flag**, **Adjudication provenance marker**, **Ref-diff baseline**, **Guard-split deps edge**. Extend, don't rewrite: the existing escape-guard "never `CLEARED`" line stays as written unless the survey shows real ambiguity. Then run the End-state-15 verdict-enumeration sweep (§10.4, widened scope): `grep -rn 'CLEARED-WITH-NOTES'` across `skills/`, `docs/adr/`, `CONTEXT.md`, `README.md` — every enumeration hit must list ADJUDICATED or be individually justified in the done report — plus the mandatory manual same-scope survey of the spec-named surfaces and the four `## Adjudications` consumers (End state 10) for verdict-list prose the token misses (grep is the floor). Routing: a straggler inside this task's four files is fixed in-task as a survey-derived correction; a straggler anywhere else — including ADR 0033, which is never edited (spec non-goal) — is recorded as a follow-up finding (disposition `follow-up`), never fixed in-task and never prose-waived (ADR 0017). The done report states both sweep outcomes explicitly, even when "zero stragglers".
- requiresTest: false
- requiresPackaging: false
- deps: [1, 3, 4]
- target repo: superproject

## Phase 2 — Release

### Task 7: Version bump — all four release slots

- Files: `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`, `README.md`
- Plan slice: Bump all four version slots in lock-step to the **next free patch above the live integration base at land time** — never a plan literal; re-read the slots at the rebased tip. `version-slots.test.mjs` (lock-step + monotonic floor) is the arbiter. Replace the README `## Status` paragraph in place (never an empty field, no badge), describing: the ADJUDICATED terminal verdict (typed per-finding flag, stamp-never-remove, distinct from CLEARED — no probe re-proof), the escape guard's exact ref-diff snapshot/baseline modes (say "flags any added, removed, or moved local ref against the pre-run snapshot" — never overstate to origin, whose probe-invented-name ceiling remains; the snapshot mode also refuses to baseline pre-existing residue), and the guard-split deps-edge doctrine (probe arm + war-machine directive + war-strategy rule + ADR 0025 amendment). Blurb discipline from the recorded lessons: the opening count word must match the blurb's own enumeration; any quoted code literal must byte-match the landed identifier (grep the rendered literal before commit); guard semantics stated no wider than the implementation.
- requiresTest: false
- requiresPackaging: false
- deps: []
- target repo: superproject

## Deferred validations (backstops — AI-declared)

- Cross-plan shared-doc contention: `CONTEXT.md`, `README.md`, `skills/war-campaign/SKILL.md`, `skills/war-machine/SKILL.md`, `skills/war-strategy/SKILL.md`, and `skills/war/references/design.md` are common doc touchpoints later plans in this campaign may also edit · **DISCHARGED at red-team time (2026-08-02)** — restated against live state: all three siblings are converted **plans**, not unconverted specs (all four plans landed in the same commit as this one), and the roadmap this backstop deferred to already exists with its `## Shared-file contention` table recording this plan as single-owner of every file listed above. Verified against the siblings' actual plan `Files:` footprints (not spec §5 tables): none touches any of these six doc surfaces, and none mints an ADR, so `0043` is uncontested · runner: discharged — no re-verification owed; a later inbox drop into this campaign re-opens it via /war-campaign's own `sweep` contention check.
- Manual same-scope survey halves of the §10.4 verdict-enumeration sweep (Task 6) and §10.5 count-word sweep (Task 4): done-report-only evidence, which gate-audit reads as SOFT and never a hold (deliberately-uncommitted-probe lesson class) · why deferred: a hand-scan cannot be a mechanical gate member · runner: the owning task's worker records the outcome (mandatory statement even when empty); the Lead re-runs both greps and spot-checks one named survey surface per sweep at phase close.
- Origin-side probe-invented-name escapes remain heuristic-only detection, and pattern-slipping refs that predate the first baselined run cannot be detected by snapshot mode (nothing can see them without an earlier baseline) · why deferred: named residual ceilings, not buildable checks in this plan (the ref-diff half is deliberately local — network flake stays out of a floor script, spec §8) · runner: the rewritten guard header and the test-file header both name the ceilings and the upgrade trigger; a recurrence reopens it (ponytail posture, ADR 0025 family).
- **Gitignored leak paths are invisible to the whole guard** — `git status --porcelain` does not report ignored files, so a probe writing into `node_modules/`, `*.log`, `screenshots/`, `.claude/teams/`, or `war-memory-queries.jsonl` escapes check (a), the snapshot-mode pre-run refusal, and the ref-diff alike (reproduced at the phase base, red-team 2026-08-02) · why deferred: widening check (a) to `git status --porcelain --ignored` is a behavior change needing its own back-compat pin and a ruling on legitimately-ignored directories (`.claude/teams/` is written by every WAR run), which is out of this plan's scope · runner: this plan names it as the third ceiling in the rewritten ponytail and the test-file header; the widening is a follow-up issue filed at phase close, and a demonstrated ignored-path escape reopens it immediately.
- Adjudicated-flag stamping is prompt-enforced (no hook verifies the Lead stamps only patched + adjudication-rowed findings) · why deferred: accepted residual per spec §8 — same trust class as, and strictly narrower than, today's removal-of-resolved-findings mechanic · runner: the report itself is the audit surface (adjudicated blockers stay listed in `blockers` with their rows); any /red-team consumer can re-pipe the finding set through the gate to reproduce the verdict, per the Step-5 mechanic this plan pins.

## Notes / conscious deviations

Grill adjudications (--afk: no operator; self-adjudicated per the ADR triad, ratify in /red-team). Classification key: [R] hard to reverse, [S] surprising without context, [T] genuine trade-off. Predecessor-consistency check (ADR 0014): the intent blocks of `2026-08-02-structural-test-nonvacuity`, `2026-07-28-prompt-surface-simplification`, and `2026-07-28-audit-evidence-precedence` were read — this plan's intent matches their tone, scope discipline (no re-litigation of the ratified tree), HARD/SOFT end-state tagging, and guards-travel-with-text constraints; no divergence to record.

1. **[S] Intent-heading provenance.** `/war-machine --afk` conversion: `## AI-Commander's Intent` and the AI-declared backstop heading (ADR 0014); any heading-extraction surface must recognize both intent headings.
2. **[S] One task owns each shared prose file; code tasks + one prose task.** `skills/red-team/SKILL.md` carries edits for #1207 (Step 5), #1242 (probe arm 3), and #1244 (Steps 3/4) — a task-per-issue carve is a triple same-file collision, so Task 4 owns the file whole (same for `lenses.md`), with `deps` edges onto every code task so the prose is audited against landed behavior, never dangling refs at a frozen base. The Step-4 doc-lock collision is resolved by ownership: Task 4 preserves the lock's three pinned token families verbatim-in-substance, the lock needs no update, and any new prose pin lands in Task-4-owned `workflow-scaffold.test.mjs`, never in Task-1-owned `red-team-gate.test.mjs`.
3. **[S] All three mechanical guards ride Task 4.** The `guard-split-deps-edge` presence-pair test, the verdict-enumeration drift guard, and the sandbox-idiom drift guard (added at red-team round 2) land in Task 4; the first two sit in the same task as the prose surfaces they guard, and the third guards a surface Task 2 authors — reached by the `deps: [2]` edge, which is this plan's own doctrine rather than an exception to it — a deliberate self-application of the mirror-and-guard-one-task rule (ADR 0025) and of the very deps-edge doctrine this plan establishes; splitting either into a sibling task would itself be the #1242 defect. The verdict-enumeration guard also answers the "who catches the NEXT verdict widening" question mechanically instead of accepting silent drift.
4. **[S] Count flip guarded as a default-flip.** "Two → Three" is a default-flip-shaped change: the presence-pair guard asserts the OLD count phrase family ABSENT **file-wide** on both surfaces (markdown-normalized — the live Step-2 instance splits the token with bold markers; a precise phrase-family pattern, not a bare `two`; split-fragment patterns so the guard never self-matches), never NEW-present alone (default-flip-must-audit-all-doc-surfaces). Region scope is deliberately **split** from the presence half per End state 8 — the OLD-absent assert is file-wide because the Step-2 instance lies 7,455 chars outside the probe section, while the presence/NEW-count half uses a heading-delimited section region; an earlier draft of this note claimed section scope for both halves and was wrong (red-team adjudication 2).
5. **[S] Cascade folded in; sweep scope widened to README.** The spec conditioned folding `design.md`/`war-campaign` on a contention check: performed and recorded (the cross-plan shared-doc contention backstop carries the verified facts). Conversion-time survey also found a live straggler the spec's §10.4 scope misses — README's "patches the plan in place until it is **CLEARED**" — so the sweep scope gains `README.md` and Task 6 fixes that line (Task 6/Task 7 both touch README across a phase edge, which is serial by construction — no collision).
6. **[S] ADR number is next-free, non-authoritative.** `0043` is the drafting-time next free number; Task 3 resolves the real number at land and Tasks 4/5 cite the landed filename after their dep rebases (version-literal discipline applied to ADR numbers). Verified: no sibling spec in this campaign mints a competing ADR at conversion time (the references-pointer spec defers its optional one out of scope); the roadmap re-verifies.
7. **[T] Demonstrated-red encoding.** The spec's "pre-change guard exits 0" repro cannot literally run a pre-change script inside the post-change suite; but no-baseline check mode is check-equivalent to the pre-change behavior by design, so the standing `rogue`-branch/no-baseline → exit 0 case IS the recorded miss, preserved as a live control (plus the advisory assert), and the `--baseline` → exit 1 case is the fix. The equivalence is stated in the case banner as fixture history — an End state demanding pre-change behavior would force the auditor into a cannot-confirm hold (deliberately-uncommitted-probe lesson).
8. **[S] "Byte-for-byte" back-compat scoped.** The no-`--baseline` advisory contradicts a literal byte-for-byte claim, so back-compat is scoped to exit codes + stdout + check behavior, with the advisory as the sole stderr delta. Verified safe at conversion: the existing suite's `run_guard` discards stdout and stderr (`>/dev/null 2>&1`), so no pre-existing case can red on the advisory; the new advisory case uses a stderr-capturing runner variant.
9. **[T] Guard tightenings beyond spec rows 11–12, all logged for ratification.** (a) Snapshot mode refuses a dirty start: porcelain + b1 local-pattern checks run before writing, exit 1 on a hit — a baseline taken over pre-existing residue would launder exactly the #1244 cross-run shape, and pre-run detection also stops a probe-escape misattribution; origin is deliberately not polled pre-run (network stays out of the pre-launch path; b2 remains the check-mode floor). Residual: a pattern-slipping ref predating the first baselined run is undetectable by construction — documented in both headers and the origin-side/pattern-slipping backstop (cited by subject; the backstop list is unnumbered bullets). (b) Path containment: a non-absolute or in-repo-tree snapshot/baseline path is `die 2` at parse time — an in-tree artifact would false-trip the porcelain half into a fake escape; prose-only enforcement was rejected because the self-trip fires as a confusing exit 1, not a diagnosable 2. (c) `--snapshot` + `--baseline` combined is a usage error (`die 2`). (d) **Ref-diff namespace narrowing** to `refs/heads/`+`refs/tags/`, excluding `refs/remotes/*` — departs from spec row 11's "any added, removed, or moved ref … regardless of name"; taken because `refs/remotes` moves on any background fetch and this repo shares one ref store across 47 linked worktrees, so the unscoped form makes `CLEARED` unreachable (red-team adjudication 4, reproduced both ways). Name-agnosticism is preserved *within* the scoped namespaces. (e) **Footprint expansion**: `skills/red-team/assets/workflow-scaffold.js` is added to Task 2, a file the spec's surface list never contemplates (it names only `workflow-scaffold.test.mjs`) — required because End state 3's ref-diff is unusable while the sanctioned sandbox idiom writes refs into the target (red-team adjudication 3/11). All five are small, testable, and reversible; the spec left snapshot-mode internals open.
10. **[T] war-strategy §3 gains rule 7 — a fourth doctrine surface beyond the spec's three.** The spec's design row 9 places the authoring-side forcing function only in `/war-machine`, but plans converted directly by `/war-strategy` (the documented non-machine path) would get zero forcing function, and `/war-machine` consumes §3 by reference — so one rule there covers both paths. Deviates from the resolved design tree's "three surfaces" count; logged for /red-team ratification. The subsection's "two authoring rules" count word updates with it (same task), guarded by its own OLD-absent assert in `war-pipeline-structure.test.sh` — an earlier draft of this note called it "pinned" by the presence pins, which cannot assert an old phrase absent (red-team adjudication 8).
11. **[S] The war-machine grep criterion is superseded by structure-test pins.** Spec §10.8's "a grep for `deps` in that file hits it" fails the delete-the-feature test (any future `deps` mention satisfies it vacuously); `war-pipeline-structure.test.sh` already pins MACHINE and WAR_STRATEGY prose, so both directive texts get `has_i` pins there — case-insensitive, mid-sentence anchors; a `has` pin would red on a benign sentence-case rewrite (red-team adjudication 7) — same task (guards-follow-their-text). Task 5's `requiresTest: true` is earned by that test edit.
12. **[S] Step-5 stamping mechanic pinned in prose.** "Gate-emitted, never hand-written" is only as strong as the reproducibility of the stamp-and-re-pipe path, so Step 5 names it exactly: working copy of the gate-input object, stamp there, `--stdin` re-pipe, top-level `probeResults` wins over the `.result` envelope, persisted task output never edited in place. Two Leads can no longer invent divergent mechanics.
13. **[S] `## Adjudications` consumers adjudicated no-change.** The per-row provenance token rides inside each preformatted row string; the four downstream consumers (`skills/war/SKILL.md` step 5, `schemas.md` "row shapes unchanged", `workflow-template.js` `adjudicationClause`, `agents/war-auditor.md`) describe rows as freeform preformatted strings, so no consumer edit is needed — Task 4's done report states this per consumer, and Task 6's manual survey covers those files so the claim is checked, not assumed.
14. **[S] Wave edges are citation/measurement dependencies, never collision dodges.** All six Phase-1 file sets are disjoint; Task 4's deps make its prose document landed behavior, its extraction guard read landed gate source, and its ADR citation resolve; Task 5 cites the amended ADR 0025; Task 6's deps make the §10.4 sweep measure the integrated tip (within-phase-dep-gate-must-rerun-on-integrated-tip). ADR 0025 append-only verification is owned (Task-3 auditors, audit_sha vs dispatch base) rather than left as an unrunnable free-floating criterion (ADR 0017).
15. **[S] Release phase included.** Gate behavior, a floor script, and skill doctrine all ship to marketplace-pinned users. Expected integration base: the landed tip of `docs/plans/2026-08-02-structural-test-nonvacuity.md` (ADR 0011 stack-and-plow; the roadmap may reorder — the directive form is base-agnostic). Standalone-fallback rule: a plan run through plain /war resolves the next free patch from the four slots itself.
16. **Out of scope (spec §9):** reopening prompt-surface-simplification phases 4.1/5.1 or editing the 2026-07-28 report (read-only precedent); re-litigating the operator directive; any ADR 0017 or ADR 0033 edit; gate-side NLP/prose parsing; origin-side ref-diff and any porcelain-half change; a mechanical decompose-time deps-edge hook (doctrine + probe, not a scanner — ADR 0025 posture); /war-review adjudication-frequency telemetry.

## Open decisions

- None. The spec's design tree is fully resolved; every conversion-time judgment is logged above for /red-team ratification.
