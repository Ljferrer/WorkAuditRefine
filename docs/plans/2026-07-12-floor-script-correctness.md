# Floor-script correctness: Dockerfile continuation join, ref-accurate .gitmodules, guard-message extraction fidelity, vacuous-packaging advisory

Source spec: docs/specs/2026-07-12-floor-script-correctness-design.md
Issues addressed: #800, #802, #803, #819

## AI-Commander's Intent

- **Purpose:** Four recorded correctness ceilings in the refiner-side merge floors silently degrade what each floor proves: the packaging floor drops backslash-continued `COPY`/`ADD` sources and fail-opens on exactly the Dockerfiles it polices while re-parsing each Dockerfile O(N×M) times (#800); the submodule floor's `--declared` mode reads the working tree's `.gitmodules` instead of the branch ref's, so declared-path judgment can diverge from the diff endpoints (#802); the guard-specificity floor's `extract_msg()` records the wrong quoted token on stderr-emit guards and perpetually flags bare-`$var` `die` messages, polluting the gate-audit `guard_specificity` evidence (#803); and a plan that explicitly declares `requiresPackaging: true` gets no signal when the packaging floor run is structurally vacuous under its ADR-0017-ratified scope (#819, advisory surfacing only — the ratified no-op shapes stand). Fix all four inside the existing contracts.
- **Method:** Pre-join backslash continuations into logical lines inside `parse_dockerfile` and invert the main scan loop to Dockerfile-outer/added-file-inner so each Dockerfile parses exactly once (flagged-pair *set* proven unchanged via sort-both-sides comparison; single call site locked by a structural count assertion); add an opt-in `--advise-vacuous` flag that prints one stderr advisory line per ratified vacuous shape (no Dockerfile; non-empty `diff_out` with zero A/R/C paths — the existing `diff_out` capture already distinguishes this from an entirely empty diff), citing the existing 2026-07-08 ADR 0017 addendum, with stdout and all exit codes byte-identical without the flag; thread ` --advise-vacuous` into all three dispatched refiner invocations of the packaging floor in `workflow-template.js` (initial merge, floor-retry, baseline-proceed) via a second named const `advisePackagingVacuous = r.task.requiresPackaging === true` (strict equality — never the existing `!== false` default const, which stays beside it with a comment explaining why two predicates on one field coexist), leaving the polish-sweep skip clause and `requiresPackaging:false` clauses flagless; replace the submodule floor's Step-3 working-tree read with a `cat-file -e` existence probe plus `git config --blob "$branch:.gitmodules"` (absent → clean skip; read-failure after a successful probe → exit 2; working-tree fallback removed outright — no `rev-parse` pre-check needed because Step 1's `git diff` already dies 2 on a bad ref in both modes before Step 3 runs); scope `extract_msg` to the emit segment between the `echo`/`printf` keyword and `>&2`, and in `record_guard` skip bare-variable messages and truncate `printf` format strings to their pre-`%` literal prefix (empty/whitespace prefix → skip; `%%` spuriously truncates but the prefix stays a true substring of emitted text — safe, ceiling-commented). Every change stays inside the 0/1/2 exit contract (1 = named route, 2 = git error, never collapsed), touches no discovery set, keeps the guard-specificity floor advisory-evidence-only, updates every header/contract comment stating a superseded mechanism in the same diff (grep sweep + manual same-scope survey, recorded as a `Survey:` block in the implementation commit message body, stragglers listed as survey-derived corrections), and proves every RED-pre-change claim in-run: the worker re-runs each new fixture against the base-ref script (`git show <phase-base>:<script>` to a temp copy) and records the RED evidence in its done-report for the auditor to reproduce at the pinned `audit_sha`.
- **End state:**
  1. `bash skills/war/assets/assert-packaging-in-diff.test.sh` passes with a backslash-continued multi-line enumerated-`COPY` fixture where the floor flags an added uncovered sibling (exit 1) and passes a covered one (exit 0) — both RED against the pre-change script, evidence in the worker done-report — plus a mid-continuation `#` comment fixture parsing to the same source set as its comment-free equivalent, plus a dangling final-line-`\` fixture (file's last line ends in `\`) parsing without hang or dropped line.
  2. `parse_dockerfile` has exactly one call site, in the Dockerfile-outer loop, locked by a test assertion counting invocation sites in the script (definition excluded), and a multi-F × multi-D fixture's flagged-pair **set** equals the pre-change set via sorted-lines comparison plus pair-count equality (a dropped pair cannot hide in a reorder).
  3. With `--advise-vacuous`: no-Dockerfile fixture → exit 0 + one stderr advisory naming the no-Dockerfile shape; Modified-only-diff fixture → exit 0 + the A/R/C-empty advisory; both advisories grep-locked to contain `ratified scope` and `ADR 0017`; a flagged run → exit 1, unchanged stdout pairs, no advisory; an entirely empty diff with the flag → exit 0, stderr byte-empty. Without the flag: stderr byte-empty on both vacuous shapes.
  4. `node --test skills/war/assets/workflow-template.test.mjs` passes with cases proving **all three** dispatched packaging-floor prompts (initial merge, floor-retry, baseline-proceed) carry `--advise-vacuous` for a task with explicit `requiresPackaging: true`, the initial merge prompt omits it for a defaulted (undefined) task (the delete-the-feature lock: collapsing `=== true` into the `!== false` const turns this RED), and a source-level assertion counts the packaging-floor invocation literals in dispatched run-instructions and requires every one to carry the conditional flag interpolation (a two-of-three thread is RED).
  5. `bash skills/war/assets/assert-no-submodule-mutation.test.sh` passes with a `--declared` fixture where the branch itself adds/edits `.gitmodules` and refusal keys on the branch-ref declaration, plus the inverse fixture proving a working-tree-only declaration no longer influences Step 3; `grep -c 'git config -f' skills/war/assets/assert-no-submodule-mutation.sh` → 0; the test file's header note and case-10/12 rationale comments no longer assert the working-tree read (reworked in the same commit).
  6. `bash skills/war/assets/assert-guard-specificity-in-diff.test.sh` passes with: shape-1 stderr-emit fixture covered via the real message (RED pre-change: `$path` mis-extracted); shape-1 negative (test asserts only the old mis-extracted token) flagged; `printf 'error: %s' … >&2` guard covered via the truncated `error: ` prefix; a `%%`-bearing format string recorded as its pre-`%` prefix without crash; shape-2 `die "$msg"` fixture producing no uncovered line at all (RED pre-change: perpetual flag); a partially-interpolated literal still recorded and matched; the already-landed drift-guard-tightening parity cases (10b, match_default extraction) untouched and green.
  7. The updated comment surfaces (packaging main-loop banner + `D_COVERS` "most recent parse" note, submodule header usage line + Step-3 banner + test-file old-semantics comments, guard-specificity `MSG =` contract + KNOWN LIMITS block) contain no statement of the superseded mechanism, verified by the three manual same-scope surveys recorded as `Survey:` blocks in the implementation commit messages, stragglers listed as survey-derived corrections.
  8. `docs/adr/0017-packaging-floor-docker-gate-ratified-backstops.md` carries a dated addendum note recording the explicit-declaration advisory surface, scope itself unchanged.
  9. Full suites green: `node --test 'skills/**/*.test.mjs'` and the anchored shell-test loop over `hooks/` and `skills/`.
  10. All four release slots bumped in lock-step (version-slots test green) as the trailing phase.

## Build order (for /war)

1. **Phase 1 — Floor fixes** — four tasks. Wave 1: Task 1 (packaging script), Task 3 (submodule floor), Task 4 (guard-specificity floor) in parallel, file-disjoint. Wave 2: Task 2 (engine flag threading + ADR note), `deps: [1]`.
2. **Phase 2 — Release** — version bump, lands last.

Launch precondition (stacking): Phase 1's frozen base must already carry BOTH prior plans landed on the working branch — verify before provisioning: `grep -q 'FILES_CHANGED_RULE' skills/war/assets/workflow-template.js` (war-launch-entry-validation landed) AND `grep -A6 'integratedTipGate' skills/war/assets/workflow-template.js | grep -q 'gate_log_path'` (audit-gate-evidence-fidelity landed). Either grep failing = the base is stale — halt, do not provision; /red-team re-proves both probes at ratification.

## Phase 1 — Floor fixes

### Task 1: Packaging floor — continuation join, parse-once inversion, `--advise-vacuous` flag (#800 + #819 script side)

- Files: `skills/war/assets/assert-packaging-in-diff.sh`, `skills/war/assets/assert-packaging-in-diff.test.sh`
- Plan slice: (spec §4.A + §4.B.1) Inside `parse_dockerfile`, after `pd_body` capture, insert a join pass folding physical lines whose last non-whitespace character is `\` into single-space-joined logical lines, dropping full-line `#` comments encountered while a continuation is open; a dangling continuation on the file's LAST line terminates the logical line at EOF (no hang, no dropped tokens — fixture below); the existing tokenizing loop reads logical lines unchanged; `# ponytail:` comments naming the ceilings (trailing backslash + trailing spaces = no continuation, strict BuildKit; a backslash inside a quoted token is treated as a continuation marker only when line-final — quoted-token backslashes are a documented ceiling, no fixture; heredoc/JSON forms stay deferred per the existing spec-§9 comment). Invert the main scan loop to outer `while` over `$dockerfiles` calling `parse_dockerfile "$D"` once, inner `while` over `$added_files` running the existing Step 1–4 probes against `D_LIT_DIRS`/`D_COVERS`; the per-F skips (`.dockerignore`, `is_dockerfile "$F"`) move inside the inner loop or hoist to a pre-filtered list (behavior identical). Add optional flag `--advise-vacuous` to the argument parser (style mirrors `--repo`); with it, the two structural no-op exits each print one stderr advisory line naming its shape and citing the ratified scope (the advisory cites the EXISTING 2026-07-08 ADR 0017 addendum already named in the script's SCOPE RATIFICATION header — no dependency on Task 2's new note): (a) the `dockerfiles`-empty exit; (b) the `added_files`-empty exit **only when `diff_out` is non-empty** — the existing `diff_out` variable already distinguishes this from an entirely empty diff, which stays silent; without the flag, behavior byte-identical. Stdout and all exit codes unchanged everywhere (pivotal constraints 1–2). Token sweep: grep `parse_dockerfile` across the script and rework every call/comment site for the parse-once shape, then hand-scan the section banners and helper comments (the `D_COVERS` "most recent parse_dockerfile call" note above `covers_file`, the main-loop banner, the header's per-F/per-D algorithm prose) for stale per-pair phrasing; record the survey as a `Survey:` block in the commit message body listing each surface and verdict; stragglers = survey-derived corrections. Tests: continuation fixture flagging an uncovered sibling (exit 1) and passing a covered one (exit 0); mid-continuation comment parity; dangling final-line-`\` fixture; advisory cases (flag + no-Dockerfile → exit 0 + stderr advisory; flag + Modified-only diff → exit 0 + advisory; flag + flagged case → exit 1, no advisory; flag + entirely empty diff → exit 0, stderr empty; no flag → stderr silent on both vacuous shapes); advisory prose lock — both advisory lines grep-match `ratified scope` and `ADR 0017`; a structural assertion counting `parse_dockerfile` invocation sites in the script (exactly one call, definition excluded — this is the delete-the-inversion lock; the continuation fixtures are the delete-the-join lock: each feature's removal REDs its own assertion independently); a multi-F × multi-D fixture asserting the flagged-pair set via sorted-lines comparison PLUS pair-count equality — never a hand-rewritten ordered block (existing single-pair `grep -qF` assertions are order-insensitive and expected to survive the inversion unmodified; any that don't are re-baselined with the set proven equal). RED-pre-change evidence: run each new continuation fixture against a temp copy of the base-ref script (`git show <phase-base>:skills/war/assets/assert-packaging-in-diff.sh`), record the RED exit/output in the done-report; the auditor reproduces this at the pinned audit_sha. bash-3.2-safe, cwd-independent.
- requiresTest: true
- requiresPackaging: false
- deps: []
- target repo: superproject

### Task 2: Engine-side conditional flag threading + ADR 0017 addendum note (#819 engine side)

- Files: `skills/war/assets/workflow-template.js`, `skills/war/assets/workflow-template.test.mjs`, `docs/adr/0017-packaging-floor-docker-gate-ratified-backstops.md`
- Plan slice: (spec §4.B.2–4) In `workflow-template.js`, beside the existing `const requiresPackaging = r.task.requiresPackaging !== false` (the floor-run default), add a SECOND named const `const advisePackagingVacuous = r.task.requiresPackaging === true` with an adjacent comment stating why two predicates on one field coexist (floor runs by default; the advisory fires only on explicit plan declaration — collapsing them re-noises every defaulted task in every non-docker repo, the exact #819 anti-noise requirement). Append the flag AFTER the branch token (`assert-packaging-in-diff.sh ${ph.integrationBranch} ${r.task.branch}${advisePackagingVacuous ? ' --advise-vacuous' : ''}` shape) at ALL THREE dispatched invocation sites — the initial merge-task prompt, the floor-retry merge prompt, and the baseline-proceed re-merge prompt — suffix position deliberately: every existing `workflow-template.test.mjs` packaging assertion matches the script-name-plus-refs prefix or uses defaulted/false tasks, so ZERO existing assertions re-baseline (verified at reconciliation: the §4.2 main-merge regex and clause slice run a defaulted task, the `requiresPackaging:false` and polish-skip cases never carry the flag, the retry case matches the script name only). Where the flag is threaded, extend each prompt with one clause: the flag may print an informational `advisory` line on stderr — exit 0 still means proceed, never treat the advisory as an error or report it as a finding. The polish-sweep skip clause and the `requiresPackaging:false` skip clauses stay flagless and skip-worded (spec §8). Token sweep: grep `assert-packaging-in-diff.sh` across `workflow-template.js` and confirm the conditional at every dispatched invocation site, then hand-scan the same-scope prompt-building blocks plus `agents/war-refiner.md` and `agents/war-worker.md` for prose describing the packaging floor's invocation or output; `agents/war-refiner.md` step 5 documents the bare invocation and is EXPECTED to surface as a straggler once dispatches conditionally append the flag — the pre-adjudicated survey-derived correction is one clause on that step-5 line (flag appended when the plan explicitly declares `requiresPackaging: true`; its stderr advisory is informational, exit 0 stands), landing on top of the audit-gate-evidence-fidelity plan's already-merged shape of that file; record the survey as a `Survey:` block in the commit message. Add a short dated addendum note to ADR 0017: the ratified scope now carries an explicit-declaration advisory surface; scope itself unchanged. Tests (`workflow-template.test.mjs`): with explicit `requiresPackaging: true`, capture and assert `--advise-vacuous` in ALL THREE built prompts — initial merge (main-merge driver), floor-retry (unpackaged-then-merged driver, existing pattern at the §4.2 retry case), baseline-proceed (baseline-classified driver, existing #598 pattern); with a defaulted (undefined) task the initial merge prompt OMITS the flag — the delete-the-feature lock (collapsing `=== true` into `!== false` makes defaulted tasks carry the flag → RED; lesson `weak-test-assertion-passes-without-feature-being-exercised`); a source-level assertion extracting every `assert-packaging-in-diff.sh` invocation literal inside dispatched run-instructions from the workflow source and requiring each to carry the `advisePackagingVacuous` conditional interpolation (count = 3; a two-of-three thread is RED — pivotal constraint 4's silent-drift-between-rounds). No MergeResult field, no schema change, no evidence relay (spec §3 YAGNI row).
- requiresTest: true
- requiresPackaging: false
- deps: [1]
- target repo: superproject

### Task 3: Submodule floor — ref-accurate `.gitmodules` read (#802)

- Files: `skills/war/assets/assert-no-submodule-mutation.sh`, `skills/war/assets/assert-no-submodule-mutation.test.sh`
- Plan slice: (spec §4.C) Replace Step 3's working-tree resolution (the `gitmodules_file` existence check + `git config -f`) with: probe `$git_cmd cat-file -e "$branch:.gitmodules"` — on failure, skip Step 3 clean (no declared paths, today's no-op semantics); on success, `submod_paths="$($git_cmd config --blob "$branch:.gitmodules" --get-regexp '\.path$' | awk '{print $2}')"` with a non-zero pipeline dying `2` (git error, never a clean pass). No `rev-parse --verify` pre-check: Step 1's `git diff --raw "$base...$branch"` already dies 2 on an unresolvable ref in BOTH default and `--declared` modes before Step 3 runs (locked by existing test case 4), so a `cat-file -e` failure after Step 1 succeeded can only mean the file is absent at `$branch` — state this in the Step-3 comment. Retire the `gitmodules_file` variable; remove the working-tree fallback outright (spec §3). The Step-2 gitlink-mode check remains the ref-independent backstop, untouched. Engine-side `--declared` threading in `workflow-template.js` is untouched — script-level-only coverage, ratified. Token sweep: grep `git config -f` and `gitmodules_file` across the script and convert every working-tree read, then hand-scan the header comment block and Step-3 banner (the usage line citing "cross-checked via `git config -f .gitmodules`", the "collect submodule paths from .gitmodules" banner, the "Resolve .gitmodules location" comment) PLUS the test file's known old-semantics comments — the header note ("The guard reads the WORKING-TREE .gitmodules, so the step-3/12 fixtures leave the working tree ON the task branch") and the case-10 and case-12 fixture rationales that deliberately park the working tree on the task branch — retitling/reworking each in the same commit (lesson `relaxed-assertion-test-title-must-update-together`); record the survey as a `Survey:` block in the commit message. Tests: a `--declared` fixture where the **branch itself adds/edits `.gitmodules`** declaring a new submodule path and touches non-gitlink content under it — refusal keys on the branch-ref declaration (RED pre-change with the working tree parked off-branch; RED evidence against the base-ref script in the done-report); the inverse fixture (working tree carries a declaration the branch ref lacks) proving the working tree no longer leaks in; build fixtures with plumbing (`git update-index`/commit, then dirty the working tree), not checkout gymnastics, cwd-independent (spec §8); existing cases 10/12 re-verified — their fixtures no longer NEED the working tree parked on the task branch and are simplified or comment-corrected accordingly. Attempt an assertion that blob-read failure after a successful `cat-file -e` probe exits 2; if not cheaply manufacturable, document in the test file and rely on the backstop below.
- requiresTest: true
- requiresPackaging: false
- deps: []
- target repo: superproject

### Task 4: Guard-specificity floor — `extract_msg` fidelity (#803)

- Files: `skills/war/assets/assert-guard-specificity-in-diff.sh`, `skills/war/assets/assert-guard-specificity-in-diff.test.sh`
- Plan slice: (spec §4.D) In `handle_added_line`'s stderr branch, extract the message from the emit segment: locate the emitting keyword (`echo`/`printf` — exactly the token set the header contract names, never widened) and call `extract_msg` on the text between it and the `>&2` token instead of the whole line; lines with `>&2` but no recognizable emit keyword keep today's whole-line behavior (documented ceiling; `# ponytail:` multi-command emit segments and `cat <<EOF >&2` heredocs stay undetected). In `record_guard` (the single choke point both the `die` branch and the stderr branch route through): a message that is a bare variable reference (`$name`/`${name}` and nothing else) → return without recording; a message containing `%` → truncate to the literal prefix before the FIRST `%` (a `%%` literal-percent is spuriously truncated too — safe, the prefix remains a true substring of the emitted text; `# ponytail:` ceiling comment naming a conversion-aware parse or a minimum-prefix-length floor as the upgrade path if gate-audit evidence ever shows near-vacuous prefix matches), and if the trimmed prefix is empty/whitespace → return without recording; partially-interpolated literals (`"error: $x missing"`) still record. No minimum-prefix-length floor beyond empty-skip — the spec ratified the `error: `-class prefix trade and the floor is advisory-evidence-only. Out of bounds (pivotal constraint 3): `match_sh_suite`, `match_default`, and every discovery case arm untouched — the ALREADY-LANDED drift-guard-tightening parity cases (10b set-equality, match_default body extraction, the `return 1 ;;` tripwire) live in this test file and are construct-disjoint from these fixtures; do not disturb them. The floor stays advisory-evidence-only: exit codes, MergeResult statuses, and `HARD_ESCALATION_REASONS` untouched. Token sweep: grep `extract_msg` across the script and apply the adjacency/bare-var rules at every caller, then hand-scan the header's `MSG = the first quoted string literal …` contract comment and the KNOWN LIMITS block for statements of the old first-quote-on-line rule, updating them to the new contract in the same diff; record the survey as a `Survey:` block in the commit message. Tests: shape-1 fixture (`[ -f "$path" ] || { echo "real msg" >&2; exit 1; }` with a test asserting `real msg`) → covered, not flagged (RED pre-change; base-ref evidence in the done-report); shape-1 negative (test asserts the old mis-extracted `$path` token but not the real message) → flagged; `printf 'error: %s' … >&2` guard with a test containing `error: ` → covered via the truncated prefix; a `%%`-bearing format guard → recorded as its pre-`%` prefix, no crash; shape-2 fixture (`die "$msg"`) → no uncovered line at all (RED pre-change); partially-interpolated literal → still recorded and matched.
- requiresTest: true
- requiresPackaging: false
- deps: []
- target repo: superproject

## Phase 2 — Release

### Task 5: Version bump — all four slots

- Files: `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`, `README.md`
- Plan slice: Bump all four slots to the next free patch above the live integration base at land time (the /war-strategy §2 next-free-patch convention — never a resolved semver literal in this plan): `.claude-plugin/plugin.json` `version`, `.claude-plugin/marketplace.json` `metadata.version` and `plugins[0].version`, and the `README.md` `## Status` line (replace-in-place, never emptied, no badge). Expected integration base: the tip already carrying the war-launch-entry-validation release AND the audit-gate-evidence-fidelity release (this plan stacks third per the manifest dependsOn spine) — resolve the next free patch from what those have actually consumed at land time. Standalone fallback: a run of this plan through plain /war resolves the next free patch from the four slots themselves. `skills/war/assets/version-slots.test.mjs` is the arbiter (fail-closed lock-step).
- requiresTest: false
- requiresPackaging: false
- deps: []
- target repo: superproject

## Deferred validations (backstops — AI-declared)

- Blob-read failure after a successful `cat-file -e` probe exits 2, never 0 (spec §10.7) · why deferred: manufacturing a ref-present-but-unreadable `.gitmodules` blob cwd-independently in a bash-3.2 fixture may be prohibitively expensive; Task 3 attempts the assertion first and documents the skip in the test file if it stands · runner: /red-team sandbox probe against the patched script, at plan ratification.
- Live dispatched advisory rendering — a real refiner in a real /war run sees the `--advise-vacuous` stderr line for an explicitly-declared `requiresPackaging: true` task in a docker-less repo, and treats it as informational (exit 0 stands, no spurious finding) · why deferred: only observable in a live run; the engine-side tests prove prompt threading and the script-side tests prove stderr emission, but the joined path has no harness · runner: operator spot-check of refiner merge output on the next /war run that declares `requiresPackaging: true`.

## Notes / conscious deviations

- **Stacking (third in line) + launch precondition:** `skills/war/assets/workflow-template.js` and `workflow-template.test.mjs` are also modified by war-launch-entry-validation (lands first) and audit-gate-evidence-fidelity (lands second). "Build after" means both are LANDED on the working branch before this plan's Phase 1 provisions — proven by the two Build-order precondition greps (`FILES_CHANGED_RULE`; `integratedTipGate`→`gate_log_path`), not by roadmap position; a stale base silently re-anchors three prompt sites. The release phase's four slots collide with both prior plans' release phases by design — strict landing order, next-free-patch at land time (lesson `stacked-release-plan-version-literal-lags-operator-target`).
- **Task carving (grill Q1):** Task 1 fuses #800 and #819's script side — both edit `assert-packaging-in-diff.sh` + its test (code-boundary rule 1; spec §8). Task 1 owns the shared test file outright; Task 2 is file-disjoint (engine + ADR) with `deps: [1]` because the dispatched prompt instructs a flag Task 1 defines — threading before the script parses it would make every merge invocation die on `unknown argument`.
- **Two predicates, one field (grill Q3/Q4):** the advisory predicate is a second named const `advisePackagingVacuous` beside the existing `requiresPackaging !== false` const, comment-explained; the collapse guard is behavioral (defaulted-task omit assertion) plus a source-count assertion covering all three invocation sites, so a per-site collapse or a two-of-three thread is independently RED.
- **Zero existing engine-test re-baselines (grill Q5):** verified at reconciliation — the flag appends after the branch token and every existing packaging case in `workflow-template.test.mjs` runs a defaulted or `false` task or matches the script name/prefix only; no existing assertion sees the flag.
- **Vacuous shape (b) needs no new capture (grill Q6):** the script's existing `diff_out` variable distinguishes non-empty-diff-with-zero-A/R/C from an entirely empty diff; the empty-diff-with-flag-stays-silent case is an explicit test (the shape a naive implementation gets wrong).
- **Refiner advisory semantics on both surfaces (grill Q7):** the dispatched prompts gain the informational-advisory clause where the flag is threaded; `agents/war-refiner.md` step 5 (which documents the bare invocation) is pre-adjudicated as the survey straggler with a one-clause correction — the spec's survey-derived-correction mechanism, so the file stays out of Task 2's `Files:` and the phantom collision with audit-gate-evidence-fidelity (which edits that file, and lands first) is avoided.
- **No cross-task prose dependency (grill Q8):** the advisory cites the EXISTING 2026-07-08 ADR 0017 addendum (already named in the script's SCOPE RATIFICATION header), not Task 2's new note; the prose is stderr-grep-locked (`ratified scope`, `ADR 0017`) in Task 1's tests.
- **Ordering consumers audited (grill Q9):** no consumer is order-sensitive — the PACKAGE_IT fix prompt quotes no floor output, and the script test's existing pair assertions are per-pair `grep -qF`. Set-equality is proven by sort-both-sides plus pair-count, never a hand-rewritten ordered block.
- **Continuation edge cases (grill Q10):** dangling final-line `\` gets a fixture (the shape a naive join loop mishandles); backslash inside a quoted token is a ceiling comment only.
- **RED-pre-change is proven in-run (grill Q11):** the worker reproduces each RED-claimed fixture against `git show <phase-base>:<script>` and records the evidence in its done-report; the auditor reproduces at the pinned audit_sha (lesson `worker-self-report-count-can-overstate-diff-additions` — claims are re-proven, not trusted). Delete-the-feature mapping: continuation fixtures ↔ join; call-site-count assertion ↔ inversion — each REDs independently.
- **No `rev-parse` probe in Task 3 (grill Q12):** Step 1's `git diff` dies 2 on a bad ref in both modes before Step 3 (existing test case 4 locks it), so `cat-file -e` failure after Step 1 succeeded = absent file, cleanly skippable.
- **Old-semantics test comments enumerated (grill Q13):** the submodule test's header note and case-10/12 rationales explicitly assert the working-tree read and are reworked in the same commit; engine-side `--declared` threading is ratified script-level-only coverage.
- **`%`-truncation trade stands (grill Q14):** spec-ratified; blast radius is advisory-evidence-only; no minimum-prefix-length floor beyond empty-skip, ceiling-commented as the upgrade path; `%%` spurious truncation is safe (prefix remains a true substring of emitted text) and fixture-covered.
- **drift-guard-tightening already landed (grill Q15):** PR #751 merged — its parity cases are PRESENT in `assert-guard-specificity-in-diff.test.sh` today and construct-disjoint from Task 4's `extract_msg`/`record_guard` fixtures. The draft's roadmap-contention edge for that file is dropped; Task 4 simply must not disturb the landed parity cases (asserted by End state 6).
- **Survey verifiability (grill Q16):** every manual same-scope survey is recorded as a `Survey:` block in the implementation commit message body (sibling-plan precedent), listing each scanned surface and verdict; the auditor re-runs the greps and spot-checks the named surfaces at audit_sha — a bare "surveyed, clean" claim without the block is a Major finding. Diff containment stays checkable: every diff file outside a task's `Files:` must be named in that block as a survey-derived correction (spec §10.11 = `Files:` ∪ named stragglers).
- **Backstops are exactly two (grill Q17):** the blob-read exit-2 conditional and the live advisory rendering. RED-pre-change proof and the parse-once claim are in-run tests/assertions, not backstops; the comment surveys are audit-verified via the `Survey:` blocks; the ADR-prose coupling dissolved (Q8).
- **`requiresPackaging: false` on every task:** this plugin repo carries no Dockerfile, so the packaging floor is structurally vacuous here — the exact shape #819 names. Explicit false keeps the skip logged and honest.
- **No discovery-set changes anywhere** (pivotal constraint 3; lesson `floor-script-discovery-set-must-mirror-gate-exclusions`): `is_dockerfile`, the `git diff --raw` inspection, and the guard-specificity `match_sh_suite`/`match_default` discoverers are untouched; `resolveGate` in `skills/war/assets/war-config.mjs` is a read-only reference.
- **Exit-code contract frozen** (pivotal constraint 2): no change moves any exit code; 1 = named route, 2 = git error, never collapsed; all new advisory output is stderr-only, stdout stays the machine-read surface.

## Open decisions

None — every grill question resolved at reconciliation (recorded above); anything surfacing at ratification is /red-team's to adjudicate.
