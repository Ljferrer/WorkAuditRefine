# Land-path correctness and standing-surface truth sweep — Gate-2 unpushed-range probe, re-land `landResult` symmetry, refiner-card accuracy, ADR/tour prose truth

Source spec: [2026-08-02-war-engine-and-standing-doc-truth-design.md](../specs/2026-08-02-war-engine-and-standing-doc-truth-design.md)
Issues addressed: #1192, #1245, #1219, #1161, #1221, #1240, #1225, #1211.
Issue → task mapping: #1219, #1161, #1221 → Task 1.1; #1245 → Task 1.2; #1240, #1225, #1211-2 → Task 1.3; #1192, #1211-1, #1211-3 → Task 1.4. `/war` files its own epic + task issues regardless (war-execution-must-file-issues); closing the eight source issues is Lead checkpoint work at phase close (war-checkpoint-must-close-task-issues), never assumed from the epic close.

Stacking (ADR 0011 stack-and-plow): this plan is the **last of the three claimants** on the two hardened pin files. `skills/war/assets/skill-doc-contracts.test.mjs` and `skills/war/assets/workflow-template.test.mjs` are campaign-owned by `2026-08-02-structural-test-nonvacuity` (the manifest `dependsOn` edge, must be **landed** first) and also edited by `2026-08-02-references-pointer-link-truth` (Task 1.1 / conditional Task 1.2 there) — this plan lands after **both**, rebasing its pin edits onto the hardened suites. Expected integration base: the campaign tip carrying the landed `redteam-doctrine-and-guards` release, the landed `structural-test-nonvacuity` phase, and the landed `references-pointer-link-truth` phase. Every current-bytes claim below was verified at conversion (2026-08-02) against the pre-campaign tip and is anchored by named construct (`D22_ORDERED_SPAN`, `D22_REGION_WITHOUT_UNDO`, the `**Post-servitor publication (Gate 2` marker, dispatch labels), never restated bytes; because the actual dispatch base is the advancing campaign tip, workers **re-verify each measured fact at their frozen phase base** (verify-stacked-plan-facts-at-campaign-base lesson) — the specific re-verify duties are named in the task slices. The ONE pinned probe command in End state 1 is quoted as **NEW canonical text this plan introduces**, not a snapshot of any base.

## AI-Commander's Intent

- Purpose: close the backlog's only two behavior-changing land-path defects — the Gate-2 pre-push probe that inspects only `HEAD` (leaving a crash-window path for a poisoned commit at `HEAD~1` to reach origin) and the re-land arms' stale `landResult` — and make every standing surface in the sweep (refiner card, recovery reference, ADR 0037, ADR 0008, tour step 16, D22 rationale, Outcome-handling bullet) state the truth about the code it describes.
- Method: three families, one phase, four file-disjoint tasks in two waves. Behavior changes are confined to Task 1.4's Gate-2 flow semantics (#1192, with its D22 pins in the same task) and Task 1.2's one assignment added to two arms (#1245, with its D6 arm-symmetry pin in the same task); everything else is prose/comments/additive pins, operationalized as a phase-level diff census (End state 15) — any diff that widens a guard, changes an exit code, or touches an enum (`HARD_ESCALATION_REASONS`, `KNOWN_LAND_DECISIONS`), a floor script, a hook, or the push-first CAS is out of scope. Every refiner-card fix is verified against its dispatched-prompt carrier in `workflow-template.js` (standing-vs-dispatched mirror discipline), with the carrier-proof greps recorded in the task's **commit body** (durable across the serial merge queue) and done report. Every retirement grep is a floor backed by a manual same-scope survey with a fixed routing rule, and **every** OLD-absent check in this plan — End states 2, 4, 7, 8, 12, 14 — inherits one **compliant guard form** (red-team round 1, 2026-08-03: five of the six were reproduced false-negating on a re-cased or re-wrapped copy in which the retired claim was still live; this is the third recurrence of the case/whitespace-brittleness class in this campaign): match **case-insensitively** and **newline-tolerantly**, keyed on a stable **mid-sentence** fragment, never a sentence-initial capitalised phrase and never a whitespace-literal multi-word run. Single-token retirements (`four-point`, `D4`, `--declared`) use `grep -rin`; multi-word phrases collapse internal whitespace before matching — a `perl -0777`/`grep -rizP` form at the shell, or in a test a read-then-`text.replace(/\s+/g,' ')` normalization matched with `/…/i`. Each retirement check additionally records its **pre-fix hit count (must be >= 1)** so a vacuously-green grep — one whose pattern never matched anything, before or after — is impossible. A retirement clause below that quotes a phrase verbatim is quoting the *subject*, not prescribing a byte-literal pattern: a straggler inside the task's own `Files:` is fixed in-task; a straggler in any other file is recorded as a follow-up finding in the done report (disposition `follow-up`), never fixed in-task — an out-of-footprint edit breaches scope and breaks the diff census. New/rewritten pins conform to the nonvacuity-hardened suite conventions: fragment-built patterns, descriptive referential failure messages that never restate the byte pattern they police, and — for `skill-doc-contracts.test.mjs` — zero raw occurrences of either embedded-args token name (the landed four-surface census asserts that file stays out of the sweep). Fail-first evidence follows the pinned in-worktree protocol: author the pin, run the named suite against the pre-fix source → capture the named assert's failing message verbatim; apply the fix, re-run green; `git status --porcelain` clean before commit — never a committed mutation. Auditor calibration (uniform across tasks): a seat verifies source shape at the pinned `audit_sha` via Read/Grep; suite runs, fail-first proofs, and carrier-proof greps are execution evidence belonging to the done report, commit body, and gate-audit (ADR 0041 ladder) — SOFT, never a seat's hold (deliberately-uncommitted-worker-probe lesson). ADR 0037 is corrected only through its append-only amendment channel; ADR 0008 gets a sanctioned in-place factual-pointer edit (the asymmetry is plan-mandated — see Task 1.3); adjudication row O is respected, not re-opened.
- End state (each numbered condition individually checkable; HARD = auditor-verifiable at the pinned `audit_sha` via Read/Grep/allowlisted read-only git, SOFT = done-report/commit-body evidence, gate = a standing test):
  1. [HARD] The **Pre-push staged-file check (never skip)** clause of the `**Post-servitor publication (Gate 2` flow in `skills/war/SKILL.md` probes the full **unpushed range** with ONE pinned copy-pasteable command — `git log --name-only --format='commit %H' '@{upstream}'..HEAD` — plus the ONE deterministic no-upstream fallback (when `git rev-parse --abbrev-ref '@{upstream}'` fails in the transient publication worktree: `git log --name-only --format='commit %H' origin/<working>..HEAD`, the working branch's remote counterpart, i.e. the fork point against the `ensure-origin` push target), and condemns **any** commit in the range whose file set escapes the resolved promotion destination (default `docs/learnings/`, or a non-null `overrides.learningsTarget`) or the `CLAUDE.md` pointer duty (spec D2). The recorded-incident sentence (stale version slots, silent release revert) stays. This End state's two command quotes are the NEW canonical text; `<working>` is the literal placeholder convention, matching the flow's existing `ensure-origin <working>` rendering.
  2. [HARD] The flow's undo routing follows spec D3, in this prose order (matching the D22 key's arm order): condemned commit **is** `HEAD` **and** is the only condemned commit → the sanctioned `git reset --hard HEAD~1` carve-out with its scope sentence intact (unpushed commit at `HEAD` in the transient Lead-owned publication worktree, nothing else); condemned commit **not** `HEAD`, or multiple condemned → `git revert` each condemned commit (never rewind); conflicted revert → `git revert --abort` + escalate with the worktree left in place for inspection; the bounded-retry escalation sentence (condemned again after `remove-publication-worktree` + re-provision + re-commit → escalate, never loop) carries over unchanged. OLD-absent, **scoped to the extracted Gate-2 region only** (marker → next `##` heading): the retired precondition prose and the retired HEAD-only probe command are both absent from that region. The precondition retirement keys on a **markup-tolerant adjacent fragment pair** — `precondition holds` together with `by construction` within one sentence — never the paraphrase quoted in earlier drafts (red-team round 1: the draft quote "by construction, the condemned commit *is* `HEAD`" is **not a byte-run in the file**; the live text differs on three axes at once — intervening `**by construction**` bold markup, an elided em-dash sub-clause, and "the commit it just condemned" word order — so a literal grep would have been vacuously green from the start). Pre-fix hit count for both retirements must be >= 1, recorded in the done report. Sanctioned survivors of the retired probe bytes, by design: negative reference (a) in `skill-doc-contracts.test.mjs`, this plan file, its source spec, the historical plan `docs/plans/2026-07-24-gate-evidence-and-release-integrity.md`, and the incident record `docs/learnings/gate2-commit-from-stale-verify-worktree-can-revert-a-release-bump.md` (its "Remedies landed" bullet 2 describes the *then-landed* HEAD-only probe in the present tense; adjudicated to a follow-up, not an in-task edit — see adjudication row D2 in the red-team report) — the retirement grep is region-scoped, never repo-wide (backstop-retirement-grep false-red lesson).
  3. [gate] The D22 row in `skills/war/assets/skill-doc-contracts.test.mjs` is still ONE ordered key (commit step → range probe → do-not-push clause → `reset --hard HEAD~1` undo arm → revert-routing arm → `ensure-origin` push), shared by the live assertion and **two** unwired negative references each differing from the live shape by exactly one arm: (a) the retired HEAD-only-probe region — the pre-fix live shape (probe `git show --name-only --format= HEAD`, undo and revert prose present) — red through the key at the range-probe arm; (b) an undo-absent region carrying the NEW range probe and revert routing but only a bare `reset --hard` doctrine mention, red at the undo arm. The range-probe arm keys on the adjacent `git log --name-only` form plus a later `..HEAD` range token (the two fragments the retired shape lacks) — **not** on the `@{upstream}` literal (deviation 6). **Three** unwired negative references, not two: red-team round 1 proved only the `git log --name-only` fragment had a both-ways proof, leaving the `..HEAD` fragment load-bearing but droppable with zero test signal — the exact structural-test blind-spot class the predecessor plan `structural-test-nonvacuity` (which this plan rebases onto) exists to close. So a third negative reference (c) carries the adjacent `git log --name-only` form **without** any `..HEAD` range token — the tip-only regression shape — and is asserted red through the same live key at the range-probe arm, giving that fragment its own both-ways proof; the revert-routing arm keys on the `git revert` adjacent form (never bare `revert` — the incident sentence's "silently reverted" would green it) and matches tolerantly per the D18/D21 markup-tolerance idiom; the undo arm stays pinned on `reset --hard HEAD~1` exactly as today. Extraction by construct (marker → next `##` heading) unchanged; never independent presence checks; both negative references are asserted red through the same live key on every suite run (standing both-ways proof).
  4. [HARD] The D22 rationale comment's "carry both tokens" claim is absent from `skill-doc-contracts.test.mjs` (file-scoped OLD-absent); the corrected per-file distribution is present (`references/setup.md`: `remove-publication-worktree` only; `references/resume-and-recovery.md`: `ensure-origin` only).
  5. [HARD] In `skills/war/assets/workflow-template.js`, both re-land arms (the blocks following the dispatches labeled `land:phase-${ph.id}:environment-proceed` and `land:phase-${ph.id}:baseline-proceed`) assign `landResult = reLand` inside their `reLand.status === 'submodule-pr'` branches, beside the existing `escalated.push(...)` + `landDecision = 'held:submodule-pr'` pair. Code-traced inertness pin (the auditor's cover): the only observable change is the phase return object's `landResult` field on those two paths (previously the stale first `gate_failed` attempt, now the dispatched `submodule-pr` MergeResult, matching the initial land's shape by construction) — the `tipSha` landed-tip anchor, the Wrap-up servitor gate, and the handoff block are all `status === 'landed'` / `landDecision`-gated and read identically before and after; no enum, escalation-surface, `landDecision`, or new-consumer change anywhere in the diff.
  6. [gate + SOFT proof] The arm-symmetry pin (D6) in `skills/war/assets/workflow-template.test.mjs` is keyed by **count** — the number of re-land `submodule-pr` guard branches (extracted via the re-land dispatch-label **shape**: a fragment-built pattern matching the `land:phase-` + `${ph.id}` + `:` prefix followed by any `-proceed` flavor, which by construction excludes the initial land's suffix-less `land:phase-${ph.id}` label) must equal the number of those branches carrying the `landResult = reLand` assignment — so a mirrored third arm lacking the assignment reds it. **The shape key is load-bearing and supersedes the two-literal extraction of earlier drafts** (red-team round 1, reproduced by execution: extracting via the two hardcoded literals `land:phase-${ph.id}:environment-proceed` / `land:phase-${ph.id}:baseline-proceed` makes the pin *structurally* vacuous — a third arm carries a third label, is therefore never inside an extracted region, and the count stays 2 == 2 forever no matter how many unmirrored arms are added; the non-vacuity property D6 is sold on was absent). Region boundary is explicit: each region runs from its matched dispatch label to the end of that arm's `submodule-pr` guard branch. The pin also asserts the extracted region count is **>= 2**, so a 0 == 0 or 1 == 1 degenerate green (extraction matching nothing after a future label rename) is impossible; fail-first proof recorded (pin red against the pre-fix source, green post-fix).
  7. [HARD] In `agents/war-refiner.md`: the `## Submodule-as-repo provisioning` closing sentence names the **provision-barrier dispatch prompt's `submodNote`** (built in `workflow-template.js`) and its `git submodule update --init --recursive` step as the carrier of targetRepo/targetBase; the `### Submodule phase (2A WAR-owned / 2B PR-and-hold)` closing sentence attributes the 2A/2B routing to the **land** prompt (`submodLandNote`, threaded into the three land prompts only). OLD-absent, file-wide: the exact phrase `merge/land prompt` has zero hits in the file (at conversion it appears exactly twice — the two closers). Sanctioned surviving sibling phrasings, untouched by design: `merge/land dispatch`, `merge/land status`, `merge/land return` (frontmatter description), and the reland-discrimination sentence "Your dispatched land prompt carries the same discrimination verbatim".
  8. [HARD] The step-6 gitlink-bump bullet in `agents/war-refiner.md` shows `assert-no-submodule-mutation.sh <integrationBranch> <taskBranch> --declared` — positionals first, flag trailing, matching the script's own `usage:` line. The flag-first-absent check is **scoped to the invocation code span** (extract from the `assert-no-submodule-mutation.sh` token to the end of that backtick span, then assert `--declared` trails both positionals inside it), **never file-wide** (red-team round 1: after the plan's own fix `--declared` still legitimately precedes both positionals in byte order elsewhere in the same bullet — the "pass `--declared`" lead-in and the closing "refused even with `--declared`" parenthetical — so a file-wide check is literally false about the corrected file and a faithful End state 9 predicate would false-red on plan-mandated sanctioned text). Sanctioned survivors, enumerated: the bullet's bare `--declared` lead-in, its trailing `--declared` parenthetical, and the no-flag "other task" sub-bullet.
  9. [gate] The D9 card↔script usage guard in `skill-doc-contracts.test.mjs` asserts shape agreement between the card's gitlink-bump-bullet invocation and the `usage:` line in `skills/war/assets/assert-no-submodule-mutation.sh`: in both, `--declared` follows the two positional placeholders (extraction + agreement — never byte equality, the placeholder names legitimately differ: card `<integrationBranch> <taskBranch>`, script `<base> <branch>`); the guard's patterns are fragment-built so its own source carries no contiguous flag-first byte-run, and its failure message describes the contract referentially.
  10. [HARD at audit_sha] `skills/war/references/refiner-recovery.md` carries exactly three top-level `## ` sections and exactly three `Trigger:` lines, one opening each — the new line on `## Submodule land arms (2A / 2B)` mirrors the card-side pointer's condition (a land-phase — or 2A merge — dispatch whose phase `target repo` is a submodule); the file's diff against the frozen phase base is additive-only (no pre-existing byte moves — must not disturb any previously moved block's byte-identity). No standing regression lock is added for the three-sections/three-Triggers convention — recorded as backstop 4, never waived in prose.
  11. [HARD] `docs/adr/0037-run-scoped-staged-phase-scripts.md` carries a new dated correction note — `**Correction (2026-08-02, #1240):**` — appended under the existing 2026-07-27 Amendment of the mirror-registry Consequences bullet (the bullet beginning `- **A second anchor-literal mirror joins the repo's existing mirror registry.**`), stating the direction explicitly: the `const A =` ternary's args-fallback tail in `workflow-template.js` is the **canonical source**; the stager's third exported anchor (`ARGS_FALLBACK_ANCHOR` in `stage-workflow.mjs`) is the **hand-maintained mirror site**, matching the direction of the existing `export const meta` ↔ stager-export pair; derivation cited to the `COUPLING (ADR 0037, #1134)` comment at the ternary, described — never restating the tail's bytes or any grep pattern. Every pre-existing byte of the ADR is unchanged — checked HARD by an allowlisted read-only `git diff <frozen-phase-base>..<audit_sha> -- <the ADR>` showing an append-only hunk at that bullet; the ADR's header/Status line is **not touched** (this is a Consequences-bullet correction note, so the byte-unchanged mandate binds the whole pre-existing file including the Status line — the status-currency-line exemption is not needed and not invoked). The D12 mirror-direction guard is consciously dropped — backstop 1.
  12. [HARD] `docs/adr/0008-git-is-the-resume-source-of-truth.md`'s Consequences bullet locates the reconciliation pre-flight **mechanics** in `skills/war/references/resume-and-recovery.md` (the A/B/C disposition table and the landed-phase check), noting that `skills/war/SKILL.md` keeps the trigger pointer **plus** a summary of the precedence chain, the one-way-repair invariant and the unexplained-commit halt — **not** "only the trigger pointer" (red-team round 1: that wording is false about the live `SKILL.md`, whose Resume paragraph inlines three of the discipline's clauses; pinning it as an End-state literal would retire one imprecision and land another in the same bullet, in the pass whose Purpose is making standing surfaces true); OLD-absent, scoped to that ADR: the phrase locating the discipline in "`SKILL.md` prose" is absent. The rest of the bullet's accepted-risk framing is byte-intact.
  13. [HARD] Tour step 16 ("16 · Wrap-up — the write-scoped servitor") in `.tours/architect-war-system.tour` narrates the live three-discipline `Memory admission checklist` prompt ("four-point" and "D4" absent from the step's description), keeps the accurate Mechanism/Implication material (no-Bash allowlist, exactly-once capture per landed phase, the `servitorResult` absent-guard), corrects the stale "gates to `learningsTarget`" write-path claim per the two-root split (the local memory root is the servitor's only writable path; repo-root publication is the Lead's Gate-2 promotion), and its file/line anchor resolves to the servitor Wrap-up dispatch region in `workflow-template.js` — line derived from the named construct (the `Memory admission checklist` prompt text) **after** the Task 1.3 worker's first-action rebase onto the integration tip carrying Task 1.2's merged insertions (`deps: [1.2]` exists exactly for this; residual post-plan rot accepted — backstop 3).
  14. [HARD] The `- **Outcome handling (§4.3):**` bullet in `skills/war/SKILL.md` triggers on "a merge/land `gate_failed` arrives (with or without a `gate_failure_class`)"; OLD-absent, scoped to that bullet: the narrower "carries a `gate_failure_class`" trigger form is absent from it.
  15. [HARD] Phase diff census: the phase's cumulative diff against the frozen phase base (`git diff --name-only <phase-base>..<tip>`) touches exactly the nine files in this plan's Files footprint — `skills/war/SKILL.md`, `skills/war/assets/skill-doc-contracts.test.mjs`, `skills/war/assets/workflow-template.js`, `skills/war/assets/workflow-template.test.mjs`, `agents/war-refiner.md`, `skills/war/references/refiner-recovery.md`, `docs/adr/0037-run-scoped-staged-phase-scripts.md`, `docs/adr/0008-git-is-the-resume-source-of-truth.md`, `.tours/architect-war-system.tour` — operationalizing the behavior-change boundary (no floor script, hook, exit-code contract, enum, redaction-lint, or push-first CAS change) as a refiner/gate-audit-checkable condition.
  16. [HARD via gate artifact] `node --test 'skills/**/*.test.mjs'` green at the task tips and the self-discovery gate (`resolveGate` in `war-config.mjs`) green at merge — with the refiner's captured gate log threaded per the engine's gate-capture step, so gate-audit reads this from the artifact, never SOFT from prose (full-gates-green-without-threaded-gate-log lesson).
  17. [HARD] Tour step 4 ("4 · The Lead and the invariants") in `.tours/architect-war-system.tour` states the servitor's write path as the **local memory root**, not `learningsTarget`; after the fix `grep -in learningsTarget .tours/architect-war-system.tour` returns hits only where the token names the **read-path repo root** or the Lead's Gate-2 promotion destination, never a servitor write path (pre-fix hit count 2, at lines 27 and 99 — recorded so the check cannot be vacuously green). **Scope override, adjudicated:** source spec §9 says "Not fixing other tour steps; #1225 is step 16 only", and the plan's straggler rule routes out-of-scope findings to follow-ups; red-team round 1 proved step 4 carries the *identical* falsehood step 16 is being fixed for, in a file already inside Task 1.3's `Files:`. The §9 non-goal exists to bar creep into *unrelated* tour steps, not to preserve a twin of the exact defect this pass retires — leaving it would ship a tour whose step 4 contradicts its own step 16. Fixed in-task; End state 15's nine-file census is unaffected (same file).
  18. [HARD] The Gate-2 revert arm **terminates**: a commit in the probed range is exempt from condemnation when a later commit in the same range reverts it, linked by git's own `This reverts commit <sha>.` body token, and that revert commit is itself exempt (its file set is the same escaping paths by construction). The flow states this exemption explicitly beside the D3 routing, and the D22 key's revert-routing arm covers it. **Why this condition exists** (red-team round 1, 2026-08-03, Major): once condemnation is range-scoped rather than HEAD-scoped, `git revert` does not remove the condemned commit from `@{upstream}..HEAD` — it appends a second commit touching the very same escaping paths — so on the next pass BOTH are condemned, the surviving bounded-retry sentence fires, and the one re-entry shape this whole fix is written for deterministically ends in escalation instead of a successful publication push. Without this the Purpose's first half is not met. **Adjudicated (`--afk`, ADR 0014, Lead):** of the candidate resolutions, "exempt the neutralized pair" is chosen over "push the revert first" — the latter would need a sanctioned exception to the do-not-push clause and would send a poisoned-then-reverted pair to origin, which is the exact outcome the Gate-2 probe exists to prevent. The chosen form is self-contained, deterministic, converges in one pass, and is mechanically checkable from git's own revert-body token.

## Build order (for /war)

Single phase, two waves. All four tasks are file-disjoint; both wave-2 edges are genuine content dependencies, never family-ordering aesthetics or collision dodges:

- Phase 1, wave 1: Task 1.1 (refiner standing surfaces), Task 1.2 (re-land `landResult` symmetry + D6 pin).
- Phase 1, wave 2: Task 1.3 (doc-truth sweep) — `deps: [1.2]`, because its tour-step line literal is derived from a `workflow-template.js` construct that Task 1.2's insertions shift (an intra-plan anchor-rot loop closed by the dep-wave rebase); Task 1.4 (Gate-2 range probe + D22/D15 rewrite + D9 guard + Outcome-handling widen) — `deps: [1.1]`, because the D9 guard is red at the frozen phase base by construction until the card fix it guards is merged and visible via the dep-wave worker's first-action rebase (guard-task-split lesson — same wave is insufficient).

No release phase (deviation 14).

## Phase 1 — Land-path fixes and standing-surface truth

### Task 1.1: Refiner standing surfaces — carrier naming, `--declared` order, Trigger line (#1219, #1161, #1221)

- Files: `agents/war-refiner.md`, `skills/war/references/refiner-recovery.md`
- Plan slice:
  - **#1219 (D7), two sentence rewrites in `agents/war-refiner.md`, anchored by their section headings.** (a) The closing sentence of `## Submodule-as-repo provisioning` currently attributes the submodule targetRepo/targetBase threading and 2A/2B routing to "Your dispatched merge/land prompt" — but that section fires on a **provision** dispatch. Rewrite it to name the **provision-barrier dispatch prompt's `submodNote`** (built in `workflow-template.js`) as the carrier of targetRepo/targetBase and the `git submodule update --init --recursive` step. (b) The closing sentence of `### Submodule phase (2A WAR-owned / 2B PR-and-hold)` over-attributes 2A/2B routing to the "merge/land prompt"; narrow to the **land** prompt — `submodLandNote` is threaded into the three land prompts only (initial land, environment-proceed re-land, baseline-proceed re-land). Do NOT introduce any reference-path carrier: adjudication row O stands — these inline sentences are the sole carrier in the cross-repo context where they fire; the fix makes them accurate, nothing more.
  - **Carrier proof (standing-vs-dispatched mirror discipline; recording medium per deviation 8):** greps against `skills/war/assets/workflow-template.js` showing `submodNote` (provision-barrier prompt) and `submodLandNote` (the three land prompts) each carry what the card now attributes to them — recorded verbatim in the task's **commit body** (durable across the serial merge queue, readable by the gate-audit execution-evidence lens at the landed tip) and in the done report. Grep is a floor: after the greps, hand-scan the full refiner card AND the barrier prompt + three land prompts in `workflow-template.js` for sibling claims about dispatched carriers (manual same-scope title/comment survey); a straggler inside this task's two files is fixed in-task; a straggler in any other file (including `workflow-template.js`) is a follow-up finding in the done report, never an in-task edit.
  - **#1161 (D8):** reorder the step-6 gitlink-bump bullet's shown invocation to `assert-no-submodule-mutation.sh <integrationBranch> <taskBranch> --declared` — positionals first, flag trailing, matching the script's `usage:` line (`base="$1" branch="$2"` bind before the option loop) and all five dispatched-prompt sites. The surrounding prose (gitlink-only exits 0; a non-gitlink submodule-path content change still refused) is correct and stays. OLD-absent: the flag-first rendering (`--declared` before the positionals) appears nowhere in the file after the edit — grep, then hand-scan the whole card for any other rendering of this invocation (manual same-scope survey; the no-flag form in the adjacent "other task" sub-bullet is a different invocation and stays).
  - **#1221 (D10):** add ONE `Trigger:` line opening `## Submodule land arms (2A / 2B)` in `refiner-recovery.md`, mirroring the card-side pointer's condition (a land-phase — or 2A merge — dispatch whose phase `target repo` is a submodule). No other bytes in the file move; the diff is additive-only (must not disturb any previously moved block's byte-identity). Post-edit check, recorded in the done report: exactly three top-level `## ` sections, exactly three `Trigger:` lines, one opening each (End state 10; the auditor re-checks HARD at `audit_sha`).
  - Stacked-fact re-verify duty: confirm at the frozen phase base that the two `merge/land prompt` occurrences are still exactly the two closers and that the reland-discrimination "land prompt" sentence still reads as at conversion (sanctioned-survivor set, End state 7); adjust the survey findings accordingly, never the plan's scoping rule.
- requiresTest: false
- requiresPackaging: false
- deps: []
- target repo: superproject

### Task 1.2: Re-land arm `landResult` symmetry + arm-symmetry pin (#1245, D5, D6)

- Files: `skills/war/assets/workflow-template.js`, `skills/war/assets/workflow-template.test.mjs`
- Plan slice:
  - **D5:** in `skills/war/assets/workflow-template.js`, in each re-land arm (the blocks following the dispatches labeled `land:phase-${ph.id}:environment-proceed` and `land:phase-${ph.id}:baseline-proceed`), add `landResult = reLand` inside the `reLand.status === 'submodule-pr'` branch, beside the existing `escalated.push(...)` + `landDecision = 'held:submodule-pr'` pair — restoring the shape the initial land has by construction (it assigns the dispatched MergeResult directly to `landResult`, so `pr_number`/`pr_remote` stay readable). **No other behavioral change, code-traced (End state 5):** the downstream `landResult` consumers — the hoisted `tipSha` landed-tip anchor, the Wrap-up servitor gate, and the landed-arm resync — all require `status === 'landed'` and read identically before/after; the handoff block emits only on `landed`/`held:escalation`; the phase return object's `landResult` field is the single observable change on the two re-land `submodule-pr` paths, and that change is the point. No enum, no escalation-surface change, no new consumer of `landResult.pr_number`. This is Workflow-sandbox code (no imports) touching no mirrored enum — no mirror-registry row is implicated (the registry grows by row only for new inline mirrors; this adds none).
  - **D6 (arm-symmetry pin):** additive source-scan pin in `workflow-template.test.mjs` (the suite already reads the template source into `const src`): extract the land-phase re-land regions by their two dispatch-label literals and key by **count** — the number of re-land `submodule-pr` guard branches must equal the number of those branches whose body carries the `landResult = reLand` assignment — so a third arm authored by mirroring one of these two without the assignment goes red. Failure message is referential (describes the symmetry duty and names the initial-land direct-assignment shape as the model), never restating a grep pattern as prose a future sweep could self-match; patterns fragment-built per the hardened suite conventions. Place the pin as its own test, clear of the D2 mirror-registry block and the nonvacuity-tightened retry-merge/rung-body constructs (expected clean rebase onto the hardened suite — re-verify at the frozen phase base that those constructs are where conversion measured them).
  - **Fail-first proof (in-worktree protocol):** author the pin first, run `node --test skills/war/assets/workflow-template.test.mjs` against the pre-fix template → red (capture the failing assert message verbatim); apply the D5 assignment, re-run → green; `git status --porcelain` clean before commit. Record both runs in the done report and the commit body.
- requiresTest: true
- requiresPackaging: false
- deps: []
- target repo: superproject

### Task 1.3: Doc-truth sweep — ADR 0037 direction, ADR 0008 attribution, tour step 16 (#1240, #1225, #1211-2)

- Files: `docs/adr/0037-run-scoped-staged-phase-scripts.md`, `docs/adr/0008-git-is-the-resume-source-of-truth.md`, `.tours/architect-war-system.tour`
- Plan slice:
  - **#1240 (D11):** under the mirror-registry Consequences bullet of ADR 0037 (the bullet beginning `- **A second anchor-literal mirror joins the repo's existing mirror registry.**`), append a new dated correction note — `**Correction (2026-08-02, #1240):**` — through the ADR's established amendment-note channel, nested beside the existing 2026-07-27 Amendment note it corrects: that Amendment states the mirror direction backwards; the `const A =` ternary's args-fallback tail in `workflow-template.js` is the **canonical source** and the stager's third exported anchor (`ARGS_FALLBACK_ANCHOR` in `stage-workflow.mjs`) is the **hand-maintained mirror site**, matching the direction of the existing `export const meta` ↔ stager-export pair; cite the `COUPLING (ADR 0037, #1134)` comment at the ternary as the derivation source. The note **describes** the coupling comment and never restates the tail's bytes or any grep pattern (self-match rule; also keeps `docs/` clear of anything the skills/-scoped four-surface census could ever be widened onto). Every pre-existing byte of the ADR stays intact — append-only diff at that bullet, Status line untouched (End state 11 names the HARD check). The D12 mirror-direction guard is consciously dropped — backstop 1. **Auditor note, threaded here so a doctrine-lens seat has plan cover:** ADR 0037 uses the append-only amendment channel because it corrects a *decision-record note*; ADR 0008 below is edited **in place** because it corrects a *rotted factual pointer*, not a ratified decision sentence — this asymmetry is spec-mandated (spec D11 vs D14), not worker improvisation.
  - **#1211-2 (D14):** in-place single-clause edit of ADR 0008's Consequences bullet: the reconciliation pre-flight's discipline lives in `skills/war/references/resume-and-recovery.md` (evicted from `SKILL.md`, which keeps only the trigger pointer) — correct the factual attribution, keep the rest of the bullet's accepted-risk framing byte-intact. OLD-absent grep, scoped to that ADR: the phrase locating the discipline in "`SKILL.md` prose" absent; then hand-scan the whole ADR for sibling stale pointers (manual same-scope survey) — in-file stragglers fixed in-task, out-of-file stragglers filed as follow-up findings.
  - **#1225 (D13):** rewrite tour step 16 ("16 · Wrap-up — the write-scoped servitor") in `.tours/architect-war-system.tour` — and hand-scan the **whole tour file**, not just that step (red-team round 1: step 4 carries the same stale write-path claim; End state 17): drop the "four-point memory-admission checklist (D1 dedup, D2 correction-priority, D3 verify-cue, D4 index-hygiene)" narration for the live three-discipline wording (the servitor Wrap-up prompt says the standing card carries the FULL text of the **three** disciplines — follow ALL three; D4 was retired). Keep the accurate Mechanism/Implication material (no-Bash allowlist, exactly-once capture per landed phase, the `servitorResult` absent-guard). Correct the pre-named straggler: the "gates to `learningsTarget`" write-path claim predates the two-root split — the local memory root is the servitor's only writable path (repo-root publication is the Lead's Gate-2 promotion; `learningsTarget` is the read-path repo root). Re-point the step's file/line anchor at the servitor Wrap-up dispatch in `workflow-template.js`, located by named construct — the `Memory admission checklist` prompt text — with the line number derived from that construct **after this task's first-action rebase onto the integration tip** (the `deps: [1.2]` edge exists so Task 1.2's merged insertions are already counted; the tour format requires a literal line; the named construct in the description is the durable anchor; residual rot accepted — backstop 3).
  - **Per-medium NEW-present / OLD-absent checks, each a floor:** tour step — three-discipline narration present; "four-point" and "D4" absent from the step's description; ADR 0037 — correction note present, pre-existing bytes unchanged (verified with an append-only diff inspection against the frozen phase base, not grep alone); ADR 0008 — new attribution present, old phrase absent. After each check, hand-scan the enclosing scope (the whole ADR / the whole tour step) for sibling stale claims — e.g. any other step-16 claim contradicted by the live Wrap-up prompt — routing stragglers per the Method rule (in-file → fix in-task; out-of-file → follow-up finding).
- requiresTest: false
- requiresPackaging: false
- deps: [1.2]
- target repo: superproject

### Task 1.4: Gate-2 unpushed-range probe + D22 rewrite + D9 guard + Outcome-handling widen (#1192, #1211-1, #1211-3, D9)

- Files: `skills/war/SKILL.md`, `skills/war/assets/skill-doc-contracts.test.mjs`
- Plan slice:
  - **#1192 flow rewrite (D1–D3)** in the `**Post-servitor publication (Gate 2` flow of `skills/war/SKILL.md`, `**Pre-push staged-file check (never skip)**` clause, staying inside the D22 extraction region (marker → next `##` heading):
    - **Probe (D1):** replace the HEAD-only probe (`git show --name-only --format= HEAD`) with the pinned range probe — `git log --name-only --format='commit %H' '@{upstream}'..HEAD` — enumerating every unpushed commit with its file set. ONE deterministic fallback, not a choice: when `git rev-parse --abbrev-ref '@{upstream}'` fails (the transient publication worktree has no upstream configured), run `git log --name-only --format='commit %H' origin/<working>..HEAD` — the working branch's remote counterpart, the fork point against the `ensure-origin` push target. Both command texts are pinned NEW canonical text (End state 1); D22 locks the wording — treat them as pinned.
    - **Condemnation (D2):** ANY commit in the probed range whose file set escapes the resolved promotion destination (default `docs/learnings/`, or a non-null `overrides.learningsTarget`) or `CLAUDE.md` (the pointer duty) is condemned — not just `HEAD`. **Exemption (End state 18, red-team round 1):** a commit is NOT condemned when a later commit in the same range reverts it (linked by git's own `This reverts commit <sha>.` body token), and that revert commit is likewise exempt — without this the revert arm below never converges, because `git revert` leaves the condemned commit in the range and adds a second commit touching the same escaping paths. The recorded-incident sentence (stale version slots, silent release revert) stays.
    - **Undo routing (D3):** delete the "the precondition holds **by construction**" prose (true only while the probe looked at nothing else). New routing, authored as the carve-out sentence followed by a separate revert-routing sentence in that order (deviation 7 — matching the D22 key's arm order): condemned commit **is** `HEAD` and is the **only** condemned commit → the existing sanctioned `git reset --hard HEAD~1` carve-out, keeping the carve-out sentence's exact scope wording (unpushed commit at `HEAD`, transient Lead-owned publication worktree, nothing else); condemned commit is **not** `HEAD`, or multiple commits condemned → `git revert` each condemned commit, never rewind; conflicted revert → `git revert --abort` + escalate, worktree left in place for inspection. After a successful revert pass the range is **re-probed**, and the D2 exemption makes it clean, so the flow proceeds to the `ensure-origin` push rather than looping — state that termination explicitly beside this routing (End state 18). The bounded-retry escalation sentence carries over verbatim (condemned again after `remove-publication-worktree` + re-provision + re-commit → escalate; the staleness is not worktree-local).
  - **#1211-3:** widen the `- **Outcome handling (§4.3):**` bullet's trigger clause elsewhere in the same file (outside the D22 region — no key interaction) to "a merge/land `gate_failed` arrives (with or without a `gate_failure_class`)" — matching the referenced block, which also covers the class-less introduced fail-safe. OLD-absent, scoped to that bullet: the narrower "carries a `gate_failure_class`" form absent; hand-scan the bullet's full arm list for sibling narrowness (manual same-scope survey).
  - **D22 rewrite (D4)** in `skill-doc-contracts.test.mjs`, rebased onto the hardened suite (the nonvacuity banner/census edits sit in the adjacent Task-2.1 comment block, a distinct construct — expected clean rebase; re-verify at the frozen phase base): rewrite `D22_ORDERED_SPAN` to the new ordered arms per End state 3 — commit step (`docs(learnings): phase N`) → range probe (keyed on the adjacent `git log --name-only` form plus a later `..HEAD` range token, which the retired HEAD-only shape lacks; never the `@{upstream}` literal, so the fallback sentence needs no key alternation — deviation 6) → do-not-push clause → undo arm (`reset --hard HEAD~1` pinned exactly as today) → revert-routing arm (`git revert` adjacent form, matched tolerantly per the D18/D21 markup-tolerance idiom so a bold/backtick reshuffle does not false-red; never bare `revert`, which the incident sentence's "silently reverted" would green) → `ensure-origin` push. Keep the ONE-ordered-key discipline — never independent presence checks — and keep extraction by construct (marker → next `##` heading) unchanged. Carry **two** unwired negative references through the same live key, each differing from the live shape by exactly one arm (End state 3): (a) the retired HEAD-only-probe region — a const string capturing the pre-fix live shape (probe `git show --name-only --format= HEAD`, undo and revert prose present) — asserted red (proves the range-probe arm discriminates; spec D4's "retired shape becomes the negative reference"); (b) the undo-absent region, updated to carry the NEW range probe and revert routing but only a bare `reset --hard` doctrine mention — asserted red (proves the undo arm still discriminates AND re-proves the `HEAD~1` pinning decision). Assertion prose updated to name the new arms; failure messages stay referential and never restate the key's byte pattern; zero raw occurrences of either embedded-args token name enter this file (the landed four-surface census is the arbiter).
  - **Fail-first proof (in-worktree protocol):** rewrite the D22 test first, run `node --test skills/war/assets/skill-doc-contracts.test.mjs` against the pre-fix `SKILL.md` → red at the range-probe arm (capture the failing message verbatim); apply the flow rewrite, re-run → green; `git status --porcelain` clean before commit. Record both runs in the done report and the commit body.
  - **#1211-1 (D15), same comment block:** while editing the D22 block, correct its "Extraction is BY CONSTRUCT" rationale paragraph's false claim that `references/setup.md` and `references/resume-and-recovery.md` each "carry both tokens" — the true distribution: `setup.md` carries only `remove-publication-worktree`; `resume-and-recovery.md` carries only `ensure-origin`. Comment-only; no assertion changes for this item. OLD-absent grep for the "carry both tokens" phrasing in the file, then hand-scan the whole D22 comment block for sibling stale claims (manual same-scope survey; straggler routing per Method).
  - **D9 (card↔script usage guard):** additive row in `skill-doc-contracts.test.mjs` asserting the shown `--declared` invocation in `agents/war-refiner.md` (fixed by Task 1.1 — visible after this task's dep-wave first-action rebase onto the integration tip; the guard would be red at the frozen phase base without the `deps: [1.1]` edge) agrees in shape with the `usage:` line in `skills/war/assets/assert-no-submodule-mutation.sh`: extraction of the card's gitlink-bump-bullet invocation and the script's `usage:` line, agreement that `--declared` trails the two positional placeholders in both (never byte equality — the placeholder names differ by design), plus the file-wide flag-first-absent assert (End state 8's lock). Patterns fragment-built (the guard's own source, comments, and messages carry no contiguous flag-first byte-run); failure message referential, describing the contract.
- requiresTest: true
- requiresPackaging: false
- deps: [1.1]
- target repo: superproject

## Deferred validations (backstops — AI-declared)

- ADR 0037 mirror-direction guard (spec D12) consciously **dropped** — check: the appended correction note's stated direction (template ternary = canonical source → stager `ARGS_FALLBACK_ANCHOR` = mirror site) agrees with the `COUPLING (ADR 0037, #1134)` comment at the ternary · why deferred: a mechanical doc-consistency extraction over an append-only ADR's nested amendment notes is high-fragility relative to the one-sentence fact it polices, AND any such guard living in `skill-doc-contracts.test.mjs` that names the anchor token would re-introduce a byte the landed nonvacuity four-surface census asserts absent from that file — the guard would red the census or need fragment-obfuscation to police one sentence; spec D12 sanctions the drop when recorded here (ADR 0017 — recorded, never waived in prose) · runner: the human review of Task 1.3's PR diff at land, plus the next standing-doc truth sweep (`/survey-corps` genre).
- Live-fire Gate-2 range-probe behavior — check: on the next real Gate-2 publication with a re-entry shape, the pinned range probe enumerates every unpushed commit and the D3 routing (reset carve-out vs revert) fires per the condemned commit's position and count · why deferred: the flow is Lead-executed prose in a transient publication worktree; no automated harness exercises it — D22 pins the wording, not the execution · runner: Lead observation at the next campaign's Gate-2 publication; escalate any divergence as an issue.
- Tour step 16 line-anchor rot — check: the step's file/line pair still resolves to the servitor Wrap-up dispatch (`Memory admission checklist` prompt text in `workflow-template.js`) · why deferred: the `.tours` format requires a literal line number, which rots across future merges by design; the intra-plan rot source is closed by Task 1.3's `deps: [1.2]` edge, and the named construct in the description is the durable anchor (accepted standing posture) · runner: the next tour-narrative sweep.
- Standing regression locks consciously not added for two one-time HARD checks: the `refiner-recovery.md` three-sections/three-`Trigger:`-lines convention (End state 10) and the refiner-card carrier-naming sentences (End state 7) — check: both still hold post-land · why deferred: each polices low-churn prose whose next edit is itself a reviewed diff; a bytes-pinning test on either would join the fragile-pin class this campaign is thinning, and D9 already locks the highest-risk card fact (the invocation shape) · runner: the next standing-doc truth sweep (`/survey-corps` genre).
- The incident record `docs/learnings/gate2-commit-from-stale-verify-worktree-can-revert-a-release-bump.md` still describes the retired HEAD-only Gate-2 probe in the present tense ("Remedies landed" bullet 2) — check: its wording matches the landed range-probe flow · why deferred: red-team round 1 (2026-08-03) found it as a third live carrier outside the plan's survivor set; adjudicated to a follow-up rather than an in-task fix because adding a repo-root `docs/learnings/` write to a worker task contradicts the two-root split this very plan asserts (the local memory root is the servitor's only writable path; repo-root publication is the Lead's Gate-2 promotion), and widening End state 15's nine-file census for a historical incident record is the wrong trade · runner: the Lead files a follow-up issue at phase close; picked up by `/lessons-learned` or the next `/survey-corps` sweep.
- Fail-first and carrier-proof evidence is done-report + commit-body only (deliberately uncommitted probe runs) — check: each recorded proof names the command, the failing assert message, and the restore-to-green · why deferred: gate-audit treats a cannot-confirm on execution evidence as SOFT, never a hold (auditor-calibration line in Method) · runner: the Lead spot-reproduces at least one recorded fail-first proof (Task 1.2's or Task 1.4's) before phase close.

## Notes / conscious deviations

This is an `/war-machine --afk` conversion (ADR 0014): no operator existed to interview, so every survivor below is self-adjudicated (question → decision → rationale) and ratified later by `/red-team`. Intent-block tone, HARD/SOFT End-state tagging, the in-worktree evidence protocol, the auditor-calibration line, the diff-census End state, and the no-release note follow the three 2026-08-02 predecessor plans (`structural-test-nonvacuity`, `references-pointer-link-truth`, `redteam-doctrine-and-guards`) — no divergence found in the predecessor-consistency check.

1. **Provenance headings (grill Q17).** `## AI-Commander's Intent` and `## Deferred validations (backstops — AI-declared)` per ADR 0014; the suites-green End state is worded against the refiner's threaded gate-log artifact (End state 16), never a bare assertion.
2. **Same-file collisions (grill Q2/Q3).** `skills/war/SKILL.md`'s two constructs (#1192 Gate-2 flow, #1211-3 Outcome-handling bullet) share Task 1.4 — same file ⇒ same task, and no phase edge exists anywhere in this plan (nothing must be *landed* first intra-plan; phase edges are for cross-repo and release only). `skill-doc-contracts.test.mjs` has exactly one owner (Task 1.4: D22 rewrite + D15 comment fix + D9 guard — #1211-1 rides the D22 rewrite because it edits the same comment block). #1211's three residuals split across Task 1.4 (items 1, 3) and Task 1.3 (item 2) along file-ownership lines. The spec's survey-rationale build order (engine → cards → doc sweep) is deliberately NOT encoded as ordering — the DAG has exactly two edges, both content dependencies (deviations 4 and 9).
3. **D6 kept, riding Task 1.2 (grill Q4/Q5).** The pin lands in the same task as the assignment it locks (two files, one task — legal), so it is green at its own dispatch base after the in-task fix and `assert-test-in-diff.sh` sees `workflow-template.test.mjs` in the diff (`requiresTest: true` routes cleanly, no `no-test`). Dropping D6 during execution requires a backstop entry, never a prose waiver.
4. **D9 kept, behind `deps: [1.1]` (grill Q1).** A drift guard split from the clause it guards is red at the frozen phase base by construction; the dep-wave worker's first act is a rebase onto the integration tip carrying the merged card fix. Same wave would be insufficient. Dropping D9 during execution requires a backstop entry.
5. **D12 dropped (grill Q4/Q7).** Recorded in backstop 1 per ADR 0017 and spec D12's own drop clause — with the conversion-discovered census-conflict rationale: the landed nonvacuity plan asserts `skill-doc-contracts.test.mjs` carries zero raw embedded-args token names, and a D12 guard would either red that census or need fragment-obfuscation to police one appended sentence.
6. **Draft open decision 1 resolved (grill Q10):** the D22 range-probe arm keys on the adjacent `git log --name-only` form plus a later `..HEAD` range token — never the `@{upstream}` literal — so the deterministic fallback sentence (`origin/<working>..HEAD`) satisfies the same key without alternation, and the retired shape (which has `git show --name-only` and no `..HEAD` token) stays red. The prose, the two pinned commands, and the key arms were authored as one triple in End states 1–3. `/red-team` ratifies against a sandbox run of the key over the drafted prose.
7. **Draft open decision 2 resolved:** the undo routing is authored as the carve-out sentence followed by a separate revert-routing sentence, in that order — cosmetic except that it fixes the prose order the ordered key's undo→revert arm sequence requires. `/red-team` ratifies the final prose against the key and both negative references.
8. **Carrier-proof recording medium (grill Q14).** Spec constraint 6's "the diff records the greps" is resolved to: the task's **commit body** (survives the serial merge queue; readable at the landed tip by the gate-audit execution-evidence lens) plus the done report. Evidence class SOFT (deliberately-uncommitted probe evidence); backstop 5 carries the Lead spot-check.
9. **Task 1.3 `deps: [1.2]` (grill Q15).** This plan's own #1245 edit inserts lines into `workflow-template.js` above the servitor Wrap-up dispatch; deriving the tour line at the frozen base would ship a wrong literal on day one. The dep-wave rebase makes Task 1.2's merged insertions visible before derivation; no later task in this plan touches that file, so no re-derivation duty falls on the refiner.
10. **#1245 inertness is code-traced, not asserted (grill Q11).** End state 5 pins the trace: every downstream `landResult` consumer is `status === 'landed'`-gated (tipSha anchor, Wrap-up gate, resync arm), handoff emits only on `landed`/`held:escalation`, and the phase return's `landResult` field is the single intended observable change on the two re-land `submodule-pr` paths — an auditor finding that field changed has plan cover; any OTHER observed change is real scope creep.
11. **ADR channel asymmetry + byte-mandate scope (grill Q18/Q19).** ADR 0037: append-only amendment channel, ALL pre-existing bytes including the Status line untouched (the status-currency-line exemption is not invoked — nothing about this correction touches the header), checked by allowlisted read-only `git diff` at `audit_sha` against the frozen phase base. ADR 0008: in-place single-clause edit, justification threaded verbatim into Task 1.3's slice so a doctrine-lens auditor applying the general append-only prior sees the plan-mandated asymmetry.
12. **OLD-absent scoping + sanctioned survivors (grill Q8/Q24).** Every retirement assert names its scope and survivor set: the retired probe command and "by construction" prose — Gate-2 region of `skills/war/SKILL.md` only (the retired bytes legitimately survive in negative reference (a) and in this plan file); `merge/land prompt` — file-wide zero-hit in `agents/war-refiner.md` with the sanctioned sibling phrasings enumerated in End state 7; "carries a `gate_failure_class`" — that bullet only; "carry both tokens" — `skill-doc-contracts.test.mjs` file-wide; flag-first `--declared` — `agents/war-refiner.md` file-wide. No repo-wide zero-hit grep exists anywhere in this plan.
13. **Stacked-fact discipline (grill Q6/Q7).** Current-bytes claims are anchored by named construct and marked measured-at-conversion; workers re-verify at the frozen phase base (duties named in the slices); new/rewritten pins conform to the hardened suite's fragment-built, referential-message conventions so this plan does not un-harden what it rebases onto.
14. **No release phase (grill Q22).** The spec specifies none (§8); the campaign roadmap owns the release decision. If a release is cut — by the campaign or a standalone run — bump all four slots to the next free patch above the live integration base at land time; expected integration base per the Stacking preamble. Standalone fallback: a plan run through plain `/war` resolves the next free patch from the four slots itself. Zero version literals anywhere in this plan (plan-literal-lint run at conversion).
15. **Straggler routing (grill Q20).** Grep-as-floor hand-scans route findings by file ownership: in-Files → fixed in-task; out-of-Files → follow-up finding in the done report (disposition `follow-up`), never an in-task edit — protecting the diff census (End state 15). Two pre-named stragglers, both in-file for Task 1.3 and both fixed in-task by plan mandate: tour step 16's `learningsTarget` write-path claim, and — added by red-team round 1 (2026-08-03) — tour step 4's identical claim (End state 17, which records the source-spec §9 scope override it rests on). The tour hand-scan is therefore **whole-file**, not step-scoped.
16. **requiresPackaging: false on all tasks (grill Q16).** The packaging floor is a Dockerfile-COPY coverage check; no packaging surface moves here (no version slots, no plugin/marketplace manifest, no seed content). requiresTest is true exactly where the diff carries a test file (Tasks 1.2, 1.4) and false on the doc-only tasks (1.1, 1.3) — no `no-test`/`unpackaged` dead-ends at the floors.
17. **Cross-plan contention (for the roadmap; grill Q23).** `skill-doc-contracts.test.mjs` and `workflow-template.test.mjs` are three-way shared: `structural-test-nonvacuity` (campaign primary owner, must land first), `references-pointer-link-truth` (lands second; its survey-manifest row *had* an empty `dependsOn`; roadmap authoring has since firmed the edge — `docs/roadmaps/2026-08-02-nonvacuity-doctrine-and-doc-truth-roadmap.md` row 3 records `Depends on: 1, 2` — so this is a closed gap, recorded for provenance only, not an outstanding action for `/war-machine`), and this plan (lands last of the three). This plan's remaining seven files (`skills/war/SKILL.md`, `workflow-template.js`, `agents/war-refiner.md`, `refiner-recovery.md`, both ADRs, the tour) appear in NO sibling footprint (verified at conversion against all three authored sibling plans). Contention is resolved by strict landing order, not file carve-up.

## Open decisions

None — both of the draft's open decisions are self-adjudicated above (deviations 6 and 7); `/red-team` ratifies them along with the rest of this section.
