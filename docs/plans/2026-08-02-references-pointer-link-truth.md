# Post-eviction references/ pointer & link truth — pointer shapes, dead links, dangling citation, revert doctrine

Source spec: [2026-08-02-references-pointer-link-truth-design.md](../specs/2026-08-02-references-pointer-link-truth-design.md)
Issues addressed: #1215, #1212, #1216, #1243.
Issue → task mapping: #1215 → Task 1.1 (pointer normalization + travelling guard); #1212 → Task 1.2 (link truth + headers); #1216 → Task 1.1 (finding 2 discharged by the citation repair; finding 1 verified already fixed — re-verify at the frozen phase base, no action expected); #1243 → Task 1.1 (first half, the dangling citation) + Task 1.2 (second half, the revert doctrine); Task 1.3 is the shared durability sweep for all four. `/war` files its own epic + task issues regardless (war-execution-must-file-issues); closing the four source issues is Lead checkpoint work at phase close (war-checkpoint-must-close-task-issues), never assumed from the epic close.

Stacking (ADR 0011 stack-and-plow): this plan edits `skills/war/assets/workflow-template.test.mjs`, which the `2026-08-02-structural-test-nonvacuity` spec declares campaign-owned — **this plan's roadmap row must carry `dependsOn: structural-test-nonvacuity`**, and it lands after the `2026-08-02-redteam-doctrine-and-guards` release in roadmap order. Every count and already-fixed claim below was verified at conversion (2026-08-02) against the pre-campaign tip; because the actual dispatch base is the advancing campaign integration tip, workers **re-verify each measured fact at their frozen phase base** (verify-stacked-plan-facts-at-campaign-base lesson) — the specific re-verify duties are named in the task slices. Expected integration base: the campaign tip carrying the landed `redteam-doctrine-and-guards` release and the landed `structural-test-nonvacuity` phase.

## AI-Commander's Intent

- Purpose: retire the four classes of references/ integrity debt that the prompt-surface-simplification plan's byte-identity mandate deliberately froze — `../`-prefixed agent-card pointers, one-directory-short relative links in moved blocks, the orphaned `SKILL.md, submodule co-source-of-truth` citation, and the unrecorded phase-close-revert adjudication doctrine — now that the byte-identity window is closed, without minting any new false doc claim.
- Method: docs-plus-tests only, no behavioral diff. Apply ratified adjudication O verbatim (O(2) D4 owner-relative pointer skeleton on agent cards; O(1)/O(3) keep `worker-servitor-edges.md` evicted with a truthful cross-repo-posture header). Every header byte-identity claim is re-qualified "at eviction time" in the same diff as the link rewrites it describes. Guards travel with the facts they guard (ADR 0025): the Task 5.1 pointer-shape guard family extends to `agents/war-auditor.md` in the same task as the pointer fix; the revert-doctrine construct guard and the header-caveat OLD-absent assert ride the Task 1.3 sweep behind an explicit `deps` edge (the sanctioned guard-split form — same phase, worker rebases onto the integrated tip, so the guard is green at its own dispatch base, never RED-by-construction). Any pre-existing guard whose pinned bytes include a rewritten link target is updated in the same commit — the full `node --test 'skills/**/*.test.mjs'` run is the discovery mechanism, never a line-number pre-enumeration (verified premise at conversion: the known pins over these files in `skill-doc-contracts.test.mjs` (D18/D21, relocated-doctrine pairs), `war-config.test.mjs` (recovery-relaunch doc-contracts), `land-decision.test.mjs` (held:land-failed anchor), and the `workflow-template.test.mjs` UNION scans/D5/D6 locks are all token- or construct-anchored and pin **no** link-target bytes and **no** header-caveat bytes — expected zero forced updates). Demonstrated-RED evidence follows the pinned mutate-in-worktree protocol (Method-level, applies to every RED demo): mutate the live file in the task worktree, run the named suite, capture the named assert's failing message verbatim, `git checkout --` the file, re-run green, show `git status --porcelain` clean before commit — never a committed mutation, never a copy-elsewhere file. Auditor calibration: seats verify source shape at the pinned `audit_sha` via Read/Grep; suite runs, sweep-survey outcomes, and demonstrated-RED evidence are done-report/gate-audit execution evidence (ADR 0041 ladder) — SOFT, never a seat's hold (deliberately-uncommitted-worker-probe lesson). Two standing pre-warnings for the roster: (a) fetch-absence in `auditor-teach.md` step 4 is deny-by-design — the auditor git guard excludes `fetch` deliberately; do not flag the no-fetch discipline as a gap while reviewing the edited step; (b) a bare-phrase Grep for `co-source-of-truth` hits two sanctioned carriers by design — `submodule-flows.md`'s own "Resume — submodule remote as co-source-of-truth" heading/body and `design.md`'s co-source-of-truth paragraph — plus the new step-4 replacement citation, which pairs `submodule-flows.md` (never `SKILL.md`) with the phrase; only the retired citation form (`SKILL.md` + `co-source-of-truth` inside one parenthetical) is a finding. Never touch `skills/war/SKILL.md`, `workflow-template.js`, any hook, or any dispatched-prompt literal.
- End state (each condition individually checkable; HARD = verifiable at the pinned tip / in the diff, SOFT = done-report evidence, sanctioned via the backstops):
  1. [HARD] `agents/war-auditor.md` carries zero `../`-prefixed references/ pointer targets, and every auditor-teach.md pointer on the card uses the owner-relative `(skills/war/references/auditor-teach.md)` form — four at the conversion-time measurement, re-verified at the frozen phase base (link text `[auditor-teach.md]` and the § fragment parentheticals unchanged). The pointer-shape guard family in `skills/war/assets/workflow-template.test.mjs` asserts both: the suite's existing shape-generic forbidden-`../` pattern applied to the already-loaded `auditorMd`, plus an exact-count assert pinned to the measured-at-base count (count-pinned, mirroring the refiner-card idiom — presence-only stays green when pointers are dropped), with adjudication O(2) and the source spec cited in the test comment, and the comment naming its own update duty (a legitimately added Nth pointer updates the count in the same diff).
  2. [HARD] Every relative markdown link target in `skills/war/references/resume-and-recovery.md`, `submodule-flows.md`, `worker-servitor-edges.md`, and `auditor-teach.md` resolves to an existing file from the file's own directory: sibling reference links in bare sibling form (`design.md`, `schemas.md` — link text updated to match the target, no text/target mismatch minted), ADR links in `../../../docs/adr/…` form; in-page `#` anchors untouched.
  3. [HARD] Zero occurrences of the retired citation form — a single parenthetical carrying both `SKILL.md` and `co-source-of-truth` — across `agents/` and `skills/war/references/`; `auditor-teach.md` Gitlink-bump `pin-validity` step 4 instead cites `` `submodule-flows.md` `` and its "Resume — submodule remote as co-source-of-truth" section by section name as a backticked filename + section-name citation, **not** a markdown link (resolved decision — one landed shape); step 4's no-fetch sentences and the best-effort `cat-file -e` allowance otherwise byte-unchanged (diff-checkable containment: within step 4, only the citation parenthetical changes).
  4. [HARD] No header under `skills/war/references/` still instructs readers to mentally re-base relative links (the retired caveat form "Relative link paths inside the moved blocks are … written relative to …" is absent from every scanned file — asserted mechanically by End state 6's sweep); every remaining byte-identity claim in the four files' headers is qualified as byte-identical **at eviction time**, with the *reason* stated PER FILE — never one blanket clause (red-team adjudication 3, 2026-08-03; measured at the phase base: `auditor-teach.md` has **0** markdown links and `submodule-flows.md` has exactly one that already resolves, so a blanket "with link targets since rewritten" would mint a NEW false doc claim in the very plan whose Purpose forbids one): **`resume-and-recovery.md`** (13 links, 8 dead) and **`worker-servitor-edges.md`** (1 link, 1 dead) carry the full form — byte-identical at eviction time, relative link targets since rewritten to resolve from this file (this pass); **`auditor-teach.md`** carries byte-identical at eviction time with the *step-4 citation repair* named as the cause (it has no links to rewrite); **`submodule-flows.md`** carries the bare byte-identical-at-eviction-time qualification only — its re-basing caveat is deleted (vacuous) and nothing in it is rewritten. The mechanical assert (End state 6(d)) checks the shared `at eviction time` token on all four; the per-file reason clause is auditor-checked prose.
  5. [HARD] `resume-and-recovery.md` carries a new trailing top-level section on phase-close polish reverts with a `Trigger:` line, an explicit added-post-eviction / not-a-moved-block provenance note, the three adjudication duties (gate at the reverted commit; green-by-deletion diff; re-land with a real rationale naming re-opened findings), and the fix-set-comparison-by-finding-title/file duty — all verifiable by section heading and duty keywords, never line numbers, and guarded by End state 6's construct test.
  6. [HARD] A new durable sweep test `skills/war/assets/reference-link-integrity.test.mjs` (auto-discovered by the `node --test` glob, no wiring): (a) enumerates `agents/*.md` and `skills/war/references/*.md` by directory scan (`readdirSync`, repo root resolved from `import.meta.url`, never `process.cwd()`), never a hand-enumerated list; (b) asserts every markdown link target exists — file-relative for references/ files, file-relative OR repo-root-relative for agents/ files, `#` anchors and absolute URLs skipped, `#fragment` suffixes stripped, failure messages naming file and target; (c) asserts zero hits of the retired citation form scoped as in (3), pattern keyed on the citation form (`SKILL.md` + `co-source-of-truth` in one parenthetical), never the bare phrase; (d) asserts the retired re-basing-caveat form absent across all scanned references/ files (the default-flip OLD-absent half) and the "at eviction time" qualification present in each of the four named headers (direct `readFileSync` of the four — fail-closed, a rename throws loud); (e) asserts the revert-doctrine construct: locate the phase-close-polish-revert section heading in `resume-and-recovery.md` (non-vacuous heading-found assert), extract heading→next-`## `-or-EOF, and assert the `Trigger:` line, the not-a-moved-block provenance token, and the duty keywords inside the extracted region.
  7. [SOFT by design] Demonstrated-RED evidence in the done reports, per the Method protocol: the extended pointer-shape guard REDs on a temporarily reintroduced `../` prefix on a war-auditor.md pointer (Task 1.1), and the sweep test REDs on a temporarily short-resolved link in a references/ file (Task 1.3).
  8. [HARD via gate artifact] `node --test 'skills/**/*.test.mjs'` green at the task tips and the self-discovery gate (`resolveGate` in `war-config.mjs`) green at merge, with the refiner's captured gate log threaded per the engine's gate-capture step so gate-audit reads this from the artifact, never SOFT from prose (full-gates-green-without-threaded-gate-log lesson); any pre-existing doc guard whose pinned fragment included a rewritten byte was updated in the same commit as the rewrite (expected zero — the verified premise in Method; a forced update in a file outside every task's Files footprint contradicts that premise and escalates as a plan defect, never a silent scope widening); the phase's cumulative diff touches only files within this plan's Files footprint (subset allowed — the three conditional guard files in Task 1.2 are expected untouched), with **zero** edits to `agents/war-worker.md`, `skills/war/SKILL.md`, `skills/war/assets/workflow-template.js`, `hooks/`, or any dispatched-prompt literal.

## Build order (for /war)

- Phase 1 — Link truth + durable sweep (single phase; wave 1 = Tasks 1.1 and 1.2 parallel, file-disjoint; wave 2 = Task 1.3 via `deps: [1.1, 1.2]` — a dependency edge, never a collision dodge: at the frozen phase base the dead links and the retired citation still exist, so the sweep REDs by construction until its worker's first-action rebase onto the integration tip).

No release phase: the source spec (§5) states no release is implied — docs-plus-tests, no behavioral diff, no version literal anywhere in this plan. If the campaign roadmap attaches one, it is the roadmap's own trailing phase using the directive form "bump all four slots to the next free patch above the live integration base at land time" (a plan run through plain `/war` resolves the next free patch from the four slots itself).

## Phase 1 — Link truth + durable sweep

### Task 1.1: Auditor-family pointer & citation truth (war-auditor.md, auditor-teach.md, worker-servitor-edges.md) + travelling guards

- Files: `agents/war-auditor.md`, `skills/war/references/auditor-teach.md`, `skills/war/references/worker-servitor-edges.md`, `skills/war/assets/workflow-template.test.mjs`
- Plan slice:
  - **Pointer normalization (spec §4.1, #1215).** In `agents/war-auditor.md`, rewrite every `(../skills/war/references/auditor-teach.md)` link target to `(skills/war/references/auditor-teach.md)` (adjudication O(2), D4 agent-card form). Four at conversion, living in: the Read-only git guard contract intro sentence; the Submodule pre-flight "submodule task" arm; the Submodule pre-flight "gitlink-bump task" arm; and the reserved-lens paragraph following the lens catalog. **Re-verify the count at the frozen phase base** — if a predecessor plan legitimately added or removed a pointer, normalize ALL of them and pin the guard to the measured post-fix count, recording the delta in the done report. Link text `[auditor-teach.md]` and the section-fragment parentheticals ("§ Guard-contract mirror architecture" etc.) stay; no other bytes on those lines change.
  - **Guard extension (same task — ADR 0025, guard travels with the fact).** In `skills/war/assets/workflow-template.test.mjs`, alongside the existing Task 5.1 pointer-shape asserts (the test titled "Task 5.1 — worker/servitor card evictions…"), extend the guard family to the auditor card: apply the suite's existing shape-generic forbidden-`../` pattern (the `/\((?:\.\.\/)+[^)]*skills\/war\/references\/[^)]+\)/` idiom) to the already-loaded `auditorMd` text, and add an exact-count assert of owner-relative `(skills/war/references/auditor-teach.md)` occurrences pinned to the measured-at-base count (count-pinned, mirroring the refiner-card idiom — presence-only stays green when pointers are dropped). The test comment cites adjudication O(2) and the source spec, and names its own update duty: a future plan that legitimately adds an Nth pointer updates this count in the same diff.
  - **Citation repair (spec §4.4, #1243 first half + #1216 finding 2, merged).** In `skills/war/references/auditor-teach.md`, step 4 of the "Gitlink-bump `pin-validity` lens" section: replace the parenthetical `` (`SKILL.md`, submodule co-source-of-truth) `` with a citation naming `` `submodule-flows.md` `` and its "Resume — submodule remote as co-source-of-truth" section by section name — **backticked filename + section name, no markdown link** (resolved decision: one landed shape, so the worker, the sweep test, and the auditors agree on what lands; never a line number). Every other byte of step 4 — the do-not-fetch discipline (fetch is deny-by-design for auditors — pre-warned in Method, not a gap) and the best-effort `cat-file -e` allowance — stays byte-intact; the containment is audit-checkable (within step 4, only the citation parenthetical changes). This supersedes #1216's header-caveat remedy. #1216 finding 1 (qualifier-lock test comments in `workflow-template.test.mjs`) was verified already fixed at conversion — **re-verify at the frozen phase base**; still-fixed → no action; regressed → escalate (an unexplained regression on the stacked base is never silently re-fixed in-task).
  - **worker-servitor-edges.md truthful header (spec §4.2 — KEEP EVICTED).** Rewrite the body's `(../docs/adr/0002-scope-by-agent-type.md)` link target (Servitor confinement section) to `(../../../docs/adr/0002-scope-by-agent-type.md)`. Header: delete the sentence instructing readers to re-base relative links ("Relative link paths inside the moved blocks are likewise written relative to `agents/` …"); qualify the byte-identity claim as at-eviction-time (relative link targets since rewritten to resolve from this file, this pass); add one sentence recording the cross-repo posture per adjudication O — on a foreign target repo no path form resolves this file, so the standing cards' decisive-rules-inline digests and the dispatched `TARGET REPO` / `GITLINK-BUMP` clauses are the operative carriers there, and this file is best-effort enrichment. The positional-words caveat ("below"/"above") stays — still true and load-bearing.
  - **Header qualification for auditor-teach.md**: qualify its header byte-identity claim as byte-identical **at eviction time**, naming the TRUE cause — the step-4 citation inside the evicted `## Gitlink-bump \`pin-validity\` lens` block is repaired this pass. Do **not** inherit the link-target formula: this file has **0** markdown links (measured at the phase base), so "link targets since rewritten" would be false here (red-team adjudication 3). Verified at conversion: its header carries no re-basing caveat sentence — none to delete.
  - **No block is pulled back inline** into `agents/war-worker.md`; that card is expected to need zero edits and is deliberately absent from this task's Files (its decisive-rules-inline digests are already asserted by the Task 5.1 moved-fragment test, which stays the arbiter). If implementation finds a blocking rule sole-carried by `worker-servitor-edges.md`, that contradicts the spec's verified D2 premise — escalate as a plan defect, do not silently pull the block inline.
  - **Pin-follows-text sweep**: run the full JS suite; any guard whose pinned fragment includes a byte this task rewrites is updated in this same commit. Verified premise at conversion: the `workflow-template.test.mjs` UNION scans and D5/D6 locks over `auditorMd`/`auditorTeachMd` are retired-token absence asserts and construct extractions — none pins a pointer link target or the step-4 citation bytes; expected zero forced updates. A forced update in a file outside this task's Files contradicts the premise — escalate as a plan defect.
  - **Grep is a floor + straggler protocol:** after `grep -n '](../skills/' agents/war-auditor.md agents/war-worker.md` reaches zero hits on the auditor card, hand-scan both cards end-to-end for any other `../`-prefixed or short-resolving link or backticked path citation. A straggler **inside this task's Files** is fixed in-task as a survey-derived correction — and if it is a fifth auditor-teach pointer, the count assert updates in the same commit. A straggler **outside this task's Files** (including anything on `agents/war-worker.md` or `agents/war-setup-scout.md` — the latter's two `../` links are pre-identified and out of scope, spec §9) is recorded as a follow-up finding in the done report (disposition `follow-up`), never fixed in-task. **Also hand-scan `skills/war/references/auditor-teach.md` and `skills/war/references/worker-servitor-edges.md` end-to-end** (both are already in this task's Files) — red-team adjudication 2, 2026-08-03: the plan's two straggler bullets between them covered only `agents/war-auditor.md`, `agents/war-worker.md`, `resume-and-recovery.md` and `submodule-flows.md`, leaving these two with no end-to-end hand-scan in any task. **And discharge spec §10.3's specific duty here** — scan `auditor-teach.md` for any FURTHER citation naming `SKILL.md` as a doctrine home beyond the step-4 one this task repairs; it had no plan home at all before this patch. Record both outcomes in the done report even when zero stragglers.
  - **Demonstrated-RED evidence** (Method protocol, SOFT by design): temporarily reintroduce one `../` prefix on a war-auditor.md pointer in-worktree, show the extended guard's failing assert verbatim, restore, re-run green, `git status --porcelain` clean before commit; record in the done report.
- requiresTest: true
- requiresPackaging: false
- deps: []
- target repo: superproject

### Task 1.2: SKILL.md-eviction link truth + phase-close revert doctrine (resume-and-recovery.md, submodule-flows.md)

- Files: `skills/war/references/resume-and-recovery.md`, `skills/war/references/submodule-flows.md`, `skills/war/assets/skill-doc-contracts.test.mjs`, `skills/war/assets/war-config.test.mjs`, `skills/war/assets/land-decision.test.mjs`
- Plan slice:
  - **Link truth (spec §4.3, #1212).** In `resume-and-recovery.md`, rewrite every short-resolving relative link target in the moved blocks, by construct class — sweep the whole file, never by line number (measured at conversion: three sibling links — `](references/design.md)` ×1, `](references/schemas.md)` ×2 — and five ADR links — 0023 ×3, 0008 ×1, 0035 ×1; re-verify at the frozen phase base):
    - sibling reference links → bare sibling form (`design.md`, `schemas.md`), updating the link text to match the new target (leaving old text would mint the same text/target-mismatch misdirection class this pass retires);
    - ADR links (`](../../docs/adr/…)`) → `](../../../docs/adr/…)`;
    - in-page anchors (`#recovery-relaunch`) are already correct — untouched.
  - `submodule-flows.md`'s body carries no short-resolving links (verified at conversion; its header link is already sibling-correct); only its header rewrite applies.
  - **Both headers:** delete the relative-link re-basing caveat sentence ("Relative link paths inside the moved blocks are likewise written relative to `skills/war/SKILL.md` …"). Then qualify the byte-identity claim **per file, not with one shared clause** (red-team adjudication 3): `resume-and-recovery.md` gets the full form — byte-identical at eviction time, relative link targets since rewritten to resolve from this file (this pass); `submodule-flows.md` gets the **bare** at-eviction-time qualification with NO rewrite clause, because its single link already resolves and this plan rewrites nothing in it — the plan says so itself one bullet above, and asserting a rewrite that never happened is the exact false-claim class this pass exists to retire. The positional-words caveat stays. (Verified at conversion: no test pins the caveat sentence's bytes — deleting it REDs nothing.)
  - **Revert doctrine (spec §4.5, #1243 second half).** Append a new trailing top-level section to `resume-and-recovery.md`, shaped to the file's conventions: a heading in the spirit of `## Phase-close polish reverts — never self-justifying`; a `Trigger:` line (you are adjudicating, or about to accept, a phase-close revert whose commit body is only git's auto-generated revert message); a one-line provenance note (added post-eviction, this pass, #1243 — **not** a moved block; the header's eviction provenance does not cover it); and the standing rule, tool-generic (no raw command recipes beyond git basics — the process-recipe rot lesson): a phase-close revert carrying only an auto-generated body is never self-justifying — (1) run the gate at the reverted commit itself (green target ⇒ the revert had no gate justification); (2) diff the revert against still-live text for green-by-deletion (an assertion whose subject is still present in the guarded file is guard, never obsolescence); (3) re-land with a real rationale naming which findings are re-opened, and diff the reverted commit's fix-set against the redo pass's fix-set by finding title/file before treating the absorb queue as drained (the redo re-derives its queue from findings open at redo time — the recorded orphaning mechanism). The section's durable guard lands in Task 1.3 (deps edge — see Build order); this task's own arbiter is the slice text above plus End state 5's keyword checklist.
  - **Pin-follows-text sweep**: run the full JS suite; the fragment/construct guards that read this file live in `skill-doc-contracts.test.mjs` (D18/D21 + relocated-doctrine pair tests), `war-config.test.mjs` (recovery-relaunch doc-contracts), and `land-decision.test.mjs` (the held:land-failed bullet anchor) — any of them whose pinned bytes include a rewritten link target is updated in this same commit. These three test files are in this task's Files **solely as pre-assigned conditional ownership** for that rule and are expected untouched (verified premise at conversion: all known pins are token/construct-anchored — heading anchors, `resumeFromRunId` pairing, arm-name regexes — none pins `](references/` or `../../docs/` bytes). The heading→EOF extraction spans in the relocated-doctrine pair tests merely enlarge when the trailing section is appended — verify via the suite, not by assumption. A forced update in a file outside this task's Files contradicts the premise — escalate as a plan defect.
  - **Grep is a floor + straggler protocol:** after the pattern sweeps (`](references/`, `](../../docs/`, `](../docs/` over `skills/war/references/*.md`), hand-scan `resume-and-recovery.md` and `submodule-flows.md` end-to-end for any remaining relative link or backticked prose path citation that misresolves from the file's location. In-Files stragglers are fixed in-task as survey-derived corrections; out-of-Files stragglers (e.g. in `worker-servitor-edges.md` or `auditor-teach.md`, which Task 1.1 owns this phase) are recorded as follow-up findings in the done report, never fixed in-task.
- requiresTest: false
- requiresPackaging: false
- deps: []
- target repo: superproject

### Task 1.3: Durable link-resolution + retired-citation + doctrine-construct sweep test

- Files: `skills/war/assets/reference-link-integrity.test.mjs`
- Plan slice: **(spec §4.6 + the mechanical homes for End states 4–5)** ship one new sibling test file in the doc-contract family (a new file rather than a `skill-doc-contracts.test.mjs` block — sanctioned by the spec's "or sibling" clause; chosen to stay file-disjoint from Task 1.2's conditional guard footprint and from the cross-plan `skill-doc-contracts.test.mjs` / `workflow-template.test.mjs` footprints of `2026-08-02-structural-test-nonvacuity` and `2026-08-02-war-engine-and-standing-doc-truth`; auto-discovered by the `node --test 'skills/**/*.test.mjs'` glob, no wiring). First action on dispatch: rebase onto the integration tip (`deps` edge) so the sweep runs against Tasks 1.1/1.2's merged doc fixes — at the frozen phase base the retired citation and the short links still exist and the test REDs by construction. The test:
  - enumerates `agents/*.md` and `skills/war/references/*.md` by directory scan (`readdirSync`), never a hand-enumerated file list (the enumerated-destination `existsSync` fail-open lesson — a renamed file must widen the scan, not silently narrow it); resolves the repo root from `import.meta.url`, never `process.cwd()` (the suite's standing idiom);
  - **link-resolution arm — RESOLUTION-ONLY, deliberately not a shape assert**: extracts every markdown link target (`](…)` form), skipping `#`-leading in-page anchors and absolute URLs, stripping any `#fragment` suffix, and asserts the target file exists — resolving file-relative for `skills/war/references/` files, and file-relative OR repo-root-relative for `agents/` files. The dual arm exists because the D4 agent-card skeleton (`skills/war/references/<file>`) is deliberately repo-root-anchored and must not false-red (adjudication O(2)); a target resolving under neither root fails with a message naming the file and the target. The accepted looseness (a dead agents/ link coincidentally resolving under the other root) is recorded in the test comment — both roots are inside the repo, so any resolution is a real file, and per-shape enumeration would reintroduce the enumerated-list fail-open hazard. **Do NOT widen the no-`../` SHAPE assert family to all `agents/*.md`** — shape stays per-card in `workflow-template.test.mjs` (worker/servitor/refiner + auditor after Task 1.1); a scan-wide shape assert REDs on `agents/war-setup-scout.md`'s two deliberately deferred `../` links (spec §9) and is out of scope here. Conversion-time evidence: a simulation of exactly this scan at the pre-campaign tip found dead links **only** in the files Tasks 1.1/1.2 fix (the 9 known targets), every other scanned file clean. Re-verify at the rebased tip: a dead link discovered in a file this plan never touches is an **escalate-with-evidence** (name file, target, and the conversion-time-clean claim it contradicts) — never a silent out-of-slice fix, never a scan carve-out, never a quiet hold;
  - **retired-citation arm**: asserts zero occurrences of the retired citation form — a parenthetical carrying both `SKILL.md` and `co-source-of-truth` (e.g. `/\([^)]*SKILL\.md[^)]*co-source-of-truth[^)]*\)/`, plus the mirrored-order arm if the worker judges it cheap) — scoped to the scanned `agents/` + `skills/war/references/` set. The pattern must NOT be the bare phrase: the test comment pre-adjudicates the sanctioned carriers by name — `submodule-flows.md`'s own "Resume — submodule remote as co-source-of-truth" heading/body, `design.md`'s co-source-of-truth paragraph, and the new step-4 replacement citation (which pairs `submodule-flows.md`, never `SKILL.md`, with the phrase) — the backstop-retirement-grep false-red lesson: sweep for the retired citation form, never the sanctioned replacement's substring;
  - **header-truth arm (End state 4's mechanical home; default-flip discipline — OLD absent across every surface, not NEW-present alone)**: the NEW-present `at eviction time` assert is scoped to the **header region** of each of the four named files — the text preceding the file's first `## ` heading — by the same construct extraction the revert-doctrine arm already gets, NEVER a whole-file substring check (red-team adjudication 4, 2026-08-03: whole-file is satisfied by body prose, and this very plan appends a new trailing section to `resume-and-recovery.md` whose mandated wording carries the phrase — a self-inflicted false green).
    Then: asserts the retired re-basing-caveat form absent from every scanned `skills/war/references/*.md` (key on a CASE-INSENSITIVE, mid-sentence-stable substring — `/link paths inside the moved blocks/i`, NEVER the sentence-initial capitalised form (red-team adjudication 1, 2026-08-03: the capitalised anchor is the recorded sentence-case false-negative class — reproduced, a lowercase mid-sentence reintroduction left the guard fully green while the retired doctrine was live; the NEW-present half is likewise `/at eviction time/i`, since retitling to `**At eviction time**,` false-REDs a case-sensitive form). Grep floor is `grep -rin`, never `grep -rn`. Original wording for reference, "Relative link paths inside the moved blocks" — the scan scope is .md files only, so the test's own pattern literal cannot self-match), and asserts the "at eviction time" qualification present in each of the four named headers (`resume-and-recovery.md`, `submodule-flows.md`, `worker-servitor-edges.md`, `auditor-teach.md`) via direct `readFileSync` — fail-closed, a renamed file throws loud rather than narrowing the check;
  - **revert-doctrine construct arm (End state 5's mechanical home)**: locates the phase-close-polish-revert section heading in `resume-and-recovery.md` by heading match (non-vacuous: assert the heading was found), extracts heading→next-`## `-or-EOF, and asserts inside the region: a `Trigger:` line, the not-a-moved-block provenance token, and the duty keywords (gate-at-the-reverted-commit, green-by-deletion, re-opened findings rationale, fix-set comparison by finding title/file) — keyword presence by construct, never line numbers, tolerant of the worker's exact heading wording;
  - **Demonstrated-RED evidence** (Method protocol, SOFT by design): temporarily short-resolve one link in a references/ file in-worktree (e.g. revert one `design.md` target back to `references/design.md`), show the sweep's failing assert verbatim, restore, re-run green, `git status --porcelain` clean before commit; record in the done report. (The `../`-prefix demo belongs to Task 1.1's pointer-shape guard — a `../` target from `agents/` file-relative-resolves inside the repo, so the sweep's existence arm is deliberately not the shape arbiter.)
- requiresTest: true
- requiresPackaging: false
- deps: [1.1, 1.2]
- target repo: superproject

## Deferred validations (backstops — AI-declared)

- `agents/war-setup-scout.md`'s two `../`-prefixed links (`../skills/_shared/provision.mjs`, `../skills/war/references/schemas.md`) remain unnormalized — both currently resolve file-relative and the new sweep test deliberately does not flag them (resolution-only arm) · why deferred: no issue in this group names the file (spec §9); normalization falls under adjudication O(2)'s general rule · runner: the Lead files a follow-up issue at phase close (checkpoint paperwork); picked up by the next `/survey-corps` sweep.
- Discoverability of the new revert-doctrine section from `skills/war/SKILL.md` — ADR 0042's trigger pointer (`when adjudicating a phase-close revert, read references/resume-and-recovery.md § <the landed heading>`) is owed and deliberately NOT landed here · why deferred: this group was carved to avoid the cross-group `skills/war/SKILL.md` collision (spec §2, §9) — a spec-ratified deviation, see Notes entry 9 · runner: the Lead files a follow-up issue at phase close naming the pointer text; owned by a later campaign group or follow-up task that owns `skills/war/SKILL.md`.
- Backticked **prose** path citations (non-link) that misresolve have no mechanical guard — the sweep reads only `](…)` targets · why deferred: a prose-path parser is out of proportion to the debt; the in-task hand-scans (Tasks 1.1/1.2 straggler protocol) are this phase's coverage · runner: the done-report surveys this phase; recurring coverage via future `/survey-corps` doc sweeps.
- Promotion of adjudication row O's cross-repo pointer doctrine into a short ADR (spec §7, optional) · why deferred: not part of this group's scope; current citations resolve to the red-team report/issue thread · runner: operator decision post-campaign.

- Demonstrated-RED mutation proofs for BOTH new guards (End state 7 — Task 1.1's `../`-prefix reintroduction on the extended pointer-shape guard, Task 1.3's short-resolved-link on the sweep test): deliberately-uncommitted in-worktree probes, so the evidence is done-report-only and reads SOFT at gate-audit per the ADR 0041 ladder, never a hold · why deferred: a mutation proof cannot be a committed gate member without permanently redding the suite · runner: each owning task's worker records the demonstrated-RED output in its done report (mandatory statement, never omitted); the **Lead spot-reproduces one proof per guard at phase close** before recording the phase landed. Added at red-team (adjudication 5, 2026-08-03): End state 7 is this plan's ONLY [SOFT by design] condition and the ONLY non-vacuity evidence for the two guards it ships, yet the backstops section carried no row for it — so the plan's own End-state header claim that SOFT conditions are "sanctioned via the backstops" was false about its own content, and the deferral was the prose-waiver class ADR 0017 forbids.

## Notes / conscious deviations

--afk self-adjudications (ADR 0014 — no operator; each entry is question → decision → rationale, for /red-team ratification):

1. **Spec §4.1 + §4.2 + §4.4 merged into Task 1.1; §4.3 + §4.5 merged into Task 1.2** (grill Q1/Q6). The war-auditor pointer fix, the auditor-teach citation repair, and the worker-servitor-edges header rewrite share one guard home — `workflow-template.test.mjs` — so splitting them leaves two wave-1 siblings each conditionally needing the same test file, a same-file collision waved edges cannot dodge; merging keeps guard-file ownership 1:1 with doc-file ownership, and the pointer fix and its guard extension travel in ONE task (ADR 0025). Likewise §4.3's link rewrites and §4.5's new section both edit `resume-and-recovery.md` — one task, never two.
2. **The sweep test lives in a NEW file** `reference-link-integrity.test.mjs`, not a `skill-doc-contracts.test.mjs` block (grill Q2) — the spec sanctions either; the new file avoids the intra-plan conditional collision with Task 1.2 and the cross-plan collisions with `2026-08-02-structural-test-nonvacuity` and `2026-08-02-war-engine-and-standing-doc-truth`, which both touch `skill-doc-contracts.test.mjs`. Auto-discovery via the `node --test` glob means no wiring.
3. **Roadmap dependency (grill Q3):** the `structural-test-nonvacuity` spec declares itself campaign-primary owner of `workflow-template.test.mjs` and mandates a `dependsOn` edge from any sibling touching it — this plan's roadmap row carries `dependsOn: structural-test-nonvacuity`, and the stacking preamble instructs workers to re-verify every measured fact (pointer count, #1216-finding-1 already-fixed, link census) at the frozen phase base. Nonvacuity's own edits to that file (retry-merge predicates, rung-body loop) are in different constructs than the Task 5.1 block this plan extends — expected clean rebase, recorded for the roadmap's contention table regardless.
4. **Grill Q4's premise is corrected, not adopted:** `war-engine-and-standing-doc-truth` D14 edits ADR 0008's Consequences bullet (in `docs/adr/`), NOT `resume-and-recovery.md` — verified against that spec's §5 surface table, which never lists the file. No resume-and-recovery.md cross-plan contention exists. The real cross-plan contention is on `workflow-template.test.mjs` (this plan Task 1.1 + nonvacuity Task 2 + war-engine's optional D6 pin) and `skill-doc-contracts.test.mjs` (this plan Task 1.2 conditional + nonvacuity Task 1 + war-engine D22/D15) — both belong in the roadmap's shared-file table with serial landing order.
5. **End state 8 vs ADR 0025 discovery (grill Q7):** resolved by stating the verified no-pin-hit premise in the plan (all known pins over the edited files are token/construct-anchored; zero link-target-byte pins found at conversion) and pre-assigning conditional ownership of the three pin-carrying test files to Task 1.2's Files. The diff census is subset-semantics (footprint is a ceiling, untouched conditional files are expected), and a suite-forced edit outside every task's Files escalates as a plan defect — never a silent widening, never an auditor false-block.
6. **Straggler protocol (grill Q8):** in-Files stragglers are fixed in-task as survey-derived corrections (a fifth war-auditor pointer also bumps the count assert in the same commit); out-of-Files stragglers are follow-up findings in the done report (disposition `follow-up`), never fixed in-task. Follow-up issue filing is Lead checkpoint work at phase close.
7. **Count-of-four is measured-at-conversion, not plan-normative (grill Q9):** the slice anchors on "every auditor-teach pointer on the card", directs re-measurement at the frozen phase base, pins the assert to the measured post-fix count, and mandates the test comment name its own update duty — the stack-fragile-literal rule applied to a count.
8. **Demonstrated-RED evidence is SOFT by design (grill Q10):** the Method pins the mutate-in-worktree protocol (named suite, verbatim failing assert, restore, green re-run, clean `git status --porcelain` before commit) and declares the evidence class done-report/gate-audit execution evidence per the ADR 0041 ladder — the execution-evidence seat cannot-confirms SOFT, never holds (deliberately-uncommitted-worker-probe lesson).
9. **ADR 0042 pointer-without-trigger deviation (grill Q13):** the revert-doctrine section lands with NO inbound trigger pointer — a defect under ADR 0042's default read, consciously accepted because the spec (§2, §9) ratifies deferring the `skills/war/SKILL.md` pointer to avoid the cross-group collision this group was carved to remove. The deviation is spec-ratified, carried as a backstop row with a named runner (Lead files the follow-up issue at phase close), and is NOT a Critical for any seat reading only plan + ADR + diff — this entry is the pre-adjudication.
10. **§10.4 and §10.5 get mechanical homes (grill Q11/Q12):** the header-truth arm (OLD-absent re-basing caveat across the scanned references/ scope + at-eviction-time NEW-present on the four named files — rule 6's default-flip discipline, both halves encoded) and the revert-doctrine construct arm both land in Task 1.3's sweep test. The doctrine guard sits in a different task than the doctrine text (Task 1.2) — sanctioned via the explicit `deps: [1.1, 1.2]` edge (guard-split-needs-deps-edge doctrine): the sweep worker rebases onto the integrated tip, so the guard is green at its own dispatch base; its only same-task home would collide with Task 1.2's conditional test-file footprint.
11. **Sanctioned `co-source-of-truth` carriers pre-adjudicated (grill Q14):** `submodule-flows.md`'s own heading/body, `design.md`'s paragraph, and the new step-4 replacement citation are named in both the Task 1.3 slice and the mandated test comment; the retired-form pattern keys on `SKILL.md` + `co-source-of-truth` in one parenthetical. A seat running the naive bare-phrase Grep and flagging the sanctioned carriers is flagging ratified text.
12. **Sweep scope is resolution-only; shape stays per-card (grill Q15):** the slice explicitly forbids widening the no-`../` shape assert to all `agents/*.md` — it would RED on war-setup-scout.md's two deferred links (backstop 1).
13. **`agents/war-worker.md` is deliberately in no Files list (grill Q17):** expected zero edits (End state 8 asserts it); it stays in scope for Task 1.1's hand-scan survey and the decisive-rules-inline verification, whose arbiter is the existing Task 5.1 test; a discovered sole-carried blocking rule escalates as a plan defect, never a silent inline pull-back.
14. **Task 1.2 is `requiresTest: false` (grill Q16):** its primary diff is prose; its three test files are conditional-ownership-only and expected untouched, so `requiresTest: true` would false-route `no-test` at the floor when the expected empty test-diff holds. Tasks 1.1/1.3 carry their tests in-diff and are `true`. `requiresPackaging: false` on all three — docs-plus-tests, no packaging surface.
15. **The auditor-teach step-4 citation is resolved to ONE form (grill Q22):** backticked `submodule-flows.md` + section name, no markdown link — the spec marked the link optional; two valid landed shapes are an end-state-literal-divergence noise generator, so the plan closes the option.
16. **Untouched-file dead-link rule (grill Q24):** the sweep's green depends on files this plan never edits. Conversion-time simulation of the exact scan found every untouched file clean (dead links only in the 9 known targets Tasks 1.1/1.2 fix); a dead link surfacing in an untouched file at the rebased tip is an escalate-with-evidence, never a silent fix, scan carve-out, or quiet hold.
17. **Phase-close polish boundary (grill Q25):** the freshly qualified header claims, the cross-repo-posture sentence, and the new doctrine section are adjudicated LANDED text once merged — the phase-close coherence sweep must not "re-improve" them, and any revert of this phase's own polish commit falls under the very revert-doctrine section this plan lands (gate at the reverted commit; green-by-deletion diff; fix-set comparison by finding title/file). The absorb-fix-can-invert-mirror-direction lesson applies squarely to this file family.
18. **Predecessor-consistency (ADR 0014):** intent blocks of `2026-08-02-structural-test-nonvacuity`, `2026-08-02-redteam-doctrine-and-guards`, and `2026-07-28-prompt-surface-simplification` were read; this plan adopts the two --afk predecessors' conventions (HARD/SOFT End-state tags, issue → task mapping line, Method-level mutation-proof protocol and auditor-calibration guardrails, gate-artifact citation, sweep-token hygiene). No tonal or standing-constraint divergence found; the only structural divergence is docs-heavy tasks with conditional test-file ownership, explained in entries 5 and 14.
19. **No release phase**, per spec §5 (docs+tests only, no behavioral diff); the roadmap may attach one using the directive form stated under Build order. Version literals anywhere in this plan or its spec are non-authoritative. This is an `--afk` conversion (ADR 0014): intent is AI-authored from the ratified spec, backstops are AI-declared, and every land-time surfacing renders them as such.

## Open decisions

- Exact heading text for the revert-doctrine section (spec gives "e.g. `## Phase-close polish reverts — never self-justifying`") — worker's judgment within the file's heading conventions; End state 5 and the Task 1.3 construct arm check by `Trigger:`/provenance/duty keywords against the landed heading, not a pinned literal.
- ~~Whether the retired-citation regex also guards the reversed token order~~ — **CLOSED at red-team (adjudication 6, 2026-08-03): both orders are MANDATORY, not worker judgment.** With the single recorded order alone the arm false-negates on a reversed-order reintroduction of the identical dangling citation (reproduced: `(submodule co-source-of-truth, see \`SKILL.md\`)` passes a single-order guard green). Mandate one case-insensitive alternation scoped to the parenthetical, e.g. `/\([^)]*(SKILL\.md[^)]*co-source-of-truth|co-source-of-truth[^)]*SKILL\.md)[^)]*\)/i`; the pattern stays parenthetical-scoped so the plan's false-red worry is unchanged.
