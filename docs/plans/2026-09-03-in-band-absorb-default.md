# In-band absorb by default — squash every seat-found fixable bug inside the run

**Evidence consumed** — one row per linked artifact:
- `docs/studies/2026-09-03-followup-filing-study/` (README, `classified.json`, `closures.json`, `versions.json`) at `782af11` on `claude/issue-1476-analysis-784c44` (PR #1996) · read
- `skills/war/assets/workflow-template.js` at `cabd25b`: DISPOSITION RULE + WIDENINGS prompt blocks, the two ace-side reserve gates and the batch-ace gate, the three ace-side `fixRounds++` charge sites, the `fixRounds` seeds, the held-phase `phaseCloseQueue` drain, the sweep-raised demote arms, the ace-off demote arms, the file-followups dispatch and consolidation, the `ace_diff_files` clause, the `Ace-Subset` trailer builds, the phase return · read
- `skills/war/references/disposition-eligibility.md`, `agents/war-auditor.md` (Latitude and disposition), `agents/war-refiner.md` (section list), ADR 0013 (rule 4 + both amendments), ADR 0012 at `cabd25b` · read
- `skills/war-campaign/assets/campaign-ledger.mjs` (`extractFiles`, `collectBlock`, exports) at `cabd25b` · read
- `skills/war/assets/prompt-surface-budgets.test.mjs` budget rows, `skill-doc-contracts.test.mjs` `D37a`/`D43`, `workflow-template.test.mjs` `DISPO` pin and `ace-group-path`, `war-config.test.mjs` roundLimit fallback guard at `cabd25b` · read
- issues #1492, #1431, #1547 · read
- #1547 `## Evidence artifacts`: target-repo epic phase reports (#321, #322) · unread — private target repo; duplicate-issue clusters in the target repo · unread — private target repo; the interview-review exchange under `~/Documents/WAR/1547-…` · unread — outside this checkout; the motivating run manifest `recseg-serving-uptime-2026-08-15.json` · unread — private target repo, not among this repo's manifests
- `.claude/war/runs/*.json` · read for the study denominator only
- `docs/red-team/2026-09-03-in-band-absorb-default.md` rounds 1 and 2 · read

## Context — the gap / problem

- 85% of the 391 `war-followup` issues were fixable in-band; 189 of them in five lines or fewer (verified: the study at `782af11`).
- The fixable share is flat at 82% to 92% across every plugin release band since 0.15. No release moved it (verified: the study, `versions.json`).
- The dominant why-not-absorbable is a scope argument ("the slice does not name this file"), not a mechanical barrier (verified: the study, class rows and agent notes).
- The #1431 mechanism-substitution class is 5% of filings; the #1547 batch-revert demotion class is under 1% (verified: the study).
- Lane-closed filings (fixable, but every WAR lane shut by rule) are 8% to 15% of filings from 0.17.11 on (verified: the study, `versions.json`).
- 52 absorb-disposed findings demoted because the phase-close sweep never ran (verified: the study, channel rows). The held-phase arm demotes the whole `phaseCloseQueue` with "held phase — the phase-close sweep never dispatched" (verified: `workflow-template.js` held-phase drain at `cabd25b`).
- A sweep-raised absorb demotes because the sweep is the phase's terminal fix round. Two demote arms carry the reason, on the merged-sweep path and the discard path, plus one census comment (verified: `workflow-template.js` at `cabd25b`; prior lesson `terminal-phase-close-polish-absorb-finding-has-no-further-round-to-land-it`).
- Today `absorb` is never a default; an omitted Minor disposition becomes `follow-up` (verified: ADR 0013 rule 4, `agents/war-auditor.md` Disposition rule, dispatched DISPOSITION RULE at `cabd25b`). The old claim lives on eight living surfaces in four wordings, in three absorb-bearing sites of `workflow-template.js` (the AUDIT_VERDICT schema comment, the `dispositionOf` header comment, the dispatched sentence) and one `ask`-only survivor (the parked-ask identity comment), and in two test pins (`workflow-template.test.mjs` `DISPO` const and the "survives byte-untouched" assert; `skill-doc-contracts.test.mjs` `D37a`) (verified: red-team round 2 at `cabd25b`).
- Two ace-side gates read `fixRounds >= roundLimit - 2`: the `aceBisect` subset-commit stop and the `aceReentry` reserve stop. The batch ace reads `fixRounds < roundLimit`, deliberately outside the reserve. Every ace-side commit charges the shared `fixRounds` counter at three `fixRounds++` sites (subset, re-entry, batch ace) that the merge-floor retry loop also draws from (verified: `workflow-template.js` at `cabd25b`). `fixRounds` is seeded from the in-process round index, never from git (verified: the two `fixRounds =` seeds at `cabd25b`). `roundLimit` is also read by the blocking fix loop, the segmented-land re-dispatch, and the CAS push loop (verified: same file).
- The retired reserve arithmetic `roundLimit − 2` is stated as live law on `skills/war/SKILL.md` (`--ace` bullet), `CONTEXT.md` (**Ace bisection** and **Re-entry** entries), `references/design.md` §18, `disposition-eligibility.md` widening 1, `schemas.md`'s re-entry row, seven `workflow-template.js` comments, and two log strings; `skill-doc-contracts.test.mjs` `D43` binds the first three (verified: red-team round 2 at `cabd25b`).
- The `Ace-Subset` trailer carries a round index only on the re-entry build; the bisect subset trailer carries task id plus files; the batch ace carries no trailer (verified: the two trailer builds at `cabd25b`).
- `run.ace: false` demotes a would-be absorb with "absorb requires --ace (off this run)" at two sites (verified: `workflow-template.js` at `cabd25b`). The economy preset pins `ace: false`; the fact is restated on `README.md` (`--ace` row), `skills/war/SKILL.md` (`--ace` bullet), `schemas.md` (`ace` comment), `skills/war-room/SKILL.md` (preset row), and `war-config.test.mjs` (the preset pin test) (verified: grep at `cabd25b`).
- The Workflow sandbox cannot read files or run git; the orchestrator's only file refusal today is a release-slot filename string check (`aceEligible`) (verified: `workflow-template.js` at `cabd25b`). The ace gate already receives a git-derived `ace_diff_files` list from a dispatched agent (verified: `ACE_DIFF_FILES_CLAUSE`). The first git-running steps after a worker returns are the pin-transfer probe and the merge dispatch, both after the ace ladder (verified: the serial merge queue at `cabd25b`).
- The file-followups dispatch runs after the land decision. By then the sweep has spliced `phaseCloseQueue` empty and the ace ladder is over. `minorsFiled` holds seat rows and engine `demote()` rows alike (verified: `workflow-template.js` at `cabd25b`).
- The handoff block emits on `landed` and `held:escalation` only. `KNOWN_LAND_DECISIONS` has seven members; `held:phase-incomplete` is never emitted by the Workflow, and `held:workflow-error` returns from a separate catch-block site (verified: `land-decision.mjs` and `workflow-template.js` at `cabd25b`).
- The sweep's polish task is a pseudo-task literal with no counter fields (verified: `polishTask` at `cabd25b`).
- `campaign-ledger.mjs` exports nine symbols; `extractFiles(lines)` takes plan lines and returns `[]` silently on a path string; `extractFilesFromPlanFile(planPath)` is module-private; `collectBlock` breaks at the first `- Files:` anchor, so a stored ledger `files` array holds the first block only (verified: `campaign-ledger.mjs` at `cabd25b`, `extractfiles-union` probe).
- Each `/war` phase is one Workflow run from a staged copy of the template (verified: `skills/war/SKILL.md` "one Workflow per phase" at `cabd25b`). Anything carried to the next phase rides the phase return and the Lead re-threads it as args.
- Byte headroom at `cabd25b` (D12 dated snapshot; re-measure at the task's rebased base): `agents/war-auditor.md` 27,915 B against 28,672 B (757 B free); `skills/war/SKILL.md` 72,712 B against 73,728 B (1,016 B free); `agents/war-refiner.md` 36,751 B against 36,864 B (113 B free, already past its advisory line). The refiner card's `## Gate-failure classification` section is 3,126 B (verified: `wc -c` + `prompt-surface-budgets.test.mjs` rows).

## Pivotal constraints

- Standing card and dispatched prompt change in the same commit (the standing/dispatched split law).
- Engine and seat-prompt behavior land before any planning-surface change. No `war-strategy`, `red-team`, or `plan-interview` doctrine edit in this plan.
- Never delete or weaken a test via absorb. Release-slot files stay refused from ace, sweep, and terminal pass. Critical/Major blocking is untouched.
- Every demotion, reroute, skip, and budget block is logged. Nothing is silent.
- Budgeted surfaces stay under their hard lines: cards keep one-sentence rules plus trigger pointers; doctrine bodies live in `references/` (ADR 0042). No `Budget-Raise:` trailer in this plan.
- `fixRounds` counts blocking fix rounds and floor retries only. No ace-side commit charges it. The merge-floor retry loop keeps its full `roundLimit` budget; the `roundLimit − 2` reserve arithmetic retires with the ace gates.
- `run.ace` gates the per-task ace ladder only. The phase-close sweep and the terminal pass run regardless.

## Resolved design tree

| # | Decision | Resolution | Source | Landing class |
|---|---|---|---|---|
| D1 | Default for an in-diff, fully specified Minor/Nit | `absorb`. `follow-up` is legal only with a barrier the seat states in a structured `barrier` field on the finding (AUDIT_VERDICT, optional enum), never as prose. The canonical enum `BARRIER_TOKENS` lives in `skills/war/assets/land-decision.mjs`: `barrier:release-slot` (release-slot file) · `barrier:underspecified` (fix not fully specified) · `barrier:rationale-comment` (the fix removes or edits a line carrying a `ponytail:` or deliberate-mirror rationale comment) · `barrier:trade-off` (behavior change with a nameable trade-off; meant for `ask`, see D4 for the no-`ask`-field case). Three follow-up barriers plus one ask-routing tag. A scope argument is not a barrier. The why-not-absorbable prose stays free text. Amends ADR 0013 rule 4 for the in-diff fully-specified case only. | (user) | PIN-1→guardrail ‡ · PIN-2→guardrail |
| D2 | Out-of-diff findings and the sweep footprint | Seat rule: a fully specified Minor/Nit whose file is outside the task diff defaults to `absorb` + `phaseClose:true`. Engine rule at sweep time: one exclusion set = `args.sweepExclude` ∪ `Files:` of every task whose status is not `merged` ∪ the release-slot filenames, both sides `aceRelPath`-normalized. A queued absorb whose file is in the set demotes to `follow-up` with a reason naming the owner (plan slug or task id). The seat never decides exclusion. | (user) | PIN-3→slice (T1.2) |
| D3a | Terminal pass | Once per phase, on the merged-sweep path only: one ace-shaped commit at the post-polish integration tip applying the terminal-born absorbs, then one read-only re-audit seat (the roster's `correctness` seat when present, else the roster's first seat with its own lens; the log names the seat). "Read-only" describes the seat, not the pass. The terminal queue is filtered through `aceEligible` before the commit; a release-slot absorb is refused and demoted with a log. The terminal commit goes through Refine (floors, gate, merge) like any ace commit, carries an `Ace-Charge` trailer, and charges `r.task.absorbRounds` on the polish task, which is seeded `absorbRounds: 0` at construction. No sweep this phase ⇒ skip with a log line. Empty terminal queue ⇒ skip with a log line. Re-audit regression ⇒ forward-revert the terminal commit, then carry (non-final phase) or demote (final phase); never a second terminal pass. A fresh absorb raised by the terminal seat: non-final phase ⇒ rides `carriedPhaseClose`, reason "carried from phase N terminal pass"; final phase ⇒ `follow-up`, reason "raised on the terminal pass". The merged-path sweep-raised demote arm is retired and the terminal pass is its successor. The discard-path demote arm stays, reworded to name the discard; on a non-final phase its absorbs ride `carriedPhaseClose` instead of demoting. Every branch logged. | (user) | PIN-4→guardrail · PIN-9→slice (T2.1) |
| D3b | Held-phase queue | `carriedPhaseClose` is a top-level key on the phase return for every `landDecision !== 'landed'`, independent of the handoff block, added at both return sites (the main return and the catch-block `held:workflow-error` return), and present as an empty array on `landed` so its absence is never ambiguous. A held phase writes its `phaseCloseQueue` there and demotes nothing. The Lead reads the phase return, not the handoff, and threads it into the relaunch as `args.seededPhaseClose`, which carries an entry-validation row and the #1413 own-token provenance treatment. Accepted residual: a Workflow that throws before returning has no phase return; the Lead re-derives the queue from the seats' audit verdicts in the run transcripts, and the relaunch log names the re-derivation (recorded in `resume-and-recovery.md`, Task 3.1). | (user) | PIN-5→slice (T2.1, T3.1) |
| D4 | Engine filing floor | The floor runs per task inside the serial merge queue, right after the merge dispatch returns `MERGE_RESULT.diff_files` and before the sweep drains `phaseCloseQueue`. It reads seat rows only: `demote()` stamps `engineFiled: true` on every row it writes, and the floor skips stamped rows. In-diff `follow-up` with no `barrier` ⇒ reroute to `absorb` + `phaseClose:true`, log "rerouted after the ace stage, sweep vehicle". Out-of-diff `follow-up` with no `barrier` ⇒ reroute to `absorb` + `phaseClose:true`, log. `barrier:trade-off` with the `ask` field present ⇒ reroute to `ask`; without the `ask` field ⇒ keep `follow-up`, log "trade-off without ask fields", never a schema throw. Any other `barrier` present ⇒ file as stated. `diff_files` absent on the merge result ⇒ the floor skips that task with a log line, never a throw. Accepted residual: a rerouted in-diff finding never re-enters the task's own ace ladder; the sweep, then the terminal pass and its re-audit, are its lanes. Deterministic only: `diff_files` (returned by the refiner's merge dispatch from `git diff --name-only`, the `ace_diff_files` shape) and `BARRIER_TOKENS` membership. The engine never estimates fix size. Worker `files_changed` is never the source. | (user) | PIN-6→guardrail |
| D5 | Absorb budget | Knob `run.absorbRounds`: integer ≥ 1, default 6, `null` reads as unset, validated in `war-config.mjs` beside `run.roundLimit`, exposed in `/war-room`. Counter `r.task.absorbRounds`, per task, charged by batch ace, re-entry batch, bisection subset commit, and the terminal pass; none of those charges `fixRounds` any more; reverts, re-audit panels, and fix rounds never charge it. All three ace gates (the `aceBisect` subset stop, the `aceReentry` stop, and the batch ace's `fixRounds < roundLimit`) read `absorbRounds < run.absorbRounds`. Every ace-side commit carries an `Ace-Charge: <task>:<n>` trailer, n = the `absorbRounds` value after the charge; reverts carry no charge trailer; the `Ace-Subset` and re-entry trailer segments stay unchanged. Ace dispatch labels carry `absorbRounds`. Relaunch source: the phase-start git-topology barrier dispatch (refiner, runs git) returns `absorbCharges: { <task>: n }`, the HIGHEST `Ace-Charge` index on the task branch (and on the integration branch for the polish task id), so a cherry-pick or duplicate trailer never double-charges; a reverted ace commit's trailer still counts. Barrier `error` or a missing field ⇒ seed 0 with a loud log line naming the task; the Lead may override through args at a sanctioned relaunch. Exhaustion routes the finding to the sweep with a log line naming the counter. | (user) | PIN-7→slice (T1.1, T1.2) |
| D6 | Contention source | The Lead builds `args.sweepExclude` at launch by calling the exported `extractFilesFromPlanFile` on the plan path of every ledger entry that is not this plan and whose status is not `landed`, and unions the result. Stored ledger `files` are not trusted for this (first-block-only until Task 1.4 lands and a sweep re-stamps them); they stay the ledger's contention record. An empty union from a present ledger logs "campaign contention set empty for <n> entries" so it is never mistaken for an absent ledger. Absent ledger ⇒ no list, one log line "no campaign contention set threaded"; the in-phase and release-slot arms still run. The run manifest records `sweepExcludeCount` (null when no list was threaded). | (user) | PIN-8→slice (T1.4, T3.1) |
| D7 | Prompt surfaces | `agents/war-auditor.md`, the dispatched DISPOSITION RULE in `workflow-template.js`, and `disposition-eligibility.md` change in one commit. `agents/war-refiner.md` and the dispatched merge prompt change in one commit. | (user) | PIN-12→guardrail ‡ |
| D8 | Ordering | Engine and seat prompts first. No planning-surface edits in this plan. | (user) | PIN-10→non-goal |
| D9 | Test law | Never delete or weaken tests via absorb. | (user) | PIN-11→guardrail ‡ |
| D10 | Task 1.2 size | D1, D2, D4, D5 land in one `workflow-template.js` task under same-file law. | (user; planner's call on the offered split: not taken) | slice (T1.2) |
| D11 | Ledger footprint | `extractFiles` unions every `- Files:` block in a plan, deduped, in order of first appearance; the backtick-absence fallback is evaluated per block before the union. The fail-loud empty-footprint throw fires only when the union is empty, never on one empty block. `extractFilesFromPlanFile` becomes an export. | (user; red-team rounds 1 and 2) | PIN-13→slice (T1.4) |
| D12 | Refiner card bytes | Task 1.2 moves `## Gate-failure classification` from `agents/war-refiner.md` to `skills/war/references/gate-failure-classification.md`, byte-identical (ADR 0042). The card keeps the fixed pointer "when the gate is red, read [gate-failure-classification.md](${CLAUDE_PLUGIN_ROOT}/skills/war/references/gate-failure-classification.md)". The `diff_files` clause lands in the freed bytes. Hard line stays 36,864 B. | (user; red-team round 2) | PIN-14→slice (T1.2) |
| D13 | Lawful engine filers | The Purpose names the class "logged engine demotion" and enumerates nothing in prose. `DEMOTE_REASONS` is a closed prefix enum, canonical in `land-decision.mjs`, hand-mirrored in `workflow-template.js` with a mirror-registry row. A default-deny census fixture asserts every `demote(..., 'follow-up', ...)` call site's reason literal starts with a member; it pins the exact site count measured at land (D12 dated snapshot) and carries one unwired negative-reference site so the matcher is non-vacuous. Every engine-filed issue body carries its `DEMOTE_REASONS` prefix on a fixed line. | (user; red-team round 2) | PIN-15→guardrail |
| D14 | Ace-off runs and the economy preset | `run.ace: false` disables the per-task ace ladder (batch ace, re-entry, bisection) only; the sweep and the terminal pass run regardless. With ace off, a defaulted `absorb` (in-diff or out-of-diff, seat-set or floor-rerouted) routes `phaseClose:true` to the sweep and never demotes on the ace-off path; the two "absorb requires --ace" demote arms retire. `absorbRounds` still bounds the terminal pass with ace off. The economy preset drops its `ace: false` override and inherits `DEFAULTS.run.ace` (true), with the same "no longer pinned, inherits" comment the preset uses for `memory.commitLearnings`; `roundLimit: 2` stays; `absorbRounds` inherits 6. Every preset resolves `run.ace === true` (default-deny census over the preset names). A user config may still set `run.ace: false`. Rule 6 sweep over the five surfaces that state the old preset fact. | (user; red-team round 2) | PIN-16→guardrail · slice (T1.1, T1.3) |

## Assumptions ledger

| ID | Assumption | Basis | Blast radius if wrong | Check |
|---|---|---|---|---|
| A1 | Sandbox args carry each task's `Files:` and status | (user) | D2's in-phase arm has no input | `sweep-exclude` fixture: in-phase hit demotes naming the task |
| A5 | The terminal pass skips with a log when no terminal-born absorb exists | [assumed: an empty ace dispatch is waste — if wrong: one no-op dispatch per phase] | one dispatch per phase | `terminal-pass` fixture: empty queue logs skip |
| A7 | The refiner's merge dispatch can return `diff_files` without a new dispatch kind | [assumed: the merge prompt already runs git in the task worktree — if wrong: a second refiner read] | D4 has no input for that task, floor skips with a log | `filing-floor` fixture: `diff_files` absent ⇒ skip + log |
| A8 | The D1 sentence rewrite plus the `barrier` clause fit the auditor card's 757 B with the body evicted to `disposition-eligibility.md`; the 3,126 B `## Gate-failure classification` eviction funds the refiner card's `diff_files` clause | [assumed: replace-in-place is net-neutral on the auditor card — if wrong: evict one more auditor-card paragraph under ADR 0042] | budget test red at land | End state 8 |
| A9 | The phase-start git-topology barrier dispatch can return one more per-task field without a new dispatch kind | [assumed: it already returns `preMerged`/`staleRemote`/`worktreeHygiene` per task — if wrong: a dedicated preflight dispatch in Task 1.2] | `absorbRounds` seeds 0 on every relaunch, logged | `absorb-budget` fixture: three charges ⇒ resumes at 3 |

## Non-goals / deferred

- Study change 5: a plan that byte-freezes a surface must name its repair lane. Deferred.
- Any widening of Critical/Major blocking.
- Planning-surface doctrine edits (`war-strategy`, `red-team`, `plan-interview`).
- Operator-found bugs. The Purpose covers seat-found findings only.
- A re-entry of a floor-rerouted finding into the task's own ace ladder. The sweep and the terminal pass are its lanes (D4 residual).

## New domain terms · Recommended ADRs

Terms for `CONTEXT.md`: **barrier list** (`BARRIER_TOKENS`, the closed enum a seat cites in the `barrier` field to file a `follow-up` or route an `ask`) · **exclusion set** (the sweep's union of foreign-owned files and release-slot files) · **terminal pass** (the one-hop ace commit plus one seat after the polish merge) · **carried queue** (a held or terminal-born absorb queue that rides the phase return into the next run) · **absorb budget** (`run.absorbRounds`, the per-task ace meter separate from `fixRounds`) · **demote reason prefix** (`DEMOTE_REASONS`, the closed enum every engine `follow-up` demotion cites).

ADRs: amend ADR 0013 with the in-diff default flip, the barrier list, and the ace-off routing (Task 1.3). ADR 0012 gains one line naming the terminal pass as the successor of the merged-sweep demote arm (Task 2.2, after the pass exists).

## Commander's Intent

- **Purpose:** every fixable bug found by a seat in a run is fixed in that run. Only a seat-cited barrier tag or a logged engine demotion may file an issue.
- **Method:** flip the in-diff default to `absorb` and route out-of-diff fixes to the sweep; give the sweep one exclusion set instead of a slice fence; add a one-hop terminal pass after the polish merge; carry a held phase's queue into the relaunch; back the seat rule with a deterministic filing floor in the merge queue; give absorbs their own round budget with a git-derived relaunch seed; make ace-off runs route to the sweep and flip the economy preset to ace on.
- **Mechanism latitude:** counter placement and log wording · exclusion-set construction order · fixture shapes and titles beyond the pinned title tokens · the carried-queue record's field names · the reroute log format · where the `sweep-exclusion.md` and `gate-failure-classification.md` bodies sit in `references/` · the `absorbCharges` field's position in the barrier result · the exact `DEMOTE_REASONS` member spellings. Substituting any of these mechanisms while the End states and binding guardrails hold is not a plan deviation and warrants no issue.
- **Binding guardrails:**
  - the barrier list is exactly `BARRIER_TOKENS`' four members, cited in the structured `barrier` field, three of them follow-up barriers and `barrier:trade-off` an ask route; a scope argument is never a barrier (PIN-1, PIN-2);
  - the terminal pass is one hop per phase, never a second pass, filtered through `aceEligible`, and a regressed terminal commit is forward-reverted then carried or demoted; the merged-path sweep-raised demote arm is retired, the discard-path arm stays (PIN-4);
  - the filing floor runs in the merge queue, reads seat rows only, and is deterministic: git-derived `diff_files` and `barrier` enum membership, never prose matching or a size estimate (PIN-6);
  - `fixRounds` counts blocking fix rounds and floor retries only; no ace-side commit charges it; the `roundLimit − 2` reserve arithmetic retires (PIN-7);
  - release-slot files stay refused from ace, sweep, and terminal pass; never delete or weaken a test (PIN-11);
  - standing card, dispatched prompt, and eligibility doc change in one commit; refiner card and dispatched merge prompt change in one commit (PIN-12);
  - every demotion, reroute, skip, and budget block is logged;
  - no planning-surface edit (PIN-10);
  - budgeted surfaces stay under their hard lines with no `Budget-Raise:` trailer; ADR 0042 evictions are byte-identical moves (PIN-14);
  - every engine `follow-up` demotion cites a `DEMOTE_REASONS` prefix (PIN-15);
  - `run.ace` gates the per-task ace ladder only; every shipped preset resolves `run.ace === true` (PIN-16).
- **End state:**
  1. The dispatched DISPOSITION RULE byte-mirrors the card, names the in-diff `absorb` default, and renders the four `BARRIER_TOKENS` from the mirror; a registry row binds the inline mirror, the card sentence, and the eligibility-doc list to the canonical export in `land-decision.mjs`; fixtures titled `barrier-list` · check: `test "$(grep -c 'barrier-list' skills/war/assets/workflow-template.test.mjs)" -ge 3 && node --test skills/war/assets/workflow-template.test.mjs skills/war/assets/land-decision.test.mjs` exits 0.
  2. The retired absorb-never-default conjunction shapes are absent from the eight living surfaces in Task 1.3's gate row, `D37a` is re-keyed to the new pair wording, and ADR 0013 carries the amendment section; row titled `old-default-absent` · check: `test "$(grep -c 'old-default-absent' skills/war/assets/skill-doc-contracts.test.mjs)" -ge 1 && node --test skills/war/assets/skill-doc-contracts.test.mjs` exits 0.
  3. Exclusion set: a campaign hit demotes naming the plan slug, an in-phase hit demotes naming the task id, an absent list reaches the sweep, a `./`-form path still matches; fixtures titled `sweep-exclude` · check: `test "$(grep -c 'sweep-exclude' skills/war/assets/workflow-template.test.mjs)" -ge 4 && node --test skills/war/assets/workflow-template.test.mjs` exits 0.
  4. Absorb budget: a task with `fixRounds` at `roundLimit` and `absorbRounds` free still runs an ace batch; a task with `absorbRounds` spent routes to the sweep with a log naming the counter; an ace commit leaves `fixRounds` unchanged; three `Ace-Charge` trailers on the branch ⇒ resumes at 3; charge then revert ⇒ resumes at 1; barrier error ⇒ 0 plus the log; the trailer grammar `Ace-Charge: <id>:<int>` is pinned; ace labels carry `absorbRounds`; `war-config.mjs` rejects `run.absorbRounds: 0` and reads `null` as unset; fixtures titled `absorb-budget` · check: `test "$(grep -c 'absorb-budget' skills/war/assets/workflow-template.test.mjs)" -ge 8 && test "$(grep -c 'absorb-budget' skills/war/assets/war-config.test.mjs)" -ge 1 && node --test skills/war/assets/workflow-template.test.mjs skills/war/assets/war-config.test.mjs` exits 0.
  5. Filing floor: a rerouted finding is present in the queue the sweep reads (order pin); an in-diff `follow-up` with no `barrier` reroutes to `absorb` + `phaseClose:true` with the residual log; an out-of-diff `follow-up` with no `barrier` reroutes the same way; an engine demotion with no `barrier` is never rerouted; a `follow-up` with `barrier:release-slot` files as stated; `barrier:trade-off` with the `ask` field reroutes to `ask`; `barrier:trade-off` without it stays `follow-up` with the "trade-off without ask fields" log; `diff_files` absent skips the task with a log; fixtures titled `filing-floor` · check: `test "$(grep -c 'filing-floor' skills/war/assets/workflow-template.test.mjs)" -ge 8 && node --test skills/war/assets/workflow-template.test.mjs` exits 0.
  6. Terminal pass: a roster without `correctness` still convenes the pass; the pass produces exactly one terminal commit with an `Ace-Charge` trailer; a polish task that never carried a counter still dispatches the pass; a terminal-born absorb on a release-slot file is refused and logged, never committed; a non-final phase emits exactly one `carriedPhaseClose` entry; a final phase files exactly one demotion with reason "raised on the terminal pass"; a re-audit regression forward-reverts the terminal commit and carries or demotes; a discarded sweep on a non-final phase carries its absorbs; no sweep or an empty terminal queue logs a skip; a spent budget logs budget-blocked and files nothing silently; the merged-path "sweep-raised absorb" demote reason is absent from `workflow-template.js`; fixtures titled `terminal-pass` · check: `test "$(grep -c 'terminal-pass' skills/war/assets/workflow-template.test.mjs)" -ge 10 && ! grep -qi 'terminal fix round; absorb has no later round' skills/war/assets/workflow-template.js && node --test skills/war/assets/workflow-template.test.mjs` exits 0.
  7. Held carry: a `held:nothing-merged` phase emits `carriedPhaseClose` on the phase return and zero demotions with reason "held phase — the phase-close sweep never dispatched"; a default-deny census asserts the key is present on every Workflow-emitted hold class (`held:escalation`, `held:nothing-merged`, `held:land-failed`, `held:submodule-pr`, `held:workflow-error`, the last from the catch-block return) and is an empty array on `landed`; a relaunch with `args.seededPhaseClose` drains the seeded entries into the sweep; fixtures titled `held-carry` · check: `test "$(grep -c 'held-carry' skills/war/assets/workflow-template.test.mjs)" -ge 3 && node --test skills/war/assets/workflow-template.test.mjs` exits 0.
  8. Every budgeted prompt surface stays under its hard line at the landed tip, with no `Budget-Raise:` trailer in the plan's commits; the evicted `## Gate-failure classification` body is present verbatim in `skills/war/references/gate-failure-classification.md` and the card's trigger pointer is pinned; fixtures titled `refiner-evict` · check: `test "$(grep -c 'refiner-evict' skills/war/assets/skill-doc-contracts.test.mjs)" -ge 2 && node --test skills/war/assets/prompt-surface-budgets.test.mjs skills/war/assets/skill-doc-contracts.test.mjs` exits 0.
  9. Lead relaunch threading: `resume-and-recovery.md` carries the step that threads the phase return's `carriedPhaseClose` into `args.seededPhaseClose`, the `absorbCharges` override note, and the transcript re-derivation residual; `skills/war/SKILL.md` carries the `sweep-exclusion.md` trigger pointer; rows titled `lead-carry-pin` and `sweep-exclusion-pin` in `skill-doc-contracts.test.mjs` · check: `test "$(grep -c 'lead-carry-pin' skills/war/assets/skill-doc-contracts.test.mjs)" -ge 1 && test "$(grep -c 'sweep-exclusion-pin' skills/war/assets/skill-doc-contracts.test.mjs)" -ge 1 && node --test skills/war/assets/skill-doc-contracts.test.mjs` exits 0.
  10. Telemetry rows present: `skills/war-review/SKILL.md` names `sweepExcludeCount`, the terminal-pass seat row, `absorbRounds`, and a follow-ups-per-`DEMOTE_REASONS`-prefix tally · check: `test "$(grep -ci 'sweepExcludeCount' skills/war-review/SKILL.md)" -ge 1 && test "$(grep -ciE 'terminal[ -]pass' skills/war-review/SKILL.md)" -ge 1 && test "$(grep -ci 'absorbRounds' skills/war-review/SKILL.md)" -ge 1 && test "$(grep -ci 'DEMOTE_REASONS' skills/war-review/SKILL.md)" -ge 1` exits 0.
  11. Ledger footprint: `extractFiles` unions every `- Files:` block with per-block fallback, and `extractFilesFromPlanFile` is exported; fixtures titled `files-union` · check: `test "$(grep -c 'files-union' skills/war-campaign/assets/campaign-ledger.test.mjs)" -ge 5 && node --test skills/war-campaign/assets/campaign-ledger.test.mjs` exits 0.
  12. Demote census: every `demote(..., 'follow-up', ...)` call site cites a `DEMOTE_REASONS` prefix, the site count is pinned, one unwired negative reference is present, and the mirror-registry row binds the inline enum to `land-decision.mjs`; fixtures titled `demote-census` · check: `test "$(grep -c 'demote-census' skills/war/assets/workflow-template.test.mjs)" -ge 3 && node --test skills/war/assets/workflow-template.test.mjs skills/war/assets/land-decision.test.mjs` exits 0.
  13. Ace-off and the economy flip: with ace off a seat absorb reaches the sweep, a floor reroute reaches the sweep, and the terminal pass convenes; the "absorb requires --ace" demote reason is absent from `workflow-template.js`; every preset resolves `run.ace === true`; the old "economy preset pins ace off" fact is absent from `README.md`, `skills/war/SKILL.md`, `skills/war/references/schemas.md`, `skills/war-room/SKILL.md`, and `war-config.test.mjs`, and each carries the new fact; fixtures titled `ace-off-route` (engine), `preset-ace-on` (config), `economy-ace-flip` (docs) · check: `test "$(grep -c 'ace-off-route' skills/war/assets/workflow-template.test.mjs)" -ge 3 && ! grep -q 'absorb requires --ace' skills/war/assets/workflow-template.js && test "$(grep -c 'preset-ace-on' skills/war/assets/war-config.test.mjs)" -ge 1 && test "$(grep -c 'economy-ace-flip' skills/war/assets/skill-doc-contracts.test.mjs)" -ge 2 && node --test skills/war/assets/workflow-template.test.mjs skills/war/assets/war-config.test.mjs skills/war/assets/skill-doc-contracts.test.mjs` exits 0.
  14. Field effect · backstop: row 1 in `## Deferred validations (backstops)`.

## Build order (for /war)

Phase 1 (knob + economy flip, engine core + seat surfaces + refiner eviction, default-flip doc sweep, ledger footprint) → Phase 2 (terminal pass + held carry) → Phase 3 (Lead surfaces) → Phase 4 (release).

## Phase 1 — Default flip, exclusion set, absorb budget, filing floor, ace-off routing

### Task 1: absorbRounds knob and the economy ace flip
- Files: `skills/war/assets/war-config.mjs`, `skills/war/assets/war-config.test.mjs`, `skills/war-room/SKILL.md`
- Plan slice: add `run.absorbRounds` to `DEFAULTS.run` (6); presets inherit it; validate integer ≥ 1 with `null` read as unset (the conventional unset value, never a hard reject); the economy preset drops `ace: false` and inherits `DEFAULTS.run.ace` with the same "no longer pinned, inherits" comment it uses for `memory.commitLearnings`, keeping `roundLimit: 2` (D14, PIN-16); re-pin the preset test to the new effective config and add a default-deny census that every preset name resolves `run.ace === true`; add the `/war-room` override row for `run.absorbRounds` beside `run.roundLimit`, stating the shape only (integer ≥ 1, `null` unset) and pointing at `war-config.mjs` `DEFAULTS.run` for the value (de-mirror, ADR 0025) (PIN-7); reword the `run.ace` row to "per-task ace ladder" and the economy preset row to "single seat, roundLimit 2, ace on". Fixtures titled `absorb-budget` (reject-0, null-unset) and `preset-ace-on`.
- Done when: `node --test skills/war/assets/war-config.test.mjs`
- requiresTest: true
- requiresPackaging: false
- deps: []
- target repo: superproject

### Task 2: engine core, seat prompts, refiner eviction, one commit
- Files: `skills/war/assets/workflow-template.js`, `skills/war/assets/workflow-template.test.mjs`, `skills/war/assets/land-decision.mjs`, `skills/war/assets/land-decision.test.mjs`, `agents/war-auditor.md`, `agents/war-refiner.md`, `skills/war/references/gate-failure-classification.md`, `skills/war/references/disposition-eligibility.md`, `skills/war/references/file-followups.md`, `skills/war/references/schemas.md`
- Plan slice: (D1) add the canonical `BARRIER_TOKENS` export to `land-decision.mjs` with its own test row, hand-mirror it in `workflow-template.js` with a mirror-registry row (rule 5), render the dispatched DISPOSITION RULE sentence from the mirror, rewrite the card sentence to the in-diff `absorb` default and the barrier list, byte-mirrored, with the body in `disposition-eligibility.md`; add the optional `barrier` enum field to AUDIT_VERDICT (schema, card, dispatched prompt, `schemas.md` row); a registry row binds the card sentence and the eligibility-doc list to the canonical export (PIN-1, PIN-2, PIN-12); clear the old absorb-never-default wording at every occurrence in the files this task owns: `agents/war-auditor.md` (the Disposition rule), `schemas.md` (the AuditVerdict disposition row, uppercase `NEVER defaulted`), and the three absorb-bearing sites in `workflow-template.js` (the AUDIT_VERDICT findings-schema disposition comment, the `dispositionOf` header comment, the dispatched sentence), rewording the `dispositionOf` header to an `ask`-only never-defaulted clause; the parked-ask identity comment stays as the one `ask`-only survivor; re-key both test pins that quote the old sentence in `workflow-template.test.mjs` (the `DISPO` const byte mirror and the "survives byte-untouched" assert in the disposition-prompt-widened test) (lesson `source-comment-lags-emitted-prompt-after-rewrite`); re-author `disposition-eligibility.md` widening 1 and the `schemas.md` re-entry row from `fixRounds < roundLimit − 2` to `absorbRounds < run.absorbRounds`, and rewrite the seven engine comments and two log strings that state the retired arithmetic; hoist `aceRelPath` out of the wave-loop block to file scope and re-anchor the `ace-group-path` test's extraction (slice from `const aceGroups`, evaluate `aceRelPath` from its own `src.match`, the recordAcedTouched idiom) and refresh the now-false remintKey comment; (D2) add the out-of-diff seat rule (`absorb` + `phaseClose:true`) to both prompt layers, and build the exclusion set at sweep time from `args.sweepExclude` ∪ `Files:` of tasks not `merged` ∪ the release-slot filenames, `aceRelPath` on both sides, demotion reason naming the owner (PIN-3); add the `sweepExclude` args entry-validation row; (D5) add `r.task.absorbRounds`, charge it at batch ace, re-entry, and subset commits, and reserve the terminal-pass charge site; remove the `fixRounds++` charge at those three sites; convert all three ace gates to `absorbRounds < run.absorbRounds`; add the `Ace-Charge: <task>:<n>` trailer to every ace-side commit prompt (reverts excluded) and carry `absorbRounds` in the ace dispatch labels; extend the phase-start git-topology barrier dispatch (card + dispatched prompt + result schema) to return `absorbCharges: { <task>: n }`, the highest `Ace-Charge` index on the task branch, and seed `r.task.absorbRounds` from it, 0 with a loud log on error or absence; hand-mirror the `absorbRounds` default and guard it by extending MIRROR_REGISTRY with a scalar-extraction mode (the three existing modes cannot express a scalar) (PIN-7); (D4) add `diff_files` to `MERGE_RESULT` with its schema pin, the refiner card clause and dispatched merge clause (the `ace_diff_files` shape); land the filing floor in the serial merge queue right after the merge dispatch returns, before the sweep drains the queue: seat rows only, `demote()` stamps `engineFiled: true` and the floor skips stamped rows, no `barrier` ⇒ `absorb` + `phaseClose:true` with the "rerouted after the ace stage, sweep vehicle" log (in-diff and out-of-diff), `barrier:trade-off` ⇒ `ask` when the `ask` field is present else keep with a log, any other barrier ⇒ file as stated, `diff_files` absent ⇒ skip with a log (PIN-6); (D12) move `## Gate-failure classification` byte-identical to `skills/war/references/gate-failure-classification.md` and leave the fixed trigger pointer on the card; the `diff_files` clause lands in the freed bytes (PIN-14); (D13) add the canonical `DEMOTE_REASONS` prefix enum to `land-decision.mjs` with its test row, mirror it with a registry row, make every `demote(..., 'follow-up', ...)` reason literal start with a member, land the default-deny census with the pinned site count and one unwired negative reference, and put the prefix on a fixed line of every engine-filed issue body in the file-followups prompt, mirrored into `file-followups.md` (PIN-15); (D14) retire the two "absorb requires --ace" demote arms: with ace off a defaulted absorb routes `phaseClose:true` to the sweep (PIN-16); add `schemas.md` rows for `diff_files`, `sweepExclude`, `absorbCharges`, `barrier`, `engineFiled`. Fixtures titled `barrier-list`, `sweep-exclude`, `absorb-budget`, `filing-floor`, `demote-census`, `ace-off-route`, each red with its arm deleted.
- Done when: `node --test skills/war/assets/workflow-template.test.mjs skills/war/assets/land-decision.test.mjs skills/war/assets/prompt-surface-budgets.test.mjs`
- requiresTest: true
- requiresPackaging: false
- deps: [1]
- target repo: superproject

### Task 3: default-flip doc sweep
- Files: `CLAUDE.md`, `CONTEXT.md`, `README.md`, `skills/war/SKILL.md`, `skills/war/references/design.md`, `skills/war/references/gastown-design-params.md`, `docs/adr/0013-commanders-intent-and-disposition-routing.md`, `skills/war/assets/skill-doc-contracts.test.mjs`
- Plan slice: rewrite the old "never a default" sentence on the living surfaces this task owns to the new rule (rule 6); append the ADR 0013 amendment (in-diff default flip, barrier list, ace-off routing; the ratified rule-4 body stays byte-untouched); add the six glossary terms; land the `old-default-absent` gate row asserting the retired conjunction shapes absent across the eight living surfaces: `CLAUDE.md`, `CONTEXT.md`, `agents/war-auditor.md`, `skills/war/SKILL.md`, `skills/war/assets/workflow-template.js`, `skills/war/references/design.md`, `skills/war/references/gastown-design-params.md`, `skills/war/references/schemas.md` (Task 2 authors the three it owns; this task's gate reads them after the rebase, rule 7). The assert matches the exact retired conjunction shapes over normalized, case-folded text (`absorb` and `ask` are never default*, `absorb` … never a default, `absorb` and `ask` never defaulted), not a raw proximity window, so the lawful new sentence that states the absorb default and keeps `ask` never-defaulted passes. ADR 0013 is exempt from the OLD-absent row (append-only law, the file's own 2026-08-20 exemption idiom): the amendment lands NEW-present with a pin that the amendment section exists. Re-key `D37a` to the new pair wording on `CONTEXT.md` **Disposition** and `CLAUDE.md` Known-traps in the same commit. Re-author the `D43` living-doc homes plus `CONTEXT.md`'s **Re-entry** entry to the `absorbRounds` boundary and re-key `D43` in the same commit, with the retired `roundLimit − 2` arithmetic as an OLD-absent assert over `skills/war/SKILL.md`, `CONTEXT.md`, `references/design.md`, `disposition-eligibility.md`, and `schemas.md` (Task 2 authors the last two). Economy flip sweep (D14, rule 6): rewrite the `README.md` `--ace` row, the `skills/war/SKILL.md` `--ace` bullet, and land the `economy-ace-flip` rows: one OLD-absent row over `README.md`, `skills/war/SKILL.md`, `skills/war/references/schemas.md`, `skills/war-room/SKILL.md`, and `war-config.test.mjs` for "economy preset" adjacent to "off" / "pins it off" / "false", plus a NEW-present row on each (Tasks 1 and 2 author the surfaces they own). Land the `refiner-evict` rows: the evicted body present verbatim in `gate-failure-classification.md` (extract and equality), the card's trigger sentence pinned. Keep `skills/war/SKILL.md` under its hard line: replace in place, no net growth.
- Done when: `node --test skills/war/assets/skill-doc-contracts.test.mjs`
- requiresTest: true
- requiresPackaging: false
- deps: [1, 2]
- target repo: superproject

### Task 4: ledger footprint union
- Files: `skills/war-campaign/assets/campaign-ledger.mjs`, `skills/war-campaign/assets/campaign-ledger.test.mjs`
- Plan slice: `extractFiles` unions every `- Files:` block in the plan, deduped, in order of first appearance, with the backtick-absence fallback evaluated per block before the union; the fail-loud empty-footprint throw fires only when the union is empty, never on one empty block; export `extractFilesFromPlanFile` (D11, PIN-13). Fixtures titled `files-union`: a three-task plan yields the union; a path repeated across tasks appears once; a plan with one empty block and one full block does not throw; a plan with one backticked block and one bare comma-separated block yields the union of both (red under a concatenate-then-scan implementation); the old single-block fixture still passes.
- Done when: `node --test skills/war-campaign/assets/campaign-ledger.test.mjs`
- requiresTest: true
- requiresPackaging: false
- deps: []
- target repo: superproject

## Phase 2 — Terminal pass and held carry

### Task 1: engine
- Files: `skills/war/assets/workflow-template.js`, `skills/war/assets/workflow-template.test.mjs`, `skills/war/references/schemas.md`
- Plan slice: (D3a) add the terminal pass on the merged-sweep path: filter the terminal queue through `aceEligible` (refuse and log a release-slot absorb), then one ace-shaped commit at the post-polish integration tip with an `Ace-Charge` trailer, through Refine like any ace commit, charging `r.task.absorbRounds` on the polish task (seed the polish pseudo-task `absorbRounds: 0` at construction), then one re-audit seat (`correctness` when present, else the roster's first seat with its lens, logged); no sweep or an empty terminal queue ⇒ skip with a log; re-audit regression ⇒ forward-revert the terminal commit, then carry (non-final) or demote (final), never a second pass; a fresh absorb from that seat rides `carriedPhaseClose` on a non-final phase and demotes to `follow-up` with "raised on the terminal pass" on the final phase; retire the merged-path "sweep-raised absorb" demote arm and rewrite the demote-reason census comment that names it; the discard-path arm stays, reworded to name the discard, and on a non-final phase its absorbs ride `carriedPhaseClose` instead (PIN-4, PIN-9). (D3b) add `carriedPhaseClose` as a top-level key on the phase return for every `landDecision !== 'landed'` at both return sites (main and catch-block), and as an empty array on `landed`; the held-phase arm writes `phaseCloseQueue` there instead of demoting; add `args.seededPhaseClose` with an entry-validation row and the #1413 own-token provenance treatment; seeded entries drain into the sweep (PIN-5). Add `schemas.md` rows for `carriedPhaseClose` and `seededPhaseClose`, each with a pin in `workflow-template.test.mjs` binding the row's key name to the engine's emitted and validated key (the `done_when_log_path` schema-slot pin shape). Fixtures titled `terminal-pass` and `held-carry`.
- Done when: `node --test skills/war/assets/workflow-template.test.mjs`
- requiresTest: true
- requiresPackaging: false
- deps: []
- target repo: superproject

### Task 2: glossary and ADR 0012
- Files: `CONTEXT.md`, `docs/adr/0012-intra-phase-visibility-and-phase-close-sweep.md`, `skills/war/assets/skill-doc-contracts.test.mjs`
- Plan slice: add the terms terminal pass and carried queue; retire the sweep-raised sentence in `CONTEXT.md`; append the ADR 0012 line naming the terminal pass as the successor of the merged-sweep demote arm; pin both terms and the ADR line.
- Done when: `node --test skills/war/assets/skill-doc-contracts.test.mjs`
- requiresTest: true
- requiresPackaging: false
- deps: [1]
- target repo: superproject

## Phase 3 — Lead surfaces

### Task 1: launch and relaunch prose
- Files: `skills/war/SKILL.md`, `skills/war/references/sweep-exclusion.md`, `skills/war/references/resume-and-recovery.md`, `skills/war/assets/skill-doc-contracts.test.mjs`
- Plan slice: new `sweep-exclusion.md` carries the Lead duty: call the exported `extractFilesFromPlanFile` on the plan path of every ledger entry not this plan and not `landed`, union the result into `args.sweepExclude` (stored ledger `files` are the contention record, not this input), log "campaign contention set empty for <n> entries" on an empty union from a present ledger, absent ledger ⇒ one log line "no campaign contention set threaded", record `sweepExcludeCount` in the run manifest (null when absent) (PIN-8); `skills/war/SKILL.md` gains one trigger pointer in the ADR 0042 shape (`when launching under a campaign, read references/sweep-exclusion.md`) and stays under its hard line; `resume-and-recovery.md` gains the relaunch steps: thread the phase return's `carriedPhaseClose` into `args.seededPhaseClose`; override `absorbCharges` through args only at a sanctioned relaunch; plus the accepted residual: a Workflow that threw before returning has no phase return, so the Lead re-derives the queue from the seats' audit verdicts in the run transcripts and the relaunch log names the re-derivation. Pins for the pointer (`sweep-exclusion-pin`) and the relaunch steps (`lead-carry-pin`), End state 9.
- Done when: `node --test skills/war/assets/skill-doc-contracts.test.mjs`
- requiresTest: true
- requiresPackaging: false
- deps: []
- target repo: superproject

### Task 2: telemetry
- Files: `skills/war-review/SKILL.md`
- Plan slice: add rows for `sweepExcludeCount` (n/a when null, 0 when empty), the terminal-pass seat and outcome per phase (write the row header as `terminal-pass`), `absorbRounds` spent per task, and follow-ups filed per `DEMOTE_REASONS` prefix. End state 10's case-insensitive, hyphen-tolerant presence greps are this task's pin.
- Done when: None — prose-only telemetry rows, no war-review suite; End state 10 carries the pin
- requiresTest: false
- requiresPackaging: false
- deps: []
- target repo: superproject

## Phase 4 — Release

### Task 1: version bump
- Files: `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`, `README.md`, `CHANGELOG.md`
- Plan slice: bump all four slots in lock-step to the next free patch above the live base; replace the `## Status` blurb in place; add the CHANGELOG head entry naming the default flip, the barrier list, the exclusion set, the terminal pass, the held carry, the filing floor, `run.absorbRounds`, the `DEMOTE_REASONS` census, the ace-off routing, and the economy preset flip.
- Done when: `node --test skills/war/assets/version-slots.test.mjs`
- requiresTest: true
- requiresPackaging: true
- deps: []
- target repo: superproject

## Deferred validations (backstops)

- Field effect: rerun the study rubric on the next two plans executed after this release, separating seat-filed from engine-filed issues by the `DEMOTE_REASONS` line. Targets: follow-ups per task under 1.0, and filed follow-ups classified yes-trivial or yes-small under 25% (today 69%) · why deferred: needs two live runs · runner: operator, with the README rubric in `docs/studies/2026-09-03-followup-filing-study/README.md`, at the close of the second plan.

## Notes / conscious deviations

- Task 1.2 carries D1, D2, D4, D5, D12, D13, and D14's engine arm in one `workflow-template.js` diff under same-file law (D10). The offered three-phase split was not taken; `absorbRounds` decouples the ace ladder from the fix budget, so a long roster round on this diff no longer starves absorbs.
- Red-team round 1 withdrew the echo-back 1 rider "no re-extraction" (D6): stored ledger footprints are first-block-only until Task 1.4 lands and a sweep re-stamps them, so the Lead re-extracts at launch with the exported `extractFilesFromPlanFile`.
- Red-team round 2 moved the filing floor from the file-followups dispatch into the merge queue: at filing time every absorb vehicle has closed. The floor cannot run earlier than the merge dispatch because no git-derived file list exists before Refine. Accepted residual: a rerouted in-diff finding skips the task's own ace ladder and lands in the sweep, one lane weaker than the panel-re-audited ace and still in-band.
- The D4 floor sits before the sweep drains the queue so a rerouted finding reaches the sweep. Prior lesson `hygiene-arm-placed-after-existing-dirty-fail-loud-guard-is-unreachable-for-the-corruption-it-targets`: ordering decides reachability, and the `filing-floor` order fixture reaches the floor's own branch.

## Open decisions

None.
