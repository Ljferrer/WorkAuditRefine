# In-band absorb by default — squash every seat-found fixable bug inside the run

**Evidence consumed** — one row per linked artifact:
- `docs/studies/2026-09-03-followup-filing-study/` (README, `classified.json`, `closures.json`, `versions.json`) at `782af11` on `claude/issue-1476-analysis-784c44` (PR #1996) · read
- `skills/war/assets/workflow-template.js` at `e7a3505`: DISPOSITION RULE + WIDENINGS prompt blocks, the two ace-side reserve gates and the batch-ace gate, the three ace-side `fixRounds++` charge sites, the `fixRounds` seeds, the held-phase `phaseCloseQueue` drain, the sweep-raised demote arms, the ace-off demote arms, the fileless-absorb ternary arms, the `demote()` writer and `fileFollowUp`, the file-followups dispatch and consolidation, the `ace_diff_files` clause, the `Ace-Subset` trailer builds, the git-topology barrier prompt, the phase return sites · read
- `skills/war/references/disposition-eligibility.md`, `agents/war-auditor.md` (Latitude and disposition), `agents/war-refiner.md` (section list and in-card anchors), `skills/war/references/budget-raise-floor.md`, `skills/war/references/refiner-recovery.md`, ADR 0013 (rule 4, both amendments, the 2026-08-27 currency clause), ADR 0012 at `e7a3505` · read
- `skills/war-campaign/assets/campaign-ledger.mjs` (`extractFiles`, `collectBlock`, exports) at `e7a3505` · read
- `skills/war/assets/prompt-surface-budgets.test.mjs` budget rows; `skill-doc-contracts.test.mjs` `D37a`, `D41`, `D43`; `workflow-template.test.mjs` `DISPO` pin, the "survives byte-untouched" assert, `ace-group-path`, `MIRROR_REGISTRY`; `war-config.test.mjs` roundLimit fallback guard and `meta-guard(F07)` marker census at `e7a3505` · read
- issues #1492, #1431, #1547 · read
- #1547 `## Evidence artifacts`: target-repo epic phase reports (#321, #322) · unread — private target repo; duplicate-issue clusters in the target repo · unread — private target repo; the interview-review exchange under `~/Documents/WAR/1547-…` · unread — outside this checkout; the motivating run manifest `recseg-serving-uptime-2026-08-15.json` · unread — private target repo, not among this repo's manifests
- `.claude/war/runs/*.json` · read for the study denominator only
- `docs/red-team/2026-09-03-in-band-absorb-default.md` rounds 1 to 3 · read

## Context — the gap / problem

- 85% of the 391 `war-followup` issues were fixable in-band; 189 of them in five lines or fewer (verified: the study at `782af11`).
- The fixable share is flat at 82% to 92% across every plugin release band since 0.15. No release moved it (verified: the study, `versions.json`).
- The dominant why-not-absorbable is a scope argument ("the slice does not name this file"), not a mechanical barrier (verified: the study, class rows and agent notes).
- The #1431 mechanism-substitution class is 5% of filings; the #1547 batch-revert demotion class is under 1% (verified: the study).
- Lane-closed filings (fixable, but every WAR lane shut by rule) are 8% to 15% of filings from 0.17.11 on (verified: the study, `versions.json`).
- 52 absorb-disposed findings demoted because the phase-close sweep never ran (verified: the study, channel rows). The held-phase arm demotes the whole `phaseCloseQueue` with "held phase — the phase-close sweep never dispatched" (verified: `workflow-template.js` held-phase drain at `e7a3505`).
- A sweep-raised absorb demotes because the sweep is the phase's terminal fix round. Two demote arms carry the reason, on the merged-sweep path and the discard path, plus one census comment and one wrapped copy in the merged-arm routing comment (verified: `workflow-template.js` at `e7a3505`; prior lesson `terminal-phase-close-polish-absorb-finding-has-no-further-round-to-land-it`).
- Today `absorb` is never a default; an omitted Minor disposition becomes `follow-up` (verified: ADR 0013 rule 4, `agents/war-auditor.md` Disposition rule, dispatched DISPOSITION RULE at `e7a3505`). The old claim lives on eight living surfaces in four wordings, in three absorb-bearing sites of `workflow-template.js` (the AUDIT_VERDICT schema comment and the `dispositionOf` header comment, both single-quoted, and the dispatched sentence) and one `ask`-only survivor (the parked-ask identity comment), and in four test pins (`workflow-template.test.mjs` `DISPO` const and the "survives byte-untouched" assert; `skill-doc-contracts.test.mjs` `D37a` over CONTEXT.md and CLAUDE.md, and `D41` over `skills/war/SKILL.md`'s Audits bullet) plus two behavioral default tests and a third `dispositionOf` assert in `workflow-template.test.mjs` (verified: red-team rounds 2 and 3 at `e7a3505`).
- Two ace-side gates read `fixRounds >= roundLimit - 2`: the `aceBisect` subset-commit stop and the `aceReentry` reserve stop. The batch ace reads `fixRounds < roundLimit`, deliberately outside the reserve. Every ace-side commit charges the shared `fixRounds` counter at three `fixRounds++` sites (subset, re-entry, batch ace) that the merge-floor retry loop also draws from (verified: `workflow-template.js` at `e7a3505`). `fixRounds` is seeded from the in-process round index, never from git (verified: the two `fixRounds =` seeds). The re-entry `Ace-Subset` trailer's only distinctness across successive batches over one file set is the `fixRounds` index (verified: the re-entry trailer build and its comment). `roundLimit` is also read by the blocking fix loop, the segmented-land re-dispatch, and the CAS push loop (verified: same file).
- The retired reserve arithmetic `roundLimit − 2` is stated as live law on `skills/war/SKILL.md` (`--ace` bullet), `CONTEXT.md` (**Ace bisection** and **Re-entry** entries), `references/design.md` §18, `disposition-eligibility.md` widening 1, `schemas.md`'s re-entry row, seven `workflow-template.js` comments, and four emitted strings (two `log()` calls and the `demote()` and `routeToSweep()` reason literals they guard); `skill-doc-contracts.test.mjs` `D43` binds the first three; ADR 0013 states it at three sites including a dated 2026-08-27 currency clause (verified: red-team round 3 at `e7a3505`).
- The `Ace-Subset` trailer carries a round index only on the re-entry build; the bisect subset trailer carries task id plus files; the batch ace carries no trailer (verified: the two trailer builds at `e7a3505`).
- `run.ace: false` demotes a would-be absorb with "absorb requires --ace (off this run)" at two sites (verified: `workflow-template.js` at `e7a3505`). The economy preset pins `ace: false`; the fact is restated on `README.md` twice (the `--ace` row and the "today's defaults" paragraph), `skills/war/SKILL.md` (`--ace` bullet), `schemas.md` (`ace` comment), `skills/war-room/SKILL.md` (the preset bullet and the `run.ace` row), and `war-config.test.mjs` (the preset pin test) (verified: grep at `e7a3505`).
- The Workflow sandbox cannot read files or run git; the orchestrator's only file refusal today is a release-slot filename string check (`aceEligible`) (verified: `workflow-template.js` at `e7a3505`). The ace gate already receives a git-derived `ace_diff_files` list from a dispatched agent (verified: `ACE_DIFF_FILES_CLAUSE`). Today the first git-running steps after a worker returns are the pin-transfer probe and the merge dispatch, both after the ace ladder (verified: the serial merge queue); the plan adds a `diff-probe` refiner dispatch ahead of the seats (D4). The git-topology barrier prompt forbids free-authored git reads beyond its named commands (verified: the barrier prompt at `e7a3505`).
- The ace stage files a seat's `follow-up` row into `minorsFiled` wave-side through `fileFollowUp`, which also stamps `filedKeys` (verified: `aceStage` and `fileFollowUp` at `e7a3505`). The file-followups dispatch runs after the land decision; by then the sweep has spliced `phaseCloseQueue` empty. `minorsFiled` holds seat rows and engine `demote()` rows alike. Five paths reach a filed seat row without a merge result (audit-blocked, dep-failed, provision-blocked, unrunnable, and a held wave) (verified: `workflow-template.js` at `e7a3505`).
- Engine `follow-up` demotions: 23 `demote(..., 'follow-up', ...)` literal call sites plus two sites that reach `follow-up` through a severity ternary (the fileless-absorb arms); two literal sites build the reason head from a runtime variable (`provDrainCause`, `sweepDrainCause`) (verified: red-team round 3 at `e7a3505`).
- The handoff block emits on `landed` and `held:escalation` only. `KNOWN_LAND_DECISIONS` has seven members; `held:phase-incomplete` is never emitted by the Workflow, and `held:workflow-error` returns from a separate catch-block site (verified: `land-decision.mjs` and `workflow-template.js` at `e7a3505`).
- The sweep's polish task is a pseudo-task literal with no counter fields, reconstructed each run (verified: `polishTask` at `e7a3505`).
- `campaign-ledger.mjs` exports nine symbols; `extractFiles(lines)` takes plan lines and returns `[]` silently on a path string; `extractFilesFromPlanFile(planPath)` is module-private; `collectBlock` breaks at the first `- Files:` anchor, so a stored ledger `files` array holds the first block only (verified: `campaign-ledger.mjs` at `e7a3505`, `extractfiles-union` probe).
- `war-config.test.mjs` carries `meta-guard(F07)`: it scans every `Keep in sync|Mirror of|MIRROR of` marker comment in `workflow-template.js`, refuses any marker not registered in its `LOGIC_MIRROR_REGISTRY` or `DATA_MIRROR_ALLOWLIST`, and pins the marker count at exactly 5. It also carries the `drift-guard: roundLimit fallback` test that reads the template's `run.roundLimit ?? 6` fallback by regex and compares it with `DEFAULTS.run.roundLimit` (verified: `war-config.test.mjs` at `e7a3505`). `MIRROR_REGISTRY` in `workflow-template.test.mjs` has three modes, and its `behavioral` mode compares arbitrary thunks with `deepEqual`, so it can express a scalar (verified: red-team round 3).
- `war-config.mjs` hard-rejects `null` for `run.roundLimit`; `null`-as-unset is the `overrides.*` / `hooks.*` / `agents.snipe` convention; `deepMerge` copies an explicit `null` over the default (verified: `war-config.mjs` at `e7a3505`).
- Each `/war` phase is one Workflow run from a staged copy of the template (verified: `skills/war/SKILL.md` "one Workflow per phase" at `e7a3505`). Anything carried to the next phase rides the phase return and the Lead re-threads it as args.
- Byte headroom at `e7a3505` (D12 dated snapshot; re-measure at the task's rebased base): `agents/war-auditor.md` 27,915 B against 28,672 B (757 B free); its `### execution-evidence gate-audit checklist (reserved lens)` section is one contiguous 4,426 B block that four `workflow-template.test.mjs` tests read literals from (the `T2.1 both-surfaces` execution-evidence test reads four duties plus the heading; the two-owner End-state ownership assert and two more read inside the block) (verified: `awk` + grep at `164d26e`); `skills/war/SKILL.md` 72,712 B against 73,728 B (1,016 B free); `agents/war-refiner.md` 36,751 B against 36,864 B (113 B free, already past its advisory line). The refiner card's `## Gate-failure classification` section is one contiguous 3,126 B block; five occurrences of the `#gate-failure-classification` token exist: three in-card anchors, and two in `budget-raise-floor.md` (the line-44 inbound link and a line-3 provenance sentence about a prior eviction); `refiner-recovery.md` carries none and names the section in prose ("the card's § Gate-failure classification") (verified: `wc -c`, `prompt-surface-budgets.test.mjs` rows, red-team round 4).

## Pivotal constraints

- Standing card and dispatched prompt change in the same commit (the standing/dispatched split law).
- Engine and seat-prompt behavior land before any planning-surface change. No `war-strategy`, `red-team`, or `plan-interview` doctrine edit in this plan.
- Never delete or weaken a test via absorb. `plugin.json` and `marketplace.json` stay refused from ace, sweep, and terminal pass (`RELEASE_SLOT_FILES`, the two JSON slots only; README.md and CHANGELOG.md are not members). Critical/Major blocking is untouched.
- Every demotion, reroute, skip, and budget block is logged. Nothing is silent.
- Budgeted surfaces stay under their hard lines: cards keep one-sentence rules plus trigger pointers; doctrine bodies live in `references/` (ADR 0042). No `Budget-Raise:` trailer in this plan.
- `fixRounds` counts blocking fix rounds and floor retries only. No ace-side commit charges it. The merge-floor retry loop keeps its full `roundLimit` budget; the `roundLimit − 2` reserve arithmetic retires with the ace gates.
- `run.ace` gates the per-task ace ladder only. The phase-close sweep and the terminal pass run regardless.
- One mechanism family per engine phase. Two tasks in one phase never share a file; a shared file moves the work to a later phase.

## Resolved design tree

| # | Decision | Resolution | Source | Landing class |
|---|---|---|---|---|
| D1 | Default for an in-diff, fully specified Minor/Nit | `absorb`. `follow-up` is legal only with a barrier the seat states in a structured `barrier` field on the finding (AUDIT_VERDICT, optional enum), never as prose. The canonical enum `BARRIER_TOKENS` lives in `skills/war/assets/land-decision.mjs`: `barrier:release-slot` (release-slot file) · `barrier:underspecified` (fix not fully specified) · `barrier:rationale-comment` (the fix removes or edits a line carrying a `ponytail:` or deliberate-mirror rationale comment) · `barrier:trade-off` (behavior change with a nameable trade-off; meant for `ask`, see D4 for the no-`ask`-field case). Three follow-up barriers plus one ask-routing tag. A scope argument is not a barrier. The why-not-absorbable prose stays free text. Phase 3 changes the seat prompt and the card only (the seat reads the diff and applies the in-diff default itself); `dispositionOf`'s omitted-Minor engine default stays `follow-up` through Phase 3. Phase 4 gives the engine a git-derived diff before the seats convene (the `diff-probe` dispatch, D4) and `dispositionOf` then applies the default deterministically: an omitted-disposition Minor/Nit whose file is in `diff_files` defaults `absorb`, an out-of-diff one defaults `absorb` + `phaseClose:true`, both only when `suggested_fix` is non-empty; otherwise the old severity default. Amends ADR 0013 rule 4 for the in-diff fully-specified case only. | (user) | PIN-1→guardrail ‡ · PIN-2→guardrail |
| D2 | Out-of-diff findings and the sweep footprint | Seat rule: a fully specified Minor/Nit whose file is outside the task diff defaults to `absorb` + `phaseClose:true`. Engine rule at sweep time: one exclusion set = the files of every `args.sweepExclude` entry ∪ `Files:` of every task whose status is not `merged` ∪ `RELEASE_SLOT_FILES`, both sides `aceRelPath`-normalized. A queued absorb whose file is in the set demotes to `follow-up` with a reason naming the owner: the entry's plan slug, the task id, or the fixed string "the release slot". `RELEASE_SLOT_FILES` is canonical in `land-decision.mjs` (`.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`, nothing else), hand-mirrored in `workflow-template.js` with a registry row; `aceEligible` and the exclusion set match on the basenames derived from it through one helper, so the refusal keeps today's breadth in any directory (`sub/dir/plugin.json` refused, `plugin.json.bak` not); the `aceEligible (criterion 1)` byte pin re-keys to the mirror read. A release-slot absorb demotes where it is born with prefix `demote:release-slot` and owner "the release slot"; the `routeToSweep` release-slot arm and its comment retire. The sweep and terminal-pass prompts keep "never touch a version literal or the README `## Status` blurb"; `version-slots.test.mjs` stays the gate for a breach. The seat never decides exclusion. | (user) | PIN-3→slice (T4.1) |
| D3a | Terminal pass | Once per phase, on the merged-sweep path only: one ace-shaped commit at the post-polish integration tip applying the terminal-born absorbs (the polish panel's absorbs, today's sweep-raised arm, plus any queued absorb the sweep could not land), then one read-only re-audit seat (the roster's `correctness` seat when present, else the roster's first seat with its own lens; the log names the seat). "Read-only" describes the seat, not the pass. The terminal queue is filtered through `aceEligible` (which reads `RELEASE_SLOT_FILES`) before the commit; a slot-file absorb is refused and demoted with `demote:release-slot` and a log. The terminal commit goes through Refine (floors, gate, merge) like any ace commit, carries an `Ace-Charge` trailer, and charges `r.task.absorbRounds` on the polish task (seeded `absorbRounds: 0` at construction) for telemetry only; one pass per phase is its bound, it never blocks, and no integration-branch derivation exists for it (recorded residual). No sweep this phase ⇒ skip with a log line. Empty terminal queue ⇒ skip with a log line. Re-audit regression ⇒ forward-revert the terminal commit, then carry (non-final phase) or demote (final phase); never a second terminal pass. Phase finality comes from `args.finalPhase` (boolean, Lead-threaded from the plan's phase count at launch; absent reads as final, so a missed thread files an issue you can see instead of carrying into nothing; any non-boolean is refused at entry). A fresh absorb raised by the terminal seat: non-final phase ⇒ rides `carriedPhaseClose`, reason "carried from phase N terminal pass"; final phase ⇒ `follow-up`, reason "raised on the terminal pass". The merged-path sweep-raised demote arm and the wrapped copy of its reason in the routing comment are retired and the terminal pass is its successor. The discard-path demote arm stays, reworded to name the discard; on a non-final phase its absorbs ride `carriedPhaseClose` instead of demoting. Every branch logged. | (user) | PIN-4→guardrail · PIN-9→slice (T5.1) |
| D3b | Held-phase queue | `carriedPhaseClose` is a top-level key on the phase return for every `landDecision !== 'landed'`, independent of the handoff block, added at both return sites (the main return and the catch-block `held:workflow-error` return), and present as an empty array on `landed` so its absence is never ambiguous. A held phase writes its `phaseCloseQueue` there and demotes nothing. The Lead reads the phase return, not the handoff, and threads it into the relaunch as `args.seededPhaseClose`, which carries an entry-validation row and the #1413 own-token provenance treatment. Accepted residual: a Workflow that throws before returning has no phase return; the Lead re-derives the queue from the seats' audit verdicts in the run transcripts, and the relaunch log names the re-derivation (recorded in `resume-and-recovery.md`, Task 6.1). | (user) | PIN-5→slice (T5.1, T6.1) |
| D4 | Diff probe and the intake floor | Phase 4 adds one refiner `diff-probe` dispatch per task, after the worker returns green and before the audit seats convene: it runs `git diff --name-only <dispatchBase>..<tip>` in the task worktree and returns `diff_files` (own result schema, label `diff-probe:<task>`, idempotent on resume; probe error ⇒ `diff_files` absent, logged). The filing floor runs at intake, right after `dispositionOf`, before any row is filed: seat rows only (`demote()` stamps `engineFiled: true` and the floor skips stamped rows). In-diff or out-of-diff `follow-up` with no `barrier` ⇒ `absorb` (in-diff) or `absorb` + `phaseClose:true` (out-of-diff), logged; `barrier:trade-off` with the `ask` field ⇒ `ask`, without it ⇒ keep `follow-up`, log "trade-off without ask fields", never a schema throw; any other `barrier` ⇒ file as stated; a seat `note` whose `file` is in `diff_files` and whose `suggested_fix` is non-empty ⇒ `absorb`, log "note with a specified fix rerouted"; a `note` with no file, an out-of-diff file, or an empty `suggested_fix` stays a note. Rerouted in-diff rows join the task's own ace batch and its panel re-audit; rerouted out-of-diff rows ride `phaseClose:true` to the sweep. `diff_files` absent (probe failed) ⇒ `dispositionOf` keeps the old severity default, the floor skips that task with a log line, and its filed rows carry `demote:floor-skipped` on the issue-body prefix line. No merge-queue floor, no `MERGE_RESULT.diff_files`, no post-filing removal arm. Deterministic only: `diff_files` and `BARRIER_TOKENS` membership; the engine never estimates fix size; worker `files_changed` is never the source. | (user; red-team round 5 revision) | PIN-6→guardrail |
| D5 | Absorb budget | Knob `run.absorbRounds`: integer ≥ 1, default 6, validated in `war-config.mjs` exactly like `run.roundLimit` (null and non-integers rejected with the same message shape; no coercion, no `fillDefaults` special case), exposed in `/war-room`. The Q2 rider "null reads as unset" is withdrawn: the binding convention is the `run.*` namespace, where every integer knob rejects null. Counter `r.task.absorbRounds`, per task, charged by batch ace, re-entry batch, bisection subset commit, and the terminal pass; none of those charges `fixRounds` any more; reverts, re-audit panels, and fix rounds never charge it. All three ace gates (the `aceBisect` subset stop, the `aceReentry` stop, and the batch ace's `fixRounds < roundLimit`) read `absorbRounds < run.absorbRounds`. Every ace-side commit carries an `Ace-Charge: <task>:<n>` trailer, n = the `absorbRounds` value after the charge; reverts carry no charge trailer. The re-entry `Ace-Subset` trailer's round segment re-anchors on `absorbRounds` so successive re-entry batches over one file set stay distinct; the bisect subset trailer is unchanged. Ace dispatch labels carry `absorbRounds`. Relaunch source: the phase-start git-topology barrier dispatch (refiner, runs git) returns `absorbCharges: { <task>: n }`, the HIGHEST `Ace-Charge` index on the task branch, so a cherry-pick or duplicate trailer never double-charges; a reverted ace commit's trailer still counts; the barrier prompt gains an explicit allowance for that one `git log` trailer read beside its named commands. Barrier `error` or a missing field ⇒ seed 0 with a loud log line naming the task; the Lead may override through args at a sanctioned relaunch. Exhaustion routes the finding to the sweep with a log line naming the counter, at all three gates. Today only `aceReentry` routes through `routeToSweep()`; the batch ace's shared exhaustion arm demotes every aceable row with "ace unavailable (open blocking findings or exhausted fix budget)". The plan splits that arm: spent `absorbRounds` with no open blockers ⇒ aceable rows go through `routeToSweep()` with `phaseClose:true`; open Critical/Major blockers ⇒ aceable rows are held on the task (`r.pendingAbsorbs`, deduped by content key) and join the aceable set at the next approve so they ride that round's ace batch; a task that ends escalated, audit-blocked, or never merged demotes its held rows with `demote:absorb-blocked`. The `aceBisect` reserve stop's still-queued (never re-audited) subsets also go through `routeToSweep()` with `phaseClose:true`, no `follow-up`, no prefix, the rows stay absorbs; a subset that already failed its own re-audit keeps its demote with prefix `demote:absorb-regressed`. | (user) | PIN-7→slice (T1.1, T2.1) |
| D6 | Contention source | The Lead builds `args.sweepExclude` at launch as slug-attributed entries `[{ slug, files[] }]`, one per ledger entry that is not this plan and whose status is not `landed`, by calling the exported `extractFilesFromPlanFile` on that entry's plan path; the engine flattens the union and keeps the owning slug for the demotion reason. Stored ledger `files` are not trusted for this (first-block-only until Task 1.2 lands and a sweep re-stamps them); they stay the ledger's contention record. An empty union from a present ledger logs "campaign contention set empty for <n> entries" so it is never mistaken for an absent ledger. Absent ledger ⇒ no list, one log line "no campaign contention set threaded"; the in-phase and release-slot arms still run. The run manifest records `sweepExcludeCount` (null when no list was threaded). | (user) | PIN-8→slice (T1.2, T6.1) |
| D7 | Prompt surfaces | `agents/war-auditor.md`, the dispatched DISPOSITION RULE in `workflow-template.js`, and `disposition-eligibility.md` change in one commit. `agents/war-refiner.md` and the dispatched refiner prompts change in one commit. | (user) | PIN-12→guardrail ‡ |
| D8 | Ordering | Engine and seat prompts first. No planning-surface edits in this plan. | (user) | PIN-10→non-goal |
| D9 | Test law | Never delete or weaken tests via absorb. | (user) | PIN-11→guardrail ‡ |
| D10 | Plan shape | Seven phases, one mechanism family per engine phase: config and footprint → absorb budget → barrier and default flip → filing floor, demote census, ace-off routing → terminal pass and held carry → Lead surfaces → release. `workflow-template.js` is touched by exactly one task per phase. | (user; red-team rounds 1 to 3) | slice (all) |
| D11 | Ledger footprint | `extractFiles` unions every `- Files:` block in a plan, deduped, in order of first appearance; the backtick-absence fallback is evaluated per block before the union. The fail-loud empty-footprint throw fires only when the union is empty, never on one empty block. `extractFilesFromPlanFile` becomes an export. | (user; red-team rounds 1 and 2) | PIN-13→slice (T1.2) |
| D12 | Refiner card bytes | Task 2.1 moves `## Gate-failure classification` from `agents/war-refiner.md` to `skills/war/references/gate-failure-classification.md`, byte-identical (ADR 0042). The card keeps the fixed pointer "when the gate is red, read [gate-failure-classification.md](${CLAUDE_PLUGIN_ROOT}/skills/war/references/gate-failure-classification.md)". The three in-card `#gate-failure-classification` anchors and the `budget-raise-floor.md` line-44 link are re-keyed to the references path, the `budget-raise-floor.md` line-3 provenance sentence is reworded to record the new home without the retired token, and the `refiner-recovery.md` prose reference is reworded to name the references file, all in the same commit. The `absorbCharges` clause (Phase 2) and the `diff-probe` dispatch clause (Phase 4) land in the freed bytes. Hard line stays 36,864 B. | (user; red-team rounds 2 and 3) | PIN-14→slice (T2.1) |
| D13 | Lawful engine filers | The Purpose names the class "logged engine demotion" and enumerates nothing in prose. `DEMOTE_REASONS` is a closed prefix enum, canonical in `land-decision.mjs`, hand-mirrored in `workflow-template.js` with a mirror-registry row and an `F07` registration. Enforcement is runtime: `demote()` validates every `follow-up` reason against `DEMOTE_REASONS`; on a miss it prepends `demote:unclassified` (itself an enum member) and logs the site loudly, never a throw, so the issue body's prefix line is never blank. The two variable-head sites (`provDrainCause`, `sweepDrainCause`) gain a literal prefix ahead of the variable. The census asserts the runtime boundary, a call-site count whose matcher includes the severity-ternary shape (pinned as a dated snapshot at land), and one unwired negative reference. `/war-review` tallies `demote:unclassified` as a defect signal. Every engine-filed issue body carries its `DEMOTE_REASONS` prefix on a fixed line. The `--afk` no-match ask lane is Lead-side at the Checkpoint (the `demote()` header comment and `references/design.md` both say so): the Checkpoint prose in `skills/war/SKILL.md` (Task 6.1) files the unmatched ask as a `follow-up` with `demote:ask-unruled-afk` (an enum member) on the body prefix line, question preserved; `demote()`'s ask refusal is untouched; behavior is unchanged (unmatched ask ⇒ `follow-up` with the question preserved; a blocking decision gap ⇒ escalate). The Checkpoint ruled-ask lane stays Lead-side with no prefix. | (user; red-team rounds 2 and 3) | PIN-15→guardrail |
| D15 | Gate-audit-family findings | The gate-audit family is per-task post-merge (`execution-evidence`), integrated-tip, and end-state-only. All three run after the merge queue and before the sweep; no seat moves. Their fully specified Minor/Nit findings route `absorb` + `phaseClose:true` into `phaseCloseQueue`, stamped with the seat label, and drain in the sweep under the exclusion set; `follow-up` from those seats needs a `barrier` tag like any seat. The three gate-audit prompts receive the shared DISPOSITION RULE clause and the `barrier` field; the card's `execution-evidence` checklist gains one sentence naming the routing (bytes per D16, the checklist eviction). The #1692 `auditLog`-only sink retires for Minor/Nits: `auditLog` keeps the record and stops being the only destination. `ask` still parks. | (user; red-team rounds 3 and 4) | PIN-17→slice (T4.1) |
| D16 | Auditor card bytes | Phase 3 Task 1 moves `### execution-evidence gate-audit checklist (reserved lens)` (4,426 B, one contiguous block) byte-identical from `agents/war-auditor.md` to `skills/war/references/gate-audit-checklist.md` (ADR 0042). The card keeps the heading and the fixed pointer "when you sit as the `execution-evidence` seat, read [gate-audit-checklist.md](${CLAUDE_PLUGIN_ROOT}/skills/war/references/gate-audit-checklist.md)". The same task re-targets the `T2.1 both-surfaces` test in `workflow-template.test.mjs`: the heading and pointer asserts read the card, the four duty asserts read the references file, the stale-sentence absence assert stays on the card. An extract-and-equality row pins the evicted body verbatim. Phase 4's gate-audit routing sentence and the `note` clause land beside the pointer. The dispatched `execution-evidence` prompt is unchanged. No `Budget-Raise:` trailer. | (user; red-team round 4) | PIN-18→slice (T3.1) |
| D14 | Ace-off runs and the economy preset | `run.ace: false` disables the per-task ace ladder (batch ace, re-entry, bisection) only; the sweep and the terminal pass run regardless. With ace off, a defaulted `absorb` (in-diff or out-of-diff, seat-set or floor-rerouted) routes `phaseClose:true` to the sweep and never demotes on the ace-off path; the two "absorb requires --ace" demote arms retire. The terminal pass charges `absorbRounds` for telemetry; one pass per phase is its bound. The economy preset drops its `ace: false` override and inherits `DEFAULTS.run.ace` (true), with the same "no longer pinned, inherits" comment the preset uses for `memory.commitLearnings`; `roundLimit: 2` stays; `absorbRounds` inherits 6. Every preset resolves `run.ace === true` (default-deny census over the preset names). A user config may still set `run.ace: false`. Rule 6 sweep over the seven surfaces that state the old preset fact. | (user; red-team rounds 2 and 3) | PIN-16→guardrail · slice (T1.1, T1.3, T4.1) |

## Assumptions ledger

| ID | Assumption | Basis | Blast radius if wrong | Check |
|---|---|---|---|---|
| A1 | Sandbox args carry each task's `Files:` and status | (user) | D2's in-phase arm has no input | `sweep-exclude` fixture: in-phase hit demotes naming the task |
| A5 | The terminal pass skips with a log when no terminal-born absorb exists | [assumed: an empty ace dispatch is waste — if wrong: one no-op dispatch per phase] | one dispatch per phase | `terminal-pass` fixture: empty queue logs skip |
| A7 | A refiner `diff-probe` dispatch per task fits between the worker's green return and the seat convene without disturbing the wave loop's resume idempotency | [assumed: the provision and pin-transfer dispatches already sit on that seam — if wrong: the probe rides the pin-transfer dispatch's result] | one extra dispatch per task, or `diff_files` absent and the old default | `filing-floor` fixture: probe absent ⇒ old default + skip log |
| A8 | The 4,426 B `execution-evidence` checklist eviction (D16) funds every auditor-card edit in Phases 3 and 4 (the D1 sentence rewrite, the `barrier` clause, the gate-audit routing sentence, the `note` clause); the 3,126 B `## Gate-failure classification` eviction funds both refiner clauses | [assumed: the four card edits total under 4 KB — if wrong: evict the `Stale-looking-but-correct calibration` subsection next] | budget test red at land | End states 6, 7, and 17 |
| A10 | `skills/war/SKILL.md` (1,016 B free) absorbs five replace-in-place edits across Phases 1 to 6 with no net growth | [assumed: each edit is a sentence swap — if wrong: evict the `--ace` bullet's Residual rule to `references/` under ADR 0042] | budget test red at the offending task's land | End state 17 |
| A9 | The phase-start git-topology barrier dispatch can return one more per-task field without a new dispatch kind, once its prompt allows the trailer read | [assumed: it already returns `preMerged`/`staleRemote`/`worktreeHygiene` per task — if wrong: a dedicated preflight dispatch in Task 2.1] | `absorbRounds` seeds 0 on every relaunch, logged | `absorb-budget` fixture: three charges ⇒ resumes at 3 |

## Non-goals / deferred

- Study change 5: a plan that byte-freezes a surface must name its repair lane. Deferred.
- Any widening of Critical/Major blocking.
- Planning-surface doctrine edits (`war-strategy`, `red-team`, `plan-interview`).
- Operator-found bugs. The Purpose covers seat-found findings only.
- An integration-branch `absorbCharges` derivation for the polish task. The terminal pass runs once per phase and its charge is telemetry only (D3a residual).
- The Checkpoint ruled-ask filing lane. It stays Lead-side with no prefix; the study backstop reports Lead-filed asks in their own column, the `--afk` no-match lane among them tagged `demote:ask-unruled-afk` (D13, Task 6.1).

## New domain terms · Recommended ADRs

Terms for `CONTEXT.md`: **absorb budget** (`run.absorbRounds`, the per-task ace meter separate from `fixRounds`) and **exclusion set** (the sweep's union of foreign-owned files and release-slot files) in Phase 2 · **barrier list** (`BARRIER_TOKENS`, the closed enum a seat cites in the `barrier` field to file a `follow-up` or route an `ask`) in Phase 3 · **demote reason prefix** (`DEMOTE_REASONS`, the closed enum every engine `follow-up` demotion cites) in Phase 4 · **terminal pass** (the one-hop ace commit plus one seat after the polish merge) and **carried queue** (a held or terminal-born absorb queue that rides the phase return into the next run) in Phase 5.

ADRs: ADR 0013 gains a Phase 2 amendment (the `roundLimit − 2` reserve is superseded by `absorbRounds < run.absorbRounds`; the 2026-08-27 currency clause is historical), a Phase 3 amendment (in-diff default flip and the barrier list), and a Phase 4 amendment (ace-off routing and the demote prefix). ADR 0012 gains one line naming the terminal pass as the successor of the merged-sweep demote arm (Phase 5, after the pass exists). ADR bodies stay byte-untouched; ADR 0013 is exempt from every OLD-absent row.

## Commander's Intent

- **Purpose:** every fixable bug found by a seat in a run is fixed in that run. Only a seat-cited barrier tag, a logged engine demotion, or a logged floor-skip on a task whose diff probe failed may file an issue from the engine.
- **Method:** in order: give absorbs their own round budget with a git-derived relaunch seed and flip the economy preset to ace on; flip the in-diff default to `absorb` behind a structured barrier enum; give the engine a per-task diff probe at intake and back the seat rule with a deterministic floor there, a demote-reason census, and ace-off routing to the sweep; add a one-hop terminal pass after the polish merge and carry a held phase's queue into the relaunch; then the Lead prose and the release.
- **Mechanism latitude:** counter placement and log wording · exclusion-set construction order · fixture shapes and titles beyond the pinned title tokens · the carried-queue record's field names · the reroute log format · where the `sweep-exclusion.md` and `gate-failure-classification.md` bodies sit in `references/` · the `absorbCharges` field's position in the barrier result · the exact `DEMOTE_REASONS` member spellings. Substituting any of these mechanisms while the End states and binding guardrails hold is not a plan deviation and warrants no issue.
- **Binding guardrails:**
  - the barrier list is exactly `BARRIER_TOKENS`' four members, cited in the structured `barrier` field, three of them follow-up barriers and `barrier:trade-off` an ask route; a scope argument is never a barrier (PIN-1, PIN-2);
  - the terminal pass is one hop per phase, never a second pass, filtered through `aceEligible`, and a regressed terminal commit is forward-reverted then carried or demoted; the merged-path sweep-raised demote arm is retired, the discard-path arm stays (PIN-4);
  - the diff probe runs before the seats convene and the filing floor runs at intake, reads seat rows only, and is deterministic: git-derived `diff_files` and `barrier` enum membership, never prose matching or a size estimate (PIN-6);
  - `fixRounds` counts blocking fix rounds and floor retries only; no ace-side commit charges it; the `roundLimit − 2` reserve arithmetic retires (PIN-7);
  - `plugin.json` and `marketplace.json` stay refused from ace, sweep, and terminal pass, per `RELEASE_SLOT_FILES`; never delete or weaken a test (PIN-11);
  - standing card, dispatched prompt, and eligibility doc change in one commit; refiner card and dispatched refiner prompts change in one commit (PIN-12);
  - every demotion, reroute, skip, and budget block is logged;
  - no planning-surface edit (PIN-10);
  - budgeted surfaces stay under their hard lines with no `Budget-Raise:` trailer; ADR 0042 evictions are byte-identical moves with every inbound reference re-keyed (PIN-14);
  - every engine `follow-up` demotion carries a `DEMOTE_REASONS` prefix (PIN-15);
  - `run.ace` gates the per-task ace ladder only; every shipped preset resolves `run.ace === true` (PIN-16);
  - gate-audit-family Minor/Nits route like any seat's: `absorb` + `phaseClose:true` to the sweep or the terminal pass, `follow-up` only with a `barrier` tag (PIN-17);
  - one task touches `workflow-template.js` per phase (D10).
- **End state:**
  1. Knob and preset: `war-config.mjs` rejects `run.absorbRounds: null` and `0` with `run.roundLimit`'s message shape, resolves an absent key to 6 and `3` to 3, the `/war-room` row's stated default equals `DEFAULTS.run.absorbRounds` by extraction and equality, and every preset resolves `run.ace === true`; fixtures titled `absorb-budget` and `preset-ace-on` · check: `test "$(grep -c 'absorb-budget' skills/war/assets/war-config.test.mjs)" -ge 5 && test "$(grep -c 'preset-ace-on' skills/war/assets/war-config.test.mjs)" -ge 1 && node --test skills/war/assets/war-config.test.mjs` exits 0.
  2. Ledger footprint: `extractFiles` unions every `- Files:` block with per-block fallback, and `extractFilesFromPlanFile` is exported; fixtures titled `files-union` · check: `test "$(grep -c 'files-union' skills/war-campaign/assets/campaign-ledger.test.mjs)" -ge 4 && node --test skills/war-campaign/assets/campaign-ledger.test.mjs` exits 0.
  3. Economy flip docs: the old "economy pins ace off" fact is absent from `README.md` (both statements), `skills/war/SKILL.md`, `skills/war/references/schemas.md`, `skills/war-room/SKILL.md`, and `war-config.test.mjs`, with the row line-scoped (a single physical line holding `economy`, an `ace` token, and off/false), case-folded, and each surface carries the new fact; rows titled `economy-ace-flip` · check: `test "$(grep -c 'economy-ace-flip' skills/war/assets/skill-doc-contracts.test.mjs)" -ge 2 && node --test skills/war/assets/skill-doc-contracts.test.mjs` exits 0.
  4. Absorb budget: a task with `fixRounds` at `roundLimit` and `absorbRounds` free still runs an ace batch; a task with `absorbRounds` spent routes to the sweep with a log naming the counter; an ace commit leaves `fixRounds` unchanged; a budget-spent bisect ladder leaves its untested subsets in the sweep queue and none in `minorsFiled`, and a re-audit-failed subset still demotes; a batch ace with a spent budget and no blockers leaves its aceable rows in the sweep queue; a blocked task then approved carries its held row into the next ace batch and not into `minorsFiled`; a blocked task then escalated demotes its held rows with `demote:absorb-blocked`; two successive re-entry batches over one file set emit distinct `Ace-Subset` values; three `Ace-Charge` trailers on the branch ⇒ resumes at 3; charge then revert ⇒ resumes at 1; barrier error ⇒ 0 plus the log; the trailer grammar `Ace-Charge: <id>:<int>` is pinned; ace labels carry `absorbRounds`; the template's `run.absorbRounds ?? 6` fallback equals `DEFAULTS.run.absorbRounds`; fixtures titled `absorb-budget` · check: `test "$(grep -c 'absorb-budget' skills/war/assets/workflow-template.test.mjs)" -ge 14 && node --test skills/war/assets/workflow-template.test.mjs skills/war/assets/war-config.test.mjs` exits 0.
  5. Reserve arithmetic retired: the `roundLimit − 2` shape is absent from `skills/war/SKILL.md`, `CONTEXT.md`, `references/design.md`, `disposition-eligibility.md`, `schemas.md`, and `skills/war/assets/workflow-template.js` (comments, log strings, and reason literals alike), `D43` is re-keyed, and ADR 0013 carries the supersession amendment; row titled `reserve-retired` · check: `test "$(grep -c 'reserve-retired' skills/war/assets/skill-doc-contracts.test.mjs)" -ge 1 && ! grep -qiE 'roundLimit *(−|-) *2' skills/war/assets/workflow-template.js && node --test skills/war/assets/skill-doc-contracts.test.mjs` exits 0.
  6. Refiner eviction: every budgeted prompt surface stays under its hard line with no `Budget-Raise:` trailer; the evicted `## Gate-failure classification` body is present verbatim in `skills/war/references/gate-failure-classification.md`; the card's trigger pointer is pinned; no `#gate-failure-classification` token survives in the card, `budget-raise-floor.md`, or `refiner-recovery.md`; rows titled `refiner-evict` · check: `test "$(grep -c 'refiner-evict' skills/war/assets/skill-doc-contracts.test.mjs)" -ge 3 && ! grep -rqi '#gate-failure-classification' agents/war-refiner.md skills/war/references/budget-raise-floor.md skills/war/references/refiner-recovery.md && node --test skills/war/assets/prompt-surface-budgets.test.mjs skills/war/assets/skill-doc-contracts.test.mjs` exits 0.
  7. Barrier list and card eviction: the dispatched DISPOSITION RULE byte-mirrors the card, carries the in-diff `absorb` default sentence and the `barrier` field clause, and renders the four `BARRIER_TOKENS` from the mirror; `dispositionOf`'s engine default is unchanged in this phase; a registry row binds the inline mirror, the card sentence, and the eligibility-doc list to the canonical export in `land-decision.mjs`; the `F07` marker census admits the new marker; the evicted `execution-evidence` checklist is present verbatim in `gate-audit-checklist.md`, the card keeps the heading and the trigger pointer, and the re-targeted `T2.1 both-surfaces` test is green; fixtures titled `barrier-list` and `auditor-evict` · check: `test "$(grep -c 'barrier-list' skills/war/assets/workflow-template.test.mjs)" -ge 3 && test "$(grep -c 'auditor-evict' skills/war/assets/workflow-template.test.mjs)" -ge 2 && node --test skills/war/assets/workflow-template.test.mjs skills/war/assets/land-decision.test.mjs skills/war/assets/war-config.test.mjs` exits 0.
  8. Old default absent: the retired conjunction shapes (`absorb` and `ask` are never default*, `absorb` and `ask` never defaulted, and `absorb` is never a default with a zero-token gap) are absent from the eight living surfaces after normalization that strips backticks and single and double quotes, collapses whitespace, and case-folds; the lawful rewrite "defaults to absorb; ask is never a default" passes; `D37a` and `D41` are re-keyed; ADR 0013 carries the amendment section; row titled `old-default-absent` · check: `test "$(grep -c 'old-default-absent' skills/war/assets/skill-doc-contracts.test.mjs)" -ge 2 && node --test skills/war/assets/skill-doc-contracts.test.mjs` exits 0.
  9. Exclusion set: a campaign hit demotes naming the plan slug, an in-phase hit demotes naming the task id, an absent list reaches the sweep, a `./`-form path still matches, a `plugin.json` absorb and a `sub/dir/plugin.json` absorb demote at birth with `demote:release-slot` while `plugin.json.bak` is not refused, a README absorb outside `## Status` reaches the sweep, the retired `routeToSweep` release-slot arm is absent, and the `RELEASE_SLOT_FILES` mirror equals the canonical export; fixtures titled `sweep-exclude` · check: `test "$(grep -c 'sweep-exclude' skills/war/assets/workflow-template.test.mjs)" -ge 10 && ! grep -qi 'release-slot absorb born at a re-audit' skills/war/assets/workflow-template.js && node --test skills/war/assets/workflow-template.test.mjs` exits 0.
  10. Diff probe and intake floor: the `diff-probe` dispatch runs per task before the seats convene and its `diff_files` reaches `dispositionOf`; an omitted-disposition in-diff Minor with a `suggested_fix` defaults `absorb`; an omitted out-of-diff one defaults `absorb` + `phaseClose:true`; an omitted Minor with no `suggested_fix` keeps the old default; a barrierless in-diff `follow-up` reroutes to `absorb` and is present in the task's ace batch, not the sweep; a barrierless out-of-diff `follow-up` reroutes to `absorb` + `phaseClose:true`; an engine demotion with no `barrier` is never rerouted; a `follow-up` with `barrier:release-slot` files as stated; `barrier:trade-off` with the `ask` field reroutes to `ask`; `barrier:trade-off` without it stays `follow-up` with the "trade-off without ask fields" log; an in-diff `note` with `suggested_fix` reroutes to `absorb` and one without stays; a failed probe leaves `diff_files` absent, keeps the old default, logs the skip, and stamps `demote:floor-skipped` on that task's filed rows; the probe is idempotent on resume; fixtures titled `filing-floor` · check: `test "$(grep -c 'filing-floor' skills/war/assets/workflow-template.test.mjs)" -ge 14 && node --test skills/war/assets/workflow-template.test.mjs` exits 0.
  11. Demote census: `demote()` validates a `DEMOTE_REASONS` prefix on every `follow-up` reason, and an unprefixed reason emits `demote:unclassified` plus the log line and the issue-body line carries it; the census counts every `demote` call whose disposition argument can evaluate to `follow-up` (literal and severity-ternary shapes), pins the count, and carries one unwired negative reference; the mirror-registry row and the `F07` registration bind the inline enum to `land-decision.mjs`; fixtures titled `demote-census` · check: `test "$(grep -c 'demote-census' skills/war/assets/workflow-template.test.mjs)" -ge 4 && node --test skills/war/assets/workflow-template.test.mjs skills/war/assets/land-decision.test.mjs skills/war/assets/war-config.test.mjs` exits 0.
  12. Ace-off routing: with ace off a seat absorb reaches the sweep and a floor reroute reaches the sweep; an `ace-off-route` fixture reads `workflow-template.js` with the same normalized whole-file scan as the terminal-pass fixture and asserts "absorb requires --ace" is absent; the retired "every absorb demotes" Residual-rule wording is absent from `skills/war/SKILL.md`; fixtures titled `ace-off-route` in both suites · check: `test "$(grep -c 'ace-off-route' skills/war/assets/workflow-template.test.mjs)" -ge 3 && test "$(grep -c 'ace-off-route' skills/war/assets/skill-doc-contracts.test.mjs)" -ge 1 && ! grep -qi 'absorb requires --ace' skills/war/assets/workflow-template.js && node --test skills/war/assets/workflow-template.test.mjs skills/war/assets/skill-doc-contracts.test.mjs` exits 0.
  13. Gate-audit routing: each of the three gate-audit seats' (`execution-evidence`, integrated-tip, end-state) fully specified Minor reaches the sweep queue stamped with its seat label; an exclusion-set hit from one of them demotes naming the owner; a gate-audit `ask` still parks; the retired `auditLog`-only sink is absent; fixtures titled `gate-audit-route` · check: `test "$(grep -c 'gate-audit-route' skills/war/assets/workflow-template.test.mjs)" -ge 5 && node --test skills/war/assets/workflow-template.test.mjs` exits 0.
  14. Terminal pass: a roster without `correctness` still convenes the pass; the pass produces exactly one terminal commit with an `Ace-Charge` trailer; a polish task that never carried a counter still dispatches the pass; the pass convenes with `run.ace` off; a terminal-born absorb on a release-slot file is refused and logged, never committed; `finalPhase:false` emits exactly one `carriedPhaseClose` entry; `finalPhase:true` files exactly one demotion with reason "raised on the terminal pass"; an absent `finalPhase` demotes the same way; a non-boolean `finalPhase` is refused at entry; a re-audit regression forward-reverts the terminal commit and carries or demotes; a discarded sweep on a non-final phase carries its absorbs; no sweep or an empty terminal queue logs a skip; the polish task's counter reads 1 after the pass; a `terminal-pass` fixture reads `workflow-template.js`, strips line-comment prefixes, joins lines, collapses whitespace, case-folds, and asserts the normalized text contains neither "absorb has no later round" nor "sweep-raised absorb at either terminal sweep arm" (the wrapped comment copies included), while the discard-path reason keeps its `sweep-raised` token; fixtures titled `terminal-pass` · check: `test "$(grep -c 'terminal-pass' skills/war/assets/workflow-template.test.mjs)" -ge 14 && ! grep -qi 'absorb has no later' skills/war/assets/workflow-template.js && node --test skills/war/assets/workflow-template.test.mjs` exits 0.
  15. Held carry: a `held:nothing-merged` phase emits `carriedPhaseClose` on the phase return and zero demotions with reason "held phase — the phase-close sweep never dispatched"; a default-deny census asserts the key is present on every Workflow-emitted hold class (`held:escalation`, `held:nothing-merged`, `held:land-failed`, `held:submodule-pr`, `held:workflow-error` from the catch-block return) and is an empty array on `landed`; a relaunch with `args.seededPhaseClose` drains the seeded entries into the sweep; fixtures titled `held-carry` · check: `test "$(grep -c 'held-carry' skills/war/assets/workflow-template.test.mjs)" -ge 3 && node --test skills/war/assets/workflow-template.test.mjs` exits 0.
  16. Lead relaunch threading: `resume-and-recovery.md` carries the step that threads the phase return's `carriedPhaseClose` into `args.seededPhaseClose`, the `absorbCharges` override note, and the transcript re-derivation residual; `skills/war/SKILL.md` carries the `sweep-exclusion.md` trigger pointer and the Checkpoint `--afk` no-match sentence with `demote:ask-unruled-afk`; rows titled `lead-carry-pin`, `sweep-exclusion-pin`, and `afk-ask-prefix-pin` · check: `test "$(grep -c 'lead-carry-pin' skills/war/assets/skill-doc-contracts.test.mjs)" -ge 1 && test "$(grep -c 'sweep-exclusion-pin' skills/war/assets/skill-doc-contracts.test.mjs)" -ge 1 && test "$(grep -c 'afk-ask-prefix-pin' skills/war/assets/skill-doc-contracts.test.mjs)" -ge 1 && node --test skills/war/assets/skill-doc-contracts.test.mjs` exits 0.
  17. Every budgeted prompt surface stays under its hard line at the final landed tip · gate: `node --test skills/war/assets/prompt-surface-budgets.test.mjs`.
  18. Telemetry rows present: `skills/war-review/SKILL.md` names `sweepExcludeCount`, the terminal-pass seat row, `absorbRounds`, and a follow-ups-per-`DEMOTE_REASONS`-prefix tally · check: `test "$(grep -ci 'sweepExcludeCount' skills/war-review/SKILL.md)" -ge 1 && test "$(grep -ciE 'terminal[ -]pass' skills/war-review/SKILL.md)" -ge 1 && test "$(grep -ci 'absorbRounds' skills/war-review/SKILL.md)" -ge 1 && test "$(grep -ci 'DEMOTE_REASONS' skills/war-review/SKILL.md)" -ge 1` exits 0.
  19. Field effect · backstop: row 1 in `## Deferred validations (backstops)`.

## Build order (for /war)

Phase 1 (config knob, economy flip, ledger footprint, flip doc sweep) → Phase 2 (absorb budget engine, refiner eviction, reserve retirement docs) → Phase 3 (barrier enum, default flip, doc sweep) → Phase 4 (filing floor, exclusion set, demote census, ace-off routing) → Phase 5 (terminal pass, held carry) → Phase 6 (Lead surfaces, telemetry) → Phase 7 (release).

## Phase 1 — Config knob, economy flip, ledger footprint

### Task 1: absorbRounds knob and the economy ace flip
- Files: `skills/war/assets/war-config.mjs`, `skills/war/assets/war-config.test.mjs`, `skills/war-room/SKILL.md`
- Plan slice: add `run.absorbRounds` to `DEFAULTS.run` (6); presets inherit it; validate exactly like `run.roundLimit` (integer ≥ 1; null and non-integers rejected with the same message shape, pinned against `run.roundLimit`'s so the two cannot drift); the economy preset drops `ace: false` and inherits `DEFAULTS.run.ace` with the same "no longer pinned, inherits" comment it uses for `memory.commitLearnings`, keeping `roundLimit: 2` (D14, PIN-16); re-pin the preset test to the new effective config and add a default-deny census that every preset name resolves `run.ace === true`; add the `/war-room` override row for `run.absorbRounds` beside `run.roundLimit`, reading "integer ≥ 1 (default 6)", and guard the restated default by extending the existing war-room extraction-and-equality test in `war-config.test.mjs` to slice that row and assert the stated default equals `DEFAULTS.run.absorbRounds` (touched-doc accuracy: guard) (PIN-7); reword the `run.ace` row to "per-task ace ladder" with no preset clause, and the economy preset bullet to "single seat, roundLimit 2, ace on". Fixtures titled `absorb-budget` (null rejected, 0 rejected, absent resolves to 6, 3 resolves to 3, message shape pinned against `run.roundLimit`, the war-room row's default equals `DEFAULTS.run.absorbRounds` and a changed doc number goes red) and `preset-ace-on`.
- Done when: `node --test skills/war/assets/war-config.test.mjs`
- requiresTest: true
- requiresPackaging: false
- deps: []
- target repo: superproject

### Task 2: ledger footprint union
- Files: `skills/war-campaign/assets/campaign-ledger.mjs`, `skills/war-campaign/assets/campaign-ledger.test.mjs`
- Plan slice: `extractFiles` unions every `- Files:` block in the plan, deduped, in order of first appearance, with the backtick-absence fallback evaluated per block before the union; the fail-loud empty-footprint throw fires only when the union is empty, never on one empty block; export `extractFilesFromPlanFile` (D11, PIN-13). Fixtures titled `files-union`: a three-task plan yields the union; a path repeated across tasks appears once; a plan with one empty block and one full block does not throw; a plan with one backticked block and one bare comma-separated block yields the union of both (red under a concatenate-then-scan implementation). The existing single-block fixture still passes.
- Done when: `node --test skills/war-campaign/assets/campaign-ledger.test.mjs`
- requiresTest: true
- requiresPackaging: false
- deps: []
- target repo: superproject

### Task 3: economy flip doc sweep
- Files: `README.md`, `skills/war/SKILL.md`, `skills/war/references/schemas.md`, `skills/war/assets/skill-doc-contracts.test.mjs`
- Plan slice: rewrite the `README.md` `--ace` row and the "today's defaults" paragraph (the `economy` sentence), the `skills/war/SKILL.md` `--ace` bullet, and the `schemas.md` `ace` comment to the new fact (`run.ace` gates the per-task ace ladder; default true; no preset pins it off) (rule 6); land the `economy-ace-flip` rows: one OLD-absent row over `README.md`, `skills/war/SKILL.md`, `skills/war/references/schemas.md`, `skills/war-room/SKILL.md`, and `war-config.test.mjs`, line-scoped and case-folded: any single physical line that contains `economy`, an `ace` token, and off/false is an OLD hit (this reaches both README statements and both statements inside `war-room`'s one-line economy bullet, and never crosses a test boundary in the post-flip `war-config.test.mjs`), plus a NEW-present row on each (Task 1 authors the two surfaces it owns). Keep `skills/war/SKILL.md` under its hard line: replace in place, no net growth.
- Done when: `node --test skills/war/assets/skill-doc-contracts.test.mjs`
- requiresTest: true
- requiresPackaging: false
- deps: [1]
- target repo: superproject

## Phase 2 — Absorb budget engine, refiner eviction, reserve retirement

### Task 1: absorb budget engine and refiner eviction, one commit
- Files: `skills/war/assets/workflow-template.js`, `skills/war/assets/workflow-template.test.mjs`, `skills/war/assets/war-config.test.mjs`, `agents/war-refiner.md`, `skills/war/references/gate-failure-classification.md` (new), `skills/war/references/budget-raise-floor.md`, `skills/war/references/refiner-recovery.md`, `skills/war/references/disposition-eligibility.md`, `skills/war/references/schemas.md`
- Plan slice: (D5) add `r.task.absorbRounds`, charge it at batch ace, re-entry, and subset commits, and reserve the terminal-pass charge site; remove the `fixRounds++` charge at those three sites; convert all three ace gates to `absorbRounds < run.absorbRounds`; add the `Ace-Charge: <task>:<n>` trailer to every ace-side commit prompt (reverts excluded); re-anchor the re-entry `Ace-Subset` round segment on `absorbRounds`; carry `absorbRounds` in the ace dispatch labels; extend the phase-start git-topology barrier dispatch (card + dispatched prompt + result schema) to return `absorbCharges: { <task>: n }`, the highest `Ace-Charge` index on the task branch, with an explicit allowance for that one `git log` trailer read beside the prompt's named commands, and seed `r.task.absorbRounds` from it, 0 with a loud log on error or absence; hand-mirror the `run.absorbRounds ?? 6` fallback and guard it in `war-config.test.mjs` with the existing `drift-guard: roundLimit fallback` idiom (PIN-7); register the new marker in `war-config.test.mjs`'s `F07` registries and bump the exact marker count; convert the `aceBisect` reserve stop's still-queued-subset `demote()` to `routeToSweep()` with `phaseClose:true` (a subset that already failed its re-audit keeps its demote, prefixed `demote:absorb-regressed` once D13 lands); split the batch ace's shared exhaustion arm: spent `absorbRounds` with no open blockers ⇒ `routeToSweep()` with `phaseClose:true`, open blockers ⇒ hold the aceable rows on `r.pendingAbsorbs` (deduped by content key) and fold them into the aceable set at the next approve, and demote held rows with `demote:absorb-blocked` (once D13 lands) when the task ends escalated, audit-blocked, or never merged; rewrite the seven engine comments and the emitted strings at both reserve stops that state `roundLimit − 2`; re-author `disposition-eligibility.md` widening 1 and the `schemas.md` re-entry row to the `absorbRounds < run.absorbRounds` boundary; add `schemas.md` rows for `absorbCharges` and `absorbRounds`; (D12) move `## Gate-failure classification` byte-identical to `skills/war/references/gate-failure-classification.md`, leave the fixed trigger pointer on the card, and re-key the three in-card `#gate-failure-classification` anchors and the `budget-raise-floor.md` line-44 link to the references path, reword the `budget-raise-floor.md` line-3 provenance sentence so it carries no `#gate-failure-classification` token, and reword the `refiner-recovery.md` prose reference to name the references file; the `absorbCharges` clause lands in the freed bytes (PIN-14). Fixtures titled `absorb-budget`, each red with its arm deleted.
- Done when: `node --test skills/war/assets/workflow-template.test.mjs skills/war/assets/war-config.test.mjs skills/war/assets/prompt-surface-budgets.test.mjs`
- requiresTest: true
- requiresPackaging: false
- deps: []
- target repo: superproject

### Task 2: reserve retirement docs and eviction pins
- Files: `skills/war/SKILL.md`, `CONTEXT.md`, `skills/war/references/design.md`, `docs/adr/0013-commanders-intent-and-disposition-routing.md`, `skills/war/assets/skill-doc-contracts.test.mjs`
- Plan slice: re-author the `D43` living-doc homes (`skills/war/SKILL.md` `--ace` bullet, `CONTEXT.md` **Ace bisection** and **Re-entry** entries, `references/design.md` §18) to the `absorbRounds` boundary and re-key `D43`, including its "reserve-stop demote arm" key, to the new route (untested subsets ride to the sweep); add the glossary terms absorb budget and exclusion set; append the ADR 0013 Phase 2 amendment (the `roundLimit − 2` reserve is superseded by `absorbRounds < run.absorbRounds`; the 2026-08-27 currency clause is historical; the ratified bodies stay byte-untouched); land the `reserve-retired` OLD-absent row over `skills/war/SKILL.md`, `CONTEXT.md`, `references/design.md`, `disposition-eligibility.md`, `schemas.md`, and `skills/war/assets/workflow-template.js`, with ADR 0013 exempt (Task 1 authors the last three surfaces; this task's gate reads them after the rebase, rule 7); land the `refiner-evict` rows: the evicted body present verbatim in `gate-failure-classification.md` (extract and equality), the card's trigger sentence pinned, no `#gate-failure-classification` token surviving in the card or the two references files (case-insensitive). Keep `skills/war/SKILL.md` under its hard line: replace in place, no net growth.
- Done when: `node --test skills/war/assets/skill-doc-contracts.test.mjs`
- requiresTest: true
- requiresPackaging: false
- deps: [1]
- target repo: superproject

## Phase 3 — Barrier enum and the default flip

### Task 1: barrier enum, seat prompts, default flip engine, one commit
- Files: `skills/war/assets/workflow-template.js`, `skills/war/assets/workflow-template.test.mjs`, `skills/war/assets/war-config.test.mjs`, `skills/war/assets/land-decision.mjs`, `skills/war/assets/land-decision.test.mjs`, `agents/war-auditor.md`, `skills/war/references/gate-audit-checklist.md` (new), `skills/war/references/disposition-eligibility.md`, `skills/war/references/schemas.md`
- Plan slice: (D16) move `### execution-evidence gate-audit checklist (reserved lens)` byte-identical to the new `skills/war/references/gate-audit-checklist.md`, leave the heading and the fixed trigger pointer on the card, re-target the `T2.1 both-surfaces` execution-evidence test (heading and pointer asserts on the card, the four duty asserts on the references file, the stale-sentence absence assert on the card) and every other assert that reads a checklist duty off the card (the two-owner End-state ownership mapping duty assert among them; grep the suite for `war-auditor.md` reads inside the checklist's byte range before the move), and land an extract-and-equality row pinning the evicted body (PIN-18); (D1) add the canonical `BARRIER_TOKENS` export to `land-decision.mjs` with its own test row, hand-mirror it in `workflow-template.js` with a mirror-registry row (rule 5) and an `F07` registration with the marker count bumped, render the dispatched DISPOSITION RULE sentence from the mirror, rewrite the card sentence to the in-diff `absorb` default and the barrier list, byte-mirrored, with the body in `disposition-eligibility.md`; add the optional `barrier` enum field to AUDIT_VERDICT (schema, card field list, dispatched prompt, `schemas.md` row) and update any default-deny field census over that list; a registry row binds the card sentence and the eligibility-doc list to the canonical export (PIN-1, PIN-2, PIN-12); add the out-of-diff seat rule (`absorb` + `phaseClose:true`) to both prompt layers (D2 seat half); clear the old absorb-never-default wording at every occurrence in the files this task owns: `agents/war-auditor.md` (the Disposition rule), `schemas.md` (the AuditVerdict disposition row, uppercase `NEVER defaulted`), and the three absorb-bearing sites in `workflow-template.js` (the AUDIT_VERDICT findings-schema disposition comment and the `dispositionOf` header comment, both single-quoted, and the dispatched sentence), rewording the `dispositionOf` header to an `ask`-only never-defaulted clause; the parked-ask identity comment stays as the one `ask`-only survivor; re-key both test pins that quote the old sentence in `workflow-template.test.mjs` (the `DISPO` const byte mirror and the "survives byte-untouched" assert in the disposition-prompt-widened test) and re-title the two behavioral default tests (`disposition defaults (criterion 3)` and `explicit dispositions route and override the severity default`) and the `dispositionOf` assert inside `#1550 — demote() refuses an ask loudly` to drop the retired wording, assertions untouched, because `dispositionOf`'s engine default stays `follow-up` until Phase 4's floor (lesson `source-comment-lags-emitted-prompt-after-rewrite`); hoist `aceRelPath` out of the wave-loop block to file scope and re-anchor the `ace-group-path` test's extraction (slice from `const aceGroups`, evaluate `aceRelPath` from its own `src.match`, the recordAcedTouched idiom) and refresh the now-false remintKey comment. Fixtures titled `barrier-list`, each red with its arm deleted.
- Done when: `node --test skills/war/assets/workflow-template.test.mjs skills/war/assets/land-decision.test.mjs skills/war/assets/war-config.test.mjs skills/war/assets/prompt-surface-budgets.test.mjs`
- requiresTest: true
- requiresPackaging: false
- deps: []
- target repo: superproject

### Task 2: default-flip doc sweep
- Files: `CLAUDE.md`, `CONTEXT.md`, `skills/war/SKILL.md`, `skills/war/references/design.md`, `skills/war/references/gastown-design-params.md`, `docs/adr/0013-commanders-intent-and-disposition-routing.md`, `skills/war/assets/skill-doc-contracts.test.mjs`
- Plan slice: rewrite the old "never a default" sentence on the five living surfaces this task owns to the new rule (rule 6); append the ADR 0013 Phase 3 amendment (in-diff default flip and barrier list; the ratified rule-4 body stays byte-untouched); add the glossary term barrier list; land the `old-default-absent` row asserting the retired conjunction shapes (`absorb` and `ask` are never default*, `absorb` and `ask` never defaulted, and `absorb` is never a default with a zero-token gap) absent from the eight living surfaces `CLAUDE.md`, `CONTEXT.md`, `agents/war-auditor.md`, `skills/war/SKILL.md`, `skills/war/assets/workflow-template.js`, `skills/war/references/design.md`, `skills/war/references/gastown-design-params.md`, `skills/war/references/schemas.md` (Task 1 authors the three it owns; this task's gate reads them after the rebase, rule 7), after normalization that strips backticks and single and double quotes, collapses whitespace, and case-folds, plus one positive fixture asserting the lawful rewrite "defaults to absorb; ask is never a default" passes and one negative fixture red against a `workflow-template.js` body carrying only the two single-quoted comment sites; ADR 0013 is exempt (append-only law): the amendment lands NEW-present with a pin that the amendment section exists; re-key `D37a` (CONTEXT.md **Disposition**, CLAUDE.md Known-traps) and `D41` (`skills/war/SKILL.md` Audits bullet) to the new pair wording in the same commit. Keep `skills/war/SKILL.md` under its hard line: replace in place, no net growth.
- Done when: `node --test skills/war/assets/skill-doc-contracts.test.mjs`
- requiresTest: true
- requiresPackaging: false
- deps: [1]
- target repo: superproject

## Phase 4 — Filing floor, exclusion set, demote census, ace-off routing

### Task 1: engine, one commit
- Files: `skills/war/assets/workflow-template.js`, `skills/war/assets/workflow-template.test.mjs`, `skills/war/assets/war-config.test.mjs`, `skills/war/assets/land-decision.mjs`, `skills/war/assets/land-decision.test.mjs`, `agents/war-auditor.md`, `agents/war-refiner.md`, `skills/war/references/file-followups.md`, `skills/war/references/schemas.md`
- Plan slice: (D15) route the three gate-audit seats' (`execution-evidence`, integrated-tip, end-state) fully specified Minor/Nit findings as `absorb` + `phaseClose:true` into `phaseCloseQueue` under the exclusion set, stamped with the seat label, one new queue producer; thread the shared DISPOSITION RULE clause and the `barrier` field onto the three gate-audit prompts; add one sentence to the card's `execution-evidence` checklist naming the routing and one clause to the card's `note` definition ("a note that names a fix in a touched file is applied"); `follow-up` from those seats needs a `barrier` tag like any seat; `auditLog` keeps the record; re-key the `#1550 (D7)` ask order-census test to the new `dispositionOf` site count and admit the new producer with its own ask-precedes-absorb arm, and reword the three #1692 sink comment blocks and the `parkAsk` header census clause that state the old sink; the card sentence and the three dispatched clauses land in the same commit (PIN-17); (D2 engine half) add the canonical `RELEASE_SLOT_FILES` export (the two JSON slots) to `land-decision.mjs` with its test row, hand-mirror it with a registry row and an `F07` registration, make `aceEligible` and the exclusion set match on basenames derived from the mirror through one helper and re-key the `aceEligible (criterion 1)` byte pin to the mirror read; build the exclusion set at sweep time from the files of every `args.sweepExclude` entry ∪ `Files:` of tasks not `merged` ∪ `RELEASE_SLOT_FILES`, `aceRelPath` on both sides, demotion reason naming the owner (plan slug, task id, or "the release slot"); a release-slot absorb demotes at birth with prefix `demote:release-slot`; retire the `routeToSweep` release-slot arm and its comment (PIN-3, PIN-11); add the `sweepExclude` args entry-validation row; (D4) add the refiner `diff-probe` dispatch per task (card clause + dispatched prompt + `DIFF_PROBE_RESULT` schema, label `diff-probe:<task>`, `git diff --name-only <dispatchBase>..<tip>` in the task worktree, idempotent on resume, error ⇒ `diff_files` absent and logged) between the worker's green return and the seat convene, in the bytes Phase 2 freed on the refiner card; make `dispositionOf` read `diff_files`: omitted in-diff with `suggested_fix` ⇒ `absorb`, omitted out-of-diff with `suggested_fix` ⇒ `absorb` + `phaseClose:true`, else the old default; land the filing floor at intake right after `dispositionOf`: seat rows only, `demote()` stamps `engineFiled: true` and the floor skips stamped rows, barrierless `follow-up` ⇒ `absorb` (in-diff, joins the task's ace batch) or `absorb` + `phaseClose:true` (out-of-diff), `barrier:trade-off` ⇒ `ask` when the `ask` field is present else keep with a log, any other barrier ⇒ file as stated, an in-diff `note` with `suggested_fix` ⇒ `absorb` with the "note with a specified fix rerouted" log while a `note` without a fix stays, probe absent ⇒ old default, skip log, and `demote:floor-skipped` on that task's filed rows (PIN-6); (D13) add the canonical `DEMOTE_REASONS` prefix enum to `land-decision.mjs` with its test row, mirror it with a registry row and an `F07` registration with the count bumped; `demote()` validates every `follow-up` reason against the enum and on a miss prepends `demote:unclassified` and logs the site loudly, never a throw; the two variable-head sites gain a literal prefix ahead of the variable; land the default-deny census over every `demote` call whose disposition argument can evaluate to `follow-up` (literal and severity-ternary shapes), with the pinned site count and one unwired negative reference; put the prefix on a fixed line of every engine-filed issue body in the file-followups prompt, mirrored into `file-followups.md`; (PIN-15); (D14 engine arm) retire the two "absorb requires --ace" demote arms: with ace off a defaulted absorb routes `phaseClose:true` to the sweep (PIN-16); add `schemas.md` rows for `DIFF_PROBE_RESULT` and its `diff_files`, `sweepExclude`, `barrier` provenance on filed rows, and `engineFiled`. Fixtures titled `sweep-exclude`, `filing-floor`, `demote-census`, `ace-off-route`, `gate-audit-route` (each seat's absorb reaches the sweep stamped with its label; exclusion-set hit demotes naming the owner; `ask` still parks; the retired sink is absent), each red with its arm deleted; `filing-floor` also covers: a rerouted in-diff row is in the task's ace batch and not the sweep, a probe failure keeps the old default; `sweep-exclude` also covers: a `plugin.json` absorb demotes at birth with `demote:release-slot`; a README absorb outside `## Status` reaches the sweep; the retired `routeToSweep` release-slot arm is absent.
- Done when: `node --test skills/war/assets/workflow-template.test.mjs skills/war/assets/land-decision.test.mjs skills/war/assets/war-config.test.mjs skills/war/assets/prompt-surface-budgets.test.mjs`
- requiresTest: true
- requiresPackaging: false
- deps: []
- target repo: superproject

### Task 2: glossary, ADR amendment, ace-off doc
- Files: `CONTEXT.md`, `skills/war/SKILL.md`, `docs/adr/0013-commanders-intent-and-disposition-routing.md`, `skills/war/assets/skill-doc-contracts.test.mjs`
- Plan slice: add the glossary term demote reason prefix; rewrite the `skills/war/SKILL.md` `--ace` bullet's Residual rule ("with `--ace` off every absorb demotes this way") to the D14 fact (with ace off a defaulted absorb routes `phaseClose:true` to the sweep; the sweep and the terminal pass run regardless), no net growth; append the ADR 0013 Phase 4 amendment (ace-off routing to the sweep and the demote prefix; ratified bodies stay byte-untouched); pin the term, the amendment section, and an `ace-off-route` OLD-absent row for the retired Residual-rule wording on `skills/war/SKILL.md`.
- Done when: `node --test skills/war/assets/skill-doc-contracts.test.mjs`
- requiresTest: true
- requiresPackaging: false
- deps: [1]
- target repo: superproject

## Phase 5 — Terminal pass and held carry

### Task 1: engine
- Files: `skills/war/assets/workflow-template.js`, `skills/war/assets/workflow-template.test.mjs`, `skills/war/references/schemas.md`
- Plan slice: (D3a) add the terminal pass on the merged-sweep path with the polish panel's absorbs (today's sweep-raised arm) plus any queued absorb the sweep could not land as its input: filter the terminal queue through `aceEligible` (refuse and log a release-slot absorb), then one ace-shaped commit at the post-polish integration tip with an `Ace-Charge` trailer, through Refine like any ace commit, charging `r.task.absorbRounds` on the polish task for telemetry only (seed the polish pseudo-task `absorbRounds: 0` at construction; the pass never budget-blocks), then one re-audit seat (`correctness` when present, else the roster's first seat with its lens, logged); the pass convenes with `run.ace` off; no sweep or an empty terminal queue ⇒ skip with a log; re-audit regression ⇒ forward-revert the terminal commit, then carry (non-final) or demote (final), never a second pass; a fresh absorb from that seat rides `carriedPhaseClose` on a non-final phase and demotes to `follow-up` with "raised on the terminal pass" on the final phase; retire the merged-path "sweep-raised absorb" demote arm, the wrapped copy of its reason in the routing comment, and the census comment that names it; the discard-path arm stays, reworded to name the discard, and on a non-final phase its absorbs ride `carriedPhaseClose` instead (PIN-4, PIN-9). (D3b) add `carriedPhaseClose` as a top-level key on the phase return for every `landDecision !== 'landed'` at both return sites (main and catch-block), and as an empty array on `landed`; the held-phase arm writes `phaseCloseQueue` there instead of demoting; add `args.seededPhaseClose` with an entry-validation row and the #1413 own-token provenance treatment; seeded entries drain into the sweep (PIN-5); add `args.finalPhase` (boolean or absent; absent reads as final; anything else refused loudly at entry) as the terminal pass's finality signal, recorded per phase in the run manifest. Add `schemas.md` rows for `carriedPhaseClose`, `seededPhaseClose`, and `finalPhase`, each with a pin in `workflow-template.test.mjs` binding the row's key name to the engine's emitted and validated key (the `done_when_log_path` schema-slot pin shape). Fixtures titled `terminal-pass` (including: a polish-panel absorb reaches the terminal pass) and `held-carry`.
- Done when: `node --test skills/war/assets/workflow-template.test.mjs`
- requiresTest: true
- requiresPackaging: false
- deps: []
- target repo: superproject

### Task 2: glossary and ADR 0012
- Files: `CONTEXT.md`, `docs/adr/0012-intra-phase-visibility-and-phase-close-sweep.md`, `skills/war/assets/skill-doc-contracts.test.mjs`
- Plan slice: add the terms terminal pass and carried queue; retire the sweep-raised sentence in `CONTEXT.md`; append the ADR 0012 line naming the terminal pass as the successor of the merged-sweep demote arm; pin both terms and the ADR line.
- Done when: `node --test skills/war/assets/skill-doc-contracts.test.mjs`
- requiresTest: true
- requiresPackaging: false
- deps: [1]
- target repo: superproject

## Phase 6 — Lead surfaces and telemetry

### Task 1: launch and relaunch prose
- Files: `skills/war/SKILL.md`, `skills/war/references/sweep-exclusion.md` (new), `skills/war/references/resume-and-recovery.md`, `skills/war/references/schemas.md`, `skills/war/assets/skill-doc-contracts.test.mjs`
- Plan slice: new `sweep-exclusion.md` carries the Lead duty: import `extractFilesFromPlanFile` from `campaign-ledger.mjs` in a Node one-liner (the ledger CLI's verb set is closed and stays so) and call it on the plan path of every ledger entry not this plan and not `landed`, and write `args.sweepExclude` as `[{ slug, files[] }]` entries (stored ledger `files` are the contention record, not this input), log "campaign contention set empty for <n> entries" on an empty union from a present ledger, absent ledger ⇒ one log line "no campaign contention set threaded", record `sweepExcludeCount` and the threaded `finalPhase` in the run manifest (null when absent), with their rows in `schemas.md` § Run manifest (PIN-8); `skills/war/SKILL.md` gains one trigger pointer in the ADR 0042 shape (`when launching under a campaign, read references/sweep-exclusion.md`) and stays under its hard line; `resume-and-recovery.md` gains the relaunch steps: thread the phase return's `carriedPhaseClose` into `args.seededPhaseClose`; thread `args.finalPhase` from the plan's phase count at every launch (one sentence beside the seeding step); override `absorbCharges` through args only at a sanctioned relaunch; plus the accepted residual: a Workflow that threw before returning has no phase return, so the Lead re-derives the queue from the seats' audit verdicts in the run transcripts and the relaunch log names the re-derivation; the Checkpoint `--afk` no-match arm gains one sentence: the unmatched ask files as a `follow-up` with `demote:ask-unruled-afk` on the body prefix line, question preserved, and `D41`'s `--afk` key is re-keyed to it (D13). Pins for the pointer (`sweep-exclusion-pin`), the relaunch steps (`lead-carry-pin`), and the Checkpoint sentence (`afk-ask-prefix-pin`), End state 16.
- Done when: `node --test skills/war/assets/skill-doc-contracts.test.mjs`
- requiresTest: true
- requiresPackaging: false
- deps: []
- target repo: superproject

### Task 2: telemetry
- Files: `skills/war-review/SKILL.md`
- Plan slice: add rows for `sweepExcludeCount` (n/a when null, 0 when empty), the terminal-pass seat and outcome per phase (write the row header as `terminal-pass`), `absorbRounds` spent per task, follow-ups filed per `DEMOTE_REASONS` prefix, and `demote:unclassified` as a defect signal. End state 18's case-insensitive, hyphen-tolerant presence greps are this task's pin.
- Done when: None — prose-only telemetry rows, no war-review suite; End state 18 carries the pin
- requiresTest: false
- requiresPackaging: false
- deps: []
- target repo: superproject

## Phase 7 — Release

### Task 1: version bump
- Files: `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`, `README.md`, `CHANGELOG.md`
- Plan slice: bump all four slots in lock-step to the next free patch above the live base; replace the `## Status` blurb in place; add the CHANGELOG head entry naming the absorb budget, the economy preset flip, the barrier list and default flip, the exclusion set, the filing floor, the `DEMOTE_REASONS` census, the ace-off routing, the terminal pass, and the held carry.
- Done when: `node --test skills/war/assets/version-slots.test.mjs`
- requiresTest: false
- requiresPackaging: true
- deps: []
- target repo: superproject

## Deferred validations (backstops)

- Field effect: rerun the study rubric on the next two plans executed after this release, separating seat-filed, engine-filed (by the `DEMOTE_REASONS` line), Lead-filed asks (their own column, the `--afk` no-match ones tagged `demote:ask-unruled-afk`), and never-merged-task rows (`demote:floor-skipped`, their own column). Targets: follow-ups per task under 1.0, and filed follow-ups classified yes-trivial or yes-small under 25% (today 69%) · why deferred: needs two live runs · runner: operator, with the README rubric in `docs/studies/2026-09-03-followup-filing-study/README.md`, at the close of the second plan.

## Notes / conscious deviations

- Red-team rounds 1 to 3 (report `docs/red-team/2026-09-03-in-band-absorb-default.md`) each found the next layer of the same defect class in a seven-mechanism `workflow-template.js` task. D10 splits the plan into seven phases with one mechanism family per engine phase; `war-config.test.mjs` moved out of the knob phase because its `F07` marker census and fallback guard belong with the tasks that add mirrors. The re-entry that follows this restructure is a fresh red-team run; its report seeds from this report's `**Rounds:** 3` line and says so.
- Red-team round 1 withdrew the echo-back 1 rider "no re-extraction" (D6): stored ledger footprints are first-block-only until Task 1.2 lands and a sweep re-stamps them, so the Lead re-extracts at launch with the exported `extractFilesFromPlanFile`.
- Red-team round 2 moved the filing floor from the file-followups dispatch into the merge queue because no git-derived file list existed before Refine. Round 5 replaced that with a per-task `diff-probe` refiner dispatch before the seats convene, so the floor runs at intake and a rerouted in-diff finding joins the task's own ace batch and panel re-audit. The merge-queue floor, `MERGE_RESULT.diff_files`, the post-filing removal arm, and the "one lane weaker" residual are gone.
- Red-team round 3 found the floor needed a removal arm (a rerouted row was already a filing record) and that the polish task's integration-branch `absorbCharges` clause had no owner; the clause is dropped and the per-run reset is a recorded residual.
- The D4 floor sits at intake, before any row is filed. Prior lesson `hygiene-arm-placed-after-existing-dirty-fail-loud-guard-is-unreachable-for-the-corruption-it-targets`: ordering decides reachability, and the `filing-floor` fixtures reach the floor's own branch (a rerouted in-diff row is in the ace batch).

## Open decisions

None. Red-team round 4's nine decisions are ruled (gate-audit family re-enumerated, the `--afk` lane labeled at the Checkpoint, `RELEASE_SLOT_FILES` as the two JSON slots, `aceBisect` untested subsets ride to the sweep, `args.finalPhase`, the terminal charge is telemetry, in-diff notes with a fix reroute, the `execution-evidence` checklist evicts, `demote:floor-skipped` on never-merged rows). Red-team round 3's four decisions are ruled: `DEMOTE_REASONS` runtime validation with the `demote:unclassified` sentinel (D13); `run.absorbRounds` hard-rejects null like `run.roundLimit` (D5); the `--afk` no-match ask lane is Lead-side and its Checkpoint prose stamps `demote:ask-unruled-afk` (D13, Task 6.1; round 4 corrected the engine-side premise); gate-audit-family Minor/Nits route like any seat's (D15).
